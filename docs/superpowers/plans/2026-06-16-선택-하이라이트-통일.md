# 선택·하이라이트·동기화·뷰포트 통일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 선택을 단일 SSOT(`useSelectionStore`)로 두고, 하나의 해석기(`resolveSelection`)와 순수 투영으로 하이라이트·동기화·뷰포트를 통일해 파편화 버그를 구조적으로 제거한다.

**Architecture:** `useSelectionStore` 선택 → 순수 해석기 `resolveSelection` → (1) 하이라이트 파생 effect 1개가 `pathHighlightStore`(파생 캐시) 기록, (2) `focusTick` 펄스에 묶인 뷰포트 effect 1개가 선택 대상으로 포커스. 명령형 하이라이트 호출 6곳·중복 navy 렌더 3겹·뷰포트 effect 2개·특수 clear 를 제거한다. `mode` 플래그 없이 `resolveSelection().kind`로 딤/카메라가 결정된다.

**Tech Stack:** React + Zustand + Canvas2D + React Flow, Vite, Vitest, TypeScript. dev: `docker compose -f docker-compose.dev.yml up -d` + `npm run dev`. 빌드검증: `cd frontend && npm run build`. 테스트: `cd frontend && npx vitest run`.

**근거 스펙:** `docs/superpowers/specs/2026-06-16-선택-하이라이트-통일-design.md`

**검증 전략:** 순수 로직(`resolveSelection`)은 실제 단위 테스트(P1). 그 외 캔버스/카메라/배선은 `npm run build` 타입체크 + `vitest run` 그린 유지 + 브라우저 수동 검증(각 Task 말미). 가짜 단위 테스트는 만들지 않는다.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `frontend/src/features/workspace/selectionHighlight.ts` | 선택→의미 해석(단일 해석기) | `resolveSelection` 추가, `cableToAddress` 역매핑 추가, `useSelectionHighlight` 단일 작성자로 재작성 |
| `frontend/src/features/workspace/selectionHighlight.test.ts` | 해석기 단위 테스트 | `resolveSelection` 테스트 추가 |
| `frontend/src/features/pathTrace/stores/pathHighlightStore.ts` | 하이라이트 파생 캐시 | 명령형 API 제거, `setHighlight`/`prepareTopology` 추가, `loadProjection` 추출 |
| `frontend/src/features/equipment/components/ConnectionDiagram.tsx` | 설비 상세 연결도 | 카드 클릭 = 선택 setter, 카드 선택표시 = 선택 파생 |
| `frontend/src/features/connections/components/AssetConnectionsSection.tsx` | 연결 섹션 | 행/토폴로지 클릭 = 선택 setter / `prepareTopology` |
| `frontend/src/features/connections/components/AssetConnectionsTab.tsx` | 연결탭 | 토폴로지 버튼 = `prepareTopology` |
| `frontend/src/features/editor/components/CanvasContextMenu.tsx` | 캔버스 우클릭 | "추적" = 선택 setter |
| `frontend/src/features/editor/hooks/useEditorKeyboard.ts` | 키보드 | Enter = 선택 setter |
| `frontend/src/features/editor/hooks/useCanvasEvents.ts` | 캔버스 클릭 | `clearHighlight` 3곳 제거, 케이블 클릭=연결 선택 |
| `frontend/src/features/editor/hooks/useCanvas.ts` | 평면도 렌더 | 딤=connection일 때만, 상세 글로우 제거(선택 styling 통합) |
| `frontend/src/features/connections/components/ConnectionOverlay.tsx` | 연결 오버레이 | 경로 글로우를 선택 요소엔 미적용(중복 제거) |
| `frontend/src/features/editor/components/FloorPlanEditor.tsx` | 평면도 effect | 뷰포트 effect 2개→1개(focusTick 펄스) |
| `frontend/src/features/workspace/useEditorSelectionBridge.ts` | 뷰 진입 브리지 | 유지(진입 시 focusTick) |
| `frontend/src/pages/WorkspacePage.tsx` | 워크스페이스 | `onClickPlan` 선택 비우기 제거, 자동선택은 빈 선택일 때만 |

---

## Task 1 (P1): `resolveSelection` — 단일 해석기 + 단위 테스트

**Files:**
- Modify: `frontend/src/features/workspace/selectionHighlight.ts`
- Test: `frontend/src/features/workspace/selectionHighlight.test.ts`

기존 `resolveHighlight`/`resolveSelectedCable`는 **유지**(이미 테스트됨). 그 위에 `resolveSelection`(연결 사실 + 노드/케이블/배치 set 반환)과 `cableToAddress`(케이블→주소 역매핑)를 추가한다.

- [ ] **Step 1: 실패 테스트 작성** — `selectionHighlight.test.ts` 끝에 추가:

```ts
import { resolveSelection, cableToAddress } from './selectionHighlight';
import type { TraceGraph } from '../trace/traceGraph';

// 최소 graph 스텁 — resolveSelection 은 trace 폴백에서만 projectTrace(graph) 사용.
const graphStub = { cables, nameById: new Map(), subNameById: new Map() } as unknown as TraceGraph;
const effAssets: never[] = [];

describe('resolveSelection', () => {
  it('연결(diagram): 슬롯 코어 → kind=connection + 그 컴포넌트의 cableIds', () => {
    const r = resolveSelection('S', 3, null, graphStub, components, effAssets);
    expect(r.kind).toBe('connection');
    if (r.kind === 'connection') expect([...r.cableIds]).toContain('o3');
  });
  it('자산만(매칭 없음) → kind=asset', () => {
    const r = resolveSelection('S', 9, null, graphStub, [], effAssets);
    expect(r).toEqual({ kind: 'asset', assetId: 'S' });
  });
  it('선택 없음 → kind=none', () => {
    expect(resolveSelection(null, 3, null, graphStub, components, effAssets)).toEqual({ kind: 'none' });
  });
  it('cableToAddress: 케이블 → (자산,core) 역매핑(보는 자산 우선)', () => {
    expect(cableToAddress('b2', 'F', graphStub)).toEqual({ assetId: 'F', core: 2 });
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `cd frontend && npx vitest run src/features/workspace/selectionHighlight.test.ts`
  Expected: FAIL — `resolveSelection`/`cableToAddress` 미정의.

- [ ] **Step 3: 구현** — `selectionHighlight.ts`에 추가 (기존 `resolveHighlight` 아래, import 보강):

```ts
import { projectTrace } from '../trace/traceProjection';
import { expandToPlacedIds } from '../pathTrace/stores/pathHighlightStore'; // export 필요(아래 Task 2 Step 0)
import type { TraceGraph } from '../trace/traceGraph';
import type { Asset } from '../../types/asset';

export type ResolvedSelection =
  | { kind: 'connection'; cableId: string; nodeIds: Set<string>; placedIds: Set<string>; cableIds: Set<string> }
  | { kind: 'asset'; assetId: string }
  | { kind: 'none' };

/** 케이블 → (자산, core) 역매핑. 보는 자산(viewedAssetId)이 끝단이면 그쪽 우선, 아니면 source 끝단. */
export function cableToAddress(
  cableId: string,
  viewedAssetId: string | null,
  graph: TraceGraph,
): { assetId: string; core: number | null } | null {
  const c = (graph.cables as CableLike[]).find((x) => x.id === cableId);
  if (!c) return null;
  const pick = (id?: string | null, role?: string | null, number?: number | null) =>
    id ? { assetId: id, core: role === 'IN' ? FEEDER_INPUT_CORE : (number ?? null) } : null;
  if (viewedAssetId && c.targetAssetId === viewedAssetId) return pick(c.targetAssetId, c.targetRole, c.number);
  if (viewedAssetId && c.sourceAssetId === viewedAssetId) return pick(c.sourceAssetId, c.sourceRole, c.number);
  return pick(c.sourceAssetId, c.sourceRole, c.number);
}

/** 선택 → 평면도/연결 측 의미. 그리드/패널 투영은 selectedCore 로 별도(이 함수 미사용). */
export function resolveSelection(
  assetId: string | null,
  core: number | null,
  anchorCableId: string | null,
  graph: TraceGraph,
  components: DiagramComponent[],
  effectiveAssets: Asset[],
): ResolvedSelection {
  if (!assetId) return { kind: 'none' };
  const cables = (graph.cables ?? []) as CableLike[];
  const action = resolveHighlight(assetId, core, anchorCableId, cables, components);
  if (action.kind === 'diagram') {
    const nodeIds = new Set(action.comp.nodeIds);
    return {
      kind: 'connection',
      cableId: action.comp.seedCableId,
      nodeIds,
      placedIds: expandToPlacedIds(nodeIds, effectiveAssets),
      cableIds: new Set(action.comp.cableIds),
    };
  }
  if (action.kind === 'trace') {
    const projection = projectTrace(action.cableId, graph);
    if (projection) {
      const nodeIds = new Set(projection.nodeIds);
      return {
        kind: 'connection',
        cableId: action.cableId,
        nodeIds,
        placedIds: expandToPlacedIds(nodeIds, effectiveAssets),
        cableIds: new Set(projection.cableIds),
      };
    }
  }
  return { kind: 'asset', assetId };
}
```

- [ ] **Step 4: 통과 확인** — Run: `cd frontend && npx vitest run src/features/workspace/selectionHighlight.test.ts`
  Expected: PASS (기존 resolveHighlight/resolveSelectedCable 테스트 + 새 resolveSelection 테스트).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/features/workspace/selectionHighlight.ts frontend/src/features/workspace/selectionHighlight.test.ts
git commit -m "feat(선택): resolveSelection 단일 해석기 + cableToAddress 역매핑"
```

---

## Task 2 (P2): `pathHighlightStore` 파생 캐시화 + 하이라이트 단일 작성자

**Files:**
- Modify: `frontend/src/features/pathTrace/stores/pathHighlightStore.ts`
- Modify: `frontend/src/features/workspace/selectionHighlight.ts` (`useSelectionHighlight`)

- [ ] **Step 0: `expandToPlacedIds` export** — `pathHighlightStore.ts:74` `function expandToPlacedIds` 앞에 `export` 추가(Task 1이 import).

- [ ] **Step 1: store에 `setHighlight` 추가, 명령형 API 제거** — `pathHighlightStore.ts`:
  - `loadProjection`(비동기 graph 빌드+projectTrace)을 모듈 헬퍼로 추출(기존 `startTrace` 본문에서). 시그니처: `async function loadProjection(cableId): Promise<{ok:true; projection; ids:Set<string>} | {ok:false; error:string}>`.
  - `startTrace`/`highlightDiagram`/`startCircuitTrace` (공개 API) **삭제**.
  - 추가:

```ts
  /** 파생 전용 — useSelectionHighlight 만 호출. */
  setHighlight: (h: { cableId: string | null; nodeIds: Set<string>; placedIds: Set<string>; cableIds: Set<string> }) =>
    set({
      active: true,
      tracingCableId: h.cableId,
      isLoading: false,
      error: null,
      highlightedNodeIds: h.nodeIds,
      highlightedPlacedIds: h.placedIds,
      highlightedEdgeIds: h.cableIds,
    }),

  prepareTopology: async (cableId: string) => {
    set({ tracingCableId: cableId, isLoading: true, error: null });
    const r = await loadProjection(cableId);
    if (!r.ok) { set({ isLoading: false, error: r.error }); return; }
    set({ projection: r.projection, isLoading: false, modalOpen: true });
  },
```
  `openTopology`/`closeTopology`/`clearHighlight` 유지. 인터페이스에서 삭제한 메서드 시그니처 제거, 새 두 개 추가. `projection` 은 `prepareTopology`만 채운다(파생 하이라이트는 projection 미사용).

- [ ] **Step 2: `useSelectionHighlight` 단일 작성자로 재작성** — `selectionHighlight.ts:79-94`:

```ts
export function useSelectionHighlight(): void {
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const selectedCore = useSelectionStore((s) => s.selectedCore);
  const selectedCableId = useSelectionStore((s) => s.selectedCableId);
  const { graph } = useTraceGraph();
  const { groups } = useAssetDiagram(selectedAssetId ?? '');
  useEffect(() => {
    if (!graph) return;
    const components = groups.flatMap((g) => g.components);
    const effAssets = useSubstationWorkingCopy.getState().effectiveAssets() as Asset[];
    const r = resolveSelection(selectedAssetId, selectedCore, selectedCableId, graph, components, effAssets);
    const hi = usePathHighlightStore.getState();
    if (r.kind === 'connection') {
      const prev = hi.tracingCableId;
      hi.setHighlight({ cableId: r.cableId, nodeIds: r.nodeIds, placedIds: r.placedIds, cableIds: r.cableIds });
      // 연결 선택은 명시적 "이 경로 보여줘" → 카메라 펄스(연결이 바뀐 경우만).
      if (prev !== r.cableId) useEditorStore.getState().bumpFocusTick();
    } else {
      hi.clearHighlight();
    }
  }, [selectedAssetId, selectedCore, selectedCableId, groups, graph]);
}
```
  import 보강: `useSubstationWorkingCopy`, `useEditorStore`, `resolveSelection`, `Asset`.

- [ ] **Step 3: 타입체크** — Run: `cd frontend && npx tsc --noEmit`
  Expected: 명령형 API(`startTrace` 등) 호출처(Task 3~5 대상)에서 에러 발생 — 정상(다음 Task에서 해소). 이 Task 범위 파일만 에러 없어야 함.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/features/pathTrace/stores/pathHighlightStore.ts frontend/src/features/workspace/selectionHighlight.ts
git commit -m "refactor(하이라이트): pathHighlightStore 파생캐시화 + useSelectionHighlight 단일 작성자"
```

---

## Task 3 (P2): 명령형 호출처 6곳 → 선택 setter

**Files:** `ConnectionDiagram.tsx`, `AssetConnectionsSection.tsx`, `AssetConnectionsTab.tsx`, `CanvasContextMenu.tsx`, `useEditorKeyboard.ts`

- [ ] **Step 1: ConnectionDiagram.tsx** — `startTrace`(32) 제거. 카드 선택표시·클릭을 선택 기준으로:
  - `tracingCableId`/`traceActive` 대신 `const selectedCableId = useSelectionStore((s)=>s.selectedCableId)`.
  - `isCardSelected = selectedCableId === cable.id` (85줄).
  - `handleClick`(87-93):

```tsx
const handleClick = () => {
  const sel = useSelectionStore.getState();
  if (sel.selectedCableId === cable.id) { sel.setSelectedAssetId(null); return; } // 토글 해제
  const addr = cableToAddress(cable.id, equipmentId, graph!);
  sel.setSelectedComponent(addr?.assetId ?? equipmentId, addr?.core ?? null, cable.id);
};
```
  - unmount `clearHighlight`(39): 선택 해제로 대체 — `useEffect(() => () => { useSelectionStore.getState().setSelectedAssetId(null); }, [])`. (또는 부모가 선택 소유 시 제거; 보수적으로 해제 유지.)
  - `handleDelete`(95-100)의 `clearHighlight()` → `useSelectionStore.getState().setSelectedAssetId(null)`.
  - import: `useSelectionStore`, `cableToAddress`. `usePathHighlightStore` 미사용 시 제거.

- [ ] **Step 2: AssetConnectionsSection.tsx** — `onRowClick`/`onTopology`(89-96):

```ts
const onRowClick = (cableId: string) => {
  const sel = useSelectionStore.getState();
  if (sel.selectedCableId === cableId) { sel.setSelectedAssetId(null); return; }
  const addr = cableToAddress(cableId, assetId, graphRef);
  sel.setSelectedComponent(addr?.assetId ?? assetId, addr?.core ?? null, cableId);
};
const onTopology = (cableId: string) => { void usePathHighlightStore.getState().prepareTopology(cableId); };
```
  `graphRef` = `useTraceGraph().graph` (이 컴포넌트에 추가). `tracingCableId` 비교는 `useSelectionStore(s=>s.selectedCableId)`로 교체. import: `useSelectionStore`, `cableToAddress`, `useTraceGraph`.

- [ ] **Step 3: AssetConnectionsTab.tsx** — 13-14: `prepareTopology = usePathHighlightStore(s=>s.prepareTopology)`. 51: `onClick={() => prepareTopology(comp.seedCableId)}`. 미사용 `startTrace`/`openTopology` 셀렉터 제거.

- [ ] **Step 4: CanvasContextMenu.tsx** — `handleTraceCable`(52-55):

```ts
const handleTraceCable = () => {
  const addr = cableToAddress(target.id, null, useTraceGraph상태대신 graph);
  useSelectionStore.getState().setSelectedComponent(addr?.assetId ?? target.id, addr?.core ?? null, target.id);
  onClose();
};
```
  graph 접근이 없으면 최소 `useSelectionStore.getState().setSelectedComponent(target.id, null, target.id)` (anchorCableId=target.id 로 resolveSelection 이 컴포넌트 확정). import 정리.

- [ ] **Step 5: useEditorKeyboard.ts** — 111: `usePathHighlightStore.getState().startTrace(es.selectedCableId)` →
```ts
useSelectionStore.getState().setSelectedComponent(es.selectedAssetId ?? '', null, es.selectedCableId);
```
  (selectedCableId 가 앵커 → resolveSelection 이 컴포넌트/trace 해소.)

- [ ] **Step 6: 타입체크 + 테스트** — Run: `cd frontend && npx tsc --noEmit && npx vitest run`
  Expected: 에러 없음, 기존 테스트 그린.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "refactor(하이라이트): 명령형 startTrace 6곳 → 선택 setter(파생 단일화)"
```

---

## Task 4 (P5): 캔버스 클릭 특수 clear 제거 + 케이블=연결 선택

**Files:** `frontend/src/features/editor/hooks/useCanvasEvents.ts:126-150`

- [ ] **Step 1: select tool 블록 교체** (Task 5(이전 계획) 근거 — `selectEquipment`/`setSelectedCableId`가 이미 SSOT 갱신):

```ts
    if (tool === 'select') {
      const found = findItemAt(x, y, null, localEquipment);
      if (found) {
        const session = createDragSession(found, { x, y });
        canvasStore.getState().setDragSession(session);
        editorStore.getState().selectEquipment(found.item.id); // SSOT 갱신 → useSelectionHighlight 파생
      } else {
        const closestCableId = findClosestCableId(x, y, editorStore.getState().zoom);
        if (closestCableId) {
          editorStore.getState().setSelectedCableId(closestCableId); // 웨이포인트 편집 UI
          // 케이블 클릭 = 연결 선택(평면도 경로 하이라이트+카메라). 끝단 주소로 SSOT 동기화.
          const addr = cableToAddress(closestCableId, null, useTraceGraphSnapshot());
          useSelectionStore.getState().setSelectedComponent(addr?.assetId ?? '', addr?.core ?? null, closestCableId);
        } else {
          canvasStore.getState().setIsPanning(true);
          canvasStore.getState().setPanStart({ x: screenX, y: screenY });
          editorStore.getState().clearSelection();
        }
      }
    }
```
  graph 스냅샷 접근: 이 훅에 graph 가 없으면 `editorStore.getState().setSelectedCableId` 만 두고 SSOT 동기화는 생략 가능(웨이포인트 편집만). **권장:** graph 접근 가능하면 동기화. `usePathHighlightStore...clearHighlight()` 3곳 제거.

- [ ] **Step 2: 타입체크** — Run: `cd frontend && npx tsc --noEmit`. Expected: 에러 없음.

- [ ] **Step 3: Commit** — `git commit -am "fix(캔버스): 중복 clearHighlight 제거 + 케이블 클릭=연결 선택 동기화"`

---

## Task 5 (P4): 렌더 dedup — 요소당 강조 1겹

**Files:** `useCanvas.ts:99-131`, `ConnectionOverlay.tsx:180-198`

- [ ] **Step 1: useCanvas 딤=connection일 때만 + 상세 글로우 제거**:
  - 99-111: 딤 조건을 그대로(`pathHighlight.active`면 0.2 딤). `active`는 connection 선택일 때만 true(파생). 변경 불필요하거나 `mode` 없이도 동작 — **확인만**.
  - 113-131(상세 글로우 strokeRect) **삭제**. 선택 자산 강조는 `renderEquipmentItems`의 `selectedIds`(navy fill/stroke)로 일원화. → navy 2겹 제거.

- [ ] **Step 2: ConnectionOverlay 경로 글로우를 선택 요소엔 미적용** — 180-198: 경로 설비 글로우 루프에서 `nodeId === useSelectionStore.getState().selectedAssetId` 인 요소는 skip(선택 styling 이 이미 표현). → navy 3겹째 제거.

- [ ] **Step 3: 빌드 + 수동 확인** — `cd frontend && npm run build`. 브라우저: 연결 선택 시 한 설비에 navy 강조가 1겹만(글로우 중첩 없음).

- [ ] **Step 4: Commit** — `git commit -am "fix(렌더): 요소당 강조 1겹 — 상세 글로우/경로 글로우 중복 제거"`

---

## Task 6 (P3): 뷰포트 effect 2개 → 1개 (focusTick 펄스)

**Files:** `frontend/src/features/editor/components/FloorPlanEditor.tsx:157-222`

- [ ] **Step 1: 두 effect 를 하나로 통합** — 157-185(focusTick) + 194-222(pathHighlight) 를 단일 effect 로:

```ts
const focusTick = useEditorStore((s) => s.focusTick);
useEffect(() => {
  const sel = useSelectionStore.getState();
  const graph = /* useTraceGraph 스냅샷 */;
  const r = resolveSelection(sel.selectedAssetId, sel.selectedCore, sel.selectedCableId, graph, components, effAssets);
  if (!containerRef.current) return;
  const rect = containerRef.current.getBoundingClientRect();
  if (rect.width === 0 || rect.height === 0) return;
  if (r.kind === 'connection') {
    // r.cableIds 의 pathPoints bounds → calculateCenterOnBounds(…, 200, 100)
  } else if (r.kind === 'asset') {
    const target = floorTargetFor(r.assetId, useSubstationWorkingCopy.getState().effectiveAssets());
    if (!target) return;
    // calculateCenterOnBounds(target rect, …, RIGHT_PANEL_WIDTH)
  } else return;
  useEditorStore.getState().setViewport(fit.zoom, fit.panX, fit.panY);
}, [focusTick]);
```
  기존 두 effect의 bounds 계산 코드를 그대로 가져와 `kind` 분기에 배치. deps = `[focusTick]` 단일. (graph/components 는 getState/hook 스냅샷으로 fresh 하게.)

- [ ] **Step 2: 빌드 + 수동 확인** — 더블클릭/딥링크/평면도진입/연결선택에서만 카메라 이동. 단순 자산 단일클릭·토폴로지 열고닫기에선 이동 없음.

- [ ] **Step 3: Commit** — `git commit -am "refactor(뷰포트): viewport effect 2→1, focusTick 펄스로 통일"`

---

## Task 7 (P6): 뷰 전환 시 선택 지속

**Files:** `frontend/src/pages/WorkspacePage.tsx`

- [ ] **Step 1: `onClickPlan`(191-200)에서 선택 비우기 제거** — `if (view === 'fiber' || view === 'power') setSelectedAssetId(null);` 줄 삭제(선택 유지).

- [ ] **Step 2: 자동선택을 빈 선택일 때만**(114-124 autoOpen effect) — `setSelectedAssetId(첫 컨테이너)` 앞에 가드:
```ts
if (useSelectionStore.getState().selectedAssetId) { autoOpenedKey.current = key; return; }
```
  (유지하던 사용자 선택을 덮지 않음. 노드 변경 리셋(81)은 그대로 → 변전소 바뀌면 비워진 뒤 자동선택.)

- [ ] **Step 3: 빌드 + 수동 확인** — 선번장에서 OFD/코어 선택 후 평면도 전환 → 선택 유지 + 진입 시 그 대상 포커스. 변전소 변경 → 리셋.

- [ ] **Step 4: Commit** — `git commit -am "feat(선택): 뷰 전환 시 선택 지속(노드 변경 시만 리셋)"`

---

## Task 8: 전체 검증

- [ ] **Step 1:** `cd frontend && npm run build` — 통과.
- [ ] **Step 2:** `cd frontend && npx vitest run` — 그린.
- [ ] **Step 3: 브라우저 수동 회귀(스펙 §13):**
  1. 연결 클릭(그리드/패널/연결도/캔버스케이블) → 평면도 경로 하이라이트 + 카메라 이동 + 패널 CB/포트 활성(1:1 동기화).
  2. 자산만 선택 → 선택 표시만(경로/카메라 없음).
  3. 토폴로지 열고 X → 평면도 카메라·딤 불변.
  4. 뷰 전환(선번장↔평면도) → 선택 유지 + 진입 포커스.
  5. 변전소 변경 → 리셋.
  6. 한 요소 navy 강조 1겹.

---

## Self-Review

- **스펙 커버리지:** §4 아키텍처=Task1-2, §6 해석기=Task1, §7 파생=Task2, §8 카메라=Task6, §9 뷰지속=Task7, §10 dedup=Task5, §11 토폴로지=Task2-3, §12 삭제맵=Task2-7 전부 매핑.
- **타입 일관성:** `ResolvedSelection`/`resolveSelection`/`cableToAddress`/`setHighlight`/`prepareTopology` 명칭 Task 전반 일치. `expandToPlacedIds` export(Task2 Step0) ← Task1 import.
- **플레이스홀더:** 코드 골격에 "/* … */" 로 표기한 2곳(Task6 bounds 계산, Task4 graph 스냅샷)은 **기존 effect 코드를 그대로 이전**한다는 지시 — 실행 시 해당 원본(스펙·현 파일)에서 복사. 가짜 테스트 없음.
- **주의(실행 시):** graph 스냅샷 접근법(hook vs getState)은 각 파일의 기존 패턴을 따른다(useSelectionHighlight/AssetConnectionsSection 은 `useTraceGraph()` 훅 보유). CanvasContextMenu/useCanvasEvents 에 graph 가 없으면 anchorCableId 기반 `setSelectedComponent(assetId,null,cableId)` 로 충분(resolveSelection 이 앵커로 컴포넌트 확정).
