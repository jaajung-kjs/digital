# 분전반 · 피더 GUI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 분전반 정보탭에 피더 3열 카드(분기 미리보기)를, 피더 정보탭에 분기(CB) DIN 레일 GUI(개폐 색·클릭 상세·하이라이트·메타편집)를 추가 — 랙→모듈, OFD→슬롯→포트와 동형의 2단계 드릴다운.

**Architecture:** 분기 = 피더 아래 CB 회로(케이블, 새 자산 없음). 기존 `buildPowerRows`(점유 CB 파생)·`commitMeta`(specParams 패치)를 재사용해 `buildFeederCircuits`(빈 위치 1..N 패딩)로 감싼다. `BreakerRail`(프레젠테이션)·`FeederCircuitsPanel`(SlotPortsPanel 패턴)을 신규로, `connectionKind==='distributor'`(피더) → `'feeder-circuits'` 단일 레지스트리 주입. 분전반 카드는 기존 `DistributionCircuits` 강화. 케이블은 전역 `graph.cables`에서 파생(SSOT).

**Tech Stack:** React + TypeScript, Zustand, @tanstack/react-query, Vitest + @testing-library/react, Tailwind(시맨틱 토큰).

**Branch:** `feat/분전반-피더-GUI` (spec 커밋 `21fd8ae` 존재).

**검증:** 각 태스크 `cd /Users/jsk/1210/digital/frontend && npx vitest run <path>`; 마지막 `npx tsc --noEmit && npm run build && npx vitest run`.

---

## 기존 코드/사실 (그대로 재사용)

- `buildPowerRows(feederId: string, cables: PowerCable[], nameById: Map<string,string>): CbRow[]` — `features/power/powerRegisterDescriptor.tsx`. **export 됨**. 피더에 닿는 OUT 케이블을 CB 행으로. `CbRow = { cableId; loadAssetId; loadName; cbNumber:string; capacity:string; switchState:string; spec:string; categoryId }`. **점유 CB 만**(빈 위치 없음).
- `commitMeta(cableId, field, value)` — 같은 파일, export. `patch('cables', id, {specParams:{...prev,[field]:value}})`.
- `PowerCable` 형태: `{ id; sourceAssetId?; targetAssetId?; sourceRole?; targetRole?; categoryName?; categoryId?; number?; specParams? }`.
- switchState 는 **문자열** — ON 판정 `s.toUpperCase()==='ON'`. (값: 'ON'/'OFF'/'')
- `DistributionCircuits({ equipmentId })` — `features/equipment/components/detail/panels/DistributionPanel.tsx:16`. 분전반 `distribution` 섹션. 3열 피더 그리드, 카드 클릭 → `useSelectionStore.getState().setSelectedAssetId(feeder.id)`. `feedersOfPanel(assets, panelId)` from `features/assets/distributionSubtree`.
- 주입 레지스트리: `types/equipmentKind.ts`(`DetailPanelKind`), `resolveAssetDetailKind.ts`(conduit→'conduit-ports' 분기 존재), `resolveSpatialSection.tsx`(switch). P2b 와 동형.
- 하이라이트: `usePathHighlightStore.getState().startTrace(cableId)` / `.clearHighlight()` — `features/pathTrace/stores/pathHighlightStore`.
- 전역 케이블: `useTraceGraph()` → `graph.cables`(전역 /cables + staged 오버레이, SSOT). `graph.nameById:Map`.
- 패턴 참고: `features/fiber/components/SlotPortsPanel.tsx`(섹션 본체), `components/PortGrid.tsx`(매트릭스), `features/fiber/slotPorts.ts`(파생).

---

## Task 0: TraceCable 에 category 필드 보존 (계통 규격 회귀 수정 + 분기 spec 근거)

**왜:** 연결 그리드가 전역 `graph.cables`(TraceCable)에서 파생하도록 바뀌었는데, TraceCable 은 `categoryName`/`categoryId`/`displayColor` 를 **버린다**. `buildPowerRows` 는 규격을 `c.categoryName`·`c.categoryId` 에서 읽으므로 계통뷰 규격 컬럼이 비고, `buildFeederCircuits.spec` 도 빈다. specParams 보존(과거 수정)과 동형으로 category 도 보존한다(additive, 무해).

**Files:**
- Modify: `frontend/src/features/trace/cableTrace.ts` (TraceCable 인터페이스)
- Modify: `frontend/src/features/trace/traceGraph.ts` (TraceCableInput + toTraceCable)
- Modify: `frontend/src/features/trace/traceGraph.test.ts` (회귀 테스트)

- [ ] **Step 1: `TraceCable` 에 필드 추가** — `cableTrace.ts` 의 `interface TraceCable` 에:
```ts
  categoryName?: string | null;
  categoryId?: string | null;
  displayColor?: string | null;
```

- [ ] **Step 2: `TraceCableInput` + `toTraceCable`** — `traceGraph.ts` 의 `interface TraceCableInput` 에 동일 3필드 추가, `toTraceCable` 에 복사(기존 specParams 복사 스타일과 동일):
```ts
  categoryName: (c as { categoryName?: string | null }).categoryName ?? null,
  categoryId: (c as { categoryId?: string | null }).categoryId ?? null,
  displayColor: (c as { displayColor?: string | null }).displayColor ?? null,
```

- [ ] **Step 3: 회귀 테스트** — `traceGraph.test.ts` 의 buildTraceGraph 테스트(specParams 보존 테스트 옆)에 추가: 입력 케이블에 `categoryName:'HIV 2.5sq', categoryId:'cat1'` 를 주고, `graph.cables.find(...).categoryName === 'HIV 2.5sq'` 단언. (specParams 회귀 테스트 패턴 그대로 — 수정 없으면 fail 함을 확인.)

- [ ] **Step 4: 검증** — `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/trace/ && npx tsc --noEmit` → green/clean. (TraceCable 소비자들은 additive 라 영향 없음.)

- [ ] **Step 5: 커밋**
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/trace/cableTrace.ts frontend/src/features/trace/traceGraph.ts frontend/src/features/trace/traceGraph.test.ts
git commit -m "fix(trace): TraceCable 가 categoryName/categoryId/displayColor 보존 — 계통뷰 규격 회귀 수정 + 분기 spec 근거(전역 graph.cables 파생 대응)"
```

---

## Task 1: `buildFeederCircuits` 파생 (점유 + 빈 위치 패딩)

**Files:**
- Create: `frontend/src/features/power/feederCircuits.ts`
- Test: `frontend/src/features/power/feederCircuits.test.ts`

- [ ] **Step 1: 실패 테스트** — `feederCircuits.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { buildFeederCircuits } from './feederCircuits';

const FEEDER = 'f1';
// 부하 eqpA(CB1, ON), eqpB(CB4, OFF). roleAt(feeder)='OUT'.
const cb1 = { id: 'c1', sourceAssetId: FEEDER, targetAssetId: 'eqpA', sourceRole: 'OUT', targetRole: null, number: 1, categoryName: 'HIV 2.5sq', categoryId: 'cat1', specParams: { capacity: '20A', switchState: 'ON' } };
const cb4 = { id: 'c4', sourceAssetId: FEEDER, targetAssetId: 'eqpB', sourceRole: 'OUT', targetRole: null, number: 4, categoryName: 'HIV 4sq', categoryId: 'cat2', specParams: { capacity: '16A', switchState: 'OFF' } };
const nameById = new Map([['eqpA', '복도등'], ['eqpB', '옥외등']]);

describe('buildFeederCircuits', () => {
  it('점유 CB + 빈 위치를 1..N 으로 채운다 (N = max(점유최대+2, 6))', () => {
    const cs = buildFeederCircuits({ id: FEEDER }, [cb1, cb4], nameById);
    // max 점유 = 4 → N = max(6, 6) = 6
    expect(cs).toHaveLength(6);
    expect(cs.map((c) => c.cbNumber)).toEqual([1, 2, 3, 4, 5, 6]);
  });

  it('점유 CB 는 부하/용량/개폐/규격을 담는다', () => {
    const cs = buildFeederCircuits({ id: FEEDER }, [cb1, cb4], nameById);
    const c1 = cs.find((c) => c.cbNumber === 1)!;
    expect(c1.occupied).toBe(true);
    expect(c1.cableId).toBe('c1');
    expect(c1.loadAssetId).toBe('eqpA');
    expect(c1.loadName).toBe('복도등');
    expect(c1.capacity).toBe('20A');
    expect(c1.switchState).toBe('ON');
    expect(c1.spec).toBe('HIV 2.5sq');
    expect(c1.categoryId).toBe('cat1');
  });

  it('빈 위치는 occupied=false, 필드 비움', () => {
    const cs = buildFeederCircuits({ id: FEEDER }, [cb1, cb4], nameById);
    const c2 = cs.find((c) => c.cbNumber === 2)!;
    expect(c2.occupied).toBe(false);
    expect(c2.cableId).toBeNull();
    expect(c2.loadAssetId).toBeNull();
    expect(c2.capacity).toBe('');
    expect(c2.switchState).toBe('');
  });

  it('점유 최대가 크면 그 + 2 까지', () => {
    const cb10 = { ...cb1, id: 'c10', number: 10 };
    const cs = buildFeederCircuits({ id: FEEDER }, [cb10], nameById);
    expect(cs).toHaveLength(12); // max(10+2, 6)
  });

  it('CB 없으면 빈 6칸', () => {
    const cs = buildFeederCircuits({ id: FEEDER }, [], nameById);
    expect(cs).toHaveLength(6);
    expect(cs.every((c) => !c.occupied)).toBe(true);
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/features/power/feederCircuits.test.ts` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** — `feederCircuits.ts`

```ts
import { buildPowerRows, type CbRow } from './powerRegisterDescriptor';

export interface FeederCircuit {
  cbNumber: number;
  occupied: boolean;
  cableId: string | null;
  loadAssetId: string | null;
  loadName: string | null;
  capacity: string;
  switchState: string;
  spec: string;
  categoryId: string | null;
}

// buildPowerRows 의 PowerCable 와 동일 입력 형태.
type Cable = Parameters<typeof buildPowerRows>[1][number];

/**
 * 피더의 분기(CB) — 점유는 buildPowerRows(SSOT) 재사용, 그 위에 빈 위치를 1..N 으로 패딩.
 * N = max(점유 최대 번호 + 2, 6). 빈 번호는 occupied=false 빈 차단기.
 */
export function buildFeederCircuits(
  feeder: { id: string },
  cables: Cable[],
  nameById: Map<string, string>,
): FeederCircuit[] {
  const rows = buildPowerRows(feeder.id, cables, nameById);
  const byNum = new Map<number, CbRow>();
  for (const r of rows) {
    const n = parseInt(r.cbNumber, 10);
    if (!Number.isNaN(n)) byNum.set(n, r);
  }
  const maxNum = byNum.size ? Math.max(...byNum.keys()) : 0;
  const N = Math.max(maxNum + 2, 6);

  const out: FeederCircuit[] = [];
  for (let n = 1; n <= N; n++) {
    const r = byNum.get(n);
    out.push(
      r
        ? {
            cbNumber: n, occupied: true, cableId: r.cableId,
            loadAssetId: r.loadAssetId, loadName: r.loadName,
            capacity: r.capacity, switchState: r.switchState, spec: r.spec, categoryId: r.categoryId,
          }
        : {
            cbNumber: n, occupied: false, cableId: null,
            loadAssetId: null, loadName: null, capacity: '', switchState: '', spec: '', categoryId: null,
          },
    );
  }
  return out;
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/features/power/feederCircuits.test.ts` → PASS(5). `npx tsc --noEmit` clean.

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/power/feederCircuits.ts frontend/src/features/power/feederCircuits.test.ts
git commit -m "feat(분전반): buildFeederCircuits — buildPowerRows(SSOT) 재사용 + 빈 위치 1..N 패딩"
```

---

## Task 2: `BreakerRail` 차단기 DIN 레일

**Files:**
- Create: `frontend/src/components/BreakerRail.tsx`
- Test: `frontend/src/components/BreakerRail.test.tsx`

- [ ] **Step 1: 실패 테스트** — `BreakerRail.test.tsx`

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { BreakerRail } from './BreakerRail';
import type { FeederCircuit } from '../features/power/feederCircuits';

const mk = (n: number, over: Partial<FeederCircuit> = {}): FeederCircuit => ({
  cbNumber: n, occupied: false, cableId: null, loadAssetId: null, loadName: null,
  capacity: '', switchState: '', spec: '', categoryId: null, ...over,
});
const circuits: FeederCircuit[] = [
  mk(1, { occupied: true, cableId: 'c1', capacity: '20A', switchState: 'ON' }),
  mk(2, { occupied: true, cableId: 'c2', capacity: '20A', switchState: 'OFF' }),
  mk(3),
];

describe('BreakerRail', () => {
  it('차단기 수만큼 번호 셀 렌더', () => {
    render(<BreakerRail circuits={circuits} selectedCb={null} onSelect={() => {}} onToggle={() => {}} />);
    expect(screen.getByRole('button', { name: /차단기 1/ })).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /차단기 3/ })).toBeInTheDocument();
  });

  it('개폐상태별 클래스 (ON=success, OFF=neutral, 빈=dashed)', () => {
    render(<BreakerRail circuits={circuits} selectedCb={null} onSelect={() => {}} onToggle={() => {}} />);
    expect(screen.getByRole('button', { name: /차단기 1/ }).className).toContain('text-success');
    expect(screen.getByRole('button', { name: /차단기 3/ }).className).toContain('border-dashed');
  });

  it('차단기 클릭 → onSelect(번호)', () => {
    const onSelect = vi.fn();
    render(<BreakerRail circuits={circuits} selectedCb={null} onSelect={onSelect} onToggle={() => {}} />);
    fireEvent.click(screen.getByRole('button', { name: /차단기 2/ }));
    expect(onSelect).toHaveBeenCalledWith(2);
  });

  it('점유 차단기의 토글 클릭 → onToggle(번호), 셀 클릭과 분리(stopPropagation)', () => {
    const onSelect = vi.fn();
    const onToggle = vi.fn();
    render(<BreakerRail circuits={circuits} selectedCb={null} onSelect={onSelect} onToggle={onToggle} />);
    fireEvent.click(screen.getByRole('button', { name: '차단기 1 개폐' }));
    expect(onToggle).toHaveBeenCalledWith(1);
    expect(onSelect).not.toHaveBeenCalled();
  });

  it('선택된 차단기에 ring', () => {
    render(<BreakerRail circuits={circuits} selectedCb={1} onSelect={() => {}} onToggle={() => {}} />);
    expect(screen.getByRole('button', { name: /차단기 1/ }).className).toContain('ring-primary');
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/components/BreakerRail.test.tsx` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** — `BreakerRail.tsx`

```tsx
import type { FeederCircuit } from '../features/power/feederCircuits';

const isOn = (s: string) => s.toUpperCase() === 'ON';

/** 차단기 DIN 레일 — 가로 배열 차단기 모듈(번호 + 개폐 토글 + 용량). 색=개폐상태. 순수 표시. */
export function BreakerRail({
  circuits,
  selectedCb,
  onSelect,
  onToggle,
}: {
  circuits: FeederCircuit[];
  selectedCb: number | null;
  onSelect: (cbNumber: number) => void;
  onToggle: (cbNumber: number) => void;
}) {
  return (
    <div className="flex flex-wrap gap-1 rounded-md bg-surface-2 p-2">
      {circuits.map((c) => {
        const on = c.occupied && isOn(c.switchState);
        const stateCls = !c.occupied
          ? 'border border-dashed border-line text-content-faint'
          : on
            ? 'bg-success-bg text-success'
            : 'bg-surface text-content-muted';
        return (
          <button
            key={c.cbNumber}
            type="button"
            onClick={() => onSelect(c.cbNumber)}
            aria-label={`차단기 ${c.cbNumber}`}
            aria-pressed={selectedCb === c.cbNumber}
            className={`relative flex w-9 flex-col items-center gap-1 rounded-[3px] py-1.5 transition-shadow ${stateCls} ${
              selectedCb === c.cbNumber ? 'ring-2 ring-primary' : ''
            }`}
          >
            <span className="text-[9px] font-mono tabular-nums leading-none">{c.cbNumber}</span>
            {/* 토글 — 점유 시만 클릭 가능. 위=ON 초록 / 아래=OFF. */}
            <span
              role={c.occupied ? 'button' : undefined}
              aria-label={c.occupied ? `차단기 ${c.cbNumber} 개폐` : undefined}
              onClick={(e) => { if (c.occupied) { e.stopPropagation(); onToggle(c.cbNumber); } }}
              className={`block h-5 w-2.5 rounded-[2px] ${
                !c.occupied ? 'bg-surface-2' : on ? 'bg-success' : 'bg-content-faint'
              }`}
            />
            <span className="text-[8px] leading-none opacity-70">{c.occupied ? c.capacity || '·' : '—'}</span>
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/components/BreakerRail.test.tsx` → PASS(5). `npx tsc --noEmit` clean.

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/components/BreakerRail.tsx frontend/src/components/BreakerRail.test.tsx
git commit -m "feat(분전반): BreakerRail — 차단기 DIN 레일(개폐 색·선택·토글, 빈 차단기)"
```

---

## Task 3: `FeederCircuitsPanel` (분기 섹션 본체)

**Files:**
- Create: `frontend/src/features/power/components/FeederCircuitsPanel.tsx`
- Test: `frontend/src/features/power/components/FeederCircuitsPanel.test.tsx`

- [ ] **Step 1: 실패 테스트** — `FeederCircuitsPanel.test.tsx`

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';

const { startTrace, clearHighlight, patch } = vi.hoisted(() => ({
  startTrace: vi.fn(), clearHighlight: vi.fn(), patch: vi.fn(),
}));

const FEEDER = 'f1';
const FEEDER_ASSET = { id: FEEDER, name: 'L1 조명', parentAssetId: 'dist1', assetType: { connectionKind: 'distributor', code: 'FEEDER' } };
const cb1 = { id: 'c1', sourceAssetId: FEEDER, targetAssetId: 'eqpA', sourceRole: 'OUT', targetRole: null, number: 1, categoryName: 'HIV', categoryId: 'cat1', specParams: { capacity: '20A', switchState: 'ON' } };

vi.mock('../../workingCopy/hooks', () => ({ useEffectiveAssets: () => [FEEDER_ASSET] }));
vi.mock('../../trace/traceGraph', () => ({
  useTraceGraph: () => ({ graph: { cables: [cb1], nameById: new Map([['eqpA', '복도등']]) }, isLoading: false }),
}));
vi.mock('../../pathTrace/stores/pathHighlightStore', () => {
  const st = { startTrace, clearHighlight };
  const hook = (sel?: (s: unknown) => unknown) => (sel ? sel(st) : st);
  (hook as unknown as { getState: () => unknown }).getState = () => st;
  return { usePathHighlightStore: hook };
});
vi.mock('../../workingCopy/substationStore', () => {
  const st = { patch };
  const hook = (sel?: (s: unknown) => unknown) => (sel ? sel(st) : st);
  (hook as unknown as { getState: () => unknown }).getState = () => st;
  return { useSubstationWorkingCopy: hook };
});
vi.mock('../../cables/hooks/useCableCategories', () => ({ useCableCategories: () => ({ data: [] }) }));

import { FeederCircuitsPanel } from './FeederCircuitsPanel';

beforeEach(() => { startTrace.mockClear(); clearHighlight.mockClear(); patch.mockClear(); });

describe('FeederCircuitsPanel', () => {
  it('차단기 레일을 렌더(1..N)', () => {
    render(<FeederCircuitsPanel feederId={FEEDER} />);
    expect(screen.getByRole('button', { name: /차단기 6/ })).toBeInTheDocument();
  });

  it('점유 차단기 클릭 → 부하 상세 + startTrace(cableId)', () => {
    render(<FeederCircuitsPanel feederId={FEEDER} />);
    fireEvent.click(screen.getByRole('button', { name: /차단기 1/ }));
    expect(screen.getByText(/복도등/)).toBeInTheDocument();
    expect(startTrace).toHaveBeenCalledWith('c1');
  });

  it('빈 차단기 클릭 → clearHighlight + 미연결', () => {
    render(<FeederCircuitsPanel feederId={FEEDER} />);
    fireEvent.click(screen.getByRole('button', { name: /차단기 3/ }));
    expect(clearHighlight).toHaveBeenCalled();
    expect(screen.getByText(/미연결/)).toBeInTheDocument();
  });

  it('점유 차단기 토글 → switchState 패치(ON↔OFF)', () => {
    render(<FeederCircuitsPanel feederId={FEEDER} />);
    fireEvent.click(screen.getByRole('button', { name: '차단기 1 개폐' }));
    // cb1 은 ON → OFF 로 패치(specParams 머지).
    expect(patch).toHaveBeenCalledWith('cables', 'c1', { specParams: { capacity: '20A', switchState: 'OFF' } });
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/features/power/components/FeederCircuitsPanel.test.tsx` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** — `FeederCircuitsPanel.tsx`

```tsx
import { useEffect, useMemo, useState } from 'react';
import { useEffectiveAssets } from '../../workingCopy/hooks';
import { useTraceGraph } from '../../trace/traceGraph';
import { usePathHighlightStore } from '../../pathTrace/stores/pathHighlightStore';
import { useSubstationWorkingCopy } from '../../workingCopy/substationStore';
import { buildFeederCircuits } from '../feederCircuits';
import { commitMeta } from '../powerRegisterDescriptor';
import { BreakerRail } from '../../../components/BreakerRail';
import { EditableField } from '../../assets/components/EditableField';
import type { Asset } from '../../../types/asset';

/**
 * 피더(distributor) 상세 "분기" 섹션 — 차단기 DIN 레일 + 선택 차단기 상세 + 부하 하이라이트.
 * 분기 = 피더 아래 CB 회로(케이블). 전역 graph.cables 에서 파생(SSOT). 새 부하 연결은 비범위.
 */
export function FeederCircuitsPanel({ feederId }: { feederId: string }) {
  const assets = useEffectiveAssets() as Asset[];
  const { graph } = useTraceGraph();
  const [selectedCb, setSelectedCb] = useState<number | null>(null);

  const feeder = useMemo(() => assets.find((a) => a.id === feederId) ?? null, [assets, feederId]);
  const circuits = useMemo(
    () => (feeder && graph ? buildFeederCircuits({ id: feeder.id }, graph.cables as never[], graph.nameById) : []),
    [feeder, graph],
  );
  const selected = circuits.find((c) => c.cbNumber === selectedCb) ?? null;

  // 선택 차단기 → 부하 트레이스(점유 시), 빈 차단기/미선택은 clear.
  useEffect(() => {
    const hi = usePathHighlightStore.getState();
    if (selectedCb === null) return;
    if (selected?.cableId) hi.startTrace(selected.cableId);
    else hi.clearHighlight();
  }, [selectedCb, selected?.cableId]);
  useEffect(() => () => usePathHighlightStore.getState().clearHighlight(), []);

  const toggle = (cbNumber: number) => {
    const c = circuits.find((x) => x.cbNumber === cbNumber);
    if (!c?.cableId) return;
    commitMeta(c.cableId, 'switchState', c.switchState.toUpperCase() === 'ON' ? 'OFF' : 'ON');
  };

  if (!circuits.length) {
    return <p className="px-1 text-[11px] text-content-faint">분기 정보를 불러올 수 없습니다.</p>;
  }

  return (
    <div className="space-y-3">
      <BreakerRail circuits={circuits} selectedCb={selectedCb} onSelect={setSelectedCb} onToggle={toggle} />
      {selected && (
        <div className="rounded border border-line p-2 text-xs space-y-1.5">
          <div className="flex items-center gap-2">
            <span className="font-medium">CB {selected.cbNumber}</span>
            <span className="rounded bg-surface-2 px-1.5 py-0.5 text-[10px] text-content-muted">
              {selected.occupied ? (selected.switchState || '—') : '미연결'}
            </span>
          </div>
          <div className="text-content-muted">부하: <span className="text-content">{selected.loadName ?? '—'}</span></div>
          {selected.occupied && selected.cableId && (
            <>
              <label className="flex items-center gap-2 text-content-muted">용량
                <EditableField value={selected.capacity} ariaLabel="용량" placeholder="용량"
                  onCommit={(v) => commitMeta(selected.cableId!, 'capacity', v || null)} />
              </label>
              <label className="flex items-center gap-2 text-content-muted">개폐
                <EditableField value={selected.switchState} type="select" ariaLabel="개폐"
                  options={[{ value: '', label: '—' }, { value: 'ON', label: 'ON' }, { value: 'OFF', label: 'OFF' }]}
                  onCommit={(v) => commitMeta(selected.cableId!, 'switchState', v || null)} />
              </label>
            </>
          )}
          <p className="text-[10px] text-content-faint">자세한 회로 정보는 계통뷰에서.</p>
        </div>
      )}
    </div>
  );
}
```
> 규격(카테고리) 편집은 계통의 `SpecCell` 패턴이 필요하나 v1 상세 카드는 용량·개폐만(규격은 계통뷰). `commitMeta` 가 specParams 머지를 보장하므로 토글 테스트의 `{capacity:'20A',switchState:'OFF'}` 가 맞다(기존 키 보존).

- [ ] **Step 4: 통과 확인** — `npx vitest run src/features/power/components/FeederCircuitsPanel.test.tsx` → PASS(4). `npx tsc --noEmit` clean.

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/power/components/FeederCircuitsPanel.tsx frontend/src/features/power/components/FeederCircuitsPanel.test.tsx
git commit -m "feat(분전반): FeederCircuitsPanel — 차단기 레일 + 선택 상세(부하/용량/개폐) + startTrace + specParams 편집"
```

---

## Task 4: 주입 (`feeder-circuits` kind)

**Files:**
- Modify: `frontend/src/types/equipmentKind.ts`
- Modify: `frontend/src/features/equipment/components/detail/panels/resolveAssetDetailKind.ts`
- Modify: `frontend/src/features/equipment/components/detail/panels/resolveAssetDetailKind.test.ts`
- Modify: `frontend/src/features/equipment/components/detail/panels/resolveSpatialSection.tsx`

- [ ] **Step 1: `DetailPanelKind` 에 `'feeder-circuits'` 추가**

`types/equipmentKind.ts` — 현재 `export type DetailPanelKind = EquipmentDetailPanelKind | 'conduit-ports';` 를:
```ts
export type DetailPanelKind = EquipmentDetailPanelKind | 'conduit-ports' | 'feeder-circuits';
```

- [ ] **Step 2: `resolveAssetDetailKind` 분기 추가 + 테스트**

`resolveAssetDetailKind.test.ts` 에 케이스 추가:
```ts
  it('distributor 자산(피더) → feeder-circuits', () => {
    const asset = { id: 'f', assetType: { connectionKind: 'distributor', code: 'FEEDER' } } as never;
    expect(resolveAssetDetailKind(asset, null)).toBe('feeder-circuits');
  });
```
`resolveAssetDetailKind.ts` 의 conduit 분기 아래에 추가:
```ts
  if (asset?.assetType?.connectionKind === 'conduit') return 'conduit-ports';
  if (asset?.assetType?.connectionKind === 'distributor') return 'feeder-circuits';
```
(기존 placed 분기·null 폴백 유지.)

- [ ] **Step 3: `resolveSpatialSection` case 추가**

import 추가:
```ts
import { FeederCircuitsPanel } from '../../../../power/components/FeederCircuitsPanel';
```
switch 에 case 추가('conduit-ports' 다음):
```tsx
    case 'feeder-circuits':
      return {
        label: '분기',
        node: <FeederCircuitsPanel feederId={equipmentId} />,
      };
```

- [ ] **Step 4: 검증**

```bash
cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/equipment/ && npx tsc --noEmit
```
Expected: resolveAssetDetailKind 테스트 green(distributor 케이스 포함), tsc clean. switch 가 `DetailPanelKind` 에 exhaustive 면 새 case 강제됨(누락 시 tsc 에러 → 추가 확인).

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/types/equipmentKind.ts \
  frontend/src/features/equipment/components/detail/panels/resolveAssetDetailKind.ts \
  frontend/src/features/equipment/components/detail/panels/resolveAssetDetailKind.test.ts \
  frontend/src/features/equipment/components/detail/panels/resolveSpatialSection.tsx
git commit -m "feat(분전반): 피더 분기 섹션 주입 — DetailPanelKind 'feeder-circuits' + resolveAssetDetailKind(distributor) + resolveSpatialSection case"
```

---

## Task 5: 분전반 피더 카드 미리보기 (DistributionCircuits 강화)

**Files:**
- Modify: `frontend/src/features/equipment/components/detail/panels/DistributionPanel.tsx`

- [ ] **Step 1: 피더 카드에 CB 미리보기 추가**

`DistributionCircuits` 는 `useEffectiveCables`·`useTraceGraph`(graph.nameById) 또는 effective 로 각 피더의 CB 를 파생해 미리보기 점을 표시한다. 상단 import 추가:
```ts
import { buildFeederCircuits } from '../../../../power/feederCircuits';
import { useTraceGraph } from '../../../../trace/traceGraph';
```
컴포넌트 본문에 graph 와 케이블 확보(기존 `localCables` 가 endpoint 만이라 buildFeederCircuits 입력으로 부족 → graph.cables 사용):
```ts
  const { graph } = useTraceGraph();
```
각 피더 `feeder.map(...)` 의 `<button>` 내부, `feeder.name` 아래에 미리보기 + 카운트 추가:
```tsx
                <span className="block text-sm font-semibold text-content-muted truncate">
                  {feeder.name}
                </span>
                {(() => {
                  const cs = graph ? buildFeederCircuits({ id: feeder.id }, graph.cables as never[], graph.nameById) : [];
                  const used = cs.filter((c) => c.occupied).length;
                  return (
                    <>
                      <span className="mt-0.5 block text-[11px] text-content-faint">CB {used}/{cs.length}</span>
                      <span className="mt-1 flex flex-wrap gap-0.5">
                        {cs.map((c) => (
                          <span key={c.cbNumber}
                            className={`inline-block h-2 w-2 rounded-[1px] ${
                              !c.occupied ? 'bg-surface-2'
                                : c.switchState.toUpperCase() === 'ON' ? 'bg-success' : 'bg-content-faint'
                            }`}
                            title={`CB ${c.cbNumber}${c.occupied ? ` · ${c.loadName ?? ''} ${c.switchState}` : ' 빈'}`}
                          />
                        ))}
                      </span>
                    </>
                  );
                })()}
```
(클릭 → `setSelectedAssetId(feeder.id)` 동작·삭제·추가는 기존 그대로. `connected` 좌측 보더 강조도 유지.)

- [ ] **Step 2: 검증**

```bash
cd /Users/jsk/1210/digital/frontend && npx tsc --noEmit && npx vitest run src/features/equipment/ src/features/power/
```
Expected: tsc clean, 관련 테스트 green. (DistributionPanel 테스트가 있으면 미리보기 추가로 깨지지 않는지 — 깨지면 미리보기를 단순 카운트로 축소하지 말고 테스트를 새 DOM 에 맞춰 갱신.)

- [ ] **Step 3: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/equipment/components/detail/panels/DistributionPanel.tsx
git commit -m "feat(분전반): 피더 카드에 CB 미리보기(점, 색=개폐) + 사용/총 카운트 — buildFeederCircuits 재사용"
```

---

## Task 6: 최종 검증

- [ ] **Step 1: 전체**

```bash
cd /Users/jsk/1210/digital/frontend && npx tsc --noEmit && npm run build && npx vitest run
```
Expected: tsc clean, build OK, 전체 vitest green.

- [ ] **Step 2: 수동 스모크(dev 서버)**
  - 분전반 상세 "회로" → 피더 3열 카드, 각 카드에 CB 미리보기 점(색=개폐) + 사용/총.
  - 피더 카드 클릭 → 그 피더 상세로 이동(eyebrow=분전반), "분기" 섹션에 차단기 DIN 레일.
  - 점유 차단기 클릭 → 아래 상세(부하/용량/개폐) + 캔버스 하이라이트. 토글 → 개폐 ON/OFF.
  - 빈 차단기 → 미연결, 하이라이트 해제.
  - 계통뷰의 CB 행과 개폐/번호/부하 일치.

- [ ] **Step 3: 비범위 확인**
  - `grep -rn "buildPowerRows" frontend/src` — 계통 descriptor + buildFeederCircuits 만 쓰는지(중복 파생 없음).

---

## Self-Review (작성자 체크)

- **Spec 커버리지:** 분전반 피더카드+미리보기(T5)·피더 분기 DIN레일(T2)·분기 상세/하이라이트/편집(T3)·파생 SSOT(T1, buildPowerRows 재사용)·주입(T4) 모두 태스크 대응. 빈 CB 새 부하연결은 비목표(명시).
- **타입 일관성:** `FeederCircuit`(T1) → BreakerRail props(T2) → FeederCircuitsPanel(T3) → DistributionPanel 미리보기(T5) 동일. switchState=문자열('ON'/'OFF'/''), `isOn`=toUpperCase 일관. `buildFeederCircuits(feeder, cables, nameById)` 시그니처 T1·T3·T5 동일. `commitMeta`(specParams 머지) T3 재사용. `DetailPanelKind += 'feeder-circuits'`(T4) 후 resolveSpatialSection/resolveAssetDetailKind 동일 리터럴.
- **플레이스홀더:** 없음(테스트/구현 코드 구체). 규격 편집은 v1 상세에서 제외(계통뷰) — 명시.
- **갭 주의:** (1) buildPowerRows 가 규격을 `categoryName`/`categoryId` 에서 읽는데 graph.cables(TraceCable)엔 그 필드가 없어 계통 규격이 비던 회귀 → **Task 0 에서 TraceCable 에 보존**(이게 선행돼야 buildFeederCircuits.spec·계통 규격이 산다). (2) DistributionCircuits 가 현재 `useEffectiveCables`(endpoint 만)라 미리보기엔 graph.cables 추가(T5). buildPowerRows 의 `PowerCable` 입력과 graph.cables(TraceCable+specParams+category) 필드 호환(sourceAssetId/sourceRole/number/specParams/categoryName 보유) — 구현 시 `as never[]` 캐스트.
