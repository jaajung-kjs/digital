# 연결 모델 대통일 — P4 광 데이터 마이그레이션(추가·되돌리기 가능) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 기존 광 데이터(FiberPath + fiber 케이블 + FiberCore)로부터 **실제** 통일 모델 엔티티(OFD-SLOT 슬롯 자산 + OPGW 케이블 + 번호 OUT 케이블 + 메타 specParams)를 **기존 옆에 추가 생성**한다. 옛 데이터는 *안 지움*(레거시 계속 동작, 되돌리기 = 새 것 삭제). P3a 어댑터에서 검증한 변환 로직을 *영속*한다.

**Architecture:** 순수 매핑 `planFiberMigration(fiberPaths, fiberCables, fiberCores, ofdSubstation)` → `{ slots, opgwCables, outCables }`(생성 스펙, 결정적). 얇은 persist 스크립트가 idempotent 하게 DB에 쓴다(슬롯 이미 있으면 skip). 추가만 — FiberPath/fiber케이블/FiberCore 그대로.

**Tech Stack:** Prisma/PostgreSQL, tsx 스크립트, Vitest.

**참조:** 스펙 §2.2/§5; 어댑터 `frontend/src/features/trace/fiberAdapter.ts`(같은 변환 로직, 거긴 가상/메모리, 여긴 실/영속); P1 시드(OFD-SLOT 타입, CBL-OPGW 카테고리 존재).

---

## 환경
- DB dev compose만. **build/기본 up 금지.**
- 스크립트 실행: `cd backend && npx tsx scripts/migrateFiberToSlots.ts`.
- psql: `docker exec ict-twin-postgres psql -U postgres -d ict_digital_twin -c "<sql>"`.
- 백 테스트 `cd backend && npx vitest run <file>`.

## 되돌리기(reversible)
새로 만든 것 = OFD-SLOT 타입 자산 + 그에 닿는 케이블. 되돌리기:
```sql
DELETE FROM cables WHERE source_asset_id IN (SELECT a.id FROM assets a JOIN asset_types t ON a.asset_type_id=t.id WHERE t.code='OFD-SLOT')
   OR target_asset_id IN (SELECT a.id FROM assets a JOIN asset_types t ON a.asset_type_id=t.id WHERE t.code='OFD-SLOT');
DELETE FROM assets WHERE asset_type_id IN (SELECT id FROM asset_types WHERE code='OFD-SLOT');
```
(스크립트에 `--revert` 플래그로 넣는다.)

## File Structure
- `backend/src/migration/fiberToSlots.ts` (Create) — 순수 매핑 `planFiberMigration`.
- `backend/src/migration/fiberToSlots.test.ts` (Create) — 매핑 단위테스트.
- `backend/scripts/migrateFiberToSlots.ts` (Create) — persist + idempotent + --revert.

---

## Task 1: 순수 매핑 planFiberMigration (TDD)

**Files:** `backend/src/migration/fiberToSlots.ts`, `backend/src/migration/fiberToSlots.test.ts`

### 매핑 규칙 (어댑터와 동형, 단 *생성 스펙* 산출)
입력: fiberPaths `{id, ofdAId, ofdBId, portCount}`, fiberCables `{id, sourceAssetId, targetAssetId, fiberPathId, fiberPortNumber}`, fiberCores `{fiberPathId, coreNumber, purpose, circuitText, spliceType, usageOverride}`, ofdInfo `Map<ofdId, {substationId, name}>`.
FiberPath fp 당:
- slotA 스펙: `{ tempKey:'slot:'+fp.id+':A', assetTypeCode:'OFD-SLOT', parentAssetId:fp.ofdAId, substationId: ofdInfo[fp.ofdAId].substationId, name: ofdInfo[fp.ofdBId]?.name ?? '대국' }` (이름=대국 OFD명). slotB 대칭.
- opgw 스펙: `{ tempKey:'opgw:'+fp.id, fromSlot:'slot:'+fp.id+':A', toSlot:'slot:'+fp.id+':B', categoryCode:'CBL-OPGW' }`.
fiber 케이블 c(fiberPathId=fp.id) 당:
- OFD-end → slotA(ofdAId) | slotB(ofdBId), 설비-end=반대. 코어 메타 = fiberCores 에서 (fp.id, c.fiberPortNumber) 매칭.
- out 스펙: `{ fromCableId:c.id, slotKey, equipmentAssetId, number:c.fiberPortNumber, specParams: { purpose, circuitText, spliceType, usageOverride } | null }`.

### Step 1: 실패 테스트 — Create `fiberToSlots.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { planFiberMigration } from './fiberToSlots';

const ofdInfo = new Map([
  ['ofdW', { substationId: 'sW', name: '원주OFD' }],
  ['ofdH', { substationId: 'sH', name: '홍천OFD' }],
]);
const fiberPaths = [{ id: 'fp1', ofdAId: 'ofdW', ofdBId: 'ofdH', portCount: 24 }];
const fiberCables = [
  { id: 'c1', sourceAssetId: 'eqA', targetAssetId: 'ofdW', fiberPathId: 'fp1', fiberPortNumber: 5 },
];
const fiberCores = [
  { fiberPathId: 'fp1', coreNumber: 5, purpose: '통합단말', circuitText: '원주 GR2링', spliceType: '패치', usageOverride: null },
];

describe('planFiberMigration', () => {
  it('FiberPath→슬롯 2 + OPGW 1, fiber케이블→OUT(번호·메타)', () => {
    const p = planFiberMigration(fiberPaths, fiberCables, fiberCores, ofdInfo);
    expect(p.slots).toHaveLength(2);
    expect(p.slots.find((s) => s.parentAssetId === 'ofdW')?.substationId).toBe('sW');
    expect(p.slots.find((s) => s.parentAssetId === 'ofdW')?.name).toBe('홍천OFD'); // 대국명
    expect(p.opgwCables).toHaveLength(1);
    expect(p.outCables).toHaveLength(1);
    const out = p.outCables[0];
    expect(out.number).toBe(5);
    expect(out.equipmentAssetId).toBe('eqA');
    expect(out.slotKey).toBe('slot:fp1:A'); // ofdW=A
    expect(out.specParams).toMatchObject({ purpose: '통합단말', circuitText: '원주 GR2링', spliceType: '패치' });
  });
});
```

### Step 2: 실패 확인 — `cd backend && npx vitest run src/migration/fiberToSlots.test.ts` → FAIL.

### Step 3: 구현 — Create `backend/src/migration/fiberToSlots.ts`:
```ts
export interface FiberPathRow { id: string; ofdAId: string; ofdBId: string; portCount?: number }
export interface FiberCableRow { id: string; sourceAssetId?: string | null; targetAssetId?: string | null; fiberPathId?: string | null; fiberPortNumber?: number | null }
export interface FiberCoreRow { fiberPathId: string; coreNumber: number; purpose?: string | null; circuitText?: string | null; spliceType?: string | null; usageOverride?: string | null }
export interface OfdInfo { substationId: string; name: string }

export interface SlotSpec { tempKey: string; parentAssetId: string; substationId: string; name: string }
export interface OpgwSpec { fromSlot: string; toSlot: string }
export interface OutSpec { fromCableId: string; slotKey: string; equipmentAssetId: string; number: number | null; specParams: Record<string, unknown> | null }
export interface MigrationPlan { slots: SlotSpec[]; opgwCables: OpgwSpec[]; outCables: OutSpec[] }

const slotKey = (fpId: string, side: 'A' | 'B') => `slot:${fpId}:${side}`;

/** 광 데이터 → 통일 모델 생성 스펙(결정적, 순수). 어댑터(fiberAdapter)와 같은 변환을 *영속용 스펙*으로. */
export function planFiberMigration(
  fiberPaths: FiberPathRow[],
  fiberCables: FiberCableRow[],
  fiberCores: FiberCoreRow[],
  ofdInfo: Map<string, OfdInfo>,
): MigrationPlan {
  const slots: SlotSpec[] = [];
  const opgwCables: OpgwSpec[] = [];
  const outCables: OutSpec[] = [];
  const byPath = new Map<string, FiberPathRow>();
  const coreOf = new Map<string, FiberCoreRow>();
  for (const fc of fiberCores) coreOf.set(`${fc.fiberPathId}|${fc.coreNumber}`, fc);

  for (const fp of fiberPaths) {
    byPath.set(fp.id, fp);
    const aInfo = ofdInfo.get(fp.ofdAId);
    const bInfo = ofdInfo.get(fp.ofdBId);
    if (!aInfo || !bInfo) continue; // OFD 정보 없으면 skip(고아 path)
    slots.push({ tempKey: slotKey(fp.id, 'A'), parentAssetId: fp.ofdAId, substationId: aInfo.substationId, name: bInfo.name });
    slots.push({ tempKey: slotKey(fp.id, 'B'), parentAssetId: fp.ofdBId, substationId: bInfo.substationId, name: aInfo.name });
    opgwCables.push({ fromSlot: slotKey(fp.id, 'A'), toSlot: slotKey(fp.id, 'B') });
  }

  for (const c of fiberCables) {
    if (!c.fiberPathId) continue;
    const fp = byPath.get(c.fiberPathId);
    if (!fp) continue;
    let side: 'A' | 'B' | null = null;
    let equip: string | null = null;
    if (c.sourceAssetId === fp.ofdAId) { side = 'A'; equip = c.targetAssetId ?? null; }
    else if (c.targetAssetId === fp.ofdAId) { side = 'A'; equip = c.sourceAssetId ?? null; }
    else if (c.sourceAssetId === fp.ofdBId) { side = 'B'; equip = c.targetAssetId ?? null; }
    else if (c.targetAssetId === fp.ofdBId) { side = 'B'; equip = c.sourceAssetId ?? null; }
    if (!side || !equip) continue;
    const core = c.fiberPortNumber != null ? coreOf.get(`${c.fiberPathId}|${c.fiberPortNumber}`) : undefined;
    const specParams = core
      ? { purpose: core.purpose ?? null, circuitText: core.circuitText ?? null, spliceType: core.spliceType ?? null, usageOverride: core.usageOverride ?? null }
      : null;
    outCables.push({ fromCableId: c.id, slotKey: slotKey(fp.id, side), equipmentAssetId: equip, number: c.fiberPortNumber ?? null, specParams });
  }

  return { slots, opgwCables, outCables };
}
```

### Step 4: 통과 + tsc — `cd backend && npx vitest run src/migration/fiberToSlots.test.ts` → PASS; `cd backend && npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add backend/src/migration/fiberToSlots.ts backend/src/migration/fiberToSlots.test.ts
git commit -m "feat(대통일P4): 광→슬롯 순수 마이그레이션 매핑 planFiberMigration + 테스트"
```

---

## Task 2: persist 스크립트 (idempotent, additive, --revert)

**Files:** `backend/scripts/migrateFiberToSlots.ts`

스크립트: prisma 로 fiberPaths/fiber케이블/fiberCores + OFD 자산(substationId/name) 로드 → `planFiberMigration` → idempotent persist.

- [ ] **Step 1: 작성** — Create `backend/scripts/migrateFiberToSlots.ts`:
  - prisma 로드: `fiberPath.findMany`, `cable.findMany({where:{cableType:'FIBER', fiberPathId:{not:null}}})`, `fiberCore.findMany`, OFD assets(`asset.findMany({where:{assetType:{code:'OFD'}}, select:{id,substationId,name}})`).
  - `--revert` 인자면 위 되돌리기 SQL 두 개 실행 후 종료.
  - idempotent: 이미 OFD-SLOT 자식이 있는 fiberPath 는 skip(슬롯 parentAssetId+이 path 추적 위해, 슬롯 name 중복 단순 검사 or 슬롯에 sourcePresetId 류 마커 없으니 — 간단히: 시작 시 OFD-SLOT 개수>0 이면 "이미 마이그레이션됨" 경고+skip, `--force` 로 덮어쓰기).
  - OFD-SLOT assetTypeId, CBL-OPGW categoryId 를 code 로 조회.
  - `planFiberMigration` 호출. tempKey→실 uuid 맵으로: 슬롯 먼저 create(assetType=OFD-SLOT, parentAssetId, substationId, name) → tempKey:realId. OPGW cable create(source/target=실 슬롯 id, cableType FIBER, categoryId CBL-OPGW, sourceRole 'IN', targetRole 'IN'). OUT cable create(source=슬롯 실 id, target=equipmentAssetId, cableType FIBER, sourceRole 'OUT', number, specParams).
  - 로그: 생성한 슬롯/OPGW/OUT 수.

- [ ] **Step 2: dev 실행** — `cd backend && npx tsx scripts/migrateFiberToSlots.ts`. 출력에 생성 수.

- [ ] **Step 3: psql 검증**
```
docker exec ict-twin-postgres psql -U postgres -d ict_digital_twin -c "
SELECT (SELECT count(*) FROM asset_types t JOIN assets a ON a.asset_type_id=t.id WHERE t.code='OFD-SLOT') AS slots,
       (SELECT count(*) FROM fiber_paths) AS fiberpaths,
       (SELECT count(*) FROM cables WHERE category_id=(SELECT id FROM cable_categories WHERE code='CBL-OPGW')) AS opgw,
       (SELECT count(*) FROM cables WHERE source_role='OUT' AND number IS NOT NULL) AS outcables;"
```
Expected: slots = 2 × fiberpaths, opgw = fiberpaths, outcables ≈ fiber 케이블 수. 옛 데이터(fiber_paths, fiberPortNumber 케이블, fiber_cores) 그대로 남아있음 확인.

- [ ] **Step 4: 멱등 확인** — 다시 실행 → "이미 마이그레이션됨" skip(중복 생성 0). psql 수 동일.

- [ ] **Step 5: Commit**
```bash
cd /Users/jsk/1210/digital
git add backend/scripts/migrateFiberToSlots.ts
git commit -m "feat(대통일P4): 광→슬롯 persist 스크립트(idempotent·additive·--revert)"
```

---

## Task 3: 검증 + 레거시 무변경

- [ ] **Step 1: 백엔드** — `cd backend && npx tsc --noEmit && npx vitest run` → PASS (기존 테스트 회귀 0 — 옛 데이터 안 건드림).
- [ ] **Step 2: 프론트** — `cd frontend && npm run build && npx vitest run` → PASS (코드 무변경, 데이터만 추가).
- [ ] **Step 3: 레거시 동작 확인** — 옛 fiber_paths/fiberPortNumber/fiber_cores 존재 → usePortStatus/선번장/토폴로지 그대로 동작(추가 데이터는 아직 어떤 뷰도 안 읽음).
- [ ] **Step 4: 최종 리뷰** — subagent-driven 리뷰(매핑 정확성·idempotency·revert·옛 데이터 보존).

---

## Self-Review
- **A 정신:** 추가만(옛 데이터 보존) → 레거시 동작·되돌리기 가능. 변환 로직은 P3a 어댑터에서 이미 검증된 것과 동형(여기선 영속).
- **순서:** P4(데이터) 먼저 → P3(뷰를 실데이터+cableTrace 로 전환, 가상 역매핑 없음) → P5(옛 데이터/레거시 삭제).
- **타입 일관성:** planFiberMigration 출력 스펙 → 스크립트 persist. OFD-SLOT/CBL-OPGW 는 P1 시드.
- **확인(구현 중):** Asset create 필수 컬럼(assetTypeId/substationId/name/parentAssetId 외 NOT NULL 있는지 — 스키마 확인), cable create 필수(bufferLength 기본 등), idempotency 마커 방식(OFD-SLOT 존재 검사로 충분한지), OFD name 형식(대국명이 선번장에 적절한지).
- **위험:** 실데이터 생성이라 dev 에서 먼저(운영은 같은 스크립트 1회). revert 로 되돌림. 옛 데이터 미변경이라 최악의 경우도 레거시는 살아있음.
