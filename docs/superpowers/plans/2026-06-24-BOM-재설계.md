# BOM 재설계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시공보고서의 자재산출·노무비를 하드코딩 `CONSTRUCTION_TEMPLATES`(코드 키)에서 **DB의 관리자 편집 노무규칙**으로 옮기고 부속자재·`BomMaterial`을 제거해, 케이블 코드 의존을 없애고 C5를 unblock 한다.

**Architecture:** 케이블 분류를 고정 5종(`CableGroup` + `kind`)으로, 이름만 사용자 정의로 유지한다. 노무규칙은 케이블=`CableGroup`(분류, m당), 설비=`AssetType`(종류, 개당) **DB 컬럼**에 둔다. 시공보고서는 스냅샷의 `categoryId`/`assetTypeId`로 DB 규칙을 조회해 노무·자재를 계산한다(코드·부속자재·템플릿 설정 제거). 본 작업은 `CableCategory.code`를 **드롭하지 않는다** — 보고서가 code를 더는 읽지 않게만 만들고, 실제 code 드롭은 C5가 담당한다.

**Tech Stack:** Prisma/PostgreSQL, Express, Zod, React 18 + TS, Zustand, React Query, vitest + @testing-library/react.

## Global Constraints

- **개발 DB:** `docker compose -f docker-compose.dev.yml up -d`. `docker compose build`/기본 `docker compose up`/프로덕션 빌드 금지. (CLAUDE.md)
- **마이그레이션:** dev DB 에 `cd backend && npx prisma migrate dev --name <n>` 로 적용·생성. 추가 컬럼은 전부 nullable(무중단). `BomMaterial` 드롭은 소비처 0 확인 후.
- **브랜치:** main 에서 분기한 새 브랜치 `feat/bom-redesign` 에서 작업. 일시중단된 `feat/cable-c5` 브랜치는 **건드리지 않는다**(본 작업 머지 후 C5 가 rebase).
- **회귀 게이트:** 백엔드 변경 → `cd backend && npx tsc --noEmit && npx vitest run`. 프론트 변경 → `cd frontend && npx tsc --noEmit && npx vitest run`(현재 497 기준, 태스크별 증감).
- **UI 규칙:** `frontend/src/components/ui/CONVENTIONS.md` 준수 — `DetailCard`·`SectionItem`·`FormRow`·`EditableField`·`Button` 재사용, `text-xs` 미만 금지.
- **고정 5분류 + 색**: 전원 `POWER` `#ef4444`, 네트워크 `NETWORK` `#3b82f6`, 광 `FIBER` `#22c55e`, 제어 `CONTROL` `#6b7280`, 접지 `GROUND` `#eab308`.
- **케이블 노무규칙 초기값**(m당 install/remove, laborType): 전원 0.03/0.015 통신내선공, 네트워크 0.02/0.01 통신내선공, 광 0.04/0.02 통신외선공, 제어 0.025/0.012 통신내선공, 접지 0.02/0.01 통신내선공.
- **비목표:** `specParams` 불변, 노무 단가(₩) 없음, 부속자재 재도입 없음, surcharge 규칙(`SURCHARGE_RULES`)은 현행 유지.

---

## File Structure

- `backend/prisma/schema.prisma` — `CableGroup`(+kind, 노무 m당 컬럼)·`AssetType`(노무 개당 컬럼) 추가; `BomMaterial` 모델 삭제(Task 6).
- `backend/prisma/migrations/<ts>_bom_labor_rules/` , `<ts>_drop_bom_materials/` — 마이그레이션.
- `backend/prisma/seed/cableCategories.ts` — `CableGroup` 5행 kind+노무규칙 시드.
- `backend/prisma/seed/assetTypes.ts` — 설비 종류 노무규칙 시드.
- `backend/src/services/cableGroup.service.ts` / `assetType.service.ts` — DTO 에 노무규칙 노출.
- `backend/src/services/constructionReport.service.ts` — DB 규칙 기반 재계산(코드/부속자재/템플릿 제거).
- `backend/src/config/constructionTemplates.ts` — `SURCHARGE_RULES`만 남기고 나머지 삭제.
- `backend/src/schemas/reportPreview.schema.ts` — 스냅샷 스키마(케이블 categoryId, 설비 assetTypeId).
- `backend/src/services/{bomMaterial.service,bom...}` , `controllers/bomMaterial.controller.ts`, `routes/bomMaterials.routes.ts`, `index.ts` — `BomMaterial` 제거(Task 6).
- `backend/src/services/catalogCommit.service.ts` — cableGroups/assetTypes 노무규칙 patch.
- `frontend/src/features/catalog/catalogStore.ts` — 노무규칙 patch 타입/payload.
- `frontend/src/features/catalog/CableTypesTab.tsx` — 고정 5분류 + 노무규칙 편집 + 이름 CRUD.
- `frontend/src/features/catalog/AssetTypesTab.tsx` — 종류 노무규칙 필드.
- `frontend/src/features/report/overlayToChanges.ts` , `frontend/src/types/constructionReport.ts` — 스냅샷 categoryId/assetTypeId.
- `frontend/src/types/{cableGroup,asset}.ts` — 노무규칙 필드.
- `frontend/src/types/bomMaterial.ts` — 삭제(Task 6).

---

## Task 1: 스키마 — 노무규칙 컬럼 + CableGroup.kind + 마이그레이션

**Files:**
- Modify: `backend/prisma/schema.prisma` (`model CableGroup` 216-228, `model AssetType` 414-439)
- Create: `backend/prisma/migrations/<ts>_bom_labor_rules/migration.sql` (prisma 생성)

**Interfaces:**
- Produces: `CableGroup { kind String? @unique, laborType String?, installHoursPerMeter Float?, removeHoursPerMeter Float?, relocateHoursPerMeter Float? }`; `AssetType { laborType String?, installHoursPerUnit Float?, removeHoursPerUnit Float?, relocateHoursPerUnit Float? }`. Task 2(시드)·3(DTO)·4(보고서)가 소비.

- [ ] **Step 1: CableGroup 에 컬럼 추가**

`schema.prisma` 의 `model CableGroup` 에 `categories CableCategory[]` 위에 추가:

```prisma
  kind                 String? @unique @db.VarChar(20) // POWER|NETWORK|FIBER|CONTROL|GROUND (고정 5분류 식별)
  laborType            String? @map("labor_type") @db.VarChar(20)
  installHoursPerMeter Float?  @map("install_hours_per_meter")
  removeHoursPerMeter  Float?  @map("remove_hours_per_meter")
  relocateHoursPerMeter Float? @map("relocate_hours_per_meter")
```

- [ ] **Step 2: AssetType 에 컬럼 추가**

`model AssetType` 에 `assets Asset[]` 위에 추가:

```prisma
  laborType            String? @map("labor_type") @db.VarChar(20)
  installHoursPerUnit  Float?  @map("install_hours_per_unit")
  removeHoursPerUnit   Float?  @map("remove_hours_per_unit")
  relocateHoursPerUnit Float?  @map("relocate_hours_per_unit")
```

- [ ] **Step 3: 마이그레이션 생성·적용**

```bash
cd backend && docker compose -f ../docker-compose.dev.yml up -d && npx prisma migrate dev --name bom_labor_rules
```
Expected: 마이그레이션 생성 + dev DB 적용 + 클라이언트 재생성. SQL 에 `ALTER TABLE "cable_groups" ADD COLUMN "kind" ...`·노무 컬럼들, `ALTER TABLE "asset_types" ADD COLUMN ...` 포함. 전부 nullable.

- [ ] **Step 4: 검증**

Run: `cd backend && npx tsc --noEmit && npx vitest run`
Expected: tsc 0, 기존 테스트 전수 PASS(컬럼 추가는 비파괴).

- [ ] **Step 5: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/migrations
git commit -m "feat(db): CableGroup.kind + 케이블/설비 노무규칙 컬럼 (BOM 재설계)"
```

---

## Task 2: 노무규칙 시드 (5분류 + 설비 종류)

**Files:**
- Modify: `backend/prisma/seed/cableCategories.ts` (GROUP_COLORS 344-346, seedCableCategories 348-384)
- Modify: `backend/prisma/seed/assetTypes.ts` (ASSET_TYPE_SEEDS 69-146, seedAssetTypes 148-173)

**Interfaces:**
- Consumes: Task 1 의 컬럼.
- Produces: `CableGroup` 5행에 kind+노무규칙, 설비 종류에 노무규칙 시드. 멱등.

- [ ] **Step 1: 케이블 그룹 kind+노무 시드 맵 정의**

`cableCategories.ts` 의 `GROUP_COLORS`(344-346) 옆에 추가:

```ts
// 분류명 → { kind, color, laborType, install/remove m당 시간 } (고정 5분류)
const GROUP_RULES: Record<string, { kind: string; color: string; laborType: string; install: number; remove: number }> = {
  '전원':     { kind: 'POWER',   color: '#ef4444', laborType: '통신내선공', install: 0.03,  remove: 0.015 },
  '네트워크': { kind: 'NETWORK', color: '#3b82f6', laborType: '통신내선공', install: 0.02,  remove: 0.01 },
  '광':       { kind: 'FIBER',   color: '#22c55e', laborType: '통신외선공', install: 0.04,  remove: 0.02 },
  '제어':     { kind: 'CONTROL', color: '#6b7280', laborType: '통신내선공', install: 0.025, remove: 0.012 },
  '접지':     { kind: 'GROUND',  color: '#eab308', laborType: '통신내선공', install: 0.02,  remove: 0.01 },
};
```

- [ ] **Step 2: 그룹 upsert 를 kind+노무 포함으로 교체**

`seedCableCategories`(348-384)의 그룹 upsert 단계(350-361)를 다음으로 교체(기존 displayGroup 이름 5종 순회):

```ts
const groupIdByName = new Map<string, string>();
let order = 1;
for (const [name, r] of Object.entries(GROUP_RULES)) {
  const g = await prisma.cableGroup.upsert({
    where: { name },
    update: { kind: r.kind, color: r.color, laborType: r.laborType, installHoursPerMeter: r.install, removeHoursPerMeter: r.remove, sortOrder: order },
    create: { name, kind: r.kind, color: r.color, laborType: r.laborType, installHoursPerMeter: r.install, removeHoursPerMeter: r.remove, sortOrder: order },
  });
  groupIdByName.set(name, g.id);
  order++;
}
```

카테고리 upsert 단계(364-382)는 `groupIdByName.get(cat.displayGroup)` 로 groupId 를 잇는 기존 동작 유지.

- [ ] **Step 3: 설비 노무 시드 맵 정의**

`assetTypes.ts` 의 `ASSET_TYPE_SEEDS` 위에 추가(코드→개당 노무; 기존 EQP 템플릿 값에서 도출, 매핑 없는 종류는 미등재=null):

```ts
// 설비 종류코드 → { laborType, install/remove/relocate 개당 시간 } (기존 EQP-* 템플릿에서 도출)
const ASSET_LABOR: Record<string, { laborType: string; install: number; remove: number; relocate?: number }> = {
  RACK:   { laborType: '통신내선공', install: 2.0, remove: 1.0, relocate: 3.0 },
  OFD:    { laborType: '통신내선공', install: 1.5, remove: 0.8 },
  RTU:    { laborType: '통신내선공', install: 4.0, remove: 2.0, relocate: 5.0 },
  SCADA:  { laborType: '통신내선공', install: 4.0, remove: 2.0, relocate: 5.0 },
  UPS:    { laborType: '통신내선공', install: 1.0, remove: 0.5, relocate: 1.5 },
  'NET-SW': { laborType: '통신내선공', install: 0.5, remove: 0.3 },
  'OPT-SWITCH': { laborType: '통신내선공', install: 0.5, remove: 0.3 },
  UTM:    { laborType: '통신내선공', install: 1.0, remove: 0.5 },
  NAC:    { laborType: '통신내선공', install: 1.0, remove: 0.5 },
  'PITR-2000': { laborType: '통신내선공', install: 3.0, remove: 1.5 },
  'PITR-5000': { laborType: '통신내선공', install: 3.0, remove: 1.5 },
  SPD:    { laborType: '통신내선공', install: 0.3, remove: 0.15 },
};
```

> 매핑 없는 종류(예: BATTERY·CHARGER·OPT-COT 등)는 노무 null = 시공보고서 노무 0 기여. 관리자가 자산관리에서 채운다.

- [ ] **Step 4: seedAssetTypes 의 type upsert 에 노무 주입**

`seedAssetTypes`(148-173)의 종류 upsert `data` 에 노무 필드 추가(create·update 양쪽):

```ts
const lab = ASSET_LABOR[t.code];
// ...upsert data 에 펼침:
...(lab ? { laborType: lab.laborType, installHoursPerUnit: lab.install, removeHoursPerUnit: lab.remove, relocateHoursPerUnit: lab.relocate ?? null } : {}),
```

- [ ] **Step 5: 시드 실행 검증**

```bash
cd backend && npx prisma db seed
```
Expected: 에러 없이 완료. 재실행(멱등) 시 충돌 없음. `psql` 또는 prisma studio 로 `cable_groups` 5행에 kind·install_hours_per_meter 채워짐 확인(선택).

- [ ] **Step 6: Commit**

```bash
git add backend/prisma/seed/cableCategories.ts backend/prisma/seed/assetTypes.ts
git commit -m "feat(seed): 5분류 kind+노무규칙, 설비 종류 노무규칙 시드 (BOM 재설계)"
```

---

## Task 3: DTO 에 노무규칙 노출 (cableGroup + assetType)

**Files:**
- Modify: `backend/src/services/cableGroup.service.ts` (`CableGroupDetail` + select/매핑)
- Modify: `backend/src/services/assetType.service.ts` (`AssetTypeDetail` 25-41 + 매핑)
- Test: `backend/tests/cableGroup.service.test.ts` (없으면 생성)

**Interfaces:**
- Produces: `CableGroupDetail { ..., kind: string|null, laborType: string|null, installHoursPerMeter: number|null, removeHoursPerMeter: number|null, relocateHoursPerMeter: number|null }`; `AssetTypeDetail { ..., laborType: string|null, installHoursPerUnit: number|null, removeHoursPerUnit: number|null, relocateHoursPerUnit: number|null }`. Task 7(commit)·8·9(UI)·4(보고서)가 소비.

- [ ] **Step 1: 실패 테스트 — cableGroup DTO 가 노무규칙 노출**

`backend/tests/cableGroup.service.test.ts` 에 추가(prisma 모킹 또는 기존 패턴 따라): `getAll()` 결과의 한 그룹이 `kind`·`installHoursPerMeter` 키를 포함하는지 단언.

```ts
const groups = await cableGroupService.getAll();
expect(groups[0]).toHaveProperty('kind');
expect(groups[0]).toHaveProperty('installHoursPerMeter');
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && npx vitest run tests/cableGroup.service.test.ts`
Expected: FAIL(현 DTO 에 해당 키 없음).

- [ ] **Step 3: cableGroup.service DTO 확장**

`CableGroupDetail` 인터페이스에 `kind`·`laborType`·`installHoursPerMeter`·`removeHoursPerMeter`·`relocateHoursPerMeter` 추가. `getAll`/`getById` 의 findMany 가 기본 전체 컬럼을 반환하면 매핑에 그대로 통과(명시 select 면 필드 추가). 매핑에 `kind: g.kind ?? null` 등 추가.

- [ ] **Step 4: assetType.service DTO 확장**

`AssetTypeDetail`(25-41)에 `laborType`·`installHoursPerUnit`·`removeHoursPerUnit`·`relocateHoursPerUnit` 추가, 매핑에 `laborType: a.laborType ?? null` 등 추가.

- [ ] **Step 5: 검증**

Run: `cd backend && npx tsc --noEmit && npx vitest run tests/cableGroup.service.test.ts && npx vitest run`
Expected: 새 테스트 PASS, 전체 PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/src/services/cableGroup.service.ts backend/src/services/assetType.service.ts backend/tests/cableGroup.service.test.ts
git commit -m "feat(catalog): cableGroup/assetType DTO 에 노무규칙 노출 (BOM 재설계)"
```

---

## Task 4: 시공보고서 재계산 — DB 규칙 기반 (코드/부속자재/템플릿 제거)

**Files:**
- Modify: `backend/src/services/constructionReport.service.ts` (전반)
- Modify: `backend/src/config/constructionTemplates.ts` (SURCHARGE_RULES 만 남김)
- Modify: `backend/src/schemas/reportPreview.schema.ts` (cableSnapshotItem 31-41, equipmentSnapshotItem 17-29)
- Test: `backend/tests/constructionReport.service.test.ts` (없으면 생성)

**Interfaces:**
- Consumes: `CableGroupDetail`/`AssetTypeDetail` 노무규칙(Task 3), DB.
- Produces: `PlanSnapshot.cables[] = { id, categoryId, name, totalLength, sourceAssetId, targetAssetId }`(no cableType/code/label); `PlanSnapshot.equipment[] = { id, name, assetTypeId, specParams?, positionX?, positionY? }`(no materialCategoryCode). `calculateConstructionReport(before, after, ruleCtx, overrides?)` 가 DB 규칙으로 노무·자재 계산. Task 5(프론트 스냅샷)가 이 모양으로 보냄.

- [ ] **Step 1: 실패 테스트 — 노무가 DB 규칙에서 산출, 부속자재 없음**

`backend/tests/constructionReport.service.test.ts` 작성: 케이블 1본(categoryId→FIBER 규칙 install 0.04/m, 길이 100m) install diff → 노무 4.0h(통신외선공), 자재 줄 1개(name·unit 'm'·qty 100), **부속자재 0**. 설비 1대(assetTypeId→install 2.0/unit) → 노무 2.0h. 규칙 컨텍스트는 테스트가 주입(아래 ruleCtx 모양).

```ts
const ruleCtx = {
  cableRuleByCategoryId: new Map([['catFiber', { groupName: '광', kind: 'FIBER', laborType: '통신외선공', installHoursPerMeter: 0.04, removeHoursPerMeter: 0.02, relocateHoursPerMeter: null }]]),
  cableNameByCategoryId: new Map([['catFiber', 'OPGW']]),
  equipRuleByTypeId: new Map([['typeRack', { name: '랙', laborType: '통신내선공', installHoursPerUnit: 2.0, removeHoursPerUnit: 1.0, relocateHoursPerUnit: 3.0 }]]),
};
const before = { equipment: [], cables: [] };
const after = {
  equipment: [{ id: 'e1', name: '랙1', assetTypeId: 'typeRack' }],
  cables: [{ id: 'c1', categoryId: 'catFiber', name: 'OPGW', totalLength: 10000 /* cm */, sourceAssetId: 'a', targetAssetId: 'b' }],
};
const r = calculateConstructionReport(before, after, ruleCtx);
expect(r.labor.find(l => l.laborType === '통신외선공')?.hours).toBeCloseTo(4.0); // 0.04 * 100m
expect(r.labor.find(l => l.workName.includes('랙'))?.hours).toBeCloseTo(2.0);
expect(r.bom.every(b => !('isAccessory' in b) || b.isAccessory === false)).toBe(true);
expect(r.bom.some(b => b.name === 'OPGW' && b.unit === 'm')).toBe(true);
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && npx vitest run tests/constructionReport.service.test.ts`
Expected: FAIL(현재 시그니처 `calculateConstructionReport(before, after, overrides?)` + 템플릿 기반).

- [ ] **Step 3: 타입·스냅샷 정리**

`constructionReport.service.ts`:
- `PlanSnapshot.cables[]` 를 `{ id: string; categoryId: string | null; name: string; totalLength?: number | null; sourceAssetId: string; targetAssetId: string }` 로 교체(cableType/materialCategoryCode/materialCategoryName/specification/label 제거).
- `PlanSnapshot.equipment[]` 에서 `materialCategoryCode`/`materialCategoryName`/`specification` 제거(`assetTypeId`·`name`·`specParams`·position 유지).
- `DiffItem` 에 `categoryId?: string|null`·`assetTypeId?: string|null` 추가, `materialCategoryCode` 제거. `BOMItem` 에서 `materialCategoryCode`/`isAccessory` 제거, `key: string`(집계 키)·`name`·`unit`·`quantity`·`action` 유지.

- [ ] **Step 4: 규칙 컨텍스트 타입 + 계산 재작성**

`constructionReport.service.ts` 상단에 추가:

```ts
export interface CableRule { groupName: string; kind: string | null; laborType: string | null; installHoursPerMeter: number | null; removeHoursPerMeter: number | null; relocateHoursPerMeter: number | null }
export interface EquipRule { name: string; laborType: string | null; installHoursPerUnit: number | null; removeHoursPerUnit: number | null; relocateHoursPerUnit: number | null }
export interface RuleContext {
  cableRuleByCategoryId: Map<string, CableRule>;
  cableNameByCategoryId: Map<string, string>;
  equipRuleByTypeId: Map<string, EquipRule>;
}
```

`computeBOM`/`addAccessory`/`computeLabor`/`resolveDisplayName`/`normalizeEquipmentCodes` 를 삭제하고 다음으로 교체:

```ts
function hoursFor(action: DiffAction, install: number | null, remove: number | null, relocate: number | null): number | null {
  if (action === 'install') return install;
  if (action === 'remove') return remove;
  if (action === 'relocate') return relocate ?? install;
  return null; // modify 등
}

function computeCableLabor(diff: DiffItem[], ctx: RuleContext): LaborItem[] {
  const map = new Map<string, LaborItem>();
  for (const it of diff) {
    if (it.type !== 'cable' || it.action === 'modify' || !it.categoryId || !it.length) continue;
    const r = ctx.cableRuleByCategoryId.get(it.categoryId);
    if (!r || !r.laborType) continue;
    const perM = hoursFor(it.action, r.installHoursPerMeter, r.removeHoursPerMeter, r.relocateHoursPerMeter);
    if (!perM) continue;
    const hours = perM * it.length;
    const workName = `${r.groupName} ${it.action === 'install' ? '포설' : it.action === 'remove' ? '철거' : '이설'}`;
    upsertLabor(map, workName, r.laborType, hours);
  }
  return [...map.values()];
}

function computeEquipmentLabor(diff: DiffItem[], ctx: RuleContext): LaborItem[] {
  const map = new Map<string, LaborItem>();
  for (const it of diff) {
    if (it.type !== 'equipment' || it.action === 'modify' || !it.assetTypeId) continue;
    const r = ctx.equipRuleByTypeId.get(it.assetTypeId);
    if (!r || !r.laborType) continue;
    const perU = hoursFor(it.action, r.installHoursPerUnit, r.removeHoursPerUnit, r.relocateHoursPerUnit);
    if (!perU) continue;
    const hours = perU * it.quantity;
    const workName = `${r.name} ${it.action === 'install' ? '설치' : it.action === 'remove' ? '철거' : '이설'}`;
    upsertLabor(map, workName, r.laborType, hours);
  }
  return [...map.values()];
}

function upsertLabor(map: Map<string, LaborItem>, workName: string, laborType: string, hours: number) {
  if (hours <= 0) return;
  const key = `${workName}|${laborType}`;
  const ex = map.get(key);
  if (ex) ex.hours += hours; else map.set(key, { workName, laborType, hours });
}

function computeMaterial(diff: DiffItem[]): BOMItem[] {
  const map = new Map<string, BOMItem>();
  for (const it of diff) {
    if (it.action === 'modify') continue;
    const key = `${it.type}:${it.categoryId ?? it.assetTypeId ?? it.id}:${it.action}`;
    const qty = it.length ?? it.quantity;
    const ex = map.get(key);
    if (ex) ex.quantity += qty;
    else map.set(key, { key, name: it.name, action: it.action, quantity: qty, unit: it.unit, isManual: false });
  }
  return [...map.values()];
}
```

`calculateConstructionReport` 시그니처를 `(before, after, ctx: RuleContext, overrides?)` 로 바꾸고, `bom = computeMaterial(diff)`, `labor = [...computeCableLabor(diff, ctx), ...computeEquipmentLabor(diff, ctx)]`. `overrides` 의 `removedItemIds`/`modifiedItems` 매칭 키를 `materialCategoryCode` → BOMItem.`key` 로 변경. `addedItems` 는 `{ key: 'MANUAL', name: added.description, ... }`. `BOMItem` 에서 `materialCategoryCode` 참조 전부 `key` 로.

`computeCableDiff`/`computeEquipmentDiff` 는 `materialCategoryCode` 대신 `categoryId`/`assetTypeId` 를 `DiffItem` 에 채우고 `name` 은 스냅샷의 `name`(케이블=category name, 설비=name)로. cm→m 변환(`totalLength/100`) 유지.

- [ ] **Step 5: reportPreview 가 규칙 컨텍스트를 DB 에서 로드**

`reportPreview`(477-504)에서 기존 assetType 로드 옆에 `cableCategory.findMany({ include: { group: true } })` 로 `RuleContext` 3개 맵을 구성해 `calculateConstructionReport(before, after, ctx, overrides)` 호출. `normalizeEquipmentCodes` 호출 제거.

- [ ] **Step 6: 스키마 정리**

`reportPreview.schema.ts`:
- `cableSnapshotItem`(31-41) → `{ id, categoryId: z.string().nullable().optional(), name: z.string(), totalLength: z.number().nullable().optional(), sourceAssetId, targetAssetId }` (cableType/materialCategoryCode/materialCategoryName/specification/label 제거).
- `equipmentSnapshotItem`(17-29) → `materialCategoryCode`/`materialCategoryName`/`specification` 제거(`assetTypeId`·`name`·`specParams`·position 유지).

- [ ] **Step 7: constructionTemplates.ts 축소**

`backend/src/config/constructionTemplates.ts` 에서 `LaborRule`·`AccessoryRule`·`ConstructionTemplate`·`EQUIPMENT_TEMPLATES`·`CABLE_TEMPLATES`·`CONSTRUCTION_TEMPLATES`·`resolveEquipmentConstructionCode` 삭제. `SurchargeRule`·`SURCHARGE_RULES` 만 남긴다. 파일 상단 주석 갱신.

- [ ] **Step 8: 테스트 통과 + 회귀**

Run: `cd backend && npx tsc --noEmit && npx vitest run tests/constructionReport.service.test.ts && npx vitest run`
Expected: 새 테스트 PASS, 전체 PASS(기존 보고서 테스트가 templates/snapshot 형태에 의존하면 그 픽스처를 categoryId/assetTypeId 형태로 갱신).

- [ ] **Step 9: Commit**

```bash
git add backend/src/services/constructionReport.service.ts backend/src/config/constructionTemplates.ts backend/src/schemas/reportPreview.schema.ts backend/tests/constructionReport.service.test.ts
git commit -m "feat(report)!: 시공보고서 DB 노무규칙 기반 재계산 — 코드/부속자재/템플릿 제거 (BOM 재설계)"
```

---

## Task 5: 프론트 스냅샷 — overlayToChanges + 타입 정렬

**Files:**
- Modify: `frontend/src/features/report/overlayToChanges.ts` (케이블/설비 스냅샷 매핑)
- Modify: `frontend/src/types/constructionReport.ts` (스냅샷 타입)
- Test: `frontend/src/features/report/overlayToChanges.test.ts`

**Interfaces:**
- Consumes: Task 4 의 스냅샷 모양(케이블 categoryId+name+totalLength, 설비 assetTypeId+name).
- Produces: report-preview 로 보내는 스냅샷이 categoryId/assetTypeId 기반. (C5 A4 회귀가 여기서 정의대로 정리됨.)

- [ ] **Step 1: 실패 테스트 갱신**

`overlayToChanges.test.ts` 의 케이블 단언을 `materialCategoryCode` → `categoryId` 로 변경: 결과 케이블 항목이 `categoryId`(= local.categoryId)·`name`(= categoryName)·`totalLength` 를 갖고, `materialCategoryCode`/`cableType`/`label` 키가 **없는지** 단언.

```ts
const cable = changes.cables.find(c => c.id === 'c1')!;
expect(cable.categoryId).toBe('cat-fiber');
expect(cable).not.toHaveProperty('materialCategoryCode');
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/features/report/overlayToChanges.test.ts`
Expected: FAIL.

- [ ] **Step 3: overlayToChanges 케이블 매핑 교체**

케이블 변환에서 `materialCategoryCode`/`cableType`/`label` 출력을 제거하고 `categoryId: local.categoryId ?? null`, `name: local.categoryName ?? ''`, `totalLength: local.totalLength ?? null` 로 교체. 설비 변환은 `materialCategoryCode` 대신 `assetTypeId: <asset.assetTypeId>`·`name` 전송(설비 스냅샷이 이미 assetTypeId 를 가지면 유지).

- [ ] **Step 4: constructionReport.ts 타입 정렬**

`frontend/src/types/constructionReport.ts` 의 케이블 스냅샷 타입에서 `cableType`/`materialCategoryCode`/`label` 제거, `categoryId: string | null`·`name: string`·`totalLength?: number | null` 추가. 설비 스냅샷 타입에서 `materialCategoryCode` 제거. `BOMItem` 표시 타입은 `materialCategoryCode` → `key`(또는 표시에 불필요하면 제거)로.

- [ ] **Step 5: 검증**

Run: `cd frontend && npx tsc --noEmit && npx vitest run`
Expected: tsc 0, 전체 PASS(ReportView 등이 `materialCategoryCode` 표시에 의존하면 `name`/`key` 로 교체).

- [ ] **Step 6: Commit**

```bash
git add frontend/src/features/report/overlayToChanges.ts frontend/src/types/constructionReport.ts frontend/src/features/report/overlayToChanges.test.ts
git commit -m "feat(report): 스냅샷을 categoryId/assetTypeId 기반으로 (BOM 재설계)"
```

---

## Task 6: BomMaterial 테이블·route·타입 제거

**Files:**
- Delete: `backend/src/services/bomMaterial.service.ts`, `backend/src/controllers/bomMaterial.controller.ts`, `backend/src/routes/bomMaterials.routes.ts`, `frontend/src/types/bomMaterial.ts`
- Modify: `backend/src/index.ts` (25, 99), `frontend/src/types/material.ts` (bomMaterial 참조 주석)
- Modify: `backend/prisma/schema.prisma` (`model BomMaterial` 234-253 삭제)
- Create: `backend/prisma/migrations/<ts>_drop_bom_materials/migration.sql`

**Interfaces:**
- Consumes: 없음(소비처 0 — 보고서가 더는 BomMaterial 참조 안 함, Task 4 후).
- Produces: `BomMaterial` 완전 제거.

- [ ] **Step 1: 소비처 0 확인**

```bash
cd /Users/jsk/1210/digital && grep -rn "BomMaterial\|bomMaterial\|bom-materials\|bom_materials" backend/src frontend/src | grep -v "bomMaterial.service\|bomMaterial.controller\|bomMaterials.routes\|types/bomMaterial"
```
Expected: `index.ts`(import/register)·`material.ts`(주석) 외 실사용 0. 있으면 그 소비처부터 정리.

- [ ] **Step 2: route 등록 제거**

`backend/src/index.ts` 의 25행 import 와 99행 `app.use('/api/bom-materials', ...)` 삭제.

- [ ] **Step 3: 파일 삭제**

```bash
cd /Users/jsk/1210/digital
git rm backend/src/services/bomMaterial.service.ts backend/src/controllers/bomMaterial.controller.ts backend/src/routes/bomMaterials.routes.ts frontend/src/types/bomMaterial.ts
```
`frontend/src/types/material.ts` 에서 BomMaterial 언급 주석/참조 정리.

- [ ] **Step 4: 스키마에서 모델 삭제 + 마이그레이션**

`schema.prisma` 의 `model BomMaterial`(234-253) 삭제. 그 다음:
```bash
cd backend && npx prisma migrate dev --name drop_bom_materials
```
Expected: `DROP TABLE "bom_materials"` 마이그레이션 생성·적용.

- [ ] **Step 5: 검증**

Run: `cd backend && npx tsc --noEmit && npx vitest run` 그리고 `cd ../frontend && npx tsc --noEmit && npx vitest run`
Expected: 양쪽 tsc 0, 전수 PASS.

- [ ] **Step 6: Commit**

```bash
git add -A backend/src/index.ts backend/prisma frontend/src/types/material.ts
git commit -m "refactor(bom)!: BomMaterial 테이블/route/타입 제거 (BOM 재설계)"
```

---

## Task 7: catalog commit + store 노무규칙 patch

**Files:**
- Modify: `backend/src/services/catalogCommit.service.ts` (`CatalogCommitInput` 6-11, cableGroups/assetTypes update 80-98)
- Modify: `frontend/src/features/catalog/catalogStore.ts` (`CgP` 24, `TypeP` 22, commit payload 110-136)
- Modify: `frontend/src/types/cableGroup.ts`, `frontend/src/types/asset.ts` (노무 필드)
- Test: `backend/tests/catalogCommit.service.test.ts`, `frontend/src/features/catalog/catalogStore.test.ts`

**Interfaces:**
- Consumes: Task 1 컬럼, Task 3 DTO.
- Produces: 카탈로그 저장이 cableGroups/assetTypes 의 노무규칙 patch 를 반영. `CgP`/`TypeP` 에 노무 필드. Task 8·9(UI)가 소비.

- [ ] **Step 1: 타입에 노무 필드 추가**

`frontend/src/types/cableGroup.ts` 의 `CableGroup` 에 `kind: string | null`·`laborType: string | null`·`installHoursPerMeter: number | null`·`removeHoursPerMeter: number | null`·`relocateHoursPerMeter: number | null` 추가. `frontend/src/types/asset.ts` 의 `AssetType` 에 `laborType: string | null`·`installHoursPerUnit: number | null`·`removeHoursPerUnit: number | null`·`relocateHoursPerUnit: number | null` 추가.

- [ ] **Step 2: 실패 테스트 — commit 이 노무 patch 전달**

`catalogStore.test.ts` 에 추가: `stageUpdateCableGroup(id, { installHoursPerMeter: 0.05 })` 후 `commit()` → `api.post('/catalog/commit', ...)` 의 `cableGroups.updates[0].patch.installHoursPerMeter === 0.05` 단언.

- [ ] **Step 3: 실패 확인**

Run: `cd frontend && npx vitest run src/features/catalog/catalogStore.test.ts`
Expected: FAIL(`CgP` 가 노무 필드를 허용 안 함 → tsc/런타임).

- [ ] **Step 4: store patch 타입 + payload 확장**

`catalogStore.ts`:
- `CgP = Partial<Pick<CableGroup, 'name' | 'color' | 'sortOrder' | 'laborType' | 'installHoursPerMeter' | 'removeHoursPerMeter' | 'relocateHoursPerMeter'>>`.
- `TypeP = Partial<Pick<AssetType, 'name' | 'categoryId' | 'laborType' | 'installHoursPerUnit' | 'removeHoursPerUnit' | 'relocateHoursPerUnit'>>`.
- commit payload(110-136)의 cableGroups.updates·assetTypes.updates 는 `patch: u.patch` 를 그대로 보내므로 추가 매핑 불필요(확인).

- [ ] **Step 5: 백엔드 commit 이 노무 patch 적용**

`catalogCommit.service.ts`:
- `CatalogCommitInput.cableGroups` 의 update patch 타입에 노무 필드 추가, update 블록(82행 부근)에서 `...(u.patch.installHoursPerMeter !== undefined ? { installHoursPerMeter: u.patch.installHoursPerMeter } : {})` 식으로 4개 노무 필드 반영.
- `assetTypes` update 블록에도 동일하게 개당 노무 4필드 반영.

- [ ] **Step 6: 백엔드 테스트**

`catalogCommit.service.test.ts` 에 cableGroup update 가 `installHoursPerMeter` 를 tx update data 에 넣는지 검증 추가.

- [ ] **Step 7: 검증**

Run: `cd frontend && npx tsc --noEmit && npx vitest run src/features/catalog/catalogStore.test.ts` + `cd ../backend && npx tsc --noEmit && npx vitest run tests/catalogCommit.service.test.ts`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add backend/src/services/catalogCommit.service.ts frontend/src/features/catalog/catalogStore.ts frontend/src/types/cableGroup.ts frontend/src/types/asset.ts backend/tests/catalogCommit.service.test.ts frontend/src/features/catalog/catalogStore.test.ts
git commit -m "feat(catalog): commit/store 에 노무규칙 patch (BOM 재설계)"
```

---

## Task 8: 케이블종류 탭 — 고정 5분류 + 노무규칙 편집

**Files:**
- Modify: `frontend/src/features/catalog/CableTypesTab.tsx`
- Test: `frontend/src/features/catalog/CableTypesTab.test.tsx`

**Interfaces:**
- Consumes: `effectiveCableGroups()`(5 고정, 노무 필드 포함), stage 액션(Task 7 `CgP`).
- Produces: 분류는 읽기전용(이름+색), 분류별 노무규칙 인라인 편집, 분류 아래 이름 CRUD. 그룹 추가/삭제/색편집 없음.

- [ ] **Step 1: 실패 테스트**

`CableTypesTab.test.tsx` 갱신: (a) '+ 그룹' 버튼이 **없고**(고정 5), (b) 그룹 헤더에 색 스와치+읽기전용 이름, (c) 분류 노무 입력(예: aria-label '설치(m당)')에 값 입력 시 `stageUpdateCableGroup(id, { installHoursPerMeter })` 호출, (d) '+ 종류'/이름 편집은 유지.

```tsx
expect(screen.queryByText('+ 그룹')).toBeNull();
fireEvent.change(screen.getAllByLabelText('설치(m당)')[0], { target: { value: '0.05' } });
// store 의 cgOverlay 에 installHoursPerMeter 0.05 반영 확인
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/features/catalog/CableTypesTab.test.tsx`
Expected: FAIL.

- [ ] **Step 3: 구현**

`CableTypesTab.tsx`:
- '+ 그룹' 버튼·`stageCreateCableGroup`·그룹 onDelete·색 `input[type=color]`(편집) 제거. 그룹 헤더는 색 스와치(읽기전용 div) + 이름 텍스트(읽기전용).
- 헤더 우측에 노무규칙 편집행: `FormRow` 로 `laborType`(EditableField text/select), `installHoursPerMeter`·`removeHoursPerMeter`(number EditableField, aria-label '설치(m당)'/'철거(m당)') → `stageUpdateCableGroup(g.id, { ... })`.
- 분류 아래 이름(CableCategory) 추가/편집/삭제는 현행 유지(`stageCreateCableCategory`/`stageUpdateCableCategory`/`stageDeleteCableCategory`).

- [ ] **Step 4: 통과 확인**

Run: `cd frontend && npx vitest run src/features/catalog/CableTypesTab.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/features/catalog/CableTypesTab.tsx frontend/src/features/catalog/CableTypesTab.test.tsx
git commit -m "feat(catalog): 케이블종류 탭 고정 5분류 + 노무규칙 편집 (BOM 재설계)"
```

---

## Task 9: 자산종류 탭 — 종류 노무규칙 필드

**Files:**
- Modify: `frontend/src/features/catalog/AssetTypesTab.tsx`
- Test: `frontend/src/features/catalog/AssetTypesTab.test.tsx`

**Interfaces:**
- Consumes: `effectiveTypes()`(노무 필드), stage 액션(`TypeP`).
- Produces: 종류 행에 개당 노무규칙(설치/철거 시간·노무종류) 편집.

- [ ] **Step 1: 실패 테스트**

`AssetTypesTab.test.tsx` 갱신: 종류 행의 노무 입력(aria-label '설치(개당)')에 값 입력 시 `stageUpdateType(id, { installHoursPerUnit })` 호출 단언.

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/features/catalog/AssetTypesTab.test.tsx`
Expected: FAIL.

- [ ] **Step 3: 구현**

`AssetTypesTab.tsx` 의 종류 행(비시스템·시스템 공통)에 노무 편집 컨트롤 추가: `installHoursPerUnit`·`removeHoursPerUnit`(number EditableField, aria-label '설치(개당)'/'철거(개당)'), `laborType`(text/select) → `stageUpdateType(t.id, { ... })`. 레이아웃은 `FormRow`/기존 행 패턴 재사용, `text-xs` 미만 금지.

- [ ] **Step 4: 통과 확인**

Run: `cd frontend && npx vitest run src/features/catalog/AssetTypesTab.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/features/catalog/AssetTypesTab.tsx frontend/src/features/catalog/AssetTypesTab.test.tsx
git commit -m "feat(catalog): 자산종류 탭 종류 노무규칙 편집 (BOM 재설계)"
```

---

## Task 10: 최종 회귀 + 데드코드 0 + 스모크

**Files:** 없음(검증 전용).

- [ ] **Step 1: 전체 빌드·테스트**

```bash
cd frontend && npx tsc --noEmit && npx vitest run && npm run build
cd ../backend && npx tsc --noEmit && npx vitest run
```
Expected: 전부 PASS, 프론트 build 성공.

- [ ] **Step 2: 데드코드 0 확인**

```bash
cd /Users/jsk/1210/digital
grep -rn "CONSTRUCTION_TEMPLATES\|resolveEquipmentConstructionCode\|BomMaterial\|isAccessory\|addAccessory\|materialCategoryCode" backend/src frontend/src | grep -v "\.test\."
```
Expected: 출력 없음(케이블/설비 보고서가 code·부속자재·BomMaterial 미참조).

- [ ] **Step 3: 스모크 (dev 서버)**

`docker compose -f docker-compose.dev.yml up -d` + `npm run dev`:
- 자산관리 케이블종류 탭: 5분류 고정 표시, 분류 노무규칙 편집·저장, 이름 추가/삭제.
- 자산관리 자산종류 탭: 종류 노무규칙 편집·저장.
- 케이블/설비 배치 후 시공보고서(ReportPanel): 노무비(시간)·자재산출이 분류·종류 규칙으로 산출, 부속자재 없음.

- [ ] **Step 4: 완료 — finishing-a-development-branch**

`superpowers:finishing-a-development-branch` 로 테스트 재확인 후 머지/푸시 옵션 제시. (머지 후 별도로 C5 `feat/cable-c5` 를 main 에 rebase 하여 재개.)

---

## 자기 점검 (작성자 기록)

- **스펙 커버리지:** §2(고정 5분류+kind, 이름 사용자정의) → Task 1·2·8. §3.1(부속자재/BomMaterial 제거) → Task 4·6. §3.2(케이블/설비 노무규칙 DB) → Task 1·2·3·4. §4(자산관리 UI) → Task 7·8·9. §5(BOM 계산·스냅샷) → Task 4·5. §6(스키마/시드/마이그레이션) → Task 1·2·6. §7(C5 unblock — code 미드롭, categoryId 스냅샷) → Task 4·5 + Global Constraints. §8(비목표) → SURCHARGE_RULES 유지(Task 4 Step 7), specParams 불변.
- **code 드롭 분리:** 본 플랜은 `CableCategory.code` 드롭 안 함(C5 담당). 보고서가 code 미참조가 되게만 함 — §7 의도.
- **타입 일관성:** `RuleContext`/`CableRule`/`EquipRule` 명칭이 Task 4(정의)→Task 4 Step5(reportPreview 구성)에서 일치. 노무 컬럼명(`installHoursPerMeter`/`installHoursPerUnit`)이 schema(1)→seed(2)→DTO(3)→report(4)→store/commit(7)→UI(8·9) 전 구간 동일. `BOMItem.key`(materialCategoryCode 대체)가 Task 4(정의)·5(프론트 표시)에서 일치.
- **플레이스홀더 스캔:** 없음(모든 코드 스텝에 구체 코드/명령/기대값).
