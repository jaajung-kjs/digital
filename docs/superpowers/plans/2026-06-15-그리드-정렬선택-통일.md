# 그리드 디자인·정렬·선택 통일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 현황 그리드(NodeStatusView)의 정렬·헤더·선택행 로직을 공유 모듈로 추출하고, 현황·케이블 그리드(ConnectionRegisterGrid) 양쪽이 동일하게 사용한다(단일 소스). 케이블 그리드에 컬럼 정렬(섹션별 적용)·정렬 헤더(Chevron)·`selectedAssetId` 기준 선택행(눌림)을 추가해 현황과 룩·정렬·선택 UX를 일치시킨다. 섹션 구조는 유지.

**Architecture:** 신규 `components/grid/`(compareBy·useGridSort·sortRows·SortableHeaderCell·rowClasses). ConnectionRegisterGrid 가 useGridSort + SortableHeaderCell + 섹션별 sortRows + RowView selectedAssetId 채택. `RegisterColumn` 에 `sortKey`/`sortType` 추가, 두 descriptor 가 정렬키 부여. NodeStatusView 가 자기 bespoke 정렬/헤더/선택행을 공유 모듈로 교체.

**Tech Stack:** React/TS/Zustand, Vitest, lucide-react.

**참조 spec:** `docs/superpowers/specs/2026-06-15-그리드-정렬선택-통일-design.md`. 추출원 `NodeStatusView.tsx`(compareBy:41-54, cycleSort:170-176, sortedItems:179-192, 헤더버튼:276-292, AssetRow 선택:78-82).

---

## 환경 규칙 (공통)
- 프론트 `cd frontend && npx tsc --noEmit && npm run build && npx vitest run`. 단일 `npx vitest run <file>`.
- 동작 보존: `NodeStatusView.test`(정렬/선택/필터)·`FiberRegisterView.test`·`PanelCircuitView.test` 통과.

## 현재 구조 (확인됨)
- `NodeStatusView`: bespoke `compareBy`(text ko/date/status, 빈값 뒤로)·`cycleSort`(3단계)·`sortedItems`(stable)·헤더 `<th><button>label+Chevron</button>`·`AssetRow` `selected? 'bg-info-bg shadow-[inset_3px_0_0_var(--primary)]' : 'hover:bg-surface-2 active:bg-surface-3'`. `useSelection()`(selectedAssetId). COLUMN_DEFS{label,type,accessor}.
- `ConnectionRegisterGrid`: TH/TD 상수, 섹션(container→child→table), `RowView` active=tracingCableId 만. 헤더 정적 텍스트(정렬 없음).
- `RegisterColumn`{label,width?,cell}. `RegisterSection`{key,title,usedLabel,rows}.

## File Structure
- `frontend/src/components/grid/compareBy.ts` (Create) + `.test.ts`.
- `frontend/src/components/grid/useGridSort.ts` (Create, sortRows 포함) + `.test.ts`.
- `frontend/src/components/grid/SortableHeaderCell.tsx` (Create) + `.test.tsx`.
- `frontend/src/components/grid/rowClasses.ts` (Create).
- `frontend/src/features/connections/registerGrid/registerTypes.ts` (Modify) — sortKey/sortType.
- `frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx` (Modify) — 정렬·헤더·선택행.
- `frontend/src/features/fiber/fiberRegisterDescriptor.tsx` (Modify) — sortKey.
- `frontend/src/features/power/powerRegisterDescriptor.tsx` (Modify) — sortKey.
- `frontend/src/features/assets/components/NodeStatusView.tsx` (Modify) — 공유 모듈 채택.

---

## Task 1: 공유 grid 모듈 (TDD)

**Files:** `frontend/src/components/grid/{compareBy.ts,useGridSort.ts,SortableHeaderCell.tsx,rowClasses.ts}` (+tests)

### Step 1: 실패 테스트 — Create `compareBy.test.ts` + `useGridSort.test.ts`:
`compareBy.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { compareBy, sortRows } from './useGridSort';

describe('compareBy', () => {
  it('text ko localeCompare', () => { expect(compareBy('text','가','나')).toBeLessThan(0); });
  it('number', () => { expect(compareBy('number', 2, 10)).toBeLessThan(0); });
  it('date', () => { expect(compareBy('date','2026-01-01','2026-02-01')).toBeLessThan(0); });
  it('빈값은 방향 무관 뒤로', () => {
    expect(compareBy('text', null, '가')).toBe(1);
    expect(compareBy('text', '가', '')).toBe(-1);
    expect(compareBy('text', null, null)).toBe(0);
  });
});
describe('sortRows', () => {
  const rows = [{ n: 3 }, { n: 1 }, { n: null as number | null }, { n: 2 }];
  it('asc: 빈값 뒤로·stable', () => {
    expect(sortRows(rows, (r) => r.n, 'number', 'asc').map((r) => r.n)).toEqual([1, 2, 3, null]);
  });
  it('desc: 빈값 여전히 뒤로', () => {
    expect(sortRows(rows, (r) => r.n, 'number', 'desc').map((r) => r.n)).toEqual([3, 2, 1, null]);
  });
});
```
`useGridSort.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useGridSort } from './useGridSort';

describe('useGridSort cycleSort', () => {
  it('asc→desc→해제, 다른 col→asc', () => {
    const { result } = renderHook(() => useGridSort());
    expect(result.current.sort).toBeNull();
    act(() => result.current.cycleSort('이름'));
    expect(result.current.sort).toEqual({ col: '이름', dir: 'asc' });
    act(() => result.current.cycleSort('이름'));
    expect(result.current.sort).toEqual({ col: '이름', dir: 'desc' });
    act(() => result.current.cycleSort('이름'));
    expect(result.current.sort).toBeNull();
    act(() => result.current.cycleSort('이름'));
    act(() => result.current.cycleSort('종류'));
    expect(result.current.sort).toEqual({ col: '종류', dir: 'asc' });
  });
});
```

### Step 2: 실패 확인 — `cd frontend && npx vitest run src/components/grid/` → FAIL.

### Step 3: 구현 —
Create `frontend/src/components/grid/useGridSort.ts` (compareBy/sortRows/useGridSort 한 파일):
```ts
import { useState } from 'react';

export type SortType = 'text' | 'date' | 'number' | 'status';
export type SortDir = 'asc' | 'desc';
export type GridSort = { col: string; dir: SortDir } | null;

/** 타입별 비교 — text=ko localeCompare, date=시간차, number=수치, status=문자. 빈값(null/'')은 방향 무관 항상 뒤로. */
export function compareBy(type: SortType, av: string | number | null, bv: string | number | null): number {
  const aEmpty = av == null || av === '';
  const bEmpty = bv == null || bv === '';
  if (aEmpty && bEmpty) return 0;
  if (aEmpty) return 1;
  if (bEmpty) return -1;
  if (type === 'number') return Number(av) - Number(bv);
  if (type === 'date') return new Date(av as string).getTime() - new Date(bv as string).getTime();
  if (type === 'text') return String(av).localeCompare(String(bv), 'ko');
  return String(av).localeCompare(String(bv));
}

/** stable 정렬(동값=원순서). 빈값은 compareBy 가 뒤로. */
export function sortRows<Row>(rows: Row[], accessor: (r: Row) => string | number | null, type: SortType, dir: SortDir): Row[] {
  const factor = dir === 'asc' ? 1 : -1;
  return rows
    .map((row, idx) => ({ row, idx }))
    .sort((a, b) => { const c = compareBy(type, accessor(a.row), accessor(b.row)); return c !== 0 ? c * factor : a.idx - b.idx; })
    .map((w) => w.row);
}

/** 3단계 정렬 상태(asc→desc→해제, 다른 col→asc). */
export function useGridSort() {
  const [sort, setSort] = useState<GridSort>(null);
  const cycleSort = (col: string) =>
    setSort((cur) => {
      if (!cur || cur.col !== col) return { col, dir: 'asc' };
      if (cur.dir === 'asc') return { col, dir: 'desc' };
      return null;
    });
  return { sort, cycleSort };
}
```
Create `frontend/src/components/grid/SortableHeaderCell.tsx`:
```tsx
import { ChevronUp, ChevronDown, ChevronsUpDown } from 'lucide-react';
import type { SortDir } from './useGridSort';

const padOf = (first?: boolean, last?: boolean) => (first ? 'pl-4 pr-2' : last ? 'px-2 pr-4' : 'px-2');

/** 현황·케이블 그리드 공용 헤더 셀. sortable=false 면 정적(클릭 무동작). */
export function SortableHeaderCell({ label, width, first, last, sortable = false, active = false, dir = 'asc', onClick }: {
  label: string; width?: string; first?: boolean; last?: boolean;
  sortable?: boolean; active?: boolean; dir?: SortDir; onClick?: () => void;
}) {
  const pad = padOf(first, last);
  if (!sortable) {
    return <th className={`text-[12px] font-medium tracking-wide text-content-muted whitespace-nowrap ${pad} py-2 ${width ?? ''}`}>{label}</th>;
  }
  return (
    <th aria-sort={active ? (dir === 'asc' ? 'ascending' : 'descending') : 'none'}
      className={`p-0 text-[12px] font-medium tracking-wide whitespace-nowrap ${active ? 'bg-surface-3' : ''} ${width ?? ''}`}>
      <button type="button" onClick={onClick} aria-label={`${label} 정렬`}
        className={`group w-full inline-flex items-center gap-1 ${pad} py-2 cursor-pointer select-none transition-colors hover:bg-surface-3 active:bg-surface-3 ${active ? 'text-content font-semibold' : 'text-content-muted'}`}>
        {label}
        {active && dir === 'asc' ? <ChevronUp className="w-3.5 h-3.5 text-content shrink-0" />
          : active && dir === 'desc' ? <ChevronDown className="w-3.5 h-3.5 text-content shrink-0" />
          : <ChevronsUpDown className="w-3.5 h-3.5 text-content-faint shrink-0" />}
      </button>
    </th>
  );
}
```
Create `frontend/src/components/grid/rowClasses.ts`:
```ts
export const ROW_BASE = 'h-9 cursor-pointer border-b border-line transition-colors';
export const ROW_SELECTED = 'bg-info-bg shadow-[inset_3px_0_0_var(--primary)]';
export const ROW_HOVER = 'hover:bg-surface-2 active:bg-surface-3';
/** 선택/트레이스 강조 행 클래스. */
export const rowClass = (highlighted: boolean) => `${ROW_BASE} ${highlighted ? ROW_SELECTED : ROW_HOVER}`;
```
Create `SortableHeaderCell.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { SortableHeaderCell } from './SortableHeaderCell';

describe('SortableHeaderCell', () => {
  it('sortable: 버튼 클릭 콜백 + aria-sort', () => {
    const onClick = vi.fn();
    render(<table><thead><tr><SortableHeaderCell label="이름" sortable active dir="asc" onClick={onClick} /></tr></thead></table>);
    const btn = screen.getByRole('button', { name: /이름 정렬/ });
    fireEvent.click(btn); expect(onClick).toHaveBeenCalled();
    expect(screen.getByRole('columnheader')).toHaveAttribute('aria-sort', 'ascending');
  });
  it('정적(sortable=false): 버튼 없음', () => {
    render(<table><thead><tr><SortableHeaderCell label="상대국측" /></tr></thead></table>);
    expect(screen.queryByRole('button')).toBeNull();
  });
});
```

### Step 4: 통과 + tsc — `npx vitest run src/components/grid/` → PASS; `npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/components/grid
git commit -m "feat(그리드통일): 공유 grid 모듈 — compareBy/sortRows/useGridSort/SortableHeaderCell/rowClasses (현황에서 추출)"
```

---

## Task 2: ConnectionRegisterGrid 정렬·헤더·선택행

**Files:** `frontend/src/features/connections/registerGrid/registerTypes.ts`, `frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx`

### Step 1: 타입 — `registerTypes.ts` 의 `RegisterColumn` 에 추가:
```ts
import type { SortType } from '../../../components/grid/useGridSort';
export interface RegisterColumn<Row> {
  label: string; width?: string; cell(row: Row): ReactNode;
  /** 있으면 정렬 가능 헤더(없으면 정적). */
  sortKey?(row: Row): string | number | null;
  sortType?: SortType; // 기본 'text'
}
```

### Step 2: 컴포넌트 — `ConnectionRegisterGrid.tsx` 재작성(정렬 상태 그리드 단위, 섹션별 적용, 헤더 SortableHeaderCell, RowView selectedAssetId):
```tsx
import { useMemo } from 'react';
import { useEffectiveAssets, useEffectiveCables } from '../../workingCopy/hooks';
import { useSelectionStore } from '../../workspace/selectionStore';
import { usePathHighlightStore } from '../../pathTrace/stores/pathHighlightStore';
import { useTraceGraph } from '../../trace/traceGraph';
import { useGridSort, sortRows, type GridSort } from '../../../components/grid/useGridSort';
import { SortableHeaderCell } from '../../../components/grid/SortableHeaderCell';
import { rowClass } from '../../../components/grid/rowClasses';
import type { Asset } from '../../../types/asset';
import type { RegisterColumn, RegisterDescriptor, RegisterSection } from './registerTypes';

const TD = 'px-2 text-[13px] align-middle whitespace-nowrap';

export function ConnectionRegisterGrid<Row>({ substationId, descriptor }: {
  substationId: string; descriptor: RegisterDescriptor<Row>;
}) {
  const assets = useEffectiveAssets() as Asset[];
  const cables = useEffectiveCables();
  const { graph, isLoading } = useTraceGraph();
  const { sort, cycleSort } = useGridSort();

  const groups = useMemo(() => {
    const ctx = { assets, cables: cables as unknown[], graph, isLoading };
    return descriptor.selectContainers(assets, substationId).map((container) => ({
      container,
      header: descriptor.containerHeader?.(container, ctx) ?? null,
      sections: assets
        .filter((a) => a.parentAssetId === container.id && a.assetType?.connectionKind === descriptor.childKind)
        .map((child) => ({ child, section: descriptor.buildSection(child, ctx) })),
    }));
  }, [assets, cables, graph, isLoading, substationId, descriptor]);

  if (!groups.length) return <p className="p-3 text-sm text-content-faint">{descriptor.emptyMessage}</p>;

  return (
    <div className="space-y-6">
      {groups.map(({ container, header, sections }) => (
        <div key={container.id} className="space-y-4">
          {header && <h2 className="px-1 text-sm font-bold text-content">{header}</h2>}
          {sections.map(({ child, section }) => (
            <SectionView key={child.id} section={section} columns={descriptor.columns}
              rowKey={descriptor.rowKey} rowTraceCableId={descriptor.rowTraceCableId}
              rowSelectedId={(row) => descriptor.onRowClick(row, child)}
              sort={sort} cycleSort={cycleSort} />
          ))}
        </div>
      ))}
    </div>
  );
}

function SectionView<Row>({ section, columns, rowKey, rowTraceCableId, rowSelectedId, sort, cycleSort }: {
  section: RegisterSection<Row>; columns: RegisterColumn<Row>[];
  rowKey: (row: Row) => string | number; rowTraceCableId?: (row: Row) => string | null;
  rowSelectedId: (row: Row) => string | null; sort: GridSort; cycleSort: (col: string) => void;
}) {
  const rows = useMemo(() => {
    if (!sort) return section.rows;
    const col = columns.find((c) => c.label === sort.col);
    if (!col?.sortKey) return section.rows;
    return sortRows(section.rows, col.sortKey, col.sortType ?? 'text', sort.dir);
  }, [section.rows, sort, columns]);

  return (
    <section>
      <header className="mb-1.5 flex items-baseline gap-2 px-1">
        <h3 className="text-sm font-semibold text-content">{section.title}</h3>
        <span className="ml-auto text-[12px] tabular-nums text-content-faint">{section.usedLabel}</span>
      </header>
      <table className="w-full border-collapse">
        <thead>
          <tr className="text-left bg-surface-2 border-b border-line-strong">
            {columns.map((c, i) => (
              <SortableHeaderCell key={i} label={c.label} width={c.width} first={i === 0} last={i === columns.length - 1}
                sortable={!!c.sortKey} active={sort?.col === c.label} dir={sort?.dir ?? 'asc'}
                onClick={c.sortKey ? () => cycleSort(c.label) : undefined} />
            ))}
          </tr>
        </thead>
        <tbody>
          {rows.map((row) => (
            <RowView key={rowKey(row)} row={row} columns={columns}
              traceCableId={rowTraceCableId?.(row) ?? null} selectedId={rowSelectedId(row)}
              onClick={() => { const id = rowSelectedId(row); if (id) useSelectionStore.getState().setSelectedAssetId(id); }} />
          ))}
        </tbody>
      </table>
    </section>
  );
}

function RowView<Row>({ row, columns, traceCableId, selectedId, onClick }: {
  row: Row; columns: RegisterColumn<Row>[]; traceCableId: string | null; selectedId: string | null; onClick: () => void;
}) {
  const tracingCableId = usePathHighlightStore((s) => s.tracingCableId);
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const highlighted = (!!traceCableId && tracingCableId === traceCableId) || (!!selectedId && selectedAssetId === selectedId);
  return (
    <tr onClick={onClick} className={rowClass(highlighted)}>
      {columns.map((c, i) => <td key={i} className={TD}>{c.cell(row)}</td>)}
    </tr>
  );
}
```
> 첫/끝 컬럼 패딩이 SortableHeaderCell 로 옮겨감 — TD 는 `px-2` 유지(현황 TD 도 첫/끝 스마트지만 케이블은 셀 내용이 `<span>`/EditableField 라 px-2 로 충분; 현황과 미세 차이 허용, 또는 TD 도 first/last 패딩 적용은 후속). RowView 가 selectedAssetId 구독 → 선택 변경 시 해당 행만 재렌더(zustand selector).

### Step 3: 검증 — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS. 기존 FiberRegisterView.test/PanelCircuitView.test 통과(헤더 구조 바뀌어 헤더 조회 단언이 있으면 최소 수정 — 컬럼 label 은 그대로라 텍스트 조회 보존). 행클릭→선택→선택행 스타일 확인.

### Step 4: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/connections/registerGrid
git commit -m "feat(그리드통일): ConnectionRegisterGrid 정렬 헤더(SortableHeaderCell)+섹션별 정렬+selectedAssetId 선택행"
```

---

## Task 3: descriptor 정렬키

**Files:** `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`, `frontend/src/features/power/powerRegisterDescriptor.tsx`

각 컬럼에 `sortKey`(+필요시 `sortType`) 추가. cell 은 그대로.

- [ ] **Step 1: fiber** — columns 에 추가:
  - 코어: `sortKey: (r) => r.coreNumber, sortType: 'number'`
  - 근접자산: `sortKey: (r) => (r.nearAssetId ? r.__nameById?.get(r.nearAssetId) ?? null : null)`
  - 상대국측: `sortKey: (r) => r.farName`
  - 용도: `sortKey: (r) => r.purpose`
  - 수용내역: `sortKey: (r) => r.circuitText`
  - 융착: `sortKey: (r) => r.spliceType`
  - 사용: `sortKey: (r) => r.usageOverride ?? (r.occupied ? '사용' : '미사용')`
  (모두 sortType 기본 'text', 코어만 'number'.)

- [ ] **Step 2: power** — columns 에 추가:
  - 번호: `sortKey: (r) => r.cbNumber` (혼합 가능성 → 'text')
  - 부하: `sortKey: (r) => r.loadName`
  - 용량: `sortKey: (r) => r.capacity`
  - 규격: `sortKey: (r) => r.spec`
  - SW: `sortKey: (r) => r.switchState`

- [ ] **Step 3: 검증** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS. 수동: 헤더 클릭 → 섹션 정렬(asc/desc/해제).

- [ ] **Step 4: Commit**
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/fiberRegisterDescriptor.tsx frontend/src/features/power/powerRegisterDescriptor.tsx
git commit -m "feat(그리드통일): 선번장·계통 컬럼에 sortKey 부여(정렬 가능)"
```

---

## Task 4: NodeStatusView 공유 모듈 채택

**Files:** `frontend/src/features/assets/components/NodeStatusView.tsx`

bespoke `compareBy`·`cycleSort`/`sort` state·헤더 버튼·`AssetRow` 선택행 클래스를 공유 모듈로 교체. COLUMN_DEFS(accessor/type)·필터·검색은 유지.

- [ ] **Step 1: 교체** —
  - `compareBy`(로컬) 삭제 → `import { compareBy, sortRows, useGridSort, type SortType } from '../../../components/grid/useGridSort'`. `SortType` 가 'text'|'date'|'number'|'status' 이므로 COLUMN_DEFS 의 `type` 호환(현황은 'status' 사용 — 공유 compareBy 가 status=문자비교로 동일).
  - 로컬 `sort` useState + `cycleSort` → `const { sort, cycleSort } = useGridSort()`.
  - `sortedItems` useMemo → `sort ? sortRows(items, def.accessor, def.type, sort.dir) : items` (def = COLUMN_DEFS.find(label===sort.col)).
  - 헤더 `<th><button>…Chevron…` → `<SortableHeaderCell label={c.label} first last sortable active={sort?.col===c.label} dir={sort?.dir} onClick={()=>cycleSort(c.label)} />`(모든 현황 컬럼 sortable=true). import 추가.
  - `AssetRow` 의 `selected? '…' : '…'` 인라인 → `rowClass(selected)` (`import { rowClass } from '../../../components/grid/rowClasses'`). 동일 클래스라 시각 무변경.
  - 남는 미사용 상수/아이콘 import 정리(ChevronUp 등은 SortableHeaderCell 로 이동 → NodeStatusView 에서 제거).

- [ ] **Step 2: 검증** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run src/features/assets/components/NodeStatusView.test.tsx` → PASS(정렬 3단계·선택·필터 보존). `npx vitest run` 전체 green.

- [ ] **Step 3: Commit**
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/assets/components/NodeStatusView.tsx
git commit -m "feat(그리드통일): NodeStatusView 가 공유 grid 모듈 채택(compareBy/useGridSort/SortableHeaderCell/rowClass) — 단일 소스"
```

---

## Task 5: 검증 + 최종 리뷰

- [ ] **Step 1: 전체** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS.
- [ ] **Step 2: 단일소스 확인** — 정렬 비교/헤더/선택행이 `components/grid/` 한 곳. NodeStatusView·ConnectionRegisterGrid 둘 다 사용(bespoke compareBy/헤더 잔재 0).
- [ ] **Step 3: 수동 스모크** — 현황·선번장·계통: 헤더 클릭 정렬(asc↑/desc↓/해제·Chevron), 행 클릭 선택행 눌림(`bg-info-bg` 좌측 바), 케이블 편집/트레이스 하이라이트 공존. 룩 동일.
- [ ] **Step 4: 최종 리뷰** — subagent-driven 최종 리뷰(compareBy 빈값/locale·sortRows stable / useGridSort 3단계 / SortableHeaderCell sortable·정적·aria / ConnectionRegisterGrid 섹션별 정렬·selectedAssetId 선택행·트레이스 공존 / NodeStatusView 동작 보존 / descriptor sortKey 정확).

---

## Self-Review
- **스펙:** 공유 grid 모듈 + 양쪽 채택 = 정렬·헤더·선택행 단일 소스. 케이블 그리드 룩·정렬·선택을 현황과 일치(섹션 유지·편집셀 등 기능 차이는 허용). ✅
- **단일소스:** compareBy/sortRows/useGridSort/SortableHeaderCell/rowClasses 한 곳. NodeStatusView 가 자기 복사 대신 공유 채택. ✅
- **케이블 정렬:** 그리드 단위 sort 1개 → 각 섹션 적용(헤더 N개 동기). sortKey 없는 컬럼 정적. 기본 원순서. ✅
- **선택행:** RowView 가 selectedAssetId 구독 → 클릭 행 눌림(현황 동일 스타일). 트레이스 하이라이트 공존(둘 중 하나). ✅
- **위험/확인(구현 중):**
  - 빈 코어 선택 충돌(여러 빈코어→OFD id) — 점유 행은 정확, 마이너(스펙에 명시). 필요 시 행-고유 selectedId 후속.
  - NodeStatusView 'status' 타입 — 공유 compareBy status=문자비교로 기존과 동일.
  - 헤더 구조 변경으로 FiberRegisterView.test/PanelCircuitView.test/NodeStatusView.test 의 헤더 조회 단언 최소 수정(컬럼 label 텍스트는 보존).
  - TD 첫/끝 패딩은 헤더만 스마트, 셀은 px-2 — 미세차이 허용(후속 정밀화 가능).
  - RowView selectedAssetId 구독 → 행 단위 재렌더(zustand selector, 효율적).
- **다음:** 접지 그리드도 동일 공유 모듈. 정렬 영속/다중정렬/검색칩은 비목표.
