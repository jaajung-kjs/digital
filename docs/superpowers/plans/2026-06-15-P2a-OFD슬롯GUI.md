# P2a — OFD 슬롯 GUI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** OFD 상세 "경로" 섹션을 랙 동형 **경로슬롯 타일 GUI**(`OfdSlotGrid`)로 재작성한다 — 공유 `SlotTile`(타일), 슬롯 타일(출발-대국 / N코어, 클릭→슬롯 자산 이동[P1 eyebrow]), "+슬롯추가"(대국 OFD + 코어수 24/48), 삭제. 경로 생성/삭제 순수 로직(`buildRouteCreate`/`routeDeleteIds`)은 재사용. `FiberRouteManager` 대체·삭제, "광 경로" 헤딩 제거.

**Architecture:** 신규 프레젠테이션 `SlotTile`(ModuleCell 페이스플레이트 룩 차용, 순수+onClick/onDelete) + `OfdSlotGrid`(FiberRouteManager 로직 이전, 타일 그리드·24/48·클릭 이동). `OfdPathsView`가 OfdSlotGrid 사용. **모델/백엔드 무변경.**

**Tech Stack:** React/TS/Zustand, React Query, Vitest.

**참조 spec:** `docs/superpowers/specs/2026-06-15-P2a-OFD슬롯GUI-design.md`. 이전원 `FiberRouteManager.tsx`(로직), 룩 `ModuleCell.tsx`(FACEPLATE var(--eq-1)/(--eq-4) + bezel boxShadow). 재사용 `fiberWrite.ts`(buildRouteCreate/routeDeleteIds).

---

## 환경 규칙 (공통)
- 프론트 `cd frontend && npx tsc --noEmit && npm run build && npx vitest run`. 단일 `npx vitest run <file>`.

## 현재 구조 (확인됨)
- `FiberRouteManager({ofdId})`: 데이터 — effectiveAssets/Cables, useTraceGraph, useAssetTypeIdByCode('OFD-SLOT'), useCableCategories(CBL-OPGW), 전역 slim(`['assets-slim']`). 로직 — peerOfds/localOfd(slim), slots(conduit 자식), `addRoute(remote)`=buildRouteCreate+put(슬롯2+OPGW), `deleteRoute(slot)`=OPGW twin 찾기+routeDeleteIds+remove. UI — "광 경로" h4 + cores 자유입력 + peer 버튼목록 + 비클릭 테이블.
- `OfdPathsView`(OfdEquipmentPanel.tsx): `<FiberRouteManager ofdId={equipmentId} />` + `<PathTraceDetail />`.
- ModuleCell 룩: `FACEPLATE_BG='var(--eq-1)'`, `FACEPLATE_FG='var(--eq-4)'`, `boxShadow:'inset 0 1px 0 rgba(255,255,255,0.07), inset 0 -1px 0 rgba(0,0,0,0.28)'`.

## File Structure
- `frontend/src/components/SlotTile.tsx` (Create) + `SlotTile.test.tsx`.
- `frontend/src/features/fiber/components/OfdSlotGrid.tsx` (Create) + test.
- `frontend/src/features/equipment/components/detail/panels/OfdEquipmentPanel.tsx` (Modify) — OfdSlotGrid 사용.
- `frontend/src/features/fiber/components/FiberRouteManager.tsx` (Delete).

---

## Task 1: 공유 SlotTile (TDD)

**Files:** `frontend/src/components/SlotTile.tsx`, `SlotTile.test.tsx`

### Step 1: 실패 테스트 — Create `frontend/src/components/SlotTile.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { SlotTile } from './SlotTile';

describe('SlotTile', () => {
  it('title/subtitle 표시 + onClick', () => {
    const onClick = vi.fn();
    render(<SlotTile title="원주S/S - 홍천S/S" subtitle="24코어" onClick={onClick} />);
    expect(screen.getByText('원주S/S - 홍천S/S')).toBeInTheDocument();
    expect(screen.getByText('24코어')).toBeInTheDocument();
    fireEvent.click(screen.getByRole('button'));
    expect(onClick).toHaveBeenCalled();
  });
  it('onDelete 는 stopPropagation(타일 클릭 미발생)', () => {
    const onClick = vi.fn();
    const onDelete = vi.fn();
    render(<SlotTile title="t" onClick={onClick} onDelete={onDelete} />);
    fireEvent.click(screen.getByRole('button', { name: /삭제/ }));
    expect(onDelete).toHaveBeenCalled();
    expect(onClick).not.toHaveBeenCalled();
  });
  it('empty 상태: 점선 타일', () => {
    render(<SlotTile state="empty" title="+ 슬롯 추가" onClick={() => {}} />);
    expect(screen.getByText('+ 슬롯 추가')).toBeInTheDocument();
  });
});
```

### Step 2: 실패 확인 — `cd frontend && npx vitest run src/components/SlotTile.test.tsx` → FAIL.

### Step 3: 구현 — Create `frontend/src/components/SlotTile.tsx`:
```tsx
import type { ReactNode } from 'react';

const FACEPLATE_BG = 'var(--eq-1)';
const FACEPLATE_FG = 'var(--eq-4)';
const BEZEL = 'inset 0 1px 0 rgba(255,255,255,0.07), inset 0 -1px 0 rgba(0,0,0,0.28)';

/** 컨테이너 자식(경로슬롯·포트 등) 타일 — ModuleCell 페이스플레이트 룩 차용, 순수 표시 + 클릭/삭제. */
export function SlotTile({ title, subtitle, state = 'occupied', selected = false, onClick, onDelete }: {
  title: string; subtitle?: ReactNode; state?: 'occupied' | 'empty';
  selected?: boolean; onClick?: () => void; onDelete?: () => void;
}) {
  if (state === 'empty') {
    return (
      <button type="button" onClick={onClick}
        className="group flex h-16 flex-col items-center justify-center rounded-md border border-dashed border-line bg-surface-2/50 text-xs text-content-faint transition-colors hover:bg-surface-2 hover:text-content">
        {title}
      </button>
    );
  }
  return (
    <div role="button" tabIndex={0} onClick={onClick}
      onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') onClick?.(); }}
      className={`group relative flex h-16 cursor-pointer flex-col justify-center rounded-md px-2.5 py-1.5 text-left transition-shadow ${selected ? 'ring-2 ring-primary' : ''}`}
      style={{ background: FACEPLATE_BG, color: FACEPLATE_FG, boxShadow: BEZEL }}>
      <span className="truncate text-[13px] font-medium">{title}</span>
      {subtitle != null && <span className="truncate text-[11px] opacity-70">{subtitle}</span>}
      {onDelete && (
        <button type="button" aria-label="삭제" title="삭제"
          onClick={(e) => { e.stopPropagation(); onDelete(); }}
          className="absolute right-1 top-1 rounded-full bg-black/30 px-1 text-[11px] leading-none text-white opacity-0 transition-opacity hover:bg-danger group-hover:opacity-100">
          ✕
        </button>
      )}
    </div>
  );
}
```

### Step 4: 통과 + tsc — `npx vitest run src/components/SlotTile.test.tsx` → PASS(3); `npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/components/SlotTile.tsx frontend/src/components/SlotTile.test.tsx
git commit -m "feat(P2a): 공유 SlotTile(컨테이너 자식 타일, ModuleCell 룩, 클릭/삭제)"
```

---

## Task 2: OfdSlotGrid + OfdPathsView 재배선

**Files:** `frontend/src/features/fiber/components/OfdSlotGrid.tsx`, `frontend/src/features/equipment/components/detail/panels/OfdEquipmentPanel.tsx`, `frontend/src/features/fiber/components/FiberRouteManager.tsx`(Delete)

### Step 1: OfdSlotGrid — Create `frontend/src/features/fiber/components/OfdSlotGrid.tsx` (FiberRouteManager 로직 이전 + 타일 그리드):
```tsx
import { useMemo, useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import { useEffectiveAssets, useEffectiveCables } from '../../workingCopy/hooks';
import { useSubstationWorkingCopy } from '../../workingCopy/substationStore';
import { useSelectionStore } from '../../workspace/selectionStore';
import { useTraceGraph, remoteSlotSubstation, type SlimAssetDTO } from '../../trace/traceGraph';
import { useAssetTypeIdByCode } from '../../assets/useAssetTypeIdByCode';
import { useCableCategories } from '../../cables/hooks/useCableCategories';
import { generateTempId } from '../../../utils/idHelpers';
import { buildRouteCreate, routeDeleteIds } from '../fiberWrite';
import { api } from '../../../utils/api';
import { SlotTile } from '../../../components/SlotTile';
import type { Asset } from '../../../types/asset';

interface CableWithRoles {
  id: string; sourceAssetId?: string | null; targetAssetId?: string | null;
  sourceRole?: string | null; targetRole?: string | null; cableType?: string | null;
  specParams?: Record<string, unknown> | null;
}

/** OFD 경로슬롯 타일 GUI — 슬롯 타일(출발-대국/N코어, 클릭 이동) + 슬롯 추가(대국+24/48) + 삭제. */
export function OfdSlotGrid({ ofdId }: { ofdId: string }) {
  const assets = useEffectiveAssets() as Asset[];
  const cables = useEffectiveCables() as unknown as CableWithRoles[];
  const { graph } = useTraceGraph();
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const slotTypeId = useAssetTypeIdByCode('OFD-SLOT');
  const { data: categories = [] } = useCableCategories();
  const opgwCat = categories.find((c) => c.code === 'CBL-OPGW');
  const [adding, setAdding] = useState(false);
  const [cores, setCores] = useState(24);

  const { data: slim = [] } = useQuery({
    queryKey: ['assets-slim'], staleTime: 30_000,
    queryFn: async () => (await api.get<{ data: SlimAssetDTO[] }>('/assets')).data.data,
  });
  const peerOfds = useMemo(() => slim.filter((a) => a.code === 'OFD' && a.id !== ofdId), [slim, ofdId]);
  const localOfd = useMemo(() => slim.find((a) => a.id === ofdId) ?? null, [slim, ofdId]);
  const slots = useMemo(
    () => assets.filter((a) => a.parentAssetId === ofdId && a.assetType?.connectionKind === 'conduit'),
    [assets, ofdId],
  );

  const opgwOf = (slotId: string) =>
    cables.find((c) => c.cableType === 'FIBER' && c.sourceRole === 'IN' && c.targetRole === 'IN'
      && (c.sourceAssetId === slotId || c.targetAssetId === slotId));
  const coresOf = (slotId: string) => Number((opgwOf(slotId)?.specParams as Record<string, unknown> | undefined)?.cores ?? 0);

  const addRoute = (remote: SlimAssetDTO) => {
    if (!slotTypeId || !opgwCat || !localOfd) return;
    const { slots: newSlots, opgw } = buildRouteCreate({
      localOfd: { id: localOfd.id, substationId: localOfd.substationId, substationName: localOfd.substationName },
      remoteOfd: { id: remote.id, substationId: remote.substationId, substationName: remote.substationName },
      cores, slotTypeId, opgwCategory: opgwCat,
      ids: { slotA: generateTempId(), slotB: generateTempId(), opgw: generateTempId() },
    });
    const wc = useSubstationWorkingCopy.getState();
    for (const s of newSlots) wc.put('assets', s as unknown as { id: string; [k: string]: unknown });
    wc.put('cables', opgw as unknown as { id: string; [k: string]: unknown });
    setAdding(false);
  };

  const deleteRoute = (slot: Asset) => {
    const op = opgwOf(slot.id);
    const twinId = op ? (op.sourceAssetId === slot.id ? op.targetAssetId : op.sourceAssetId) : undefined;
    if (!twinId) return;
    const { assetIds, cableIds } = routeDeleteIds(slot.id, twinId, cables);
    if (!confirm(`이 경로와 코어 연결 ${Math.max(0, cableIds.length - 1)}개를 삭제합니다.`)) return;
    const wc = useSubstationWorkingCopy.getState();
    for (const id of cableIds) wc.remove('cables', id);
    for (const id of assetIds) wc.remove('assets', id);
  };

  const configMissing = !slotTypeId || !opgwCat;

  return (
    <div className="space-y-2">
      <div className="grid grid-cols-2 gap-2">
        {slots.map((slot) => {
          const local = graph?.subNameById.get(ofdId) ?? null;
          const remote = graph ? remoteSlotSubstation(slot.id, graph) : null;
          const title = [local, remote].filter(Boolean).join(' - ') || slot.name;
          const n = coresOf(slot.id);
          return (
            <SlotTile key={slot.id} title={title} subtitle={n ? `${n}코어` : undefined}
              selected={selectedAssetId === slot.id}
              onClick={() => useSelectionStore.getState().setSelectedAssetId(slot.id)}
              onDelete={() => deleteRoute(slot)} />
          );
        })}
        <SlotTile state="empty" title={adding ? '닫기' : '+ 슬롯 추가'}
          onClick={() => { if (!configMissing) setAdding((v) => !v); }} />
      </div>

      {adding && (
        <div className="rounded border border-line p-2 space-y-2">
          <div className="flex items-center gap-2 text-[11px] text-content-muted">
            <span>코어 수</span>
            {[24, 48].map((n) => (
              <button key={n} type="button" onClick={() => setCores(n)}
                className={`rounded border px-2 py-0.5 text-xs ${cores === n ? 'border-primary bg-info-bg text-primary' : 'border-line text-content-muted'}`}>{n}</button>
            ))}
            <input type="number" min={1} value={cores}
              onChange={(e) => setCores(Math.max(1, Number(e.target.value) || 1))}
              className="w-16 rounded border border-line px-1 text-xs" />
          </div>
          <div className="max-h-40 space-y-0.5 overflow-auto">
            {peerOfds.map((o) => (
              <button key={o.id} onClick={() => addRoute(o)}
                className="block w-full rounded px-1.5 py-1 text-left text-xs hover:bg-surface-2">
                {o.substationName ?? o.name} 대국
              </button>
            ))}
            {peerOfds.length === 0 && <p className="px-1.5 py-1 text-xs text-content-faint">대국 OFD 없음</p>}
          </div>
        </div>
      )}

      {configMissing && <p className="px-1 text-[11px] text-content-faint">설정 로딩/누락</p>}
    </div>
  );
}
```
> 로직(addRoute/deleteRoute/peer/slots)은 FiberRouteManager 와 동일 — 타일 그리드·24/48·클릭 이동·선택 강조만 추가. "광 경로" h4 없음(섹션 제목은 resolveSpatialSection "경로").

### Step 2: OfdPathsView 재배선 — `OfdEquipmentPanel.tsx`: `import { OfdSlotGrid } from '...OfdSlotGrid'`, `<FiberRouteManager .../>` → `<OfdSlotGrid ofdId={equipmentId} />`. `<PathTraceDetail />` 유지. FiberRouteManager import 제거.

### Step 3: 구 컴포넌트 삭제 — `git rm frontend/src/features/fiber/components/FiberRouteManager.tsx`. grep `FiberRouteManager` → OfdEquipmentPanel(교체됨)·기존 테스트만. 기존 `FiberRouteManager.test.tsx`(있으면) → OfdSlotGrid 로 이전/대체.

### Step 4: OfdSlotGrid 테스트 — Create `OfdSlotGrid.test.tsx`(또는 기존 FiberRouteManager 테스트 이전): 슬롯 타일 렌더(출발-대국·N코어), 타일 클릭→`setSelectedAssetId(slot)`, "+슬롯추가"→대국 선택→`put('assets')`·`put('cables')` 호출(buildRouteCreate), 24 버튼→cores. (useTraceGraph/useCableCategories/useQuery/substationStore mock — 기존 FiberRouteManager 테스트 패턴 참조.)

### Step 5: 검증 — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS. `grep -rn "FiberRouteManager" frontend/src` → 0(삭제·무참조).

### Step 6: Commit
```bash
cd /Users/jsk/1210/digital
git add -A
git commit -m "feat(P2a): OfdSlotGrid(경로슬롯 타일·클릭 이동·24/48·삭제) + OfdPathsView 재배선, 구 FiberRouteManager 삭제"
```

---

## Task 3: 검증 + 최종 리뷰

- [ ] **Step 1: 전체** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS.
- [ ] **Step 2: 무참조 확인** — FiberRouteManager 삭제·무참조. "광 경로" 텍스트 없음(`grep "광 경로" frontend/src` → 0 또는 무관).
- [ ] **Step 3: 수동 스모크** — OFD 상세 "경로" → 슬롯 타일 그리드(출발-대국/N코어), 타일 클릭→슬롯 패널(eyebrow=OFD), "+슬롯추가"(24/48 + 대국 선택)→슬롯 생성, 삭제. 선택 시 타일 강조.
- [ ] **Step 4: 최종 리뷰** — subagent-driven 최종 리뷰(SlotTile onDelete stopPropagation / OfdSlotGrid 로직이 FiberRouteManager 와 동등[buildRouteCreate/routeDeleteIds]·클릭 이동·선택 강조·24/48 / 마운트·무참조 / P2b 포트 타일이 SlotTile 재사용 가능 구조).

---

## Self-Review
- **스펙:** OFD "경로" 섹션 = 랙 동형 타일 GUI(SlotTile + OfdSlotGrid). 슬롯 클릭→자산 이동(P1 eyebrow), "출발-대국/N코어" 라벨, "광 경로" 삭제, 24/48 — P1에서 미룬 정리를 랙 패턴으로 흡수. ✅
- **DRY:** SlotTile 공유(P2b 포트 타일 재사용). 경로 생성/삭제 순수 로직(fiberWrite) 재사용(무변경). 랙은 안 건드림(위험 격리). ✅
- **동등성:** addRoute/deleteRoute/peer/slots = FiberRouteManager 그대로 — UI만 타일화. ✅
- **위험/확인(구현 중):**
  - SlotTile onDelete stopPropagation(타일 클릭 미발생) — 테스트로 보장.
  - staged 슬롯 라벨 대국명 커밋 전 폴백(slim 기반, P6 한계) — slot.name 폴백.
  - 기존 FiberRouteManager 테스트 → OfdSlotGrid 로 이전(단언 보존).
  - 선택 강조 selectedAssetId===slot.id — 그리드 선택행과 동일 UX.
- **다음:** P2b(슬롯 포트 GUI — SlotTile 재사용한 1~N 포트 번호 그리드, connectionKind conduit 주입). P3(연결 피커 통일).
