# C3 — 케이블 생성·편집 진입점 (그룹 피커 · 선번장 정리 · label 제거) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 케이블 생성·편집을 사용자 그룹 기반으로 통일한다 — OPGW 경로 생성 시 종류 선택(하드코딩 `CBL-OPGW` 제거), 선번장 코어↔설비 드롭다운 제거(읽기전용), `CableInspector`·표준 생성 피커를 **그룹→이름 2단**으로, `Cable.label`(읽기·UI·쓰기) 제거.

**Architecture:** C1(그룹 백엔드)·C2(트레이스 그룹화) 위에서 *진입점*만 정리. 종류 선택은 `useCableGroups`(신규 훅, /api/cable-groups) + `useCableCategories`(groupId 보유)로 그룹→이름 2단. `Cable.label`/`cableType`/`code` **컬럼 드롭은 C5**(여기선 읽기·UI·쓰기 제거).

**Tech Stack:** React 18 + TS, React Query, vitest. 백엔드 변경: cable DTO 에서 label 제거(컬럼은 C5).

## Global Constraints

- 개발: DB만 docker + `npm run dev`. docker compose build/up 금지.
- 인스턴스 동작 불변(OPGW 생성·선번장·계통·연결탭) — 기존 vitest 전수 통과 = 회귀 게이트.
- UI: CONVENTIONS.md 준수(EditableField·DetailRow·Button 등 재사용). 커밋/푸시는 각 Task 끝 명시 단계.
- **컬럼 드롭 없음**(C5): `Cable.label`/`cableType`/`code`·`CableCategory.code` 등 유지. C3은 읽기·UI·쓰기만 제거.
- 루트: 프론트 `/Users/jsk/1210/digital/frontend`, 백엔드 `/Users/jsk/1210/digital/backend`. 브랜치 `feat/asset-type-catalog-p1` 연속.
- 종류 선택 = **그룹(CableGroup) → 이름(CableCategory)** 2단. 그룹은 `useCableGroups`, 이름은 `useCableCategories`(c.groupId 로 필터).

---

### Task 1: useCableGroups 훅 + 그룹→이름 2단 피커 컴포넌트

**Files:**
- Create: `frontend/src/features/cables/hooks/useCableGroups.ts`
- Create: `frontend/src/features/cables/components/CableTypePicker.tsx`
- Create: `frontend/src/types/cableGroup.ts`
- Test: `frontend/src/features/cables/components/CableTypePicker.test.tsx`

**Interfaces:**
- Produces: `useCableGroups()` → `{ data: CableGroup[] }` (`CableGroup = { id, name, color, sortOrder, isActive }`); `<CableTypePicker value={categoryId} onChange={(categoryId) => ...} />` — 그룹 select + 그 그룹의 이름 select(2단). 재사용 컴포넌트.

- [ ] **Step 1: 타입 + 훅** — `types/cableGroup.ts`:
```ts
export interface CableGroup { id: string; name: string; color: string | null; sortOrder: number; isActive: boolean }
```
`hooks/useCableGroups.ts` (useCableCategories 패턴):
```ts
import { useQuery } from '@tanstack/react-query';
import { api } from '../../../utils/api';
import type { CableGroup } from '../../../types/cableGroup';
export const CABLE_GROUP_KEYS = { all: ['cable-groups'] as const };
export function useCableGroups() {
  return useQuery({
    queryKey: CABLE_GROUP_KEYS.all,
    queryFn: async () => (await api.get<{ data: CableGroup[] }>('/cable-groups')).data.data,
    staleTime: 1000 * 60 * 30,
  });
}
```

- [ ] **Step 2: 실패 테스트** — `CableTypePicker.test.tsx`: 그룹 select 변경 → 그 그룹 이름만 노출 / 이름 선택 → onChange(categoryId). (useCableGroups·useCableCategories 모킹.)
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { CableTypePicker } from './CableTypePicker';
vi.mock('../hooks/useCableGroups', () => ({ useCableGroups: () => ({ data: [{ id: 'g1', name: '전원', color: '#ef4444' }, { id: 'g2', name: '광', color: '#22c55e' }] }) }));
vi.mock('../hooks/useCableCategories', () => ({ useCableCategories: () => ({ data: [
  { id: 'c1', name: 'F-CV', groupId: 'g1' }, { id: 'c2', name: 'OPGW', groupId: 'g2' },
] }) }));
describe('CableTypePicker', () => {
  it('그룹 선택 → 그 그룹 이름만, 이름 선택 → onChange(categoryId)', () => {
    const onChange = vi.fn();
    render(<CableTypePicker value={null} onChange={onChange} />);
    fireEvent.change(screen.getByLabelText('그룹'), { target: { value: 'g2' } });
    fireEvent.change(screen.getByLabelText('이름'), { target: { value: 'c2' } });
    expect(onChange).toHaveBeenCalledWith('c2');
  });
});
```

- [ ] **Step 3: 컴포넌트 구현** — `CableTypePicker.tsx`: 두 `<select>`(그룹/이름). value(categoryId)에서 현재 그룹 파생, 그룹 변경 시 이름 초기화, 이름 변경 시 onChange(categoryId). 그룹 색 스와치 표시. aria-label '그룹'/'이름'. (공용 UI 패턴 — 가능하면 EditableField 조합, 안 되면 select 2개 + 색 스와치.)

- [ ] **Step 4: 통과 + 타입** — `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/cables/components/CableTypePicker.test.tsx && npx tsc --noEmit | tail -5` → PASS.

- [ ] **Step 5: 커밋** — `feat(cable): useCableGroups + CableTypePicker(그룹→이름 2단) (C3)`.

---

### Task 2: CableInspector — 2단 피커 + 라벨행 제거

**Files:** `frontend/src/features/cables/components/CableInspector.tsx`, 테스트(있으면)

- [ ] **Step 1: 종류 행을 CableTypePicker 로 교체** — `categoryOptions`(평면 목록) 제거, `종류` DetailRow 를 `<CableTypePicker value={categoryId || null} onChange={commitCategory} />` 로. `commitCategory(id)` 는 categoryId + categoryName 패치(displayColor 는 그룹색 — C5 정리, 현재 유지): 
```ts
const commitCategory = (id: string | null) => {
  const c = (categories ?? []).find((x) => x.id === id) ?? null;
  patch({ categoryId: id, categoryName: c?.name ?? null });
};
```

- [ ] **Step 2: 라벨 행 제거** — `<DetailRow label="라벨">…</DetailRow>` 블록 삭제. (표시 이름은 종류명으로 일원화.)

- [ ] **Step 3: 회귀 + 타입** — `npx vitest run src/features/cables src/features/connections && npx tsc --noEmit | tail -5` → PASS.

- [ ] **Step 4: 커밋** — `refactor(cable): CableInspector 2단 피커 + 라벨행 제거 (C3)`.

---

### Task 3: OPGW 경로 생성 — 종류 선택 (CBL-OPGW 하드코딩 제거)

**Files:** `frontend/src/features/fiber/components/OfdRoutePopover.tsx`, `frontend/src/features/fiber/components/OfdSlotRail.tsx`

**Interfaces:** `OfdRoutePopover` onPick 시그니처 `(peer, cores, categoryId)`; `OfdSlotRail.addRoute(remote, cores, categoryId)` 가 picked category 로 `buildRouteCreate`.

- [ ] **Step 1: OfdRoutePopover 에 종류 피커** — `<CableTypePicker value={categoryId} onChange={setCategoryId} />` 추가(코어수·대국 목록과 함께). `onPick` → `(peer, cores, categoryId)`. categoryId 미선택 시 확정 비활성.

- [ ] **Step 2: OfdSlotRail.addRoute** — `opgwCat`(CBL-OPGW find) 제거. `addRoute(remote, cores, categoryId)`: `const cat = categories.find(c => c.id === categoryId)` 로 `buildRouteCreate({ ..., opgwCategory: cat })`. 가드 `if (!cat) return`. `handleEmptyClick`/popover `onPick` 연결 갱신.

- [ ] **Step 3: 회귀 + 타입** — `npx vitest run src/features/fiber && npx tsc --noEmit | tail -5` → PASS. (OfdSlotGrid 테스트의 opgwCat/onPick 픽스처 갱신: code 제거, categoryId 전달.)

- [ ] **Step 4: 커밋** — `feat(fiber): OPGW 경로 생성 종류 선택 — CBL-OPGW 하드코딩 제거 (C3)`.

---

### Task 4: 선번장 코어↔설비 — 드롭다운 제거(읽기전용)

**Files:** `frontend/src/features/fiber/components/EquipmentSelectCell.tsx`, `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`, 테스트

**Interfaces:** 자국/대국 설비 셀 = **읽기전용 설비명**(연결 설비 이름 표시). 케이블 생성·변경·해제는 `SlotPortsPanel`(표준 흐름)에서. `buildCoreOutCable`/`opjCat`(CBL-OPJ) 사용 제거.

- [ ] **Step 1: EquipmentSelectCell 읽기전용화** — 드롭다운(`EditableField select`)·`commit`·`buildCoreOutCable`·`opjCat`·`touchCoreInspect` 쓰기 제거. 연결된 설비명(`nameOf(currentId)`)만 텍스트로 렌더(미연결 '—'). (코어↔설비 *연결*은 SlotPortsPanel "케이블 연결"이 이미 표준 흐름으로 담당.)
```tsx
export function EquipmentSelectCell({ slot, coreNumber, side }: { slot: Asset; coreNumber: number; side: 'local' | 'remote' }) {
  const { graph } = useTraceGraph();
  const cables = (graph?.cables ?? []) as unknown as CableLike[];
  const twinId = twinSlotIdOf(slot.id, cables);
  const targetSlotId = side === 'local' ? slot.id : twinId;
  const outCable = targetSlotId ? cables.find((c) => c.number === coreNumber && (c.sourceAssetId === targetSlotId || c.targetAssetId === targetSlotId) && roleAt(c, targetSlotId) === 'OUT') : undefined;
  const currentId = outCable && targetSlotId ? other(outCable, targetSlotId) : null;
  const name = currentId ? (graph?.nameById.get(currentId) ?? currentId) : null;
  return <span className="text-sm text-content">{name ?? <span className="text-content-faint">—</span>}</span>;
}
```
  (불필요해진 import 정리: useSubstationWorkingCopy·buildCoreOutCable·EditableField·generateTempId·coreMeta·useCableCategories.)

- [ ] **Step 2: 회귀 + 타입** — `npx vitest run src/features/fiber && npx tsc --noEmit | tail -10` → PASS. (FiberRegisterView/EquipmentSelectCell 테스트를 읽기전용 표시 검증으로 갱신 — 드롭다운/연결생성 단정 제거.)

- [ ] **Step 3: 커밋** — `refactor(fiber): 선번장 설비 셀 읽기전용화 — CBL-OPJ 하드코딩·직접연결 제거 (C3)`.

---

### Task 5: 표준 생성 피커(insert bar + CableSpecModal) 그룹화

**Files:** `frontend/src/features/editor/components/EditorInsertBar.tsx`, `frontend/src/features/editor/components/modals/CableSpecModal.tsx`

**왜:** insert bar 케이블 pill(현재 고정 `CABLE_DISPLAY_GROUPS` 5종) → 사용자 `CableGroup`. CableSpecModal 필터 displayGroup(문자열) → groupId.

- [ ] **Step 1: EditorInsertBar 케이블 pill** — `CABLE_DISPLAY_GROUPS`/`CABLE_DISPLAY_GROUP_COLORS` → `useCableGroups()`. pill 은 그룹별(이름·색). `handleCableGroupClick(group)` → `startCableConnection({ group: group.id })`(groupId 전달). `preselectedCableDisplayGroup`(문자열) → `preselectedCableGroupId`.
- [ ] **Step 2: editorStore/startCableConnection** — `preselectedCableDisplayGroup` → `preselectedCableGroupId`(string|null). `startCableConnection({ group })` 의 group 의미를 groupId 로.
- [ ] **Step 3: CableSpecModal 필터** — `c.displayGroup === preselectedGroup` → `c.groupId === preselectedGroupId`. 미선택 시 전체. 색 스와치 = `cat.groupColor`.
- [ ] **Step 4: 회귀 + 빌드** — `npx vitest run src/features/editor && npm run build 2>&1 | tail -5` → PASS. (CableSpecModal/insert bar 테스트 픽스처 groupId 로.)
- [ ] **Step 5: 커밋** — `refactor(cable): 표준 생성 피커(insert bar·CableSpecModal) 그룹 기반 (C3)`.

---

### Task 6: Cable.label 읽기·UI·쓰기 제거

**Files:** `frontend/src/features/connections/components/ConnectionOverlay.tsx`(57), `frontend/src/features/editor/renderers/connectionRenderer.ts`(112), `frontend/src/features/workingCopy/cableToLocal.ts`(63), `backend/src/services/cable.service.ts`(DTO label), 관련 타입(`RoomConnection`/`cableToLocal` label 필드)

**왜:** 표시 이름 = 종류명. label 필드는 불필요(컬럼 드롭은 C5). 읽기·UI·쓰기·DTO 제거.

- [ ] **Step 1: ConnectionOverlay:57** — `cable.label || cable.categoryName || cable.categoryCode` → `cable.categoryName ?? undefined`.
- [ ] **Step 2: connectionRenderer:112** — `conn.label || conn.materialCategoryCode || conn.cableType` → `conn.label ?? conn.materialCategoryCode ?? ''` 에서 label 제거 → `conn.materialCategoryCode ?? ''`(또는 categoryName). cableType 라벨 폴백도 제거(C2 잔여).
- [ ] **Step 3: cableToLocal** — `label: c.label ?? null` 제거 + 로컬 타입 `label?` 제거.
- [ ] **Step 4: 백엔드 cable.service** — DTO 인터페이스 `label` 제거 + 매핑(198행) 제거. (`Cable.label` 컬럼은 C5 드롭.)
- [ ] **Step 5: 잔재 확인** — `grep -rn "\.label\b" frontend/src backend/src | grep -i cable | grep -v test` → cable.label 읽기/쓰기 0(스키마 컬럼만 잔존).
- [ ] **Step 6: 회귀 + 빌드 + 타입(양쪽)** — 프론트 `npx vitest run && npm run build`; 백엔드 `npx vitest run && npx tsc --noEmit | tail -5` → PASS.
- [ ] **Step 7: 커밋** — `refactor(cable): Cable.label 읽기·UI·쓰기 제거(컬럼은 C5) (C3)`.

---

### Task 7: 전체 회귀

- [ ] **Step 1: 프론트** — `cd /Users/jsk/1210/digital/frontend && npx vitest run && npm run build 2>&1 | tail -5` → 전체 PASS.
- [ ] **Step 2: 백엔드** — `cd /Users/jsk/1210/digital/backend && npx vitest run && npx tsc --noEmit | tail -5` → PASS.
- [ ] **Step 3: 수동 스모크** — `npm run dev`: OPGW 경로(종류 선택)·선번장 설비명 읽기전용·연결탭 2단 피커·표준 케이블 생성(그룹 pill)·계통 규격·케이블 색 정상.

---

## Self-Review

**1. Spec(§5) 커버:** 표준 생성 그룹→이름(T1·T5) / OPGW 종류피커(T3) / 선번장 드롭다운 제거(T4) / CableInspector 2단(T2) / 계통 규격셀(현행 — group-scoped, 변경 없음) / Cable.label 제거(T6). 하드코딩 CBL-OPGW(T3)·CBL-OPJ(T4) 제거.
**2. Placeholder scan:** 코드/명령 구체. T4 EquipmentSelectCell 은 전체 교체 코드 제공. select 2단 컴포넌트(T1)는 구현 방향 + 테스트로 계약 고정.
**3. Type consistency:** `CableGroup`·`useCableGroups`·`CableTypePicker(value:categoryId, onChange)`·`preselectedCableGroupId` 일관.

## 주의 / 의존
- **계통 SpecCell**(같은 그룹 내 이름 선택)은 이미 group-scoped 라 C3 변경 없음(C2에서 role/group 정합). 단 displayGroup→groupId 필터로 바꿀지는 C5 정리 시 함께(현재 displayGroup 유지되어 동작).
- **C4(케이블종류 CRUD UI)**: 자산관리 셸 미구현 → C4 는 셸 선행. C3 는 그 셸 없이도 동작(생성·편집 진입점만).
- **컬럼 드롭(label/cableType/code)·displayGroup 정리·cableConnection 의 cableType 쓰기**는 C5.
