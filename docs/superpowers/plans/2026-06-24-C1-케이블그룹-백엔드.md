# C1 — 케이블 그룹 백엔드 (CableGroup + CableCategory.groupId) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 사용자 정의 `CableGroup`(이름+색) 테이블을 신설하고 `CableCategory`에 `groupId` FK를 추가, 기존 `displayGroup` 문자열에서 백필한다. 케이블 종류를 "그룹(이름·색) → 이름" 2단 사용자 정의 모델로 만드는 백엔드 토대.

**Architecture:** 2단계 마이그레이션의 1단계 — `CableGroup` 테이블 + `CableCategory.groupId`를 **추가(additive)**하고 기존 `displayGroup`에서 백필. 구필드(`displayGroup`/`displayColor`/`code`/`specTemplate` 등)는 **유지**(프론트가 C2까지 displayGroup 사용). CableGroup CRUD(admin) 신설. 색은 그룹이 보유.

**Tech Stack:** Prisma 5.22 / PostgreSQL, Express, Zod, vitest. 마이그레이션 `prisma migrate dev`.

## Global Constraints

- 개발: DB만 docker(`docker-compose.dev.yml`) + `npm run dev`. docker compose build/up 금지.
- C1은 **additive only** — 어떤 기존 컬럼도 드롭하지 않는다(드롭은 C5).
- 커밋/푸시는 각 Task 끝 명시 단계에서만. 메시지 말미: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- 작업 루트: `/Users/jsk/1210/digital/backend`. 테스트: 서비스 로직 = `vi.mock('../src/config/prisma.js')`, 백필/시드 = 실제 dev DB.
- 작업 전 DB 기동: `docker compose -f ../docker-compose.dev.yml up -d`.
- **그룹 5종 + 색(시드 백필값, 기존 프론트 CABLE_DISPLAY_GROUP_COLORS 유래):** 전원 `#ef4444` · 접지 `#eab308` · 네트워크 `#3b82f6` · 광 `#22c55e` · 제어 `#6b7280`.
- 브랜치: `feat/asset-type-catalog-p1` 연속.

---

### Task 1: 스키마 — CableGroup + CableCategory.groupId + 백필 마이그레이션

**Files:**
- Modify: `backend/prisma/schema.prisma` (CableCategory 193-211 부근)
- Create: `backend/prisma/migrations/<ts>_add_cable_group/migration.sql` (생성 후 백필 SQL 추가)
- Test: `backend/tests/cableGroupBackfill.integration.test.ts`

**Interfaces:**
- Produces: `model CableGroup { id, name(unique), color, sortOrder, isActive, timestamps, categories }`; `CableCategory.groupId String?` + relation.

- [ ] **Step 1: 스키마에 모델·필드 추가** — `model CableCategory { ... }` 의 `cables Cable[]` 위에:
```prisma
  groupId String?     @map("group_id")
  group   CableGroup? @relation(fields: [groupId], references: [id])
```
`@@map("cable_categories")` 닫힘 아래에:
```prisma
model CableGroup {
  id        String   @id @default(uuid())
  name      String   @unique @db.VarChar(50)
  color     String?  @db.VarChar(7)
  sortOrder Int      @default(0) @map("sort_order")
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  categories CableCategory[]

  @@map("cable_groups")
}
```

- [ ] **Step 2: 마이그레이션 생성(미적용)** — Run: `npx prisma migrate dev --create-only --name add_cable_group`
Expected: `migration.sql` 생성(CREATE TABLE cable_groups, ALTER cable_categories ADD group_id + FK).

- [ ] **Step 3: 백필 SQL을 migration.sql 끝에 추가**
```sql
-- gen_random_uuid() 보장
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- displayGroup(5종) → CableGroup (색 동반)
INSERT INTO "cable_groups" ("id","name","color","sort_order","is_active","created_at","updated_at") VALUES
  (gen_random_uuid(), '전원',   '#ef4444', 1, true, now(), now()),
  (gen_random_uuid(), '접지',   '#eab308', 2, true, now(), now()),
  (gen_random_uuid(), '네트워크','#3b82f6', 3, true, now(), now()),
  (gen_random_uuid(), '광',     '#22c55e', 4, true, now(), now()),
  (gen_random_uuid(), '제어',   '#6b7280', 5, true, now(), now())
ON CONFLICT ("name") DO NOTHING;

-- 카테고리 displayGroup → groupId 연결
UPDATE "cable_categories" c SET "group_id" = g."id"
FROM "cable_groups" g WHERE g."name" = c."display_group";
```

- [ ] **Step 4: 적용 + 클라이언트 재생성** — Run: `npx prisma migrate dev`
Expected: 적용 성공, Prisma Client 재생성.

- [ ] **Step 5: 백필 검증 테스트** — `backend/tests/cableGroupBackfill.integration.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import prisma from '../src/config/prisma.js';

/** 마이그레이션 백필 검증 — dev DB(seed 선행) 의 케이블 카테고리가 그룹에 연결됐는지. */
describe('CableGroup 백필', () => {
  it('5개 그룹이 색과 함께 생성된다', async () => {
    const groups = await prisma.cableGroup.findMany();
    const names = groups.map((g) => g.name).sort();
    expect(names).toEqual(['광', '네트워크', '전원', '접지', '제어']);
    expect(groups.find((g) => g.name === '광')?.color).toBe('#22c55e');
  });

  it('카테고리가 displayGroup 으로 groupId 에 연결된다', async () => {
    const cats = await prisma.cableCategory.findMany({ where: { displayGroup: { not: null } }, include: { group: true } });
    expect(cats.length).toBeGreaterThan(0);
    for (const c of cats) expect(c.group?.name).toBe(c.displayGroup);
  });
});
```

- [ ] **Step 6: 실행** — Run: `npx vitest run tests/cableGroupBackfill.integration.test.ts`
Expected: PASS (2 tests). 실패 시 `npm run db:seed` 선행 확인.

- [ ] **Step 7: 커밋**
```bash
cd /Users/jsk/1210/digital/backend
git add prisma/schema.prisma prisma/migrations tests/cableGroupBackfill.integration.test.ts
git commit -q -m "$(cat <<'EOF'
feat(schema): CableGroup 테이블 + CableCategory.groupId 백필 (C1)

기존 displayGroup → CableGroup(이름+색) 백필, 구필드 유지(additive).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: CableGroup CRUD (service + controller + routes + 등록)

**Files:**
- Create: `backend/src/services/cableGroup.service.ts`
- Create: `backend/src/controllers/cableGroup.controller.ts`
- Create: `backend/src/routes/cableGroups.routes.ts`
- Modify: `backend/src/index.ts` (라우터 등록)
- Test: `backend/tests/cableGroup.service.test.ts`

**Interfaces:**
- Consumes: Task 1 `prisma.cableGroup`, `prisma.cableCategory.count`.
- Produces: `cableGroupService.{getAll,create,update,delete}`; `GET /api/cable-groups`(인증), `POST/PATCH/DELETE`(adminOnly). delete 는 사용 중(category.groupId)이면 `ConflictError`.

- [ ] **Step 1: 실패 테스트** — `backend/tests/cableGroup.service.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { cableGroupService } from '../src/services/cableGroup.service.js';
import prisma from '../src/config/prisma.js';

vi.mock('../src/config/prisma.js', () => ({
  default: {
    cableGroup: { findMany: vi.fn(), findUnique: vi.fn(), create: vi.fn(), update: vi.fn(), delete: vi.fn() },
    cableCategory: { count: vi.fn() },
  },
}));

describe('CableGroupService', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  it('create 는 이름 trim + 색 저장', async () => {
    vi.mocked(prisma.cableGroup.create).mockImplementation(async (a: any) => ({ id: 'g1', sortOrder: 0, isActive: true, ...a.data }) as any);
    const res = await cableGroupService.create({ name: ' 전원케이블 ', color: '#ef4444' });
    expect(res.name).toBe('전원케이블');
    expect(res.color).toBe('#ef4444');
  });

  it('delete 는 사용 중이면 차단', async () => {
    vi.mocked(prisma.cableGroup.findUnique).mockResolvedValue({ id: 'g1', name: '전원' } as any);
    vi.mocked(prisma.cableCategory.count).mockResolvedValue(3);
    await expect(cableGroupService.delete('g1')).rejects.toThrow('사용 중인 종류');
  });

  it('delete 는 미사용이면 삭제', async () => {
    vi.mocked(prisma.cableGroup.findUnique).mockResolvedValue({ id: 'g1', name: '전원' } as any);
    vi.mocked(prisma.cableCategory.count).mockResolvedValue(0);
    await cableGroupService.delete('g1');
    expect(prisma.cableGroup.delete).toHaveBeenCalledWith({ where: { id: 'g1' } });
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd /Users/jsk/1210/digital/backend && npx vitest run tests/cableGroup.service.test.ts` → FAIL(모듈 없음).

- [ ] **Step 3: 서비스** — `backend/src/services/cableGroup.service.ts`:
```ts
import prisma from '../config/prisma.js';
import { ConflictError, NotFoundError } from '../utils/errors.js';

export interface CableGroupDetail { id: string; name: string; color: string | null; sortOrder: number; isActive: boolean }
export interface CreateCableGroupInput { name: string; color?: string | null; sortOrder?: number }
export interface UpdateCableGroupInput { name?: string; color?: string | null; sortOrder?: number; isActive?: boolean }

class CableGroupService {
  private map(g: { id: string; name: string; color: string | null; sortOrder: number; isActive: boolean }): CableGroupDetail {
    return { id: g.id, name: g.name, color: g.color, sortOrder: g.sortOrder, isActive: g.isActive };
  }

  async getAll(): Promise<CableGroupDetail[]> {
    const rows = await prisma.cableGroup.findMany({ where: { isActive: true }, orderBy: [{ sortOrder: 'asc' }, { name: 'asc' }] });
    return rows.map((r) => this.map(r));
  }

  async create(input: CreateCableGroupInput): Promise<CableGroupDetail> {
    const row = await prisma.cableGroup.create({ data: { name: input.name.trim(), color: input.color ?? null, sortOrder: input.sortOrder ?? 0 } });
    return this.map(row);
  }

  async update(id: string, input: UpdateCableGroupInput): Promise<CableGroupDetail> {
    const existing = await prisma.cableGroup.findUnique({ where: { id } });
    if (!existing) throw new NotFoundError('케이블 그룹');
    const row = await prisma.cableGroup.update({
      where: { id },
      data: {
        ...(input.name !== undefined ? { name: input.name.trim() } : {}),
        ...(input.color !== undefined ? { color: input.color } : {}),
        ...(input.sortOrder !== undefined ? { sortOrder: input.sortOrder } : {}),
        ...(input.isActive !== undefined ? { isActive: input.isActive } : {}),
      },
    });
    return this.map(row);
  }

  async delete(id: string): Promise<void> {
    const existing = await prisma.cableGroup.findUnique({ where: { id } });
    if (!existing) throw new NotFoundError('케이블 그룹');
    const inUse = await prisma.cableCategory.count({ where: { groupId: id } });
    if (inUse > 0) throw new ConflictError(`이 그룹을 사용 중인 종류 ${inUse}개가 있어 삭제할 수 없습니다.`);
    await prisma.cableGroup.delete({ where: { id } });
  }
}

export const cableGroupService = new CableGroupService();
```

- [ ] **Step 4: 컨트롤러** — `backend/src/controllers/cableGroup.controller.ts`:
```ts
import { Request, Response, NextFunction } from 'express';
import { cableGroupService } from '../services/cableGroup.service.js';

export const cableGroupController = {
  async getAll(_req: Request, res: Response, next: NextFunction): Promise<void> {
    try { res.json({ data: await cableGroupService.getAll() }); } catch (e) { next(e); }
  },
  async create(req: Request, res: Response, next: NextFunction): Promise<void> {
    try { res.status(201).json({ data: await cableGroupService.create(req.body) }); } catch (e) { next(e); }
  },
  async update(req: Request, res: Response, next: NextFunction): Promise<void> {
    try { res.json({ data: await cableGroupService.update(req.params.id, req.body) }); } catch (e) { next(e); }
  },
  async delete(req: Request, res: Response, next: NextFunction): Promise<void> {
    try { await cableGroupService.delete(req.params.id); res.json({ message: '케이블 그룹이 삭제되었습니다.' }); } catch (e) { next(e); }
  },
};
```

- [ ] **Step 5: 라우트** — `backend/src/routes/cableGroups.routes.ts`:
```ts
import { Router } from 'express';
import { z } from 'zod';
import { cableGroupController } from '../controllers/cableGroup.controller.js';
import { authenticate, adminOnly } from '../middleware/auth.js';
import { validate } from '../middleware/validate.js';

const router = Router();
const createSchema = z.object({ name: z.string().min(1).max(50), color: z.string().max(7).optional().nullable(), sortOrder: z.number().int().min(0).optional() });
const updateSchema = z.object({ name: z.string().min(1).max(50).optional(), color: z.string().max(7).optional().nullable(), sortOrder: z.number().int().min(0).optional(), isActive: z.boolean().optional() });

router.get('/', authenticate, cableGroupController.getAll);
router.post('/', authenticate, adminOnly, validate(createSchema), cableGroupController.create);
router.patch('/:id', authenticate, adminOnly, validate(updateSchema), cableGroupController.update);
router.delete('/:id', authenticate, adminOnly, cableGroupController.delete);

export { router as cableGroupsRouter };
```

- [ ] **Step 6: index.ts 등록** — `import { cableCategoriesRouter }` 줄 다음에 `import { cableGroupsRouter } from './routes/cableGroups.routes.js';`; `app.use('/api/cable-categories', cableCategoriesRouter);` 다음에 `app.use('/api/cable-groups', cableGroupsRouter);`.

- [ ] **Step 7: 통과 + 타입** — Run: `npx vitest run tests/cableGroup.service.test.ts && npx tsc --noEmit | tail -5` → PASS + 에러 없음.

- [ ] **Step 8: 커밋**
```bash
cd /Users/jsk/1210/digital/backend
git add src/services/cableGroup.service.ts src/controllers/cableGroup.controller.ts src/routes/cableGroups.routes.ts src/index.ts tests/cableGroup.service.test.ts
git commit -q -m "feat(cableGroup): 그룹 CRUD API (admin, FK 가드) (C1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: CableCategory — groupId 기반으로 (DTO + create/update)

**Files:**
- Modify: `backend/src/services/cableCategory.service.ts`
- Modify: `backend/src/routes/cableCategories.routes.ts`
- Test: `backend/tests/cableCategory.service.test.ts` (기존 갱신)

**Interfaces:**
- Consumes: Task 1 `prisma.cableCategory.group`.
- Produces: `CableCategoryDetail` 에 `groupId: string | null` + `groupName`/`groupColor`(group 조인). create/update 가 `groupId` 수용·검증. (P1 의 `displayGroup` enum 검증 경로는 groupId 로 대체; displayGroup 컬럼은 C5 까지 유지하되 신규 생성 시 group.name 으로 동기화.)

- [ ] **Step 1: 실패 테스트** — `tests/cableCategory.service.test.ts` 에 추가(기존 describe 유지):
```ts
  it('create 는 groupId 를 싣고 group.name 을 displayGroup 에 동기화', async () => {
    vi.mocked(prisma.cableGroup.findUnique).mockResolvedValue({ id: 'g1', name: '전원' } as any);
    vi.mocked(prisma.cableCategory.create).mockImplementation(async (a: any) => ({
      id: 'x', code: a.data.code, description: null, displayColor: null, iconName: null, unit: null,
      specTemplate: null, sortOrder: 0, isActive: true, createdAt: new Date(), updatedAt: new Date(),
      group: { id: 'g1', name: '전원', color: '#ef4444' }, ...a.data,
    }) as any);
    const res = await cableCategoryService.create({ name: 'Fr-sq3.5', groupId: 'g1' });
    const data = vi.mocked(prisma.cableCategory.create).mock.calls[0][0].data as any;
    expect(data.groupId).toBe('g1');
    expect(data.displayGroup).toBe('전원');
    expect(res.groupId).toBe('g1');
  });

  it('create 는 없는 groupId 거부', async () => {
    vi.mocked(prisma.cableGroup.findUnique).mockResolvedValue(null);
    await expect(cableCategoryService.create({ name: 'x', groupId: 'nope' })).rejects.toThrow('그룹');
  });
```
그리고 mock 에 `cableGroup: { findUnique: vi.fn() }` 추가.

- [ ] **Step 2: 실패 확인** — `npx vitest run tests/cableCategory.service.test.ts` → FAIL.

- [ ] **Step 3: 서비스 수정** — `cableCategory.service.ts`:
  - 입력 타입: `CreateCableCategoryInput = { name: string; groupId: string; sortOrder?: number }`, `UpdateCableCategoryInput = { name?: string; groupId?: string; sortOrder?: number; isActive?: boolean }` (displayGroup 제거).
  - `CableCategoryDetail` 에 `groupId: string | null; groupName: string | null; groupColor: string | null` 추가.
  - `mapToDetail` 에 group 조인 반영: `groupId: c.groupId ?? null, groupName: c.group?.name ?? null, groupColor: c.group?.color ?? null`. (조회 시 `include: { group: true }`.)
  - `create`: groupId 검증(`cableGroup.findUnique`, 없으면 `ValidationError('존재하지 않는 그룹입니다')`), `data: { code, name: name.trim(), groupId, displayGroup: group.name, sortOrder }` (displayGroup 동기화 — C2까지 프론트 호환). `CABLE_GROUPS` enum 검증 제거.
  - `update`: groupId 주면 검증 + displayGroup 동기화.
  - `getAll`/`getById`: `include: { group: true }`.

- [ ] **Step 4: 라우트 수정** — `cableCategories.routes.ts`: `groupEnum` 제거, 스키마를 `groupId: z.string().uuid()`(create 필수, update optional)로:
```ts
const createSchema = z.object({ name: z.string().min(1).max(100), groupId: z.string().uuid(), sortOrder: z.number().int().min(0).optional() });
const updateSchema = z.object({ name: z.string().min(1).max(100).optional(), groupId: z.string().uuid().optional(), sortOrder: z.number().int().min(0).optional(), isActive: z.boolean().optional() });
```

- [ ] **Step 5: 통과 + 타입** — `npx vitest run tests/cableCategory.service.test.ts && npx tsc --noEmit | tail -5` → PASS.

- [ ] **Step 6: 커밋**
```bash
cd /Users/jsk/1210/digital/backend
git add src/services/cableCategory.service.ts src/routes/cableCategories.routes.ts tests/cableCategory.service.test.ts
git commit -q -m "feat(cableCategory): groupId 기반 CRUD + DTO(group 색/이름) (C1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 시드 — CableGroup + categoryId.groupId (멱등)

**Files:**
- Modify: `backend/prisma/seed/cableCategories.ts`
- Test: `backend/tests/cableCategorySeed.integration.test.ts`

**Interfaces:**
- Consumes: Task 1 스키마.
- Produces: 신규/빈 DB 에서도 시드가 CableGroup 5개(색) + 각 카테고리 groupId 를 채운다(백필과 동일 결과 멱등).

- [ ] **Step 1: 실패 테스트** — `tests/cableCategorySeed.integration.test.ts`:
```ts
import { describe, it, expect, beforeAll } from 'vitest';
import prisma from '../src/config/prisma.js';
import { seedCableCategories } from '../prisma/seed/cableCategories.js';

describe('seedCableCategories group', () => {
  beforeAll(async () => { await seedCableCategories(prisma); });
  it('그룹 5개 + 색', async () => {
    const g = await prisma.cableGroup.findMany();
    expect(g.map((x) => x.name).sort()).toEqual(['광', '네트워크', '전원', '접지', '제어']);
  });
  it('카테고리 groupId 연결', async () => {
    const cats = await prisma.cableCategory.findMany({ where: { displayGroup: { not: null } }, include: { group: true } });
    for (const c of cats) expect(c.group?.name).toBe(c.displayGroup);
  });
});
```
> `seedCableCategories` 의 export 명·시그니처는 기존 파일 확인 후 정확히(예: `export async function seedCableCategories(prisma)`).

- [ ] **Step 2: 실패 확인** — `npx vitest run tests/cableCategorySeed.integration.test.ts` → FAIL(그룹 미생성/미연결).

- [ ] **Step 3: 시드 수정** — `prisma/seed/cableCategories.ts` 의 시드 함수에 그룹 upsert + 연결 추가:
  - 색 맵 상수: `const GROUP_COLORS: Record<string,string> = { '전원':'#ef4444','접지':'#eab308','네트워크':'#3b82f6','광':'#22c55e','제어':'#6b7280' };`
  - 함수 시작에서 distinct displayGroup 별 `cableGroup.upsert({ where:{name}, update:{}, create:{ name, color: GROUP_COLORS[name] ?? null } })` → 이름→id 맵.
  - 각 카테고리 upsert 의 create/update data 에 `groupId: groupIdByName.get(t.displayGroup)` 추가.

- [ ] **Step 4: 실행 + 통과** — `npm run db:seed && npx vitest run tests/cableCategorySeed.integration.test.ts` → 성공 로그 + PASS.

- [ ] **Step 5: 전체 백엔드 회귀** — `npx vitest run && npx tsc --noEmit | tail -5` → 전체 PASS + 에러 없음.

- [ ] **Step 6: 커밋**
```bash
cd /Users/jsk/1210/digital/backend
git add prisma/seed/cableCategories.ts tests/cableCategorySeed.integration.test.ts
git commit -q -m "feat(seed): CableGroup + categoryId.groupId 시드 (멱등) (C1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec(§3.1, §7) 커버:** CableGroup 테이블 → T1 ✓ / CableCategory.groupId + 백필 → T1 ✓ / CableGroup CRUD(admin·FK가드) → T2 ✓ / CableCategory groupId 기반 + DTO(group 색·이름) → T3 ✓ / 시드 → T4 ✓. **C1 범위 밖(후속):** 프론트 트레이스(C2)·진입점(C3)·CRUD UI(C4)·구컬럼 드롭(C5)은 별도 plan.
**2. Placeholder scan:** 모든 코드 단계 실제 코드/명령. T4-S1 의 seed export 명만 파일 확인 지시(환경 의존, 방법 명시).
**3. Type consistency:** `CableGroupDetail`(id/name/color/sortOrder/isActive)·`groupId`·`CableCategoryDetail`(+groupId/groupName/groupColor) 가 T1~T4 일관. 색 5종 값이 Global Constraints·T1·T4 동일.

## 주의 / 인터림
- **additive:** displayGroup/displayColor/code 등 유지(프론트 C2 전환 전까지 displayGroup 사용). 신규 카테고리는 displayGroup=group.name 동기화로 호환.
- 구컬럼 드롭(displayGroup/displayColor/code/specTemplate)·`Cable.cableType`/color/label 드롭은 **C5**(프론트 C2~C4 후).
