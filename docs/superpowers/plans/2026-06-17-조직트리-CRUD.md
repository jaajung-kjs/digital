# 조직 트리 CRUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 조직 트리에서 본부·지사·변전소·층을 추가/이름변경/삭제(호버 케밥 메뉴 + 공용 모달 + confirm). 즉시 API 반영.

**Architecture:** 순수 매핑(`orgNodeActions`) → 모달(`OrgNodeModal`) + 케밥(`TreeNodeMenu`) → CRUD 훅(`useOrgNodeCrud`: organizationApi + organizationStore + RQ 무효화) → `TreePanel` 배선. rename PATCH→PUT 버그 수정 포함.

**Tech Stack:** React + Zustand + React Query + 공용 ui(Modal/Input/Button) + Vitest. 빌드 `cd frontend && npm run build`, 테스트 `npx vitest run`. **UI는 CONVENTIONS.md 준수**(text-sm/xs, 시맨틱 토큰, 공용 컴포넌트, 일회용 div 금지).

**근거 스펙:** docs/superpowers/specs/2026-06-17-조직트리-CRUD-design.md

**범위 메모:** 편집 = **이름변경**(rename 엔드포인트). 추가 시 변전소 주소·층 층번호 입력 가능. 주소/층번호 *편집*은 후속(update 스키마 미확인).

---

## Task 1: 순수 매핑 + organizationApi rename 버그 수정

**Files:**
- Create: `frontend/src/components/tree/orgNodeActions.ts`
- Create: `frontend/src/components/tree/orgNodeActions.test.ts`
- Modify: `frontend/src/services/organizationApi.ts` (renameSubstation/renameFloor PATCH→PUT)

- [ ] **Step 1: 실패 테스트** — `orgNodeActions.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { childType, childLabel } from './orgNodeActions';

describe('orgNodeActions', () => {
  it('childType: hq→branch, branch→substation, substation→floor, floor→null', () => {
    expect(childType('headquarters')).toBe('branch');
    expect(childType('branch')).toBe('substation');
    expect(childType('substation')).toBe('floor');
    expect(childType('floor')).toBeNull();
  });
  it('childLabel: 자식 추가 라벨', () => {
    expect(childLabel('headquarters')).toBe('지사 추가');
    expect(childLabel('branch')).toBe('변전소 추가');
    expect(childLabel('substation')).toBe('층 추가');
    expect(childLabel('floor')).toBeNull();
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd frontend && npx vitest run src/components/tree/orgNodeActions.test.ts` → FAIL.

- [ ] **Step 3: 구현** — `orgNodeActions.ts`:
```ts
import type { NodeType } from '../../types/organization';

export function childType(t: NodeType): NodeType | null {
  switch (t) {
    case 'headquarters': return 'branch';
    case 'branch': return 'substation';
    case 'substation': return 'floor';
    default: return null;
  }
}

const CHILD_NOUN: Record<NodeType, string | null> = {
  headquarters: '지사', branch: '변전소', substation: '층', floor: null,
};
export function childLabel(t: NodeType): string | null {
  const n = CHILD_NOUN[t];
  return n ? `${n} 추가` : null;
}
```

- [ ] **Step 4: 통과 확인** — vitest run 위 파일 → PASS.

- [ ] **Step 5: rename 버그 수정** — `organizationApi.ts`: `renameSubstation`·`renameFloor`의 `api.patch` → `api.put` (백엔드가 PUT만 정의 — 현재 404). 본부/지사 rename은 PATCH 유지(백엔드 일치).

- [ ] **Step 6: 타입체크** — `cd frontend && npx tsc --noEmit` → 0.

- [ ] **Step 7: Commit**
```
git add frontend/src/components/tree/orgNodeActions.ts frontend/src/components/tree/orgNodeActions.test.ts frontend/src/services/organizationApi.ts
git commit -m "feat(트리): orgNodeActions 매핑 + organizationApi rename PUT 버그 수정

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: OrgNodeModal (추가/편집 공용 모달)

**Files:**
- Create: `frontend/src/components/tree/OrgNodeModal.tsx`
- Test: `frontend/src/components/tree/OrgNodeModal.test.tsx`

정석 패턴: `frontend/src/features/editor/components/modals/EquipmentMaterialModal.tsx`(공용 `Modal`+`Input`+`Button` footer). 먼저 그 파일과 `components/ui/Modal.tsx`/`Input.tsx`/`Button.tsx` props를 읽고 동일 스타일로.

- [ ] **Step 1: 컴포넌트 작성** — props:
```ts
interface OrgNodeModalProps {
  open: boolean;
  mode: 'add' | 'edit';
  // add: 생성될 자식 타입. edit: 대상 노드 타입.
  targetType: NodeType;
  initialName?: string;       // edit prefill
  onSubmit: (values: { name: string; address?: string; floorNumber?: string }) => Promise<void>;
  onClose: () => void;
}
```
동작:
- 제목: `mode==='add'` → `\`${noun} 추가\`` (noun = 타입의 한글명: 본부/지사/변전소/층), `edit` → `'이름 변경'`.
- 필드: `이름`(항상, 공용 `Input`, autofocus). `mode==='add'` 이고 `targetType==='substation'` → `주소`(선택) Input. `mode==='add'` 이고 `targetType==='floor'` → `층번호`(선택) Input. edit 모드는 이름만.
- footer: `<Button variant="secondary" onClick={onClose}>취소</Button>` + `<Button disabled={!name.trim() || submitting} onClick={submit}>{mode==='add'?'추가':'저장'}</Button>`.
- Enter=submit, Esc=onClose(공용 Modal이 Esc 처리하면 위임). 실패 시 `text-danger text-xs` 인라인 메시지. 제출 중 disabled.
- 라벨은 `text-xs text-content-muted`, 공용 컴포넌트만. 일회용 div 금지(레이아웃 div는 허용, 카드/필드 스타일 일회용 금지).

- [ ] **Step 2: 테스트** — `OrgNodeModal.test.tsx`: (a) add+substation → 이름·주소 필드 렌더, (b) add+floor → 이름·층번호, (c) add+branch → 이름만, (d) edit → 이름만 + initialName prefill, (e) 빈 이름이면 확인 버튼 disabled. (testing-library; 공용 Modal이 portal이면 적절히 렌더.)

- [ ] **Step 3: 검증** — `cd frontend && npx tsc --noEmit && npx vitest run src/components/tree/OrgNodeModal.test.tsx` → 0 에러, 그린.

- [ ] **Step 4: Commit** — `git add ... && git commit -m "feat(트리): OrgNodeModal 추가/편집 공용 모달"` (+ Co-Authored-By trailer).

---

## Task 3: TreeNodeMenu (호버 케밥 드롭다운)

**Files:**
- Create: `frontend/src/components/tree/TreeNodeMenu.tsx`
- Test: `frontend/src/components/tree/TreeNodeMenu.test.tsx`

- [ ] **Step 1: 컴포넌트** — props:
```ts
interface TreeNodeMenuProps {
  node: TreeNodeData;
  onAddChild: (node: TreeNodeData) => void;
  onRename: (node: TreeNodeData) => void;
  onDelete: (node: TreeNodeData) => void;
}
```
동작:
- `⋯`(lucide `MoreHorizontal`/`MoreVertical`) `IconButton`. 클릭 → 절대배치 드롭다운(시맨틱 토큰: `bg-surface border border-line shadow rounded`, 항목 `text-sm hover:bg-surface-2 px-3 py-1.5`).
- 항목: `childLabel(node.type)` 있으면 "+자식추가"(onAddChild), 항상 "이름 변경"(onRename), "삭제"(onDelete, `text-danger`).
- 외부 클릭/Esc 닫힘(document mousedown 리스너 + cleanup). 메뉴 클릭이 트리 노드 onClick으로 버블링되지 않게 `e.stopPropagation()`.
- 케밥은 부모 노드 행에서 hover 시 노출(`opacity-0 group-hover:opacity-100`) — 노출 제어는 TreePanel의 행 컨테이너 group에 의존(Task 5).

- [ ] **Step 2: 테스트** — 타입별 항목 렌더: substation 노드 → "층 추가"·"이름 변경"·"삭제"; floor 노드 → "이름 변경"·"삭제"(자식추가 없음). 항목 클릭 시 콜백 호출.

- [ ] **Step 3: 검증** — tsc + vitest 그린.

- [ ] **Step 4: Commit** — `feat(트리): TreeNodeMenu 호버 케밥 메뉴`.

---

## Task 4: useOrgNodeCrud (API + store + RQ 무효화)

**Files:**
- Create: `frontend/src/components/tree/useOrgNodeCrud.ts`

먼저 `organizationStore`(setChildren/renameNode/removeNode/expandNode/setRoots/viewingNodeId), `organizationApi`(create*/rename*/delete*/listHeadquarters), `fetchChildNodes`, `useQueryClient`를 읽는다.

- [ ] **Step 1: 훅 작성**:
```ts
export function useOrgNodeCrud() {
  const qc = useQueryClient();
  const store = useOrganizationStore; // getState 사용

  async function addChild(parent: TreeNodeData, v: { name: string; address?: string; floorNumber?: string }) {
    const ct = childType(parent.type);
    if (ct === 'branch') await organizationApi.createBranch(parent.id, { name: v.name });
    else if (ct === 'substation') await organizationApi.createSubstation(parent.id, { name: v.name, address: v.address });
    else if (ct === 'floor') {
      await organizationApi.createFloor(parent.id, { name: v.name, floorNumber: v.floorNumber });
      qc.invalidateQueries({ queryKey: ['substation-floors', parent.id] });
    } else return;
    const children = await fetchChildNodes(parent);
    store.getState().setChildren(parent.id, children); // setChildren이 펼침 포함이면 expand 생략
  }

  async function addHeadquarters(name: string) {
    await organizationApi.createHeadquarters({ name });
    const hqs = await organizationApi.listHeadquarters();
    store.getState().setRoots(hqs.map(hqToNode)); // 기존 펼침 보존이 가능하면 머지(구현 시 단순한 쪽)
  }

  async function rename(node: TreeNodeData, name: string) {
    if (node.type === 'headquarters') await organizationApi.renameHeadquarters(node.id, { name });
    else if (node.type === 'branch') await organizationApi.renameBranch(node.id, { name });
    else if (node.type === 'substation') await organizationApi.renameSubstation(node.id, { name });
    else await organizationApi.renameFloor(node.id, { name });
    store.getState().renameNode(node.id, name);
  }

  async function remove(node: TreeNodeData) {
    if (node.type === 'headquarters') await organizationApi.deleteHeadquarters(node.id);
    else if (node.type === 'branch') await organizationApi.deleteBranch(node.id);
    else if (node.type === 'substation') await organizationApi.deleteSubstation(node.id);
    else await organizationApi.deleteFloor(node.id);
    store.getState().removeNode(node.id);
    if (node.type === 'substation') qc.invalidateQueries({ queryKey: ['substation-floors', node.id] });
    if (node.type === 'floor' && node.parentId) qc.invalidateQueries({ queryKey: ['substation-floors', node.parentId] });
  }

  return { addChild, addHeadquarters, rename, remove };
}
```
- `setRoots` 존재 확인(없으면 store에 추가하거나 신규 본부만 append). `hqToNode`/루트 매핑은 `fetchChildNodes`의 HQ 매핑 형태를 참고(또는 listHeadquarters 결과를 TreeNodeData로). 펼침 상태 보존이 어려우면 신규 본부 노드만 roots에 append하는 단순 경로 택.
- 본부 추가는 `setChildren` 대상 부모가 없으므로 roots 갱신 경로 사용.

- [ ] **Step 2: 타입체크** — tsc 0.

- [ ] **Step 3: Commit** — `feat(트리): useOrgNodeCrud CRUD 훅`.

---

## Task 5: TreePanel 배선

**Files:**
- Modify: `frontend/src/components/tree/TreePanel.tsx`

먼저 TreePanel 전체(renderNode 행 구조, 헤더, click/drag 핸들러)를 읽는다.

- [ ] **Step 1: 케밥 배선** — renderNode의 노드 행 컨테이너를 `group relative`로(이미 hover 배경 있으면 거기에), 우측에 `<TreeNodeMenu node={node} onAddChild onRename onDelete />` 추가. 케밥/메뉴 클릭이 행 onClick(네비게이션)으로 전파되지 않게 stopPropagation. drag(draggable)와 충돌 없게 케밥 버튼은 `draggable={false}`.

- [ ] **Step 2: 헤더 +본부** — "조직 트리" 헤더 영역(라인 ~218)에 `IconButton`(+, `Plus`) 추가 → add-HQ 모달 오픈.

- [ ] **Step 3: 모달/확인 상태** — TreePanel에 상태:
```ts
const crud = useOrgNodeCrud();
const [modal, setModal] = useState<{ mode:'add'|'edit'; targetType: NodeType; parent?: TreeNodeData; node?: TreeNodeData; initialName?: string } | null>(null);
```
  - onAddChild(node) → `setModal({ mode:'add', targetType: childType(node.type)!, parent: node })`
  - 헤더 + → `setModal({ mode:'add', targetType:'headquarters' })`
  - onRename(node) → `setModal({ mode:'edit', targetType: node.type, node, initialName: node.name })`
  - onDelete(node) → `confirm(deleteMessage(node))` 통과 시 `crud.remove(node)`.
  - `<OrgNodeModal>` 렌더: onSubmit → add면 `parent ? crud.addChild(parent, v) : crud.addHeadquarters(v.name)`, edit면 `crud.rename(node, v.name)`. 성공 시 `setModal(null)`.
- `deleteMessage(node)`: 타입별 cascade 경고. 예) substation → `\`${node.name} 변전소를 삭제하면 하위 층·자산·연결이 모두 삭제됩니다. 계속할까요?\``. 본부/지사는 더 강한 경고. `node.meta`의 개수 있으면 문구에 포함.

- [ ] **Step 4: 검증** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → 0 에러, 빌드 통과, 전체 그린.

- [ ] **Step 5: Commit** — `feat(트리): 조직 트리 CRUD 배선(케밥·모달·삭제확인·+본부)`.

---

## Task 6: 검증

- [ ] **Step 1:** `cd frontend && npm run build` 통과, `npx vitest run` 전체 그린, `cd ../backend && npm run build` 통과(백엔드 변경 없으면 생략 가능).
- [ ] **Step 2: 브라우저 수동**(dev): 각 계층 추가/이름변경/삭제, 삭제 cascade 경고, 트리 즉시 반영, 평면도 층 드롭다운 동기화(층 추가/삭제 후 `['substation-floors']` 무효화 확인). rename이 404 없이 동작(특히 변전소·층 — PUT 수정).
- [ ] **Step 3:** 수동 검증 중 수정 있으면 별도 커밋.

---

## Self-Review
- **스펙 커버리지:** UX(케밥/헤더/모달/confirm)=Task3+5, 컴포넌트=Task2-4, rename버그=Task1, 데이터흐름=Task4, 타입매핑=Task1. 매핑됨.
- **타입 일관성:** `childType`/`childLabel`/`OrgNodeModal`/`TreeNodeMenu`/`useOrgNodeCrud`/`NodeType` 통일.
- **주의(구현 시 확인):** `organizationStore`에 `setRoots` 존재 여부 / `setChildren`이 expand 포함 여부 / 공용 `Modal`·`Input`·`Button`·`IconButton` props / TreePanel renderNode 행 구조. 실제 파일 읽고 맞출 것.
- **컨벤션:** 모든 신규 UI는 공용 컴포넌트 + text-sm/xs + 시맨틱 토큰. 일회용 카드/필드 금지.
