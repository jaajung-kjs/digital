# 연결 모델 대통일 — P3a 선번장 범용 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 선번장(FiberRegister)을 범용 모델 위에서 재구축한다 — OFD(컨테이너 자산) → OFD-SLOT(conduit 자식) → 번호 OUT 케이블(=코어) 그리드. 상대국측(far)은 **단일 `cableTrace`** 를 전 변전소 피드(global assets+cables)에 돌린 투영으로 채우고, 코어 메타(용도/수용내역/융착/사용)는 **OUT 케이블 `specParams`** 에서 읽고 쓴다. 계통(PanelCircuitView)과 동형. `usePortStatus`/`buildFiberCoreRows`/`FiberCore` 의존을 그리드에서 제거(파일은 P5까지 보존).

**Architecture:** (1) 백엔드 — Cable DTO 에 `sourceRole/targetRole/number` 노출 + 전 변전소 slim assets 엔드포인트 `GET /api/assets`(cableTrace 가 노드 connectionKind 를 알아야 cross-substation fan-out). (2) 프론트 — `traceGraph` 피드(global assets+cables + 이 변전소 staged 오버레이 → `{assets,cables}`), `cableTrace` 의 실데이터 입력. fiberAdapter 가상 remap 대체. (3) 선번장 컴포넌트를 슬롯/케이블 기반으로 재작성. **추가/전환만 — 레거시 파일·데이터 무삭제(P5에서).**

**Tech Stack:** Node/Prisma/TS, React/TS/Zustand, React Query, Vitest.

**참조:** P2 `frontend/src/features/trace/cableTrace.ts`(범용 트레이스, 변경 없음), P3a 어댑터 `fiberAdapter.ts`(검증용, 대체 예정), 계통 템플릿 `frontend/src/features/power/components/PanelCircuitView.tsx`.

---

## 환경 규칙 (공통)
- DB: dev compose 만(이미 떠있음). **docker build/up 금지.** psql: `docker exec ict-twin-postgres psql -U postgres -d ict_digital_twin -c "<sql>"`.
- 백 빌드/테스트: `cd backend && npx tsc --noEmit && npx vitest run`. 프론트: `cd frontend && npm run build && npx vitest run`. 단일 파일 테스트: `npx vitest run <path>`.
- 마이그레이션된 실데이터가 이미 dev DB 에 있다(OFD-SLOT 자산 20 / OPGW 10 / 번호 OUT 20, 모두 `cable_type='FIBER'`). 새 시드/마이그레이션 불필요.
- 무관한 `gangwonSubstations.ts` seed 가 깨질 수 있음(사전 부채) — 결과는 psql 로 검증.

## 데이터 형태 (현재 — 확인됨)
- OFD 자산: `assetType.placementKind==='OFD'`, `substationId` 보유.
- OFD-SLOT 자산: `assetType.code==='OFD-SLOT'`, `assetType.connectionKind==='conduit'`, `parentAssetId=OFD.id`, `name=대국명`. 양 변전소에 각각 존재(slot.substationId = 그 OFD 의 변전소).
- OUT 케이블: `cableType='FIBER'`, `sourceRole='OUT'`(또는 target), `number=코어번호`, 한쪽 끝=slot, 다른 끝=로컬 설비. `specParams` 에 `{purpose,circuitText,spliceType,usageOverride}`(있을 때) + `__migration` 마커.
- OPGW 케이블: `cableType='FIBER'`, `sourceRole='IN' & targetRole='IN'`, 양 끝=두 slot. `specParams.cores`=용량(있으면). substationId polymorphic 로 양 변전소 effectiveCables 에 모두 노출.
- `cableTrace(start, 'FIBER', assets, cables)`: 설비에서 시작 → OUT#K→slot(conduit)→OPGW→대국 slot→OUT#K→대국 설비. `assets` 는 `{id, connectionKind}`, `cables` 는 `{id,cableType,sourceAssetId,targetAssetId,sourceRole,targetRole,number}`.

## File Structure
- `backend/src/services/cable.service.ts` (Modify) — `CableDetail` + `mapToDetail` 에 roles/number.
- `backend/src/services/asset.service.ts` (Modify) — `SlimAsset` 타입 + `toSlimAsset()`(pure, export) + `listAllSlim()`.
- `backend/src/controllers/asset.controller.ts` (Modify) — `listAll`.
- `backend/src/routes/assets.routes.ts` (Modify) — `GET /`.
- `backend/src/services/__tests__/toSlimAsset.test.ts` (Create) — pure 매퍼 단위테스트.
- `frontend/src/features/trace/traceGraph.ts` (Create) — global 피드 + `buildTraceGraph` + `useTraceGraph` + `traceRemoteEndpoints`.
- `frontend/src/features/trace/traceGraph.test.ts` (Create) — 단위테스트.
- `frontend/src/features/fiber/slotRegister.ts` (Create) — pure 행 빌더 `buildSlotCoreRows`.
- `frontend/src/features/fiber/slotRegister.test.ts` (Create) — 단위테스트.
- `frontend/src/features/fiber/components/OfdFiberRegister.tsx` (Rewrite) — 슬롯/케이블 그리드.
- `frontend/src/features/fiber/components/FiberRegisterView.tsx` (Modify) — 변경 거의 없음(슬롯 기반 OfdFiberRegister 호출 유지).

---

## Task 1: 백엔드 — Cable DTO 에 sourceRole/targetRole/number

**Files:** `backend/src/services/cable.service.ts`

`Cable` 모델엔 이미 `sourceRole/targetRole/number` 스칼라 컬럼이 있다(P1). `mapToDetail` 이 이를 DTO 로 노출만 하면 된다 — `cableInclude` 는 `include` 라 스칼라는 기본 반환되므로 select 변경 불필요.

- [ ] **Step 1: 인터페이스** — `CableDetail`(파일 29~56) 의 `cableType` 아래에 추가:
```ts
  sourceRole: 'IN' | 'OUT' | null;
  targetRole: 'IN' | 'OUT' | null;
  number: number | null;
```

- [ ] **Step 2: 매퍼** — `mapToDetail`(파일 241~268) 의 `cableType: c.cableType,` 아래에 추가:
```ts
      sourceRole: (c.sourceRole ?? null) as 'IN' | 'OUT' | null,
      targetRole: (c.targetRole ?? null) as 'IN' | 'OUT' | null,
      number: c.number ?? null,
```

- [ ] **Step 3: tsc + 실데이터 확인** — `cd backend && npx tsc --noEmit` → PASS.
서비스가 실제로 채우는지 prisma 로 직접 확인(인증 우회):
```bash
cd backend && npx tsx -e "import {cableService} from './src/services/cable.service.js'; const r = await cableService.getAll(); const o = r.find(c=>c.sourceRole==='OUT'); console.log(JSON.stringify({sourceRole:o?.sourceRole, number:o?.number, type:o?.cableType})); process.exit(0)"
```
Expected: `{"sourceRole":"OUT","number":1,"type":"FIBER"}` (또는 number 가 해당 코어번호).

- [ ] **Step 4: Commit**
```bash
cd /Users/jsk/1210/digital
git add backend/src/services/cable.service.ts
git commit -m "feat(대통일P3a): Cable DTO 에 sourceRole/targetRole/number 노출 — 전역 피드가 cableTrace 입력을 들도록"
```

---

## Task 2: 백엔드 — 전 변전소 slim assets 엔드포인트 (TDD)

**Files:** `backend/src/services/asset.service.ts`, `backend/src/controllers/asset.controller.ts`, `backend/src/routes/assets.routes.ts`, `backend/src/services/__tests__/toSlimAsset.test.ts`

cableTrace 가 cross-substation 으로 노드 `connectionKind` 를 알아야 하므로, 전 변전소 자산의 최소 필드(`id,name,substationId,parentAssetId,connectionKind,code`)를 내리는 전역 엔드포인트가 필요하다(전역 `GET /api/cables` 와 짝).

> **대국명 주의(구현 중 발견):** dev 의 OFD 자산은 전부 이름이 `"OFD"` 라 마이그레이션이 슬롯명을 모두 `"OFD"` 로 넣었다 — 섹션 헤더로 무의미. 의미있는 대국 식별자는 **상대 변전소 이름**(화천S/S 등)이고 substations 엔 실제 이름이 있다. 그래서 slim asset 에 `substationName`(asset→substation 조인)을 포함시켜, 그리드가 `로컬변전소 - 대국변전소` 섹션 헤더를 만들 수 있게 한다(대국 = 슬롯의 OPGW twin 슬롯의 substationName).

- [ ] **Step 1: 실패 테스트** — Create `backend/tests/toSlimAsset.test.ts` (레포의 지배적 테스트 위치 = `backend/tests/`):
```ts
import { describe, it, expect } from 'vitest';
import { toSlimAsset } from '../src/services/asset.service.js';

describe('toSlimAsset', () => {
  it('자산 row 를 trace 용 최소 필드로 좁힌다 (connectionKind 는 assetType, substationName 은 substation 조인)', () => {
    const row = {
      id: 'slot1', name: 'OFD', substationId: 'subA', parentAssetId: 'ofd1',
      assetType: { code: 'OFD-SLOT', connectionKind: 'conduit' },
      substation: { name: '홍천S/S' },
      extra: 'ignored',
    };
    expect(toSlimAsset(row as never)).toEqual({
      id: 'slot1', name: 'OFD', substationId: 'subA', substationName: '홍천S/S',
      parentAssetId: 'ofd1', connectionKind: 'conduit', code: 'OFD-SLOT',
    });
  });

  it('connectionKind 없으면 null, substation 없으면 substationName null', () => {
    const row = {
      id: 'eq1', name: '광단말', substationId: 'subA', parentAssetId: null,
      assetType: { code: 'OPT-TRANS', connectionKind: null },
      substation: null,
    };
    const s = toSlimAsset(row as never);
    expect(s.connectionKind).toBe(null);
    expect(s.substationName).toBe(null);
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd backend && npx vitest run src/services/__tests__/toSlimAsset.test.ts` → FAIL(`toSlimAsset` 미존재).

- [ ] **Step 3: 서비스** — `asset.service.ts` 상단(다른 export 근처)에 타입 + pure 매퍼 + 목록 메서드 추가:
```ts
export interface SlimAsset {
  id: string;
  name: string;
  substationId: string;
  substationName: string | null;
  parentAssetId: string | null;
  connectionKind: 'distributor' | 'conduit' | null;
  code: string | null;
}

/** prisma asset row(assetType + substation 포함)을 trace 용 최소 필드로 좁힌다. 순수 함수. */
export function toSlimAsset(a: {
  id: string; name: string; substationId: string; parentAssetId: string | null;
  assetType: { code: string | null; connectionKind: string | null };
  substation?: { name: string | null } | null;
}): SlimAsset {
  return {
    id: a.id,
    name: a.name,
    substationId: a.substationId,
    substationName: a.substation?.name ?? null,
    parentAssetId: a.parentAssetId ?? null,
    connectionKind: (a.assetType?.connectionKind ?? null) as SlimAsset['connectionKind'],
    code: a.assetType?.code ?? null,
  };
}
```
`AssetService` 클래스(메서드들 사이, 예: `listBySubstation` 아래)에 추가:
```ts
  /** 전 변전소 자산을 trace 용 최소 필드로(전역 cableTrace 피드). */
  async listAllSlim(): Promise<SlimAsset[]> {
    const rows = await prisma.asset.findMany({
      select: {
        id: true, name: true, substationId: true, parentAssetId: true,
        assetType: { select: { code: true, connectionKind: true } },
        substation: { select: { name: true } },
      },
      orderBy: { createdAt: 'asc' },
    });
    return rows.map(toSlimAsset);
  }
```
> `Asset` 모델에 `substation` 관계가 있는지 확인(대개 있음). 관계명이 다르면(예: 없음) `prisma.substation` 별도 조회 후 map 으로 합쳐도 됨 — 단, 단순 select 조인이 우선.
> `prisma` 가 이 파일에 import 돼 있는지 확인(이미 `listBySubstation` 등에서 사용 중이면 OK). 없으면 기존 import 패턴을 따른다.

- [ ] **Step 4: 컨트롤러** — `asset.controller.ts` 에 메서드 추가(`listBySubstation` 패턴 모방):
```ts
  async listAll(_req: Request, res: Response, next: NextFunction): Promise<void> {
    try {
      const assets = await assetService.listAllSlim();
      res.json({ data: assets });
    } catch (err) {
      next(err);
    }
  }
```
> `assetService`/`Request,Response,NextFunction` import 가 이미 있는지 확인(파일 상단). 기존 메서드와 동일 시그니처.

- [ ] **Step 5: 라우트** — `assets.routes.ts` 에서 `/:id` **위**(더 구체적 경로 우선 규칙)에 루트 GET 추가:
```ts
// 전 변전소 자산 최소 목록(전역 cableTrace 피드) — '/:id' 보다 먼저.
router.get('/', authenticate, assetController.listAll);
```
(`/:assetId/connections` 와 `/:id` 사이 또는 맨 위. `/` 는 `/:id` 와 충돌하지 않으나 순서 안전하게 위에 둔다.)

- [ ] **Step 6: 통과 + 실데이터 확인**
`cd backend && npx vitest run src/services/__tests__/toSlimAsset.test.ts` → PASS(2). `npx tsc --noEmit` → PASS.
```bash
cd backend && npx tsx -e "import {assetService} from './src/services/asset.service.js'; const r = await assetService.listAllSlim(); const s = r.filter(a=>a.connectionKind==='conduit'); console.log('total', r.length, 'conduit', s.length, JSON.stringify(s[0])); process.exit(0)"
```
Expected: `conduit 20` 이상, 샘플에 `connectionKind:'conduit'`, `code:'OFD-SLOT'`, `substationName`(예: '화천S/S') 채워짐.

- [ ] **Step 7: Commit**
```bash
cd /Users/jsk/1210/digital
git add backend/src/services/asset.service.ts backend/src/controllers/asset.controller.ts backend/src/routes/assets.routes.ts backend/src/services/__tests__/toSlimAsset.test.ts
git commit -m "feat(대통일P3a): 전 변전소 slim assets 엔드포인트 GET /api/assets — 전역 cableTrace 노드 connectionKind 피드"
```

---

## Task 3: 프론트 — 전역 trace 그래프 피드 (TDD)

**Files:** `frontend/src/features/trace/traceGraph.ts`, `frontend/src/features/trace/traceGraph.test.ts`

global assets(slim) + global cables 를 가져와 이 변전소 staged 변경을 오버레이한 `{assets:TraceAsset[], cables:TraceCable[]}` 를 만든다 — `cableTrace` 의 실데이터 입력. fiberAdapter 의 가상 remap 을 실데이터로 대체. 또한 far 투영 헬퍼 `traceRemoteEndpoints` 를 제공한다.

### 매퍼 규칙
- global cable DTO → `TraceCable`: `{id, cableType, sourceAssetId, targetAssetId, sourceRole, targetRole, number}`.
- staged cable(effectiveCables Cable row) → 동형(이미 roles/number 보유).
- slim asset → `TraceAsset`: `{id, connectionKind}`. staged asset(effectiveAssets) → `{id, connectionKind: a.assetType?.connectionKind ?? null}`.
- 오버레이: cable 은 `overlayStagedOntoGlobal` 과 같은 규칙(deletes 제거 → 이 변전소 id 는 staged 로 교체 → 임시 id staged-create 추가). asset 도 동일 규칙(이 변전소 staged asset 으로 교체/추가, deletes 제거).
- `traceRemoteEndpoints(startAssetId, graph)`: `cableTrace` 결과 nodeIds 중 **passive(connectionKind 없음) && != start** 인 노드 id 들(=대국 설비). 이름은 호출측이 nameById 로 해소.

### Step 1: 실패 테스트 — Create `frontend/src/features/trace/traceGraph.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildTraceGraph, traceRemoteEndpoints, remoteSlotSubstation } from './traceGraph';

// 원주 slotA ──OPGW── 홍천 slotB. eqA─OUT#5─slotA ; eqB─OUT#5─slotB ; eqC─OUT#6─slotB
const slimAssets = [
  { id: 'slotA', name: 'OFD', substationId: 'subW', substationName: '원주S/S', parentAssetId: 'ofdW', connectionKind: 'conduit' as const, code: 'OFD-SLOT' },
  { id: 'slotB', name: 'OFD', substationId: 'subH', substationName: '홍천S/S', parentAssetId: 'ofdH', connectionKind: 'conduit' as const, code: 'OFD-SLOT' },
  { id: 'eqA', name: '광단말A', substationId: 'subW', substationName: '원주S/S', parentAssetId: null, connectionKind: null, code: 'OPT-TRANS' },
  { id: 'eqB', name: '광단말B', substationId: 'subH', substationName: '홍천S/S', parentAssetId: null, connectionKind: null, code: 'OPT-TRANS' },
  { id: 'eqC', name: '광단말C', substationId: 'subH', substationName: '홍천S/S', parentAssetId: null, connectionKind: null, code: 'OPT-TRANS' },
];
const globalCables = [
  { id: 'opgw', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'slotB', sourceRole: 'IN', targetRole: 'IN', number: null },
  { id: 'oA5', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'eqA', sourceRole: 'OUT', targetRole: null, number: 5 },
  { id: 'oB5', cableType: 'FIBER', sourceAssetId: 'slotB', targetAssetId: 'eqB', sourceRole: 'OUT', targetRole: null, number: 5 },
  { id: 'oB6', cableType: 'FIBER', sourceAssetId: 'slotB', targetAssetId: 'eqC', sourceRole: 'OUT', targetRole: null, number: 6 },
];

describe('buildTraceGraph + traceRemoteEndpoints', () => {
  it('global 만으로 그래프를 만들고 같은 번호 대국 설비를 투영한다', () => {
    const g = buildTraceGraph({ slimAssets, globalCables, stagedAssets: [], stagedCables: [], deletes: [] });
    expect(g.assets.find((a) => a.id === 'slotA')?.connectionKind).toBe('conduit');
    expect(g.cables.find((c) => c.id === 'oA5')?.number).toBe(5);
    const remote = traceRemoteEndpoints('eqA', g);
    expect(remote).toContain('eqB');     // 번호 5 짝
    expect(remote).not.toContain('eqC'); // 번호 6
    expect(remote).not.toContain('eqA'); // 시작 자신 제외
    expect(remote).not.toContain('slotA'); // conduit 제외(passive 만)
    // 대국 섹션 헤더용: slotA 의 대국 변전소 = twin slotB 의 substationName
    expect(remoteSlotSubstation('slotA', g)).toBe('홍천S/S');
  });

  it('이 변전소 staged cable 이 global 위에 오버레이된다 (deletes 제거 + 임시 id 추가)', () => {
    const g = buildTraceGraph({
      slimAssets,
      globalCables,
      stagedAssets: [],
      stagedCables: [{ id: 'tmp-new', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'eqA', sourceRole: 'OUT', targetRole: null, number: 7 }],
      deletes: ['oA5'],
    });
    expect(g.cables.some((c) => c.id === 'oA5')).toBe(false);  // 삭제
    expect(g.cables.some((c) => c.id === 'tmp-new')).toBe(true); // 추가
  });
});
```

### Step 2: 실패 확인 — `cd frontend && npx vitest run src/features/trace/traceGraph.test.ts` → FAIL.

### Step 3: 구현 — Create `frontend/src/features/trace/traceGraph.ts`:
```ts
import { type QueryClient } from '@tanstack/react-query';
import { useQuery } from '@tanstack/react-query';
import { api } from '../../utils/api';
import { useSubstationWorkingCopy } from '../workingCopy/substationStore';
import { cableTrace, type TraceAsset, type TraceCable } from './cableTrace';

/** 전 변전소 slim asset DTO (백엔드 GET /api/assets). */
export interface SlimAssetDTO {
  id: string;
  name: string;
  substationId: string;
  substationName: string | null;
  parentAssetId: string | null;
  connectionKind: 'distributor' | 'conduit' | null;
  code: string | null;
}

/** trace 입력에 필요한 cable 필드만(전역 DTO / 로컬 staged 공통). */
export interface TraceCableInput {
  id: string;
  cableType?: string | null;
  sourceAssetId?: string | null;
  targetAssetId?: string | null;
  sourceRole?: 'IN' | 'OUT' | null;
  targetRole?: 'IN' | 'OUT' | null;
  number?: number | null;
}

export interface TraceGraph {
  assets: TraceAsset[];
  cables: TraceCable[];
  /** 표시용 자산 이름 해소(전역+로컬). */
  nameById: Map<string, string>;
  /** 자산 id → 그 자산이 속한 변전소 이름(대국 섹션 헤더용). */
  subNameById: Map<string, string>;
}

const ASSET_KEYS = { all: ['assets-slim'] as const };
const CABLE_KEYS = { all: ['cables'] as const };

/** 전 변전소 slim assets 캐시 fetch (React 밖 소비처용). */
export function fetchAllSlimAssetsCached(qc: QueryClient): Promise<SlimAssetDTO[]> {
  return qc.fetchQuery({
    queryKey: ASSET_KEYS.all,
    staleTime: 30_000,
    queryFn: async () => (await api.get<{ data: SlimAssetDTO[] }>('/assets')).data.data,
  });
}

const toTraceCable = (c: TraceCableInput): TraceCable => ({
  id: c.id,
  cableType: c.cableType ?? null,
  sourceAssetId: c.sourceAssetId ?? null,
  targetAssetId: c.targetAssetId ?? null,
  sourceRole: c.sourceRole ?? null,
  targetRole: c.targetRole ?? null,
  number: c.number ?? null,
});

/**
 * 전역(slim assets + cables) 위에 이 변전소 staged 변경을 오버레이해 cableTrace 입력 그래프를 만든다.
 * 순수 함수 — 테스트/재사용. (overlayStagedOntoGlobal 과 같은 규칙: deletes 제거 →
 * 이 변전소 id 는 staged 로 교체 → 임시 id staged-create 추가.)
 */
export function buildTraceGraph(input: {
  slimAssets: SlimAssetDTO[];
  globalCables: TraceCableInput[];
  stagedAssets: { id: string; assetType?: { connectionKind?: string | null } | null; name?: string }[];
  stagedCables: TraceCableInput[];
  deletes: string[];
}): TraceGraph {
  const deleted = new Set(input.deletes);

  // ── cables ──
  const stagedCableById = new Map(input.stagedCables.map((c) => [c.id, c]));
  const mergedCables: TraceCableInput[] = input.globalCables
    .filter((c) => !deleted.has(c.id))
    .map((c) => stagedCableById.get(c.id) ?? c);
  const seenC = new Set(mergedCables.map((c) => c.id));
  for (const c of input.stagedCables) {
    if (!seenC.has(c.id) && !deleted.has(c.id)) mergedCables.push(c);
  }

  // ── assets ──
  const stagedAssetById = new Map(input.stagedAssets.map((a) => [a.id, a]));
  const nameById = new Map<string, string>();
  const subNameById = new Map<string, string>();
  const assetById = new Map<string, TraceAsset>();
  for (const a of input.slimAssets) {
    if (deleted.has(a.id)) continue;
    const staged = stagedAssetById.get(a.id);
    assetById.set(a.id, {
      id: a.id,
      connectionKind: staged
        ? ((staged.assetType?.connectionKind ?? null) as TraceAsset['connectionKind'])
        : a.connectionKind,
    });
    nameById.set(a.id, (staged?.name ?? a.name));
    if (a.substationName) subNameById.set(a.id, a.substationName);
  }
  for (const a of input.stagedAssets) {
    if (assetById.has(a.id) || deleted.has(a.id)) continue;
    assetById.set(a.id, { id: a.id, connectionKind: (a.assetType?.connectionKind ?? null) as TraceAsset['connectionKind'] });
    if (a.name) nameById.set(a.id, a.name);
  }

  return { assets: [...assetById.values()], cables: mergedCables.map(toTraceCable), nameById, subNameById };
}

/**
 * 한 슬롯(conduit)의 대국 변전소 이름 — 슬롯의 OPGW(IN-IN FIBER) 케이블 → twin 슬롯 →
 * 그 substationName. 없으면 null. 선번장 섹션 헤더(`로컬 - 대국`)용.
 */
export function remoteSlotSubstation(slotId: string, graph: TraceGraph, cableType = 'FIBER'): string | null {
  const opgw = graph.cables.find(
    (c) => c.cableType === cableType && c.sourceRole === 'IN' && c.targetRole === 'IN'
      && (c.sourceAssetId === slotId || c.targetAssetId === slotId),
  );
  if (!opgw) return null;
  const twin = opgw.sourceAssetId === slotId ? opgw.targetAssetId : opgw.sourceAssetId;
  return twin ? (graph.subNameById.get(twin) ?? null) : null;
}

/**
 * start(설비) 에서 cableType 'FIBER' 로 trace → 도달한 **passive(설비) 노드 중 자신 제외** id 들.
 * = 대국측 설비. 이름 해소는 호출측이 graph.nameById 로.
 */
export function traceRemoteEndpoints(startAssetId: string, graph: TraceGraph, cableType = 'FIBER'): string[] {
  const kindOf = new Map(graph.assets.map((a) => [a.id, a.connectionKind ?? null]));
  const r = cableTrace(startAssetId, cableType, graph.assets, graph.cables);
  return r.nodeIds.filter((id) => id !== startAssetId && (kindOf.get(id) ?? null) === null);
}

/**
 * React 훅 — 전역 피드 + 이 변전소 staged 오버레이로 TraceGraph 를 만든다.
 * 선번장/(추후)하이라이트가 공유. React Query 가 fetch 를 dedupe.
 */
export function useTraceGraph(): { graph: TraceGraph | null; isLoading: boolean } {
  const slimQ = useQuery({
    queryKey: ASSET_KEYS.all,
    staleTime: 30_000,
    queryFn: async () => (await api.get<{ data: SlimAssetDTO[] }>('/assets')).data.data,
  });
  const cableQ = useQuery({
    queryKey: CABLE_KEYS.all,
    staleTime: 30_000,
    queryFn: async () => (await api.get<{ data: TraceCableInput[] }>('/cables')).data.data,
  });
  // 이 변전소 staged 변경 구독(overlay 슬라이스 ref 변경 시 재계산).
  const savedCables = useSubstationWorkingCopy((s) => s.saved.cables);
  const overlayCables = useSubstationWorkingCopy((s) => s.overlays.cables);
  const savedAssets = useSubstationWorkingCopy((s) => s.saved.assets);
  const overlayAssets = useSubstationWorkingCopy((s) => s.overlays.assets);

  const isLoading = slimQ.isLoading || cableQ.isLoading;
  if (!slimQ.data || !cableQ.data) return { graph: null, isLoading };

  const wc = useSubstationWorkingCopy.getState();
  const graph = buildTraceGraph({
    slimAssets: slimQ.data,
    globalCables: cableQ.data,
    stagedAssets: wc.effectiveAssets() as never[],
    stagedCables: wc.effectiveCables() as unknown as TraceCableInput[],
    deletes: overlayCables.deletes,
  });
  void savedCables; void overlayCables; void savedAssets; void overlayAssets; // 구독 트리거(값은 getState 로 읽음)
  return { graph, isLoading };
}
```
> 주의: `api` 응답 형태(`{data:{data:[]}}`)는 `useFiberPaths`/pathHighlightStore 와 동일 패턴. `cableTrace`/`TraceAsset`/`TraceCable` 는 P2 export 그대로.

### Step 4: 통과 + tsc — `cd frontend && npx vitest run src/features/trace/traceGraph.test.ts` → PASS(2); `npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/trace/traceGraph.ts frontend/src/features/trace/traceGraph.test.ts
git commit -m "feat(대통일P3a): 전역 trace 그래프 피드(buildTraceGraph/useTraceGraph) + 대국 투영 — fiberAdapter 가상 remap 을 실데이터로 대체"
```

---

## Task 4: 프론트 — 선번장 행 빌더 (pure, TDD)

**Files:** `frontend/src/features/fiber/slotRegister.ts`, `frontend/src/features/fiber/slotRegister.test.ts`

한 OFD-SLOT 의 코어 행을 만든다 — OUT 케이블(=점유 코어) + 용량까지 빈 코어. 메타는 OUT 케이블 specParams. far(대국측)는 graph 가 있으면 trace 투영, 없으면 null. 순수 함수.

### 타입/규칙
- 입력: `slot`(자산), `localCables`(이 변전소 effectiveCables — slot OUT/OPGW 포함), `graph`(TraceGraph|null).
- OUT 행: `localCables` 중 `cableType==='FIBER'` && slot 끝점 && 그 끝의 role==='OUT'. `coreNumber=c.number`, `nearAssetId`=slot 반대편, 메타=`c.specParams`. far=graph?`traceRemoteEndpoints(nearAssetId).map(name).join(', ')`:null.
- 용량: slot 의 OPGW(role IN-IN) `specParams.cores`(number). 없으면 점유 최대번호.
- 빈 행: 1..capacity 중 점유번호 제외 → `occupied:false`, near/far/메타 빈.
- 정렬: coreNumber asc.

### Step 1: 실패 테스트 — Create `frontend/src/features/fiber/slotRegister.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildSlotCoreRows } from './slotRegister';
import { buildTraceGraph } from '../trace/traceGraph';

const slot = { id: 'slotA', name: 'OFD', parentAssetId: 'ofdW' };
const localCables = [
  { id: 'opgw', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'slotB', sourceRole: 'IN', targetRole: 'IN', number: null, specParams: { cores: 4 } },
  { id: 'oA3', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'eqA', sourceRole: 'OUT', targetRole: null, number: 3, specParams: { purpose: '보호', __migration: 'x' } },
];
const slim = [
  { id: 'slotA', name: 'OFD', substationId: 'subW', substationName: '원주S/S', parentAssetId: 'ofdW', connectionKind: 'conduit' as const, code: 'OFD-SLOT' },
  { id: 'slotB', name: 'OFD', substationId: 'subH', substationName: '홍천S/S', parentAssetId: 'ofdH', connectionKind: 'conduit' as const, code: 'OFD-SLOT' },
  { id: 'eqA', name: '광단말A', substationId: 'subW', substationName: '원주S/S', parentAssetId: null, connectionKind: null, code: 'X' },
  { id: 'eqB', name: '광단말B', substationId: 'subH', substationName: '홍천S/S', parentAssetId: null, connectionKind: null, code: 'X' },
];
const globalCables = [
  ...localCables.map((c) => ({ ...c })),
  { id: 'oB3', cableType: 'FIBER', sourceAssetId: 'slotB', targetAssetId: 'eqB', sourceRole: 'OUT', targetRole: null, number: 3, specParams: {} },
];

describe('buildSlotCoreRows', () => {
  it('점유 행(메타·근접) + 용량까지 빈 행 + far 투영, 코어 번호 1..N 연속', () => {
    const graph = buildTraceGraph({ slimAssets: slim, globalCables, stagedAssets: [], stagedCables: [], deletes: [] });
    const rows = buildSlotCoreRows(slot, localCables, graph);
    expect(rows.map((r) => r.coreNumber)).toEqual([1, 2, 3, 4]); // cores=4, 연속
    const r3 = rows.find((r) => r.coreNumber === 3)!;
    expect(r3.occupied).toBe(true);
    expect(r3.cableId).toBe('oA3');
    expect(r3.nearAssetId).toBe('eqA');
    expect(r3.purpose).toBe('보호');
    expect(r3.farName).toBe('광단말B');                  // 번호3 대국 설비
    const r1 = rows.find((r) => r.coreNumber === 1)!;
    expect(r1.occupied).toBe(false);
    expect(r1.cableId).toBe(null);
  });

  it('graph 없으면 far 는 null, 점유 행은 그대로', () => {
    const rows = buildSlotCoreRows(slot, localCables, null);
    expect(rows.find((r) => r.coreNumber === 3)!.farName).toBe(null);
  });

  it('점유 코어가 cores 를 초과하면 용량이 확장되고 1..max 연속(가려지지 않음)', () => {
    const lc = [
      { id: 'opgw', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'slotB', sourceRole: 'IN', targetRole: 'IN', number: null, specParams: { cores: 4 } },
      { id: 'oA6', cableType: 'FIBER', sourceAssetId: 'slotA', targetAssetId: 'eqA', sourceRole: 'OUT', targetRole: null, number: 6, specParams: {} },
    ];
    const rows = buildSlotCoreRows(slot, lc, null);
    expect(rows.map((r) => r.coreNumber)).toEqual([1, 2, 3, 4, 5, 6]); // max(4,6)=6, 연속
    expect(rows.find((r) => r.coreNumber === 6)!.occupied).toBe(true);
    expect(rows.find((r) => r.coreNumber === 4)!.occupied).toBe(false);
  });
});
```

### Step 2: 실패 확인 — `cd frontend && npx vitest run src/features/fiber/slotRegister.test.ts` → FAIL.

### Step 3: 구현 — Create `frontend/src/features/fiber/slotRegister.ts`:
```ts
import { traceRemoteEndpoints, type TraceGraph } from '../trace/traceGraph';

export interface SlotCoreRow {
  coreNumber: number;
  cableId: string | null;     // 점유 코어의 OUT 케이블 id (메타 편집 대상)
  occupied: boolean;
  nearAssetId: string | null; // slot 반대편(이 변전소 설비)
  farName: string | null;     // 대국측 설비 이름(trace 투영)
  purpose: string | null;
  circuitText: string | null;
  spliceType: string | null;
  usageOverride: string | null;
  usage: '사용' | '미사용';
}

interface SlotLike { id: string; name?: string; parentAssetId?: string | null }
interface CableLike {
  id: string; cableType?: string | null;
  sourceAssetId?: string | null; targetAssetId?: string | null;
  sourceRole?: 'IN' | 'OUT' | null; targetRole?: 'IN' | 'OUT' | null;
  number?: number | null; specParams?: Record<string, unknown> | null;
}

const roleAt = (c: CableLike, a: string) => (c.sourceAssetId === a ? c.sourceRole : c.targetRole) ?? null;
const other = (c: CableLike, a: string) => (c.sourceAssetId === a ? c.targetAssetId : c.sourceAssetId) ?? null;
const str = (v: unknown): string | null => (v === null || v === undefined || v === '' ? null : String(v));

/** 한 OFD-SLOT 의 코어 행(점유 OUT 케이블 + 용량까지 빈 코어). graph 있으면 far 투영. */
export function buildSlotCoreRows(slot: SlotLike, localCables: CableLike[], graph: TraceGraph | null): SlotCoreRow[] {
  const fiber = localCables.filter((c) => c.cableType === 'FIBER' && (c.sourceAssetId === slot.id || c.targetAssetId === slot.id));
  const outs = fiber.filter((c) => roleAt(c, slot.id) === 'OUT');
  const opgw = fiber.find((c) => roleAt(c, slot.id) === 'IN');

  const cores = Number((opgw?.specParams as Record<string, unknown> | undefined)?.cores ?? 0);
  const occupiedNums = new Set(outs.map((c) => c.number ?? 0).filter((n) => n > 0));
  const capacity = Math.max(cores, ...(occupiedNums.size ? [...occupiedNums] : [0]));

  const byNum = new Map<number, CableLike>();
  for (const c of outs) if (c.number != null) byNum.set(c.number, c);

  const rows: SlotCoreRow[] = [];
  for (let n = 1; n <= capacity; n++) {
    const c = byNum.get(n);
    if (c) {
      const sp = (c.specParams ?? {}) as Record<string, unknown>;
      const nearAssetId = other(c, slot.id);
      const usageOverride = str(sp.usageOverride);
      const farIds = graph && nearAssetId ? traceRemoteEndpoints(nearAssetId, graph) : [];
      const farName = graph && nearAssetId
        ? (farIds.map((id) => graph.nameById.get(id) ?? id).join(', ') || null)
        : null;
      rows.push({
        coreNumber: n, cableId: c.id, occupied: true,
        nearAssetId, farName,
        purpose: str(sp.purpose), circuitText: str(sp.circuitText),
        spliceType: str(sp.spliceType), usageOverride,
        usage: (usageOverride as '사용' | '미사용' | null) ?? '사용',
      });
    } else {
      rows.push({
        coreNumber: n, cableId: null, occupied: false,
        nearAssetId: null, farName: null,
        purpose: null, circuitText: null, spliceType: null, usageOverride: null,
        usage: '미사용',
      });
    }
  }
  return rows;
}
```

### Step 4: 통과 + tsc — `cd frontend && npx vitest run src/features/fiber/slotRegister.test.ts` → PASS(2); `npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/slotRegister.ts frontend/src/features/fiber/slotRegister.test.ts
git commit -m "feat(대통일P3a): 선번장 행 빌더 buildSlotCoreRows(slot→OUT 케이블=코어, far=trace 투영, 메타=specParams)"
```

---

## Task 5: 프론트 — 선번장 컴포넌트 재작성

**Files:** `frontend/src/features/fiber/components/OfdFiberRegister.tsx`(Rewrite), `frontend/src/features/fiber/components/FiberRegisterView.tsx`(거의 무변경 — 확인만)

OFD 의 OFD-SLOT 자식별 섹션 + `buildSlotCoreRows` 행. 메타 편집 → `wc.patch('cables', cableId, {specParams})`(기존 specParams 머지). `usePortStatus`/`buildFiberCoreRows`/`useEffectiveFiberCores`/`FiberCore` 의존 제거(파일은 보존).

- [ ] **Step 1: 재작성** — Replace `OfdFiberRegister.tsx` 전체:
```tsx
import { useMemo } from 'react';
import { useEffectiveAssets, useEffectiveCables } from '../../workingCopy/hooks';
import { useSelectionStore } from '../../workspace/selectionStore';
import { usePathHighlightStore } from '../../pathTrace/stores/pathHighlightStore';
import { useSubstationWorkingCopy } from '../../workingCopy/substationStore';
import { useTraceGraph, remoteSlotSubstation } from '../../trace/traceGraph';
import { buildSlotCoreRows, type SlotCoreRow } from '../slotRegister';
import type { Asset } from '../../../types/asset';

const TH = 'px-2 py-2 text-[12px] font-medium tracking-wide text-content-muted whitespace-nowrap';
const TD = 'px-2 text-[13px] align-middle whitespace-nowrap';
const CELL_INPUT = 'w-full text-[13px] border border-line rounded px-1.5 py-1 bg-surface text-content';

/** 점유 코어 한 필드를 OUT 케이블 specParams 에 머지 스테이징(기존 키 보존). */
function commitMeta(cableId: string, field: string, value: string | null) {
  const wc = useSubstationWorkingCopy.getState();
  const cable = wc.effectiveCables().find((c) => c.id === cableId);
  const prev = ((cable?.specParams as Record<string, unknown>) ?? {});
  wc.patch('cables', cableId, { specParams: { ...prev, [field]: value } });
}

/** 한 OFD 의 선번장 — OFD-SLOT 자식별 섹션 + 코어 행(범용 모델). */
export function OfdFiberRegister({ ofdId }: { ofdId: string }) {
  const assets = useEffectiveAssets() as Asset[];
  const cables = useEffectiveCables();
  const { graph, isLoading } = useTraceGraph();

  const slots = useMemo(
    () => assets.filter((a) => a.parentAssetId === ofdId && a.assetType?.connectionKind === 'conduit'),
    [assets, ofdId],
  );

  const sections = useMemo(
    () => slots.map((slot) => ({
      slot,
      rows: buildSlotCoreRows(
        slot as never,
        cables as unknown as Parameters<typeof buildSlotCoreRows>[1],
        graph,
      ),
    })),
    [slots, cables, graph],
  );

  if (!sections.length) return null;

  return (
    <div className="space-y-5">
      {sections.map(({ slot, rows }) => {
        const used = rows.filter((r) => r.usage === '사용').length;
        // 섹션 헤더 = `로컬변전소 - 대국변전소`. 로컬 = OFD(=slot 부모, 로컬)의 변전소,
        // 대국 = 슬롯 OPGW twin 슬롯의 변전소. slot.name 은 dev 에서 전부 'OFD' 라 안 씀.
        const localSub = graph?.subNameById.get(ofdId) ?? null;
        const remoteSub = graph ? remoteSlotSubstation(slot.id, graph) : null;
        const title = [localSub, remoteSub].filter(Boolean).join(' - ') || (slot.name ?? '광경로');
        return (
          <section key={slot.id}>
            <header className="mb-1.5 flex items-baseline gap-2 px-1">
              <h3 className="text-sm font-semibold text-content">{title}</h3>
              <span className="ml-auto text-[12px] tabular-nums text-content-faint">
                사용 {used}/{rows.length}{isLoading ? ' · 대국 불러오는 중…' : ''}
              </span>
            </header>
            <table className="w-full border-collapse">
              <thead>
                <tr className="text-left bg-surface-2 border-b border-line-strong">
                  <th className={`${TH} w-14`}>코어</th>
                  <th className={TH}>근접자산</th>
                  <th className={TH}>상대국측</th>
                  <th className={TH}>용도</th>
                  <th className={TH}>수용내역</th>
                  <th className={`${TH} w-24`}>융착</th>
                  <th className={`${TH} w-28`}>사용</th>
                </tr>
              </thead>
              <tbody>
                {rows.map((r) => <CoreRow key={r.coreNumber} ofdId={ofdId} nameById={graph?.nameById} row={r} />)}
              </tbody>
            </table>
          </section>
        );
      })}
    </div>
  );
}

function CoreRow({ ofdId, row, nameById }: { ofdId: string; row: SlotCoreRow; nameById?: Map<string, string> }) {
  const tracingCableId = usePathHighlightStore((s) => s.tracingCableId);
  const active = !!row.cableId && tracingCableId === row.cableId;
  const nearName = row.nearAssetId ? (nameById?.get(row.nearAssetId) ?? null) : null;

  const onClick = () => {
    useSelectionStore.getState().setSelectedAssetId(row.nearAssetId ?? ofdId);
  };

  return (
    <tr
      onClick={onClick}
      className={`h-9 cursor-pointer border-b border-line transition-colors ${
        active ? 'bg-info-bg shadow-[inset_3px_0_0_var(--primary)]' : 'hover:bg-surface-2 active:bg-surface-3'
      }`}
    >
      <td className={`${TD} tabular-nums text-content-muted`}>{row.coreNumber}</td>
      <td className={`${TD} text-content max-w-[12rem] truncate`} title={nearName ?? undefined}>
        {nearName ?? <span className="text-content-faint">—</span>}
      </td>
      <td className={`${TD} text-content-muted max-w-[12rem] truncate`} title={row.farName ?? undefined}>
        {row.farName ?? <span className="text-content-faint">—</span>}
      </td>
      <td className={TD} onClick={(e) => e.stopPropagation()}>
        <input aria-label="용도" placeholder="용도" defaultValue={row.purpose ?? ''} disabled={!row.cableId}
          key={`${row.coreNumber}-purpose-${row.purpose ?? ''}`}
          onBlur={(e) => { const v = e.target.value || null; if (row.cableId && v !== row.purpose) commitMeta(row.cableId, 'purpose', v); }}
          className={CELL_INPUT} />
      </td>
      <td className={TD} onClick={(e) => e.stopPropagation()}>
        <input aria-label="수용내역" placeholder="수용내역" defaultValue={row.circuitText ?? ''} disabled={!row.cableId}
          key={`${row.coreNumber}-circuit-${row.circuitText ?? ''}`}
          onBlur={(e) => { const v = e.target.value || null; if (row.cableId && v !== row.circuitText) commitMeta(row.cableId, 'circuitText', v); }}
          className={CELL_INPUT} />
      </td>
      <td className={TD} onClick={(e) => e.stopPropagation()}>
        <select aria-label="융착" value={row.spliceType ?? ''} disabled={!row.cableId}
          onChange={(e) => { const v = e.target.value || null; if (row.cableId && v !== row.spliceType) commitMeta(row.cableId, 'spliceType', v); }}
          className={CELL_INPUT}>
          <option value="">—</option>
          <option value="융착">융착</option>
          <option value="패치">패치</option>
        </select>
      </td>
      <td className={TD} onClick={(e) => e.stopPropagation()}>
        <select aria-label="사용" value={row.usageOverride ?? '자동'} disabled={!row.cableId}
          onChange={(e) => { const v = e.target.value === '자동' ? null : e.target.value; if (row.cableId && v !== row.usageOverride) commitMeta(row.cableId, 'usageOverride', v); }}
          className={CELL_INPUT}>
          <option value="자동">자동{row.occupied ? '(사용)' : '(미사용)'}</option>
          <option value="사용">사용</option>
          <option value="미사용">미사용</option>
        </select>
      </td>
    </tr>
  );
}
```

- [ ] **Step 2: FiberRegisterView 확인** — `FiberRegisterView.tsx` 는 OFD 목록(placementKind 'OFD')만 만들어 OfdFiberRegister 에 ofdId 를 넘긴다. **변경 불필요** — 다만 `import { OfdFiberRegister }` 가 유효한지, OFD 가 0개일 때 빈 메시지가 그대로인지 확인. (그대로 두면 됨.)

- [ ] **Step 3: tsc + 빌드** — `cd frontend && npx tsc --noEmit && npm run build` → PASS.
  - 만약 `OfdFiberRegister` 의 기존 import(usePortStatus/buildFiberCoreRows/useEffectiveFiberCores/FiberCoreRow)를 참조하는 **다른** 곳이 있으면 그대로 둔다(이 파일에서만 제거). `usePortStatus` 는 `OfdPortPicker` 도 쓰므로 파일 삭제 금지(P5).

- [ ] **Step 4: 렌더 스모크(선택)** — dev 서버가 떠있으면 선번장 뷰를 열어 섹션(슬롯명)·코어 행·근접/상대국 표시 확인. (브라우저 자동화 없이 수동.)

- [ ] **Step 5: Commit**
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/components/OfdFiberRegister.tsx frontend/src/features/fiber/components/FiberRegisterView.tsx
git commit -m "feat(대통일P3a): 선번장을 범용 모델로 재작성 — OFD→슬롯(conduit)→OUT 케이블 그리드, far=cableTrace 투영, 메타=cable.specParams"
```

---

## Task 6: P3a 검증

- [ ] **Step 1: 백엔드** — `cd backend && npx tsc --noEmit && npx vitest run` → PASS(기존 67 + toSlimAsset 2).
- [ ] **Step 2: 프론트** — `cd frontend && npm run build && npx vitest run` → PASS(기존 + traceGraph 2 + slotRegister 2).
- [ ] **Step 3: 무삭제 확인** — `git diff main --stat` 에 레거시 파일(`usePortStatus.ts`/`fiberRegister.ts`/`OfdPortPicker.tsx`/`fiberToSlots*`) 변경/삭제 없음. FiberCore 테이블·fiber 데이터 무변경(psql: `SELECT count(*) FROM fiber_cores; SELECT count(*) FROM fiber_paths;` 그대로).
- [ ] **Step 4: 최종 리뷰** — subagent-driven 최종 리뷰(매핑·far 투영 정확성·specParams 머지가 마커 보존·빈 행 용량 산출·레거시 무삭제).

---

## Self-Review
- **스펙(대통일 design §2.2/2.3 + 사용자 합의):** 선번장 = OFD(컨테이너)→슬롯(conduit 자식)→OUT 케이블(코어), 메타=케이블 specParams, far=단일 cableTrace 투영 = 계통과 동형. ✅
- **Option A(클라 전역피드) 충실:** 백엔드는 slim assets + cable roles 노출만(읽기). 트레이스는 클라 단일 `cableTrace` 가 (전역 cables + 전역 assets) 위에서. 이 변전소 staged 오버레이로 미저장 변경도 반영. ✅
- **추가/전환만:** 레거시 파일·fiber 데이터·FiberCore 무삭제(P5). `cableTrace`(P2) 무변경. fiberAdapter 는 실데이터 피드(traceGraph)로 대체되지만 파일은 P5까지 둠. ✅
- **타입 일관성:** `SlimAssetDTO`(백 SlimAsset 과 동형), `TraceCableInput`→`TraceCable`, `SlotCoreRow`. `cableType='FIBER'` 일치. `cable.number`(범용) 사용 — fiberPortNumber 아님. ✅
- **위험/확인(구현 중):**
  - `traceRemoteEndpoints` 는 start=설비(passive) 전제(cableTrace precondition). 행 빌더가 near=설비에서 시작하므로 충족.
  - 빈 코어 메타 편집 불가(케이블 없음) = 설계상 의도(메타는 연결에 귀속). disabled 처리.
  - 용량(cores) 미설정 시 빈 행은 점유 최대번호까지만 — OPGW specParams.cores 없으면 빈 행 0(점유만). 실데이터엔 cores 없을 수 있음 → 점유 행만 보일 수 있음(회귀 아님, 신규).
  - `useTraceGraph` 가 전역 `/assets`+`/cables` 를 매 선번장 진입 시 fetch(30s staleTime 캐시) — 토폴로지와 동일 비용. 허용.
- **다음(P3b):** pathHighlightStore 를 `cableTrace`+traceGraph 로 전환(레거시 `utils/cableTracer` 대체), 토폴로지 재구성(엣지/링), 평면도 하이라이트=trace 노드의 현재층 배치 투영. 그 후 P5(레거시 삭제: usePortStatus/OfdPortPicker/buildPortStatuses/fiberRegister/fiberAdapter/split traces + 옛 fiber 데이터).
