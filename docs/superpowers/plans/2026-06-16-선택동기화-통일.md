# 선택 동기화 통일 + 포트 케이블 진입점 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 그리드·포트/CB 패널·연결탭의 선택을 `selectionStore (selectedAssetId, selectedCore)` 단일 소스로 통일하고, 하이라이트는 중앙 1곳에서 파생해 이중 선택 버그·파편화를 없앤다. 이후 경로슬롯 포트뷰에 자국 OUT 케이블 진입점을 추가.

**Architecture:** `resolveSelectedCable(asset, core)` 가 (자산, 코어)→케이블 하나를 풀고, `useSelectionHighlight` 훅(WorkspacePage 1회)이 `selectedCore` 변화→`startTrace`/`clearHighlight` 로 하이라이트를 파생한다. 그리드는 `selectedCore` 만으로 하이라이트(tracingCableId 분기 제거), 연결탭은 `DiagramComponent.core` 로 `setSelected` 동기화, per-panel `startTrace` effect 는 제거.

**Tech Stack:** TypeScript, React, Zustand, Vitest. 검증: `npx tsc --noEmit` + `npx vitest run` (frontend 에서).

**환경:** tsc/vitest 는 `cd /Users/jsk/1210/digital/frontend` 후. git 은 `git -C /Users/jsk/1210/digital`. 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## 파일 구조
- **신규** `frontend/src/features/workspace/selectionHighlight.ts` — `resolveSelectedCable`(순수) + `useSelectionHighlight`(훅).
- **신규** `frontend/src/features/workspace/selectionHighlight.test.ts` — resolveSelectedCable 단위.
- **수정** `connections/connectionDiagram.ts`(+test) — `DiagramComponent.core`.
- **수정** `connections/registerGrid/ConnectionRegisterGrid.tsx` — RowView selectedCore 단일 하이라이트, rowTraceCableId prop 제거.
- **수정** `connections/registerGrid/registerTypes.ts` — `rowTraceCableId` 필드 제거.
- **수정** `fiber/fiberRegisterDescriptor.tsx` — `rowTraceCableId` 제거.
- **수정** `connections/components/AssetConnectionsTab.tsx`(+test) — selectedCore 동기화.
- **수정** `fiber/components/SlotPortsPanel.tsx`(+test) — startTrace effect 제거 + 자국 진입점.
- **수정** `power/components/FeederCircuitsPanel.tsx` — startTrace effect 제거.
- **수정** `pages/WorkspacePage.tsx` — `useSelectionHighlight()` 마운트.

---

## Task 1: `resolveSelectedCable` (순수 함수)

**Files:**
- Create: `frontend/src/features/workspace/selectionHighlight.ts`
- Create: `frontend/src/features/workspace/selectionHighlight.test.ts`

- [ ] **Step 1: 실패 테스트**

`frontend/src/features/workspace/selectionHighlight.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { resolveSelectedCable } from './selectionHighlight';

const cables = [
  { id: 'in', sourceAssetId: 'src', targetAssetId: 'F', sourceRole: 'OUT', targetRole: 'IN', number: null },
  { id: 'b2', sourceAssetId: 'F', targetAssetId: 'L2', sourceRole: 'OUT', targetRole: null, number: 2 },
  { id: 'o3', sourceAssetId: 'S', targetAssetId: 'e3', sourceRole: 'OUT', targetRole: null, number: 3 },
  { id: 'opgw', sourceAssetId: 'S', targetAssetId: 'RS', sourceRole: 'IN', targetRole: 'IN', number: null },
];

describe('resolveSelectedCable', () => {
  it('슬롯 OUT 코어: number===core 케이블', () => {
    expect(resolveSelectedCable('S', 3, cables)).toBe('o3');
  });
  it('피더 분기: number===core(CB)', () => {
    expect(resolveSelectedCable('F', 2, cables)).toBe('b2');
  });
  it('피더 입력(core 0): 그 자산의 IN 케이블', () => {
    expect(resolveSelectedCable('F', 0, cables)).toBe('in');
  });
  it('core null: 자산에 닿는 대표 케이블(전체 연결 하이라이트)', () => {
    expect(resolveSelectedCable('F', null, cables)).toBe('in');
  });
  it('자산 없음/매칭 없음 → null', () => {
    expect(resolveSelectedCable(null, 2, cables)).toBeNull();
    expect(resolveSelectedCable('S', 9, cables)).toBeNull();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/workspace/selectionHighlight.test.ts`
Expected: FAIL — module 없음.

- [ ] **Step 3: 구현**

`frontend/src/features/workspace/selectionHighlight.ts`:
```ts
import { useEffect } from 'react';
import { useTraceGraph } from '../trace/traceGraph';
import { usePathHighlightStore } from '../pathTrace/stores/pathHighlightStore';
import { useSelectionStore } from './selectionStore';
import { FEEDER_INPUT_CORE } from '../power/powerRegisterDescriptor';
import type { TraceGraph } from '../trace/traceGraph';

type CableLike = { id: string; sourceAssetId?: string | null; targetAssetId?: string | null; sourceRole?: string | null; targetRole?: string | null; number?: number | null };

/**
 * (자산, 코어) → 하이라이트 seed 케이블 한 개.
 * - core 센티넬(0=FEEDER_INPUT_CORE) → 그 자산의 IN 케이블(피더 입력).
 * - core 숫자 → 그 자산에 닿는 number===core 케이블(슬롯 OUT코어 / 피더 OUT분기 통일).
 * - core null → 그 자산에 닿는 대표 케이블(자산 전체 연결 하이라이트).
 * 매칭 없으면 null.
 */
export function resolveSelectedCable(
  assetId: string | null,
  core: number | null,
  cables: CableLike[],
): string | null {
  if (!assetId) return null;
  const roleAt = (c: CableLike) => (c.sourceAssetId === assetId ? c.sourceRole : c.targetRole) ?? null;
  const touching = cables.filter((c) => c.sourceAssetId === assetId || c.targetAssetId === assetId);
  if (core === FEEDER_INPUT_CORE) return touching.find((c) => roleAt(c) === 'IN')?.id ?? null;
  if (core != null) return touching.find((c) => (c.number ?? null) === core)?.id ?? null;
  return touching[0]?.id ?? null;
}

/**
 * 선택(selectedAssetId, selectedCore) → 하이라이트 파생. 워크스페이스에서 1회 마운트.
 * per-panel startTrace 를 대체하는 단일 소스 — 어느 뷰/탭에서 선택해도 동일 하이라이트.
 */
export function useSelectionHighlight(): void {
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const selectedCore = useSelectionStore((s) => s.selectedCore);
  const { graph } = useTraceGraph();
  useEffect(() => {
    const cables = ((graph as TraceGraph | null)?.cables ?? []) as CableLike[];
    const id = resolveSelectedCable(selectedAssetId, selectedCore, cables);
    const hi = usePathHighlightStore.getState();
    if (id) hi.startTrace(id);
    else hi.clearHighlight();
  }, [selectedAssetId, selectedCore, graph]);
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/workspace/selectionHighlight.test.ts && npx tsc --noEmit`
Expected: PASS(5), tsc clean.

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add frontend/src/features/workspace/selectionHighlight.ts frontend/src/features/workspace/selectionHighlight.test.ts
git -C /Users/jsk/1210/digital commit -m "feat(선택): resolveSelectedCable + useSelectionHighlight — (자산,코어)→하이라이트 파생 단일 소스

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: `DiagramComponent.core`

**Files:**
- Modify: `frontend/src/features/connections/connectionDiagram.ts`
- Modify: `frontend/src/features/connections/connectionDiagram.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

`connectionDiagram.test.ts` 의 `describe('buildConnectionDiagram', ...)` 안에 추가:
```ts
  it('component.core: 슬롯 회로=코어번호, 피더 다분기=null', () => {
    // 광 OFD: 코어 #1·#2 별 회로 → 각 component.core = 1·2
    const fiber = buildConnectionDiagram({ graph: fiberGraph, assets: fiberAssets, assetId: 'ofd', categoryGroupOf: catGroupOf })[0].components;
    expect(fiber.map((c) => c.core).sort()).toEqual([1, 2]);
    // 피더: 입력 + 2분기(부하1·부하2, number 다름) → 한 회로, core=null
    const graph = withCables([
      { id: 'in', cableType: 'POWER', sourceAssetId: 'SRC', targetAssetId: 'F', sourceRole: 'OUT', targetRole: 'IN', categoryId: 'pwr' },
      { id: 'b1', cableType: 'POWER', sourceAssetId: 'F', targetAssetId: 'L1', sourceRole: 'OUT', targetRole: null, number: 1, categoryId: 'pwr' },
      { id: 'b2', cableType: 'POWER', sourceAssetId: 'F', targetAssetId: 'L2', sourceRole: 'OUT', targetRole: null, number: 2, categoryId: 'pwr' },
    ]);
    const fd = buildConnectionDiagram({ graph, assets, assetId: 'F', categoryGroupOf: catGroupOf })[0].components;
    expect(fd).toHaveLength(1);
    expect(fd[0].core).toBeNull();
  });
```
주: 광 픽스처 `f1`(number 1)·`f2`(number 2)는 이미 존재. 피더 분기에 `number` 를 넣어 다분기 → null 검증.

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/connections/connectionDiagram.test.ts`
Expected: FAIL — `core` 없음(undefined).

- [ ] **Step 3: 구현**

`connectionDiagram.ts` line 15 인터페이스에 `core` 추가:
```ts
export interface DiagramComponent { seedCableId: string; root: DiagramNode; cableIds: string[]; nodeIds: string[]; core: number | null }
```
`connectionDiagram.ts` 의 `addToGroup(cg, { seedCableId, root, cableIds, nodeIds });` (line 206) 직전에 core 계산 후 포함:
```ts
      // 컴포넌트의 sub-선택(core): self 에 닿고 number 있는 케이블의 distinct number 가 1개면 그 값.
      // 슬롯 회로=OUT 코어 1개→코어번호, 피더 다분기=여러 number→null.
      const selfNums = [...new Set(
        cableIds
          .map((cid) => cableById.get(cid))
          .filter((c): c is Cable => !!c && c.number != null && (selfSet.has(c.sourceAssetId ?? '') || selfSet.has(c.targetAssetId ?? '')))
          .map((c) => c.number as number),
      )];
      const core = selfNums.length === 1 ? selfNums[0] : null;
      addToGroup(cg, { seedCableId, root, cableIds, nodeIds, core });
```

- [ ] **Step 4: 통과 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/connections/connectionDiagram.test.ts && npx tsc --noEmit`
Expected: PASS, tsc clean.

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add frontend/src/features/connections/connectionDiagram.ts frontend/src/features/connections/connectionDiagram.test.ts
git -C /Users/jsk/1210/digital commit -m "feat(연결도): DiagramComponent.core — self 닿는 코어/CB 번호(슬롯=코어, 피더 다분기=null)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 그리드 selectedCore 단일 하이라이트 (이중 선택 버그 제거)

**Files:**
- Modify: `frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx`
- Modify: `frontend/src/features/connections/registerGrid/registerTypes.ts`
- Modify: `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`

- [ ] **Step 1: RowView 하이라이트를 selectedCore 단일로**

`ConnectionRegisterGrid.tsx` `RowView`(line 129-150) 교체 — `traceCableId`/`tracingCableId` 제거:
```tsx
function RowView<Row>({ row, columns, selectedId, rowCore, onClick }: {
  row: Row;
  columns: RegisterColumn<Row>[];
  selectedId: string | null;
  rowCore: number | null;
  onClick: () => void;
}) {
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const selectedCore = useSelectionStore((s) => s.selectedCore);
  const highlighted = !!selectedId && selectedAssetId === selectedId && (rowCore ?? null) === (selectedCore ?? null);
  return (
    <tr onClick={onClick} className={rowClass(highlighted)}>
      {columns.map((c, i) => (
        <td key={i} className={`${TD_BASE} ${cellPad(i === 0, i === columns.length - 1)}`}>{c.cell(row)}</td>
      ))}
    </tr>
  );
}
```

- [ ] **Step 2: SectionView·grid 에서 rowTraceCableId 배선 제거**

`ConnectionRegisterGrid.tsx`:
- `SectionView` props 에서 `rowTraceCableId` 제거(line 62-71 의 타입 + 구조분해), `RowView` 호출(line 109-120)의 `traceCableId={...}` 줄 제거.
- 상위 `ConnectionRegisterGrid` 의 `<SectionView ... rowTraceCableId={descriptor.rowTraceCableId} ... />`(line 49) 줄 제거.
- 미사용이 되면 `usePathHighlightStore` import(line 4) 제거.

- [ ] **Step 3: 타입·디스크립터에서 rowTraceCableId 제거**

`registerTypes.ts` line 37 `rowTraceCableId?(row: Row): string | null;` 줄 삭제.
`fiberRegisterDescriptor.tsx` line 40 `rowTraceCableId: (row) => row.cableId,` 줄 삭제.

- [ ] **Step 4: tsc + 전체 테스트**

Run: `cd /Users/jsk/1210/digital/frontend && npx tsc --noEmit && npx vitest run`
Expected: tsc clean, green. (그리드/디스크립터 테스트가 rowTraceCableId 를 참조하면 갱신.)

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add -A
git -C /Users/jsk/1210/digital commit -m "fix(그리드): 행 하이라이트를 selectedCore 단일 기준으로 — tracingCableId 이중 선택 제거 + rowTraceCableId 데드코드 정리

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 중앙 하이라이트 마운트 + per-panel startTrace 제거

**Files:**
- Modify: `frontend/src/pages/WorkspacePage.tsx`
- Modify: `frontend/src/features/fiber/components/SlotPortsPanel.tsx`
- Modify: `frontend/src/features/power/components/FeederCircuitsPanel.tsx`

- [ ] **Step 1: WorkspacePage 에 훅 마운트**

`WorkspacePage.tsx` 상단 import 에 추가:
```ts
import { useSelectionHighlight } from '../features/workspace/selectionHighlight';
```
`useEditorSelectionBridge(selectedAssetId, view === 'plan');`(line 170) 바로 아래 추가:
```ts
  useSelectionHighlight(); // 선택(selectedCore)→하이라이트 파생, 어느 뷰/탭에서 선택해도 단일 소스
```

- [ ] **Step 2: SlotPortsPanel startTrace/clear effect 제거**

`SlotPortsPanel.tsx` 의 두 effect(line 62-73 의 `traceCableId` 계산 + 두 useEffect) 삭제:
```tsx
  const selected = ports.find((p) => p.coreNumber === selectedCore) ?? null;
  const traceCableId = selected ? (selected.localCableId ?? selected.remoteCableId) : null;

  useEffect(() => {
    const hi = usePathHighlightStore.getState();
    if (selectedCore === null) return;
    if (traceCableId) hi.startTrace(traceCableId);
    else hi.clearHighlight();
  }, [selectedCore, traceCableId]);

  useEffect(() => () => usePathHighlightStore.getState().clearHighlight(), []);
```
→ 교체(상세카드는 `selected` 필요하므로 남김, 하이라이트 effect만 제거):
```tsx
  const selected = ports.find((p) => p.coreNumber === selectedCore) ?? null;
```
그리고 미사용이 된 `usePathHighlightStore` import(line 4)·`useEffect` import(line 1)에서 useEffect 제거(useMemo 만 남김).

- [ ] **Step 3: FeederCircuitsPanel startTrace/clear effect 제거**

`FeederCircuitsPanel.tsx` 의 하이라이트 effect 2개 삭제:
```tsx
  useEffect(() => {
    const hi = usePathHighlightStore.getState();
    if (showInput && input?.cableId) { hi.startTrace(input.cableId); return; }
    if (selectedCb === null) return;
    if (selected?.cableId) hi.startTrace(selected.cableId);
    else hi.clearHighlight();
  }, [selectedCb, selected?.cableId, showInput, input?.cableId]);
  useEffect(() => () => usePathHighlightStore.getState().clearHighlight(), []);
```
(두 useEffect 줄을 통째 삭제.) 미사용이 되면 `usePathHighlightStore` import 제거. `useEffect` 가 파일에서 더 안 쓰이면 import 에서 제거(다른 effect 가 남아있으면 유지).

- [ ] **Step 4: tsc + 전체 테스트**

Run: `cd /Users/jsk/1210/digital/frontend && npx tsc --noEmit && npx vitest run`
Expected: tsc clean, green. (SlotPortsPanel/FeederCircuitsPanel 테스트가 startTrace 를 단언하면 그 단언 제거 — 하이라이트는 이제 중앙 훅 담당.)

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add -A
git -C /Users/jsk/1210/digital commit -m "refactor(선택): 하이라이트 파생을 useSelectionHighlight 중앙 1곳으로 — per-panel startTrace 제거

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 연결탭 selectedCore 동기화

**Files:**
- Modify: `frontend/src/features/connections/components/AssetConnectionsTab.tsx`
- Modify: `frontend/src/features/connections/components/AssetConnectionsTab.test.tsx`

- [ ] **Step 1: 테스트 갱신(클릭→setSelected, active=selectedCore)**

`AssetConnectionsTab.test.tsx` 전체 교체:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';

const { startTrace, openTopology, clearHighlight, setSelected } = vi.hoisted(() => ({
  startTrace: vi.fn(), openTopology: vi.fn(), clearHighlight: vi.fn(), setSelected: vi.fn(),
}));

const node = (label: string, children: unknown[] = []) =>
  ({ id: label, label, kind: 'asset', isSelf: false, isOrigin: false, edgeFiber: false, children });
const groups = [{
  key: '광', label: '광', color: '#22c55e',
  components: [{ seedCableId: 'c1', cableIds: ['c1'], nodeIds: ['o1', 's'], core: 3, root: node('단말광1', [node('남춘천')]) }],
}];

vi.mock('../hooks/useAssetConnections', () => ({ useAssetDiagram: () => ({ groups, isLoading: false }) }));
vi.mock('../../workspace/selectionStore', () => {
  const st = { selectedAssetId: null as string | null, selectedCore: null as number | null, setSelected };
  const hook = (sel?: (s: unknown) => unknown) => (sel ? sel(st) : st);
  (hook as unknown as { getState: () => unknown }).getState = () => st;
  return { useSelectionStore: hook };
});
vi.mock('../../pathTrace/stores/pathHighlightStore', () => {
  const st = { startTrace, openTopology, clearHighlight };
  const hook = (sel?: (s: unknown) => unknown) => (sel ? sel(st) : st);
  (hook as unknown as { getState: () => unknown }).getState = () => st;
  return { usePathHighlightStore: hook };
});

import { AssetConnectionsTab } from './AssetConnectionsTab';

beforeEach(() => { startTrace.mockClear(); openTopology.mockClear(); setSelected.mockClear(); });

describe('AssetConnectionsTab', () => {
  it('종류 그룹 + 트리 렌더', () => {
    render(<AssetConnectionsTab assetId="s" />);
    expect(screen.getByText('광')).toBeInTheDocument();
    expect(screen.getByText('단말광1')).toBeInTheDocument();
  });
  it('트리 클릭 → setSelected(asset, comp.core)', () => {
    render(<AssetConnectionsTab assetId="s" />);
    fireEvent.click(screen.getByText('단말광1'));
    expect(setSelected).toHaveBeenCalledWith('s', 3);
  });
  it('상세 클릭 → startTrace + openTopology', () => {
    render(<AssetConnectionsTab assetId="s" />);
    fireEvent.click(screen.getByRole('button', { name: '상세' }));
    expect(startTrace).toHaveBeenCalledWith('c1');
    expect(openTopology).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/connections/components/AssetConnectionsTab.test.tsx`
Expected: FAIL.

- [ ] **Step 3: 구현 — `AssetConnectionsTab.tsx` 교체**

```tsx
import { Split } from 'lucide-react';
import { useAssetDiagram } from '../hooks/useAssetConnections';
import { usePathHighlightStore } from '../../pathTrace/stores/pathHighlightStore';
import { useSelectionStore } from '../../workspace/selectionStore';
import { DiagramTree } from './DiagramTree';

interface Props { assetId: string }

export function AssetConnectionsTab({ assetId }: Props) {
  const { groups, isLoading } = useAssetDiagram(assetId);
  const startTrace = usePathHighlightStore((s) => s.startTrace);
  const openTopology = usePathHighlightStore((s) => s.openTopology);
  const selectedAssetId = useSelectionStore((s) => s.selectedAssetId);
  const selectedCore = useSelectionStore((s) => s.selectedCore);

  if (isLoading) return <div className="flex items-center justify-center py-8 text-xs text-content-faint">불러오는 중…</div>;
  if (groups.length === 0) return <div className="flex items-center justify-center py-8 text-xs text-content-faint">연결된 케이블이 없습니다.</div>;

  return (
    <div className="flex flex-col gap-0">
      {groups.map((group) => (
        <div key={group.key}>
          <div className="flex items-center gap-1.5 px-3 py-1.5 bg-surface-2 border-b border-line">
            {group.color && <span className="inline-block w-2 h-2 rounded-full flex-shrink-0" style={{ backgroundColor: group.color }} />}
            <span className="text-xs font-semibold text-content-muted">{group.label}</span>
            <span className="ml-auto text-xs text-content-faint">{group.components.length}</span>
          </div>
          {group.components.map((comp) => {
            const isActive = selectedAssetId === assetId && selectedCore === comp.core;
            const branched = comp.root.children.length > 1;
            return (
              <div key={comp.seedCableId} className={`flex items-start border-b border-line ${isActive ? 'bg-info-bg/40' : 'hover:bg-surface-2'}`}>
                <button type="button" onClick={() => useSelectionStore.getState().setSelected(assetId, comp.core)} className="flex-1 text-left px-3 py-2 min-w-0">
                  <DiagramTree root={comp.root} />
                </button>
                <button type="button" aria-label="상세" title="네트워크 토폴로지 보기"
                  onClick={() => { startTrace(comp.seedCableId); openTopology(); }}
                  className="px-2 py-2 text-xs text-primary hover:text-primary-hover flex-shrink-0">↗</button>
              </div>
            );
          })}
        </div>
      ))}
    </div>
  );
}
```
주: `highlightDiagram`·`tracingCableId`·언마운트 `clearHighlight`·`useEffect`·`Split`(미사용이면) 제거. (위 코드에 `Split`/`branched` 가 안 쓰이면 그 두 줄과 import 도 빼라 — DiagramTree 가 자체 분기 표시.) 클릭 하이라이트는 `setSelected` → 중앙 훅이 파생.

- [ ] **Step 4: 통과 + 전체**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/connections && npx tsc --noEmit && npx vitest run`
Expected: PASS, tsc clean, green.

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add -A
git -C /Users/jsk/1210/digital commit -m "feat(연결탭): selectedCore 동기화 — 클릭=setSelected(asset,core), active=selectedCore (highlightDiagram 제거)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 포트뷰 자국 OUT 케이블 진입점

**Files:**
- Modify: `frontend/src/features/fiber/components/SlotPortsPanel.tsx`
- Modify: `frontend/src/features/fiber/components/SlotPortsPanel.test.tsx`

- [ ] **Step 1: 실패 테스트 추가**

`SlotPortsPanel.test.tsx` 에 추가(기존 mock 패턴 따름 — `startCableConnection` mock):
```tsx
// 파일 상단 mock 영역에 추가:
const startCableConnection = vi.fn();
vi.mock('../../editor/cableConnection', () => ({ startCableConnection: (...a: unknown[]) => startCableConnection(...a) }));
vi.mock('../../workspace/WorkspaceNavContext', () => ({ useWorkspaceNav: () => ({ gotoAsset: vi.fn() }) }));

// describe 안에 추가:
it('자국 free 포트 선택 → "케이블 연결" 진입점 → startCableConnection(role OUT, slotId, coreNumber)', () => {
  // 포트 1 = empty(자국 free). 선택 상태로 렌더되도록 selectionStore 코어 1 세팅.
  useSelectionStore.getState().setSelectedCore(1);
  startCableConnection.mockClear();
  render(<SlotPortsPanel slotId={SLOT} />);
  fireEvent.click(screen.getByRole('button', { name: /케이블 연결/ }));
  expect(startCableConnection).toHaveBeenCalledWith(expect.objectContaining({
    source: expect.objectContaining({ slotId: SLOT, coreNumber: 1, role: 'OUT' }),
  }));
});
```
주: 기존 테스트가 `useSelectionStore` 를 mock 하지 않고 실제 store 를 쓰는지 확인 후, 실제 store 면 `setSelectedCore(1)` 로 선택. 포트 1 이 empty 인지 픽스처 확인(empty 여야 진입점 노출).

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/fiber/components/SlotPortsPanel.test.tsx`
Expected: FAIL — 버튼 없음.

- [ ] **Step 3: 구현 — SlotPortsPanel 진입점**

`SlotPortsPanel.tsx` import 추가:
```ts
import { startCableConnection } from '../../editor/cableConnection';
import { useWorkspaceNav } from '../../workspace/WorkspaceNavContext';
import { Button } from '../../../components/ui';
```
컴포넌트 본문에 nav + 핸들러 추가(`selected` 정의 아래):
```tsx
  const nav = useWorkspaceNav();
  // 선택 포트(자국 free) 출발 케이블 진입 — 자국만(role OUT). 평면도로 이동하며 출발지 자동 주입.
  const connectFromPort = () => {
    if (!slot || !anchorRect || selectedCore == null) return;
    startCableConnection({
      source: { containerAssetId: anchorRect.anchorId, position: anchorRect.position, slotId: slot.id, coreNumber: selectedCore, role: 'OUT' },
    });
    nav?.gotoAsset(slot.id);
  };
```
선택 포트 상세카드(`{selected && (<DetailCard> … </DetailCard>)}`) 안, `<DetailNote>` 아래에 진입점 버튼 추가(자국 free + 피킹 모드 아님 + anchor 있음):
```tsx
          {!pick.active && !selected.localCableId && anchorRect && (
            <div className="mt-2">
              <Button variant="secondary" size="sm" onClick={connectFromPort}>케이블 연결</Button>
            </div>
          )}
```

- [ ] **Step 4: 통과 + 전체 + tsc**

Run: `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/fiber/components/SlotPortsPanel.test.tsx && npx tsc --noEmit && npx vitest run`
Expected: PASS, tsc clean, green. (`Button` 의 variant/size prop 이 다르면 `components/ui` 의 실제 시그니처에 맞춘다 — 없으면 `<button className="...">` 로 CONVENTIONS 스타일.)

- [ ] **Step 5: 커밋**
```bash
git -C /Users/jsk/1210/digital add -A
git -C /Users/jsk/1210/digital commit -m "feat(포트뷰): 선택 포트 출발 케이블 진입점(자국 OUT) — 빈 포트에만, 평면도 출발 자동 주입

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 완료 후
전체 `npx tsc --noEmit` + `npx vitest run` green 확인. 수동 스모크: 선번장에서 포트 클릭→연결탭/그리드 동시 활성, 연결탭 클릭→포트/그리드 동시(이중선택 없음), 포트뷰 "케이블 연결"→평면도 출발 주입. 이후 `superpowers:finishing-a-development-branch`.
