# 선번장 인라인 연결 편집 + 경로슬롯 이름 통일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 선번장에서 코어별 자국/대국 설비를 인라인 드롭다운으로 연결/변경/해제하고, 경로슬롯 이름을 "자국 - 대국 #코어수"로 파생·고정(읽기전용).

**Architecture:** 연결 = OUT 코어 케이블(설비↔슬롯) 생성/patch/삭제(자국=자기 슬롯, 대국=twin 슬롯). 끝점 자산 id만 참조하므로 cross-substation 자연 지원(전역 워킹카피/커밋). 슬롯 이름은 저장하지 않고 `fiberSlotLabel(slotId, graph)`로 모든 표시 지점에서 파생. 도면은 pathPoints=null → 직선 자동 렌더.

**Tech Stack:** React + TypeScript, Zustand(워킹카피), @tanstack/react-query, Vitest + @testing-library/react.

**Branch:** `feat/선번장-인라인연결편집` (spec 커밋 존재).

**검증:** 각 태스크 `cd /Users/jsk/1210/digital/frontend && npx vitest run <path>`; 마지막 `npx tsc --noEmit && npm run build && npx vitest run`.

---

## 기존 코드/사실(그대로 사용)

- `buildCoreOutCable(p: { id; equipmentId; slotId; coreNumber; category: CatRef; pathPoints; pathLength; bufferLength; totalLength }): CableCreate` — `fiberWrite.ts:49`. source=설비, target=슬롯, targetRole='OUT', number=coreNumber, specParams={}, pathPoints=p.pathPoints. **호출처 없음(첫 사용)**. Cable 에 substationId 컬럼 없음 → cross-substation 자연 지원.
- OUT 코어 카테고리 코드 = **`CBL-OPJ`**(광점퍼코드). `useCableCategories()` → `{ data: CableCategory[] }`, 각 `{ id, code, name, displayColor }`.
- `roleAt(c,a)`, `other(c,a)`, `interface CableLike` — `slotRegister.ts`(export 됨, P2b).
- 워킹카피: `useSubstationWorkingCopy.getState()` 의 `put(coll,obj)`, `patch(coll,id,fields)`, `remove(coll,id)`. effective: `useEffectiveAssets()`, `useEffectiveCables()`.
- 슬림 피드: `useQuery({ queryKey:['assets-slim'], queryFn: async()=>(await api.get<{data:SlimAssetDTO[]}>('/assets')).data.data })`. `SlimAssetDTO`(traceGraph) 필드: `id, name, code, substationId, substationName, parentAssetId, connectionKind`.
- `useTraceGraph()` → `{ graph, isLoading }`; `graph`: `assets, cables, nameById:Map, subNameById:Map, parentById:Map, codeById:Map`. `remoteSlotSubstation(slotId, graph)`(traceGraph) → 대국 변전소명|null.
- `EditableField`(`features/assets/components/EditableField`) — `type="select"` 지원: props `value, type, ariaLabel, disabled, options:[{value,label}], display:(v)=>string, onCommit:(v)=>void`.
- `generateTempId()` — `utils/idHelpers`.

---

## Task 1: `fiberSlotLabel` 파생 헬퍼 + OfdSlotRail 재사용

**Files:**
- Create: `frontend/src/features/fiber/fiberSlotLabel.ts`
- Test: `frontend/src/features/fiber/fiberSlotLabel.test.ts`
- Modify: `frontend/src/features/fiber/components/OfdSlotRail.tsx`

- [ ] **Step 1: 실패 테스트** — `fiberSlotLabel.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { fiberSlotLabel } from './fiberSlotLabel';
import type { TraceGraph } from '../trace/traceGraph';

const SLOT = 'slotA';
const TWIN = 'slotB';
const OFD = 'ofdA';
const opgw = { id: 'opgw', cableType: 'FIBER', sourceAssetId: SLOT, targetAssetId: TWIN, sourceRole: 'IN', targetRole: 'IN', specParams: { cores: 24 } };

function graphOf(over: Partial<TraceGraph> = {}): TraceGraph {
  return {
    assets: [], cables: [opgw],
    nameById: new Map(),
    subNameById: new Map([[OFD, '춘천S/S'], [TWIN, '북춘천S/S']]),
    parentById: new Map([[SLOT, OFD]]),
    codeById: new Map(),
    ...over,
  } as unknown as TraceGraph;
}

describe('fiberSlotLabel', () => {
  it('자국 - 대국 #코어수 포맷', () => {
    // remoteSlotSubstation(SLOT) = subNameById.get(TWIN) = 북춘천S/S
    expect(fiberSlotLabel(SLOT, graphOf())).toBe('춘천S/S - 북춘천S/S #24');
  });

  it('graph 없으면 빈 문자열', () => {
    expect(fiberSlotLabel(SLOT, null)).toBe('');
  });

  it('코어수 없으면 # 생략', () => {
    const g = graphOf({ cables: [{ ...opgw, specParams: {} }] } as unknown as TraceGraph);
    expect(fiberSlotLabel(SLOT, g)).toBe('춘천S/S - 북춘천S/S');
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/features/fiber/fiberSlotLabel.test.ts` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** — `fiberSlotLabel.ts`

```ts
import { remoteSlotSubstation, type TraceGraph } from '../trace/traceGraph';

/**
 * 경로슬롯 표시 이름(파생, SSOT) = "자국 - 대국 #코어수".
 * 자국 = 슬롯 부모 OFD 의 변전소명, 대국 = twin 슬롯 변전소명, 코어수 = OPGW(IN-IN) specParams.cores.
 * 저장하지 않고 표시 지점마다 계산한다(변전소명 변경 시 자동 반영).
 */
export function fiberSlotLabel(slotId: string, graph: TraceGraph | null): string {
  if (!graph) return '';
  const ofdId = graph.parentById.get(slotId) ?? slotId;
  const local = graph.subNameById.get(ofdId) ?? null;
  const remote = remoteSlotSubstation(slotId, graph);
  const opgw = graph.cables.find(
    (c) => c.cableType === 'FIBER' && c.sourceRole === 'IN' && c.targetRole === 'IN'
      && (c.sourceAssetId === slotId || c.targetAssetId === slotId),
  );
  const cores = Number((opgw?.specParams as Record<string, unknown> | undefined)?.cores ?? 0);
  const base = [local, remote].filter(Boolean).join(' - ');
  if (!base) return '';
  return cores ? `${base} #${cores}` : base;
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/features/fiber/fiberSlotLabel.test.ts` → PASS(3).

- [ ] **Step 5: OfdSlotRail 재사용** — `OfdSlotRail.tsx` 의 슬롯 타이틀 인라인 계산을 헬퍼로 교체.

import 추가:
```ts
import { fiberSlotLabel } from '../fiberSlotLabel';
```
타이틀 계산부(현재 `const base = ...; const n = coresOf(slot.id); const title = base ? (n ? `${base} #${n}` : base) : slot.name;`)를 교체:
```ts
          const title = fiberSlotLabel(slot.id, graph) || slot.name;
```
(이로써 `local`/`remote`/`n`/`coresOf` 가 타이틀용으론 불필요 — 다른 곳에서 안 쓰면 제거. `coresOf` 가 이 파일 다른 데서 안 쓰이면 함수·`opgwOf` 의존 정리는 tsc/lint 가 안내하는 선에서. 안 쓰는 변수만 제거하고 과한 리팩토링 금지.)

- [ ] **Step 6: 검증 + 커밋**

```bash
cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/fiber/ && npx tsc --noEmit
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/fiberSlotLabel.ts frontend/src/features/fiber/fiberSlotLabel.test.ts frontend/src/features/fiber/components/OfdSlotRail.tsx
git commit -m "feat: fiberSlotLabel(자국-대국#코어수) 파생 헬퍼 + OfdSlotRail 타이틀 재사용"
```
(OfdSlotRail 기존 테스트 `OfdSlotGrid.test.tsx` 가 "원주변전소 - 홍천변전소 #48" 을 기대 — 헬퍼가 동일 포맷이라 통과해야 함. 깨지면 헬퍼/그래프 모킹 차이를 맞춘다.)

---

## Task 2: 파생 이름 적용 — 상세 제목 + 인스펙터 읽기전용

**Files:**
- Modify: `frontend/src/features/assets/components/AssetDetailPanel.tsx` (headerTitle)
- Modify: `frontend/src/features/assets/components/AssetInspector.tsx` (name 필드 conduit 읽기전용)
- Test: `frontend/src/features/assets/components/AssetInspector.test.tsx`(있으면 보강) 또는 신규 케이스

- [ ] **Step 1: AssetDetailPanel — conduit 제목 override**

import 추가:
```ts
import { useTraceGraph } from '../../trace/traceGraph';
import { fiberSlotLabel } from '../../fiber/fiberSlotLabel';
```
함수 본문에서 `const headerTitle = title ?? asset?.name ?? '설비 상세';` 를 교체:
```ts
  const { graph } = useTraceGraph();
  const conduitLabel =
    asset?.assetType?.connectionKind === 'conduit' ? fiberSlotLabel(asset.id, graph) : '';
  const headerTitle = conduitLabel || title || asset?.name || '설비 상세';
```
(conduit 슬롯이면 파생 라벨이 평면도 overlay 의 title prop·현황 inline 둘 다 덮어씀. EquipmentDetailPanel 은 안 건드림.)

- [ ] **Step 2: AssetInspector — name 필드 conduit 읽기전용**

import 추가:
```ts
import { useTraceGraph } from '../../trace/traceGraph';
import { fiberSlotLabel } from '../../fiber/fiberSlotLabel';
```
함수 본문 상단에 `const { graph } = useTraceGraph();` 추가. `renderField` 맨 앞에 분기 추가:
```ts
  const renderField = (field: AssetFieldDef) => {
    // 경로슬롯(conduit): 이름은 파생(자국-대국#코어수) 읽기전용 — 수정 불가.
    if (field.key === 'name' && asset.assetType?.connectionKind === 'conduit') {
      return <ReadField key="name" label={field.label} value={fiberSlotLabel(asset.id, graph) || (asset.name ?? '')} />;
    }
    // ... 기존 분기 유지 ...
```

- [ ] **Step 3: 테스트** — `AssetInspector.test.tsx` 에 conduit 이름 읽기전용 케이스 추가(파일 없으면 생성). 기존 테스트(이름 편집 가능 등)는 비-conduit 자산이라 영향 없음. mock `useTraceGraph` 반환 graph 로 `fiberSlotLabel` 이 라벨을 내도록.

```tsx
// (요지) conduit 자산 렌더 시 이름이 ReadField(편집 ✎ 없음) + 파생 라벨로 표시되는지.
// useTraceGraph 모킹: subNameById/parentById/cables 로 "춘천S/S - 북춘천S/S #24" 산출.
// 비-conduit 자산은 이름 편집 Field 가 그대로(회귀 없음) 확인.
```
구체 단언: conduit 자산에서 `screen.getByText('춘천S/S - 북춘천S/S #24')` 존재 + 그 이름에 편집 버튼(aria-label '이름' 의 ✎)이 **없음**. 비-conduit 자산에선 이름 편집 가능(기존 단언 유지).

- [ ] **Step 4: 검증 + 커밋**

```bash
cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/assets/ && npx tsc --noEmit
cd /Users/jsk/1210/digital
git add frontend/src/features/assets/components/AssetDetailPanel.tsx frontend/src/features/assets/components/AssetInspector.tsx frontend/src/features/assets/components/AssetInspector.test.tsx
git commit -m "feat: 경로슬롯(conduit) 이름을 파생 라벨로 — 상세 제목 override + 인스펙터 읽기전용"
```

---

## Task 3: `twinSlotIdOf` 헬퍼 (slotPorts export)

**Files:**
- Modify: `frontend/src/features/fiber/slotPorts.ts`
- Test: `frontend/src/features/fiber/slotPorts.test.ts`(보강)

- [ ] **Step 1: 실패 테스트 추가** — `slotPorts.test.ts` 에:

```ts
import { buildSlotPorts, twinSlotIdOf } from './slotPorts';
// ...
describe('twinSlotIdOf', () => {
  it('OPGW(IN-IN) 반대편 슬롯 id', () => {
    expect(twinSlotIdOf('slotA', [opgw])).toBe('slotB');
    expect(twinSlotIdOf('slotB', [opgw])).toBe('slotA');
  });
  it('OPGW 없으면 null', () => {
    expect(twinSlotIdOf('slotA', [])).toBeNull();
  });
});
```
(`opgw` 픽스처는 기존 slotPorts.test.ts 의 것 재사용 — `{ id:'opgw', cableType:'FIBER', sourceAssetId:'slotA', targetAssetId:'slotB', sourceRole:'IN', targetRole:'IN', specParams:{cores:24} }`. 없으면 추가.)

- [ ] **Step 2: 실패 확인** — `npx vitest run src/features/fiber/slotPorts.test.ts` → FAIL(twinSlotIdOf 없음).

- [ ] **Step 3: 구현** — `slotPorts.ts` 에 export 추가하고 `buildSlotPorts` 내부 twin 추출을 이 헬퍼로 재사용(중복 제거):

```ts
/** 슬롯의 OPGW(IN-IN) 반대편(twin) 슬롯 id. 없으면 null. */
export function twinSlotIdOf(slotId: string, cables: CableLike[]): string | null {
  const opgw = cables.find(
    (c) => c.cableType === 'FIBER' && roleAt(c, slotId) === 'IN'
      && (c.sourceAssetId === slotId || c.targetAssetId === slotId),
  );
  return opgw ? other(opgw, slotId) : null;
}
```
`buildSlotPorts` 내부의 `const twinId = other(opgw, slot.id);` 는 그대로 둬도 되나, 일관성 위해 `const twinId = twinSlotIdOf(slot.id, cables);` 로 교체(동작 동일).

- [ ] **Step 4: 통과 확인** — `npx vitest run src/features/fiber/slotPorts.test.ts` → PASS(기존 6 + 신규 2).

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/slotPorts.ts frontend/src/features/fiber/slotPorts.test.ts
git commit -m "feat: twinSlotIdOf 헬퍼(OPGW 반대편 슬롯) export + buildSlotPorts 재사용"
```

---

## Task 4: `EquipmentSelectCell` (자국/대국 설비 드롭다운)

**Files:**
- Create: `frontend/src/features/fiber/components/EquipmentSelectCell.tsx`
- Test: `frontend/src/features/fiber/components/EquipmentSelectCell.test.tsx`

- [ ] **Step 1: 실패 테스트** — `EquipmentSelectCell.test.tsx`

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import type { ReactNode } from 'react';

const { put, patch, remove, generateTempId } = vi.hoisted(() => ({
  put: vi.fn(), patch: vi.fn(), remove: vi.fn(), generateTempId: vi.fn(() => 'tmp-out'),
}));

const SLOT = 'slotA';
const TWIN = 'slotB';
const SLOT_ASSET = { id: SLOT, name: '슬롯', parentAssetId: 'ofdA', substationId: 's1', assetType: { connectionKind: 'conduit', code: 'OFD-SLOT' } };
const opgw = { id: 'opgw', cableType: 'FIBER', sourceAssetId: SLOT, targetAssetId: TWIN, sourceRole: 'IN', targetRole: 'IN', specParams: { cores: 24 } };
const localOut3 = { id: 'c-l3', cableType: 'FIBER', sourceAssetId: 'eqpL', targetAssetId: SLOT, sourceRole: null, targetRole: 'OUT', number: 3 };

const SLIM = [
  { id: 'eqpL', name: '자국장비', code: 'EQP', substationId: 's1', substationName: '춘천', parentAssetId: null, connectionKind: null },
  { id: 'eqpL2', name: '자국장비2', code: 'EQP', substationId: 's1', substationName: '춘천', parentAssetId: null, connectionKind: null },
  { id: 'eqpR', name: '대국장비', code: 'EQP', substationId: 's2', substationName: '북춘천', parentAssetId: null, connectionKind: null },
  { id: TWIN, name: '북춘천슬롯', code: 'OFD-SLOT', substationId: 's2', substationName: '북춘천', parentAssetId: 'ofdB', connectionKind: 'conduit' },
  { id: 'ofdA', name: 'OFD', code: 'OFD', substationId: 's1', substationName: '춘천', parentAssetId: null, connectionKind: null },
];
const CATS = [{ id: 'cat-opj', code: 'CBL-OPJ', name: '광점퍼코드', displayColor: null }];

vi.mock('../../workingCopy/hooks', () => ({ useEffectiveCables: () => [opgw, localOut3] }));
vi.mock('../../trace/traceGraph', () => ({
  useTraceGraph: () => ({ graph: { nameById: new Map([['eqpL','자국장비'],['eqpR','대국장비']]) }, isLoading: false }),
}));
vi.mock('../../cables/hooks/useCableCategories', () => ({ useCableCategories: () => ({ data: CATS }) }));
vi.mock('../../workingCopy/substationStore', () => {
  const st = { put, patch, remove };
  const hook = (sel?: (s: unknown) => unknown) => (sel ? sel(st) : st);
  (hook as unknown as { getState: () => unknown }).getState = () => st;
  return { useSubstationWorkingCopy: hook };
});
vi.mock('../../../utils/idHelpers', () => ({ generateTempId }));

function wrap(ui: ReactNode) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  qc.setQueryData(['assets-slim'], SLIM);
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>);
}

import { EquipmentSelectCell } from './EquipmentSelectCell';

beforeEach(() => { put.mockClear(); patch.mockClear(); remove.mockClear(); });

describe('EquipmentSelectCell', () => {
  it('자국 빈 코어: 후보(자국 변전소 설비, 구조자산 제외) 노출 + 설비 선택 → buildCoreOutCable put(자국슬롯)', () => {
    wrap(<EquipmentSelectCell slot={SLOT_ASSET as never} coreNumber={5} side="local" />);
    fireEvent.click(screen.getByRole('button', { name: '자국설비' }));
    // 후보: 자국장비/자국장비2 (s1), OFD·슬롯 제외
    fireEvent.click(screen.getByText('자국장비2'));
    expect(put).toHaveBeenCalledTimes(1);
    const [coll, cable] = put.mock.calls[0];
    expect(coll).toBe('cables');
    expect(cable).toMatchObject({ sourceAssetId: 'eqpL2', targetAssetId: SLOT, targetRole: 'OUT', number: 5, categoryCode: 'CBL-OPJ' });
  });

  it('자국 점유 코어 설비 변경 → patch(sourceAssetId)', () => {
    wrap(<EquipmentSelectCell slot={SLOT_ASSET as never} coreNumber={3} side="local" />);
    fireEvent.click(screen.getByRole('button', { name: '자국설비' }));
    fireEvent.click(screen.getByText('자국장비2'));
    expect(patch).toHaveBeenCalledWith('cables', 'c-l3', { sourceAssetId: 'eqpL2' });
    expect(put).not.toHaveBeenCalled();
  });

  it('대국 빈 코어: 후보=대국 변전소 설비 + 선택 → put(twin 슬롯)', () => {
    wrap(<EquipmentSelectCell slot={SLOT_ASSET as never} coreNumber={5} side="remote" />);
    fireEvent.click(screen.getByRole('button', { name: '대국설비' }));
    fireEvent.click(screen.getByText('대국장비'));
    const [, cable] = put.mock.calls[0];
    expect(cable).toMatchObject({ sourceAssetId: 'eqpR', targetAssetId: TWIN, number: 5 });
  });
});
```
(주의: `EditableField type="select"` 의 실제 상호작용 DOM — ✎ 버튼 → select/options — 은 기존 패턴을 따른다. 위 fireEvent 흐름이 EditableField 구현과 다르면, EditableField 의 select 렌더를 읽고 셀렉트 `change` 이벤트로 맞춘다. 핵심 단언(put/patch 인자)은 유지.)

- [ ] **Step 2: 실패 확인** — `npx vitest run src/features/fiber/components/EquipmentSelectCell.test.tsx` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** — `EquipmentSelectCell.tsx`

```tsx
import { useQuery } from '@tanstack/react-query';
import { useEffectiveCables } from '../../workingCopy/hooks';
import { useSubstationWorkingCopy } from '../../workingCopy/substationStore';
import { useTraceGraph, type SlimAssetDTO } from '../../trace/traceGraph';
import { useCableCategories } from '../../cables/hooks/useCableCategories';
import { roleAt, other, type CableLike } from '../slotRegister';
import { twinSlotIdOf } from '../slotPorts';
import { buildCoreOutCable } from '../fiberWrite';
import { generateTempId } from '../../../utils/idHelpers';
import { api } from '../../../utils/api';
import { EditableField } from '../../assets/components/EditableField';
import type { Asset } from '../../../types/asset';

/** 선번장 자국/대국 설비 드롭다운 — OUT 코어 케이블 생성/변경/해제. */
export function EquipmentSelectCell({ slot, coreNumber, side }: {
  slot: Asset; coreNumber: number; side: 'local' | 'remote';
}) {
  const cables = useEffectiveCables() as unknown as CableLike[];
  const { graph } = useTraceGraph();
  const { data: categories = [] } = useCableCategories();
  const { data: slim = [] } = useQuery({
    queryKey: ['assets-slim'], staleTime: 30_000,
    queryFn: async () => (await api.get<{ data: SlimAssetDTO[] }>('/assets')).data.data,
  });
  const opjCat = categories.find((c) => c.code === 'CBL-OPJ');

  const twinId = twinSlotIdOf(slot.id, cables);
  const targetSlotId = side === 'local' ? slot.id : twinId;
  const targetSub = side === 'local'
    ? slot.substationId
    : (twinId ? (slim.find((a) => a.id === twinId)?.substationId ?? null) : null);

  const outCable = targetSlotId
    ? cables.find((c) => c.cableType === 'FIBER' && roleAt(c, targetSlotId) === 'OUT' && c.number === coreNumber)
    : undefined;
  const currentId = outCable && targetSlotId ? other(outCable, targetSlotId) : null;

  // 후보 = 대상 변전소 배치 설비, 구조/통로 자산(OFD·conduit) 제외.
  const candidates = slim.filter(
    (a) => a.substationId === targetSub && a.code !== 'OFD' && a.connectionKind !== 'conduit',
  );

  const disabled = !targetSlotId || !targetSub || !opjCat;
  const ariaLabel = side === 'local' ? '자국설비' : '대국설비';

  const commit = (v: string) => {
    if (v === (currentId ?? '')) return;
    const wc = useSubstationWorkingCopy.getState();
    if (v === '') {
      if (outCable && window.confirm('이 코어 연결을 해제할까요?')) wc.remove('cables', outCable.id);
      return;
    }
    if (outCable) {
      wc.patch('cables', outCable.id, { sourceAssetId: v });
    } else if (targetSlotId && opjCat) {
      const cable = buildCoreOutCable({
        id: generateTempId(), equipmentId: v, slotId: targetSlotId, coreNumber,
        category: opjCat, pathPoints: null, pathLength: null, bufferLength: 4, totalLength: null,
      });
      wc.put('cables', cable as unknown as { id: string; [k: string]: unknown });
    }
  };

  const nameOf = (id: string) => graph?.nameById.get(id) ?? slim.find((a) => a.id === id)?.name ?? id;
  const options = [{ value: '', label: '—' }, ...candidates.map((a) => ({ value: a.id, label: a.name ?? a.id }))];

  return (
    <EditableField
      value={currentId ?? ''}
      type="select"
      ariaLabel={ariaLabel}
      disabled={disabled}
      options={options}
      display={(v) => (v ? nameOf(v) : '—')}
      onCommit={commit}
    />
  );
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/features/fiber/components/EquipmentSelectCell.test.tsx` → PASS(3). 필요시 EditableField select 상호작용에 맞춰 테스트 이벤트 조정(단, 핵심 단언 유지). `npx tsc --noEmit` clean.

- [ ] **Step 5: 커밋**

```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/components/EquipmentSelectCell.tsx frontend/src/features/fiber/components/EquipmentSelectCell.test.tsx
git commit -m "feat: EquipmentSelectCell — 선번장 자국/대국 설비 드롭다운(OUT 코어 생성/patch/해제, 대국=twin슬롯)"
```

---

## Task 5: 선번장 descriptor 배선 (워딩 + 셀 교체 + 섹션 제목)

**Files:**
- Modify: `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`
- Test: `frontend/src/features/fiber/components/FiberRegisterView.test.tsx`(보강)

- [ ] **Step 1: descriptor 수정**

import 추가:
```ts
import { fiberSlotLabel } from './fiberSlotLabel';
import { EquipmentSelectCell } from './components/EquipmentSelectCell';
import type { Asset } from '../../types/asset';
```
`FiberRow` 에 슬롯 첨부:
```ts
export type FiberRow = SlotCoreRow & { __nameById?: Map<string, string>; __slot?: Asset };
```
`buildSection` 에서 (a) 섹션 제목을 헬퍼로, (b) 각 행에 `__slot` 첨부:
```ts
    const rows = buildSlotCoreRows(slot as never, ctx.cables as never[], graph)
      .map((r): FiberRow => ({ ...r, __nameById: graph?.nameById, __slot: slot as unknown as Asset }));
    // ...
    const title = (graph ? fiberSlotLabel(slot.id, graph) : '') || (slot.name ?? '광경로');
```
(기존 `localSub`/`remoteSub`/`title` 인라인 계산 제거 — 헬퍼로 단일화.)

`columns` 의 `근접자산`/`상대국측` 두 컬럼을 교체:
```ts
    {
      label: '자국설비',
      sortKey: (r) => (r.nearAssetId ? r.__nameById?.get(r.nearAssetId) ?? null : null),
      cell: (r) => (r.__slot ? <EquipmentSelectCell slot={r.__slot} coreNumber={r.coreNumber} side="local" /> : null),
    },
    {
      label: '대국설비',
      sortKey: (r) => r.farName,
      cell: (r) => (r.__slot ? <EquipmentSelectCell slot={r.__slot} coreNumber={r.coreNumber} side="remote" /> : null),
    },
```
(나머지 컬럼 코어/용도/수용내역/융착/사용 그대로.)

- [ ] **Step 2: FiberRegisterView 테스트 보강**

기존 `FiberRegisterView.test.tsx` 의 컬럼 라벨/근접자산 단언을 새 라벨로 갱신:
- `근접자산`/`상대국측` 텍스트 단언 → `자국설비`/`대국설비`.
- "점유 코어 행 클릭 → 근접자산 선택"(setSelectedAssetId 'a-near') 테스트는 셀이 드롭다운으로 바뀌어 클릭 동작이 달라짐 — 행 클릭 선택은 코어/용도 등 다른 셀로 검증하거나, 이 테스트를 "자국설비 드롭다운 렌더" 확인으로 대체. (행 클릭 선택 자체는 ConnectionRegisterGrid 책임 — onRowClick 은 Task 이전과 동일.)
- 섹션 제목이 `fiberSlotLabel` 포맷(예: '원주 - 홍천 #N')으로 나오는지(모킹 graph 에 맞춰) 단언.

구체 변경은 현재 테스트 파일을 읽고 최소 수정. 핵심: 라벨 자국설비/대국설비 존재, 자국/대국 셀이 EquipmentSelectCell(드롭다운, ariaLabel '자국설비'/'대국설비') 렌더.

- [ ] **Step 3: 검증 + 커밋**

```bash
cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/fiber/ && npx tsc --noEmit
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/fiberRegisterDescriptor.tsx frontend/src/features/fiber/components/FiberRegisterView.test.tsx
git commit -m "feat: 선번장 자국설비/대국설비 인라인 드롭다운 배선 + 섹션 제목 파생(#코어수)"
```

---

## Task 6: 최종 검증

- [ ] **Step 1: 전체**

```bash
cd /Users/jsk/1210/digital/frontend && npx tsc --noEmit && npm run build && npx vitest run
```
Expected: tsc clean, build OK, 전체 vitest green.

- [ ] **Step 2: 수동 스모크(dev 서버)**
  - 선번장 진입 → 컬럼 `자국설비`/`대국설비`, 섹션 제목 `자국 - 대국 #코어수`.
  - 자국설비 드롭다운 → 자국 변전소 설비 목록 → 선택 → 도면에 직선 연결 + 현황 노출 + 포트 GUI 색(편도).
  - 대국설비 선택 → 양측(포트 GUI 초록), 커밋 후 대국 변전소 선번장에 자국↔대국 반대로 반영.
  - 설비 변경 → 끝점 교체(메타 보존). `—` → 해제(확인).
  - OFD GUI 슬롯 타이틀·상세 패널 제목·인스펙터 이름 = `자국 - 대국 #코어수`(인스펙터 읽기전용).

---

## Self-Review (작성자 체크)

- **Spec 커버리지:** 워딩(T5)·인라인 연결 생성/patch/해제(T4·T5)·대국=twin슬롯 cross-substation(T4)·직선 자동(무변경, 기존 렌더러)·드롭다운 후보 필터(T4)·슬롯 이름 파생&읽기전용(T1·T2·T5) 모두 태스크 대응.
- **타입 일관성:** `fiberSlotLabel(slotId, graph)`(T1) → T2·T5 동일 시그니처. `twinSlotIdOf(slotId, cables)`(T3) → T4 사용. `EquipmentSelectCell({slot, coreNumber, side})`(T4) → T5 동일 props. `buildCoreOutCable` 인자(T4)는 fiberWrite 실제 시그니처와 일치. `CBL-OPJ` 카테고리 코드 일관.
- **플레이스홀더:** 없음(테스트/구현 코드 구체). 단, EditableField select 의 실제 상호작용 DOM 은 T4 Step1 주석대로 구현에 맞춰 이벤트만 조정(핵심 단언 고정).
- **갭 주의:** SlimAssetDTO 에 `connectionKind` 필드 존재 가정 — 구현 시 traceGraph 의 `SlimAssetDTO` 타입을 확인하고, 없으면 `codeById`/대안 필터(code !== 'OFD' && code !== 'OFD-SLOT')로 대체. FiberRegisterView 기존 테스트는 현재 파일 읽고 최소 수정.
