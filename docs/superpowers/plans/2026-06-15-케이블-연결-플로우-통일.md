# 케이블 연결 플로우 전면 통일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 케이블 생성을 "종류 → 출발 → 도착" 단일 플로우로 재배치하고, 출발/도착 연결점 선택을 기존 우측 상세 패널 뷰 재사용(pick 모드)으로 통일하며, 피더에 단일 IN 슬롯을 추가하고, 모든 진입점을 `startCableConnection` 한 함수로 수렴시킨다.

**Architecture:** 케이블 그리기 FSM(`interactionStore`)을 종류-우선으로 재배치하고 endpoint를 `EndpointRef`로 단일화한다. 도착 확정 시 `commitCable()`이 케이블을 생성한다(마지막 spec 모달 제거). 연결점 선택은 `useCablePick` 신호를 소비하는 기존 뷰(`DistributionCircuits`/`FeederCircuitsPanel`/`PortGrid`/`SlotRailGrid`)가 담당 → 별도 피커 3종 + `CableEndpointPickerHost` + 모달 역할선택부를 삭제한다.

**Tech Stack:** React 18 + TypeScript, Zustand(스토어), Vitest + @testing-library/react. 작업 디렉터리 `/Users/jsk/1210/digital/frontend`. 검증: `npx vitest run` + `npx tsc --noEmit`(반드시 frontend 디렉터리에서). 커밋은 `git -C /Users/jsk/1210/digital`로(cwd 드리프트 방지), 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

**Spec:** `docs/superpowers/specs/2026-06-15-케이블-연결-플로우-통일-design.md`

---

## File Structure (생성/수정/삭제)

**신규**
- `src/features/editor/cableEndpoint.ts` — `EndpointRef` 타입 + `endpointAssetId(ref)` 해소 헬퍼.
- `src/features/editor/cableConnection.ts` — `startCableConnection(opts)`(진입통일) + `commitCable()`(도착 확정 시 생성).
- `src/features/editor/hooks/useCablePick.ts` — pick 모드 신호 훅(`{ active, side, onPick }`).
- `src/features/power/feederCircuits.ts` 에 `buildFeederInput()` 추가(신규 함수).

**수정**
- `src/features/editor/stores/interactionStore.ts` — phase 재정의(selectingType 추가, selectingSpec 제거, pickingSource/TargetEndpoint), data에 category·role·number 추가, 액션 재작성.
- `src/features/editor/components/modals/CableSpecModal.tsx` — 종류 전용(역할선택부 삭제) + `selectingType`에서 노출 + 확정 시 `cableSetType`.
- `src/features/editor/hooks/useCanvasEvents.ts` — pick 모드 연동(컨테이너 클릭 → 패널 open + pending), 도착 확정 → `commitCable`.
- `src/features/editor/components/FeederCircuitsPanel`(경로: `src/features/power/components/FeederCircuitsPanel.tsx`)·`BreakerRail.tsx`·`PortGrid.tsx`·`SlotPortsPanel.tsx`·`DistributionPanel.tsx`(DistributionCircuits)·`SlotRailGrid.tsx`/`ModuleCell.tsx` — 선택적 `onPick`.
- `src/features/editor/components/EditorInsertBar.tsx`·`src/features/editor/hooks/useEditorKeyboard.ts`·`FeederCircuitsPanel`(addCb) — `startCableConnection`로 이관.

**삭제**
- `src/features/editor/components/CableEndpointPickerHost.tsx`
- `src/features/connections/components/RackModulePicker.tsx`
- `src/features/connections/components/CircuitPicker.tsx`
- `src/features/connections/components/SlotCorePicker.tsx`
- `CableSpecModal`의 `RoleSelector` + role 분기

---

# Phase 1 — FSM 종류-우선 재배치

목표: 종류 모달을 맨 앞으로, endpoint를 `EndpointRef`로 단일화, 도착 확정 시 `commitCable`로 생성(마지막 spec 모달 제거). 기존 피커 3종은 **잠정 유지**(Phase 2에서 삭제)하되 새 액션에 맞춰 호출만 갱신.

### Task 1.1: `EndpointRef` 타입 + 해소 헬퍼

**Files:**
- Create: `src/features/editor/cableEndpoint.ts`
- Test: `src/features/editor/cableEndpoint.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
// src/features/editor/cableEndpoint.test.ts
import { describe, it, expect } from 'vitest';
import { endpointAssetId, type EndpointRef } from './cableEndpoint';

const base: EndpointRef = { containerAssetId: 'eq1', position: { x: 0, y: 0 } };

describe('endpointAssetId — slot > inner > container 우선순위', () => {
  it('평면 설비: container', () => {
    expect(endpointAssetId(base)).toBe('eq1');
  });
  it('랙 모듈/피더: inner 우선', () => {
    expect(endpointAssetId({ ...base, innerAssetId: 'mod1' })).toBe('mod1');
  });
  it('경로슬롯: slot 최우선', () => {
    expect(endpointAssetId({ ...base, innerAssetId: 'x', slotId: 'slot1' })).toBe('slot1');
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run src/features/editor/cableEndpoint.test.ts` → FAIL ("Cannot find module './cableEndpoint'").

- [ ] **Step 3: 구현**

```ts
// src/features/editor/cableEndpoint.ts
/** 모든 케이블 엔드포인트(평면설비 / 랙모듈 / 피더 IN·분기 / 경로슬롯 포트)를 단일 표현. */
export interface EndpointRef {
  containerAssetId: string;            // 평면도에 배치된 anchor 설비
  position: { x: number; y: number };  // 컨테이너 중심(경로/프리뷰)
  innerAssetId?: string | null;        // 랙 모듈 / 피더(FEEDER) asset id
  slotId?: string | null;              // 경로슬롯(conduit) id
  coreNumber?: number | null;          // 광 코어 번호
  role?: 'IN' | 'OUT' | null;          // distributor 연결점 역할
  number?: number | null;              // 분기(CB) 번호 등
}

/** 케이블 endpoint assetId 해소 — slot > inner > container 우선순위. */
export function endpointAssetId(ref: EndpointRef): string {
  return ref.slotId ?? ref.innerAssetId ?? ref.containerAssetId;
}
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run src/features/editor/cableEndpoint.test.ts` → PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): EndpointRef 타입 + endpointAssetId 해소 헬퍼

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 1.2: FSM 재작성 — 종류-우선 phase + category/role/number 필드

**Files:**
- Modify: `src/features/editor/stores/interactionStore.ts`
- Test: `src/features/editor/stores/interactionStore.cable.test.ts` (신규)

먼저 현재 파일을 읽어라(`interactionStore.ts`). 아래 새 계약으로 **CableDrawingData·phase·액션을 교체**한다.

- [ ] **Step 1: 실패 테스트 작성**

```ts
// src/features/editor/stores/interactionStore.cable.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { useInteractionStore, getCableDrawing } from './interactionStore';
import type { EndpointRef } from '../cableEndpoint';

const ep = (over: Partial<EndpointRef> = {}): EndpointRef =>
  ({ containerAssetId: 'c', position: { x: 1, y: 2 }, ...over });

beforeEach(() => useInteractionStore.getState().cancel());

describe('cable FSM — 종류 우선', () => {
  it('cableActivate(빈 opts) → selectingType', () => {
    useInteractionStore.getState().cableActivate();
    expect(getCableDrawing()?.phase).toBe('selectingType');
  });
  it('category 주입 시 종류 생략 → selectingSource', () => {
    useInteractionStore.getState().cableActivate({ category: { id: 'cat', code: 'CBL', name: 'X', displayColor: null } });
    expect(getCableDrawing()?.phase).toBe('selectingSource');
  });
  it('source 주입 시 종류 고르면 바로 drawingPath', () => {
    useInteractionStore.getState().cableActivate({ source: ep({ role: 'OUT', innerAssetId: 'f1' }) });
    expect(getCableDrawing()?.phase).toBe('selectingType');
    useInteractionStore.getState().cableSetType({ id: 'cat', code: 'CBL', name: 'X', displayColor: null });
    const d = getCableDrawing()!;
    expect(d.phase).toBe('drawingPath');
    expect(d.source?.innerAssetId).toBe('f1');
    expect(d.source?.role).toBe('OUT');
  });
  it('selectingSource → cableSetSource(ref) → drawingPath', () => {
    useInteractionStore.getState().cableActivate({ category: { id: 'c', code: 'C', name: 'C', displayColor: null } });
    useInteractionStore.getState().cableSetSource(ep({ innerAssetId: 'm1' }));
    expect(getCableDrawing()?.phase).toBe('drawingPath');
  });
  it('drawingPath → setTarget(ref) → phase=ready(도착 확정, commitCable 대기)', () => {
    const s = useInteractionStore.getState();
    s.cableActivate({ category: { id: 'c', code: 'C', name: 'C', displayColor: null } });
    s.cableSetSource(ep({ containerAssetId: 'a' }));
    s.cableSetTarget(ep({ containerAssetId: 'b', role: 'OUT' }));
    const d = getCableDrawing()!;
    expect(d.target?.containerAssetId).toBe('b');
    expect(d.phase).toBe('ready'); // 도착 확정 — commitCable 대기
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run src/features/editor/stores/interactionStore.cable.test.ts` → FAIL.

- [ ] **Step 3: 구현** — `interactionStore.ts`를 아래 계약으로 교체.

```ts
// 타입 부분 교체
import type { EndpointRef } from '../cableEndpoint';

export interface SelectedCableCategory { id: string; code: string; name: string; displayColor: string | null }

export type CableDrawingPhase =
  | 'selectingType'           // 케이블 종류 모달
  | 'selectingSource'         // 출발 설비 클릭 대기
  | 'pickingSourceEndpoint'   // 출발 컨테이너 → 우측 패널 pick 모드
  | 'drawingPath'             // 출발 확정, 경유점
  | 'pickingTargetEndpoint'   // 도착 컨테이너 → 우측 패널 pick 모드
  | 'ready';                  // 도착 확정 → commitCable 대기(즉시 생성)

export interface CableDrawingData {
  phase: CableDrawingPhase;
  category: SelectedCableCategory | null;
  source: EndpointRef | null;
  target: EndpointRef | null;
  /** 컨테이너 클릭 후 pick 대기 중인 컨테이너(패널 open 대상). */
  pendingContainerId: string | null;
  waypoints: [number, number][];
  previewPoint: { x: number; y: number } | null;
  hoveredAssetId: string | null;
}
```

액션 계약(구현 시 set 패턴은 기존과 동일하게 immutable 갱신):
- `cableActivate(opts?: { source?: EndpointRef; category?: SelectedCableCategory })`: data 초기화. `category`면 `selectingSource`(source 없을 때)/`drawingPath`(source 있을 때), 없으면 `selectingType`.
- `cableSetType(category)`: `selectingType`에서만. category 저장 후 `source ? 'drawingPath' : 'selectingSource'`.
- `cableSetPendingSource(containerId)`: `selectingSource` → `pickingSourceEndpoint`, `pendingContainerId=containerId`.
- `cableSetSource(ref: EndpointRef)`: source 저장, waypoints=[], `phase='drawingPath'`, pendingContainerId=null.
- `cableSetPendingTarget(containerId)`: `drawingPath` → `pickingTargetEndpoint`, `pendingContainerId=containerId`.
- `cableSetTarget(ref: EndpointRef)`: target 저장, `phase='ready'`, pendingContainerId=null.
- `cableAddWaypoint`, `cableRemoveLastWaypoint`, `cableSetPreviewPoint`, `cableSetHovered`: 기존 유지(데이터 경로만 신규 구조에 맞춤).
- `cancel()`: 기존 유지(→ idle).
- `useCableDrawing`/`getCableDrawing`: 기존 유지.

cableInitial:
```ts
const cableInitial: CableDrawingData = {
  phase: 'selectingType', category: null, source: null, target: null,
  pendingContainerId: null, waypoints: [], previewPoint: null, hoveredAssetId: null,
};
```
`cableActivate`:
```ts
cableActivate: (opts) => set(() => {
  const source = opts?.source ?? null;
  const category = opts?.category ?? null;
  const phase: CableDrawingPhase = category ? (source ? 'drawingPath' : 'selectingSource') : 'selectingType';
  return { mode: { kind: 'cableDrawing', data: { ...cableInitial, category, source, phase } } };
}),
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run src/features/editor/stores/interactionStore.cable.test.ts` → PASS. (이 단계에서 다른 파일이 깨질 수 있음 — tsc는 Task 1.3~1.5 후 통과.)

- [ ] **Step 5: 커밋** (tsc 미통과 상태 — 다음 태스크에서 해소. 단위테스트만 통과 확인 후 커밋)

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): FSM 종류-우선 재배치 — selectingType/ready phase + EndpointRef source/target

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 1.3: `commitCable()` — 도착 확정 시 생성(모달 handleConfirm 추출)

**Files:**
- Create: `src/features/editor/cableConnection.ts`
- Test: `src/features/editor/cableConnection.test.ts`

`CableSpecModal.handleConfirm`(현재 파일 80–156줄)의 생성 로직을 그대로 옮긴다. 차이: 종류는 `data.category`에서, role/number는 `data.source/target.role/number`에서 읽는다(모달이 아니라 endpoint에서 결정).

- [ ] **Step 1: 실패 테스트 작성** — `stageCableCreate`를 모킹해 인자를 검증.

```ts
// src/features/editor/cableConnection.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

const { stageCableCreate, cancelCableDrawing, setSelectedCableId, showToast } = vi.hoisted(() => ({
  stageCableCreate: vi.fn(), cancelCableDrawing: vi.fn(), setSelectedCableId: vi.fn(), showToast: vi.fn(),
}));
vi.mock('../workingCopy/substationStore', () => ({
  useSubstationWorkingCopy: { getState: () => ({ stageCableCreate }) },
}));
vi.mock('./stores/editorStore', () => ({
  useEditorStore: { getState: () => ({ cancelCableDrawing, setSelectedCableId }) },
}));
vi.mock('./stores/toastStore', () => ({ useToastStore: { getState: () => ({ showToast }) } }));
vi.mock('../../utils/idHelpers', () => ({ generateTempId: () => 'tmp1' }));

import { commitCable } from './cableConnection';
import { useInteractionStore } from './stores/interactionStore';

beforeEach(() => { vi.clearAllMocks(); useInteractionStore.getState().cancel(); });

describe('commitCable', () => {
  it('도착 확정(ready) → stageCableCreate(종류/역할/번호) + 종료 + 토스트', () => {
    const s = useInteractionStore.getState();
    s.cableActivate({ category: { id: 'cat', code: 'CBL-XLPE', name: 'XLPE', displayColor: '#f00' } });
    s.cableSetSource({ containerAssetId: 'distA', position: { x: 0, y: 0 }, innerAssetId: 'feedA', role: 'OUT', number: 3 });
    s.cableSetTarget({ containerAssetId: 'eqB', position: { x: 10, y: 0 } });
    commitCable();
    expect(stageCableCreate).toHaveBeenCalledTimes(1);
    const arg = stageCableCreate.mock.calls[0][0];
    expect(arg).toMatchObject({
      sourceAssetId: 'feedA', targetAssetId: 'eqB',
      categoryId: 'cat', sourceRole: 'OUT', number: 3,
    });
    expect(cancelCableDrawing).toHaveBeenCalled();
    expect(showToast).toHaveBeenCalled();
  });
  it('ready 아니면 no-op', () => {
    useInteractionStore.getState().cableActivate();
    commitCable();
    expect(stageCableCreate).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run src/features/editor/cableConnection.test.ts` → FAIL.

- [ ] **Step 3: 구현**

```ts
// src/features/editor/cableConnection.ts
import { useEditorStore } from './stores/editorStore';
import { useSubstationWorkingCopy } from '../workingCopy/substationStore';
import { getCableDrawing } from './stores/interactionStore';
import { endpointAssetId } from './cableEndpoint';
import { getCableTypeFromMaterial } from '../../types/material';
import { calculatePathLength } from '../../utils/cable/pathLength';
import { generateTempId } from '../../utils/idHelpers';
import { useToastStore } from './stores/toastStore';

/** 도착 확정(phase='ready') 시 케이블 생성. CableSpecModal.handleConfirm 을 대체. */
export function commitCable(): void {
  const data = getCableDrawing();
  if (!data || data.phase !== 'ready' || !data.category || !data.source || !data.target) return;

  const pathPoints: [number, number][] = [
    [data.source.position.x, data.source.position.y],
    ...data.waypoints,
    [data.target.position.x, data.target.position.y],
  ];
  const { pathLength, bufferLength, totalLength } = calculatePathLength(pathPoints);
  const id = generateTempId();
  const number = data.target.number ?? data.source.number ?? data.target.coreNumber ?? data.source.coreNumber ?? null;

  useSubstationWorkingCopy.getState().stageCableCreate({
    id,
    sourceAssetId: endpointAssetId(data.source),
    targetAssetId: endpointAssetId(data.target),
    cableType: getCableTypeFromMaterial(data.category.code),
    categoryId: data.category.id,
    categoryCode: data.category.code,
    categoryName: data.category.name,
    displayColor: data.category.displayColor,
    specParams: {},
    specification: data.category.name,
    pathPoints, pathLength, bufferLength, totalLength,
    number,
    sourceRole: data.source.role ?? null,
    targetRole: data.target.role ?? null,
  });
  useEditorStore.getState().cancelCableDrawing();
  useEditorStore.getState().setSelectedCableId(id);
  useToastStore.getState().showToast('케이블을 연결했습니다');
}
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run src/features/editor/cableConnection.test.ts` → PASS (2 tests).

- [ ] **Step 5: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): commitCable — 도착 확정 시 생성(category/role/number는 endpoint에서)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 1.4: `CableSpecModal` 종류 전용화 + 맨 앞 노출

**Files:**
- Modify: `src/features/editor/components/modals/CableSpecModal.tsx`

- [ ] **Step 1: 구현** — 모달을 `selectingType`에서 노출, 역할선택부·role/dist/conduit 분기·`RoleSelector`·생성 로직 전부 삭제. 확정 시 `cableSetType` 호출.

핵심 변경(현재 파일 기준):
- `const phase = cable?.phase ?? 'idle';` 유지.
- `if (phase !== 'selectingSpec') return null;` → `if (phase !== 'selectingType') return null;`
- `sourceEndpointId/targetEndpointId/isDistributor/sourceIsDist/targetIsDist/sourceRole/targetRole/useEffect(role)` 블록 **삭제**.
- `handleConfirm`을 아래로 교체:
```tsx
const handleConfirm = () => {
  if (!selectedCat) return;
  useInteractionStore.getState().cableSetType({
    id: selectedCat.id, code: selectedCat.code, name: selectedCat.name, displayColor: selectedCat.displayColor,
  });
  setSelectedCategoryId(null);
};
```
- import에 `useInteractionStore` 추가, 미사용 import(`useSubstationWorkingCopy`, `getCableTypeFromMaterial`, `calculatePathLength`, `generateTempId`, `useToastStore`, `getCableDrawing`, `useEffectiveAssets`) 제거.
- JSX의 `{(sourceIsDist || targetIsDist) && (...역할...)}` 블록과 `RoleSelector` 함수 **삭제**.
- `useEffect(reset on selectingSpec)` → `selectingType`으로:
```tsx
useEffect(() => { if (phase === 'selectingType') setSelectedCategoryId(null); }, [phase]);
```

- [ ] **Step 2: 빌드 확인** — Run: `npx tsc --noEmit` (frontend). 이 시점 canvas 미연동으로 일부 호출 불일치 가능 → Task 1.5에서 해소. CableSpecModal 자체 타입 오류는 없어야 함.

- [ ] **Step 3: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): CableSpecModal 종류 전용화 + selectingType 노출(역할선택부·생성로직 제거)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 1.5: 캔버스 클릭을 새 액션·EndpointRef·commitCable에 연결(기존 피커 잠정 유지)

**Files:**
- Modify: `src/features/editor/hooks/useCanvasEvents.ts` (~307–364)
- Modify: `src/features/editor/components/CableEndpointPickerHost.tsx` (잠정: 새 phase/액션에 맞춤)

새 phase에 맞춰 캔버스 클릭 갱신. 평면설비는 `EndpointRef`로 직접 set, 컨테이너는 pending(기존 피커가 처리). 도착 확정 후 `commitCable`.

- [ ] **Step 1: 구현 — useCanvasEvents 케이블 분기**

```ts
// import 추가
import { commitCable } from '../cableConnection';
import { getEquipmentCenter } from ...; // 기존 사용 중

// selectingSource:
if (cableDrawing?.phase === 'selectingSource') {
  const found = findItemAt(x, y, null, localEquipment);
  if (found?.type === 'equipment') {
    const eq = found.item; const center = getEquipmentCenter(eq);
    if (needsEndpointPicker(kindOf(eq))) interaction.cableSetPendingSource(eq.id);
    else interaction.cableSetSource({ containerAssetId: eq.id, position: center });
  }
  return;
}
if (cableDrawing?.phase === 'pickingSourceEndpoint' || cableDrawing?.phase === 'pickingTargetEndpoint') return; // 패널이 클릭 소유
if (cableDrawing?.phase === 'drawingPath') {
  const found = findItemAt(x, y, null, localEquipment);
  if (found?.type === 'equipment' && found.item.id !== cableDrawing.source?.containerAssetId) {
    const eq = found.item; const center = getEquipmentCenter(eq);
    if (needsEndpointPicker(kindOf(eq))) interaction.cableSetPendingTarget(eq.id);
    else { interaction.cableSetTarget({ containerAssetId: eq.id, position: center }); commitCable(); }
  } else if (!found || found.type !== 'equipment') {
    // 기존 shift 직교 스냅 + cableAddWaypoint 그대로 (source.position 참조로 변경)
    ...
    interaction.cableAddWaypoint(snapped.x, snapped.y);
  }
  return;
}
// 'selectingType' / 'ready' / 'pickingX' 은 캔버스 무시
```
주의: `cableDrawing.sourcePosition`/`sourceContainerAssetId` 참조를 `cableDrawing.source?.position`/`source?.containerAssetId`로 모두 치환. 캔버스 렌더러(`CableLayer` 등)에서 동일 필드 참조하면 함께 갱신.

- [ ] **Step 2: 구현 — CableEndpointPickerHost 잠정 갱신** — 기존 onSelect가 `cableSetSource(eq.id, center, {extras})` 형태였던 것을 `cableSetSource({ containerAssetId, position, innerAssetId/slotId/coreNumber, role })`로 변경. 도착이면 `cableSetTarget(ref)` 후 `commitCable()`. phase 비교를 `pickingSourceEndpoint`/`pickingTargetEndpoint`로. (이 컴포넌트는 Phase 2에서 삭제 예정 — 여기선 빌드 통과만.)
  - 분전반 CircuitPicker onSelect: feeder는 distributor → `role:'OUT'`. (IN은 Phase 3.)
  - OFD SlotCorePicker onSelect: `slotId, coreNumber, role:'OUT'`.
  - RACK RackModulePicker onSelect: `innerAssetId: moduleId`(role 없음).

- [ ] **Step 3: 렌더러/소비처 필드 치환** — `grep -rn "sourceContainerAssetId\|sourceInnerAssetId\|sourceSlotId\|sourceCoreNumber\|targetContainerAssetId\|sourcePosition\|targetPosition" src` 로 남은 참조를 새 `source/target: EndpointRef` 구조로 모두 치환(캔버스 케이블 프리뷰 렌더 포함).

- [ ] **Step 4: 검증** — Run: `npx tsc --noEmit` → clean. Run: `npx vitest run` → 전부 green(깨진 기존 테스트는 새 구조로 갱신).

- [ ] **Step 5: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): 캔버스 클릭→EndpointRef 새 액션 연결 + 도착확정 commitCable(피커 잠정 유지)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Phase 1 종료 게이트:** 평면도에서 케이블 도구 → 종류 모달 먼저 뜨고 → 출발/도착(기존 피커 경유) → 생성. `npx tsc --noEmit` clean + `npx vitest run` green. 수동 스모크 후 main 머지.

---

# Phase 2 — 우측 패널 pick 모드 (피커 3종 삭제)

목표: `pickingSourceEndpoint`/`pickingTargetEndpoint` 동안 우측 상세 패널의 기존 뷰가 연결점 피커로 동작. `RackModulePicker`/`CircuitPicker`/`SlotCorePicker`/`CableEndpointPickerHost` 삭제.

### Task 2.1: `useCablePick` 훅

**Files:**
- Create: `src/features/editor/hooks/useCablePick.ts`
- Test: `src/features/editor/hooks/useCablePick.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
// src/features/editor/hooks/useCablePick.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useCablePick } from './useCablePick';
import { useInteractionStore } from '../stores/interactionStore';

beforeEach(() => useInteractionStore.getState().cancel());

describe('useCablePick', () => {
  it('pick 모드 아니면 active=false', () => {
    const { result } = renderHook(() => useCablePick());
    expect(result.current.active).toBe(false);
  });
  it('pickingSourceEndpoint → active=true, side=source, onPick→cableSetSource→drawingPath', () => {
    act(() => {
      const s = useInteractionStore.getState();
      s.cableActivate({ category: { id: 'c', code: 'C', name: 'C', displayColor: null } });
      s.cableSetPendingSource('distA');
    });
    const { result } = renderHook(() => useCablePick());
    expect(result.current).toMatchObject({ active: true, side: 'source' });
    act(() => result.current.onPick({ containerAssetId: 'distA', position: { x: 0, y: 0 }, innerAssetId: 'f1', role: 'OUT' }));
    expect(useInteractionStore.getState().mode).toMatchObject({ kind: 'cableDrawing', data: { phase: 'drawingPath' } });
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run src/features/editor/hooks/useCablePick.test.ts` → FAIL.

- [ ] **Step 3: 구현**

```ts
// src/features/editor/hooks/useCablePick.ts
import { useInteractionStore, useCableDrawing } from '../stores/interactionStore';
import { commitCable } from '../cableConnection';
import type { EndpointRef } from '../cableEndpoint';

export interface CablePick {
  active: boolean;
  side: 'source' | 'target' | null;
  /** pick 대기 중인 컨테이너 id(이 패널이 그 컨테이너일 때만 onPick 노출). */
  pendingContainerId: string | null;
  onPick: (ref: EndpointRef) => void;
}

/** 케이블 연결 중 우측 패널 뷰가 연결점 피커로 동작하기 위한 신호. */
export function useCablePick(): CablePick {
  const data = useCableDrawing();
  const phase = data?.phase ?? null;
  const side = phase === 'pickingSourceEndpoint' ? 'source' : phase === 'pickingTargetEndpoint' ? 'target' : null;
  const onPick = (ref: EndpointRef) => {
    const s = useInteractionStore.getState();
    if (side === 'source') s.cableSetSource(ref);
    else if (side === 'target') { s.cableSetTarget(ref); commitCable(); }
  };
  return { active: side !== null, side, pendingContainerId: data?.pendingContainerId ?? null, onPick };
}
```

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run src/features/editor/hooks/useCablePick.test.ts` → PASS.

- [ ] **Step 5: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): useCablePick — 우측 패널 pick 모드 신호

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2.2: 연결점 뷰에 `onPick` 추가 (Breaker/Port/Distribution/Rail)

**Files:**
- Modify: `src/features/power/components/FeederCircuitsPanel.tsx`, `src/components/BreakerRail.tsx`
- Modify: `src/features/fiber/components/SlotPortsPanel.tsx`, `src/components/PortGrid.tsx`
- Modify: `src/features/equipment/components/detail/panels/DistributionPanel.tsx`
- Modify: `src/components/SlotRailGrid.tsx` 소비처(랙: `RackView`/`ModuleCell`, OFD: `OfdSlotRail`)

각 뷰가 `useCablePick()`을 읽어 `active`면 연결점 클릭을 `onPick(ref)`로 분기(아니면 기존 동작). 패널은 `pendingContainerId`가 자기 컨테이너일 때만 pick. 컨테이너→하위 드릴다운은 기존 네비(분전반 카드 클릭→피더, 슬롯 선택→포트)를 그대로 사용.

- [ ] **Step 1: FeederCircuitsPanel** — `const pick = useCablePick();` 추가. BreakerRail의 분기 클릭(`onSelect`)이 pick.active면 `pick.onPick({ containerAssetId: feeder.parentAnchorId, position, innerAssetId: feeder.id, role:'OUT', number: cbNumber })` (빈칸=다음 CB번호). 점유칸도 동일 번호로. position/anchor는 `floorTargetFor(feeder.id, effectiveAssets)` 중심.
- [ ] **Step 2: SlotPortsPanel** — 포트 클릭이 pick.active면 `pick.onPick({ containerAssetId: ofdId, position, slotId, coreNumber, role:'OUT' })`.
- [ ] **Step 3: DistributionCircuits** — pick.active면 피더 카드 클릭이 "그 피더로 드릴다운"(기존 setSelectedAssetId)으로 동일하게 동작(피더 패널이 떠서 연결점 고름). 분전반 자체엔 연결점 없음.
- [ ] **Step 4: RackView/ModuleCell** — pick.active면 모듈 클릭이 `pick.onPick({ containerAssetId: rackId, position, innerAssetId: moduleId })`.
- [ ] **Step 5: 컴포넌트 테스트** — 각 뷰에 "pick.active일 때 연결점 클릭 → onPick(ref) 호출, 비활성 시 기존 동작" 테스트 추가(useCablePick 모킹). 예시(BreakerRail은 순수 컴포넌트라 onPick prop 주입 방식이면 prop 테스트, 아니면 패널 레벨에서 useCablePick 모킹).
- [ ] **Step 6: 검증·커밋** — `npx vitest run` + `npx tsc --noEmit` → green/clean.

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): 연결점 뷰(피더분기/포트/랙모듈)에 pick 모드 onPick 연결

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2.3: 캔버스 컨테이너 클릭 → 우측 패널 open + 피커 3종 삭제

**Files:**
- Modify: `src/features/editor/hooks/useCanvasEvents.ts` (pending 시 패널 open)
- Delete: `CableEndpointPickerHost.tsx`, `RackModulePicker.tsx`, `CircuitPicker.tsx`, `SlotCorePicker.tsx`
- Modify: `CableEndpointPickerHost` 렌더처(FloorPlanEditor 등)에서 제거

- [ ] **Step 1** — `cableSetPendingSource/Target(eq.id)` 직후 `useEditorStore.getState().openDetail(eq.id)`(우측 상세 패널 열어 pick 대상 노출). pick 모드에선 detail 패널의 정보 탭 대신 공간 섹션(연결점)이 onPick로 동작.
- [ ] **Step 2** — `CableEndpointPickerHost` 렌더 제거(FloorPlanEditor에서 `<CableEndpointPickerHost/>` 삭제), 파일 4개 삭제.
- [ ] **Step 3** — `grep -rn "RackModulePicker\|CircuitPicker\|SlotCorePicker\|CableEndpointPickerHost" src` → 0건 확인.
- [ ] **Step 4: 검증·커밋** — `npx vitest run` green + `npx tsc --noEmit` clean.

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "refactor(cable): 컨테이너 클릭→우측 패널 pick + 피커 3종/PickerHost 삭제

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Phase 2 종료 게이트:** 평면도에서 랙/OFD/분전반 클릭 시 우측 패널이 그 뷰로 떠 연결점을 고르고, 별도 피커 모달이 사라졌다. green/clean + 스모크 후 머지.

---

# Phase 3 — 피더 IN 슬롯

### Task 3.1: `buildFeederInput`

**Files:**
- Modify: `src/features/power/feederCircuits.ts`
- Test: `src/features/power/feederCircuits.test.ts` (기존에 추가)

- [ ] **Step 1: 실패 테스트 추가**

```ts
import { buildFeederInput } from './feederCircuits';
const inCable = { id: 'in1', sourceAssetId: 'srcEq', targetAssetId: 'f1', sourceRole: null, targetRole: 'IN', number: null, specParams: {} };

describe('buildFeederInput — role=IN 공급', () => {
  it('IN 케이블 1개 해소', () => {
    const r = buildFeederInput({ id: 'f1' }, [inCable] as never, new Map([['srcEq', '주변압기']]));
    expect(r).toEqual({ cableId: 'in1', sourceAssetId: 'srcEq', sourceName: '주변압기' });
  });
  it('없으면 null', () => {
    expect(buildFeederInput({ id: 'f1' }, [] as never, new Map())).toBeNull();
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npx vitest run src/features/power/feederCircuits.test.ts` → FAIL.

- [ ] **Step 3: 구현** (`feederCircuits.ts`에 추가; `roleAt`/`other`는 기존 `slotRegister`에서 import)

```ts
import { roleAt, other } from '../fiber/slotRegister';

export interface FeederInput { cableId: string; sourceAssetId: string | null; sourceName: string | null }

/** 피더의 공급(Input) — roleAt==='IN' 케이블 1개(없으면 null). 다중이면 첫 1개. */
export function buildFeederInput(feeder: { id: string }, cables: Cable[], nameById: Map<string, string>): FeederInput | null {
  const c = cables.find((x) => roleAt(x, feeder.id) === 'IN'
    && (x.sourceAssetId === feeder.id || x.targetAssetId === feeder.id));
  if (!c) return null;
  const src = other(c, feeder.id);
  return { cableId: c.id, sourceAssetId: src, sourceName: src ? (nameById.get(src) ?? null) : null };
}
```
(주의: `roleAt(c, feeder.id)`는 그 피더 끝의 role을 준다. IN이면 공급. 기존 `Cable` 타입/`roleAt` 시그니처 확인 후 맞춤.)

- [ ] **Step 4: 통과 확인** — Run: `npx vitest run src/features/power/feederCircuits.test.ts` → PASS.

- [ ] **Step 5: 커밋**

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(분전반): buildFeederInput — 피더 공급(role=IN) 파생

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3.2: 피더 패널 상단 IN 슬롯 + pick 연동

**Files:**
- Modify: `src/features/power/components/FeederCircuitsPanel.tsx`
- Test: `src/features/power/components/FeederCircuitsPanel.test.tsx`

- [ ] **Step 1: 테스트 추가** — IN 슬롯 렌더(점유 시 공급원명/삭제, 빈 시 "＋ 입력 연결"), pick.active면 IN 슬롯 클릭 → `onPick({innerAssetId:feeder.id, role:'IN'})`. (useCablePick·buildFeederInput 모킹.)
- [ ] **Step 2: 구현** — 6×4 그리드 위에 단일 IN 슬롯(흰 모듈 톤, CONVENTIONS). 점유: `buildFeederInput` 결과 표시 + 삭제(`stageCableDelete(cableId)`). 빈: 버튼 → `startCableConnection({ source: { containerAssetId: anchor, position, innerAssetId: feeder.id, role:'IN' } })`(Phase 4 함수; 이 태스크 시점엔 직접 setTool+activate로 임시, Phase 4에서 치환). pick.active면 IN 슬롯이 onPick 대상.
- [ ] **Step 3: 검증·커밋** — `npx vitest run` + `npx tsc --noEmit` green/clean.

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(분전반): 피더 상단 단일 IN 슬롯(공급 표시/삭제/입력연결) + pick 연동

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Phase 3 종료 게이트:** 피더 패널에 IN 슬롯이 보이고, 케이블 연결 시 IN/분기 중 선택 가능. green/clean + 스모크 후 머지.

---

# Phase 4 — `startCableConnection` 진입 통일

### Task 4.1: `startCableConnection` + 전 진입점 이관

**Files:**
- Modify: `src/features/editor/cableConnection.ts` (startCableConnection 추가)
- Test: `src/features/editor/cableConnection.test.ts` (추가)
- Modify: `EditorInsertBar.tsx`, `useEditorKeyboard.ts`, `FeederCircuitsPanel.tsx`(addCb/IN 버튼)

- [ ] **Step 1: 테스트 추가**

```ts
import { startCableConnection } from './cableConnection';
// editorStore.setTool/setPreselectedCableDisplayGroup, interaction.cableActivate 모킹/관찰
describe('startCableConnection', () => {
  it('source 주입 → tool=cable + cableActivate({source})', () => {
    const setTool = vi.fn();
    // ... 모킹 후
    startCableConnection({ source: { containerAssetId: 'd', position: { x:0,y:0 }, innerAssetId: 'f1', role: 'OUT' } });
    expect(setTool).toHaveBeenCalledWith('cable');
    expect(useInteractionStore.getState().mode).toMatchObject({ kind: 'cableDrawing', data: { phase: 'selectingType', source: { innerAssetId: 'f1' } } });
  });
});
```

- [ ] **Step 2: 구현**

```ts
// cableConnection.ts 에 추가
import { useInteractionStore } from './stores/interactionStore';
import type { EndpointRef } from './cableEndpoint';
import type { CableDisplayGroup } from '../../types/cableCategory';
import type { SelectedCableCategory } from './stores/interactionStore';

export function startCableConnection(opts?: {
  source?: EndpointRef; category?: SelectedCableCategory; group?: CableDisplayGroup;
}): void {
  const editor = useEditorStore.getState();
  editor.setTool('cable');
  editor.setPreselectedCableDisplayGroup(opts?.group ?? null);
  useInteractionStore.getState().cableActivate({ source: opts?.source, category: opts?.category });
}
```
주의: `setTool('cable')`이 FloorPlanEditor effect로 `cableActivate()`(빈)를 한 번 더 부를 수 있음 — effect는 "이미 cableDrawing이면 재호출 안 함" 가드가 있으므로(현재 동작) 순서상 `setTool` 후 `cableActivate(opts)`가 덮어쓰도록 한다. 필요 시 effect 가드 확인.

- [ ] **Step 3: 진입점 이관**
  - `useEditorKeyboard.ts`('3'/'C'): `setTool('cable')` → `startCableConnection()`.
  - `EditorInsertBar.tsx`(그룹 pill): `setTool('cable')+setPreselectedCableDisplayGroup(g)` → `startCableConnection({ group: g })`.
  - `FeederCircuitsPanel` 분기 추가(addCb): `setTool('cable')+gotoAsset` → `startCableConnection({ source: { containerAssetId: anchor, position, innerAssetId: feeder.id, role:'OUT' } })` + 필요 시 `gotoAsset`(평면도 진입).
  - `FeederCircuitsPanel` IN "입력 연결": `startCableConnection({ source: { ..., role:'IN' } })`.
- [ ] **Step 4: 흩어진 호출 제거** — `grep -rn "setTool('cable')\|cableActivate(" src` → `cableConnection.ts`·FloorPlanEditor effect 외 0건.
- [ ] **Step 5: 검증·커밋** — `npx vitest run` green + `npx tsc --noEmit` clean.

```bash
git -C /Users/jsk/1210/digital add -A && git -C /Users/jsk/1210/digital commit -m "feat(cable): startCableConnection 진입 통일 + 전 진입점 이관(출발 자동주입)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Phase 4 종료 게이트:** 모든 진입점이 `startCableConnection`으로 수렴, "분기 추가"는 종류→도착만. green/clean + 스모크 후 머지.

---

## 최종 검증

- [ ] `npx tsc --noEmit` clean
- [ ] `npx vitest run` 전체 green
- [ ] `grep -rn "RackModulePicker\|CircuitPicker\|SlotCorePicker\|CableEndpointPickerHost\|selectingSpec\|RoleSelector" src` → 0건
- [ ] 수동 스모크(평면도): ①도구→종류 모달 먼저 ②랙→피더분기 ③피더IN←타설비 ④OFD포트↔포트 ⑤"분기 추가"=도착만 ⑥ESC/패널닫기 취소 정상
- [ ] superpowers:finishing-a-development-branch 로 마무리
