# P4 — 자산관리 셸 + 설비종류 탭 (카탈로그 워킹카피) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 전역 "자산관리" 화면(헤더 메뉴·관리자 전용)을 만들고, 설비종류(AssetType)+분류(AssetCategory)를 **카탈로그 워킹카피**(스테이징 → 원자적 저장/취소)로 CRUD 한다. 키 입력마다 API ❌, "저장" 한 번에 트랜잭션 ⭕.

**Architecture:** 변전소 워킹카피와 **분리된 전역 카탈로그 워킹카피** — 기존 범용 엔진(`workingCopy/overlay.ts`·`effective.ts`·`delta.ts`)을 **재사용**(중복 0)하는 얇은 `catalogStore`. 저장 = `POST /api/catalog/commit`(prisma `$transaction` 원자 적용). 신규 id는 클라이언트 uuid(카테고리→타입 FK 즉시 참조, remapping 불필요).

**Tech Stack:** React 18 + TS, Zustand, React Query, Express/Prisma($transaction), vitest.

## Global Constraints

- 개발: DB만 docker + `npm run dev`. docker compose build/up 금지.
- **공유 마스터데이터 = 워킹카피(스테이징+원자적 commit)**. 키 입력마다 API 금지. 저장 단위 = 카탈로그(변전소 저장과 분리).
- UI: CONVENTIONS.md(DetailCard·SectionItem·FormRow·EditableField·Badge·Button 재사용, text-xs 미만 금지). 관리자 전용(`useIsAdmin`).
- 엔진 재사용: `overlay.ts`(stageCreate/stageUpdate/stageDelete/overlayDirtyCount/emptyOverlay)·`effective.ts`(mergeEffective)·`delta.ts`(buildDelta). **새 워킹카피 엔진 만들지 않는다.**
- role 보유 타입(rack/ofd/panel/slot/feeder/standalone)은 시드·삭제불가·이름만 수정. 사용자 신규 = 항상 device.
- 커밋/푸시는 각 Task 끝 명시 단계. 메시지 말미: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- 새 브랜치(main 에서): `feat/asset-management-p4`. 루트: 프론트 `/Users/jsk/1210/digital/frontend`, 백엔드 `/Users/jsk/1210/digital/backend`.
- **범위:** 셸 + 설비종류 탭만. 케이블종류 탭 = C4, 프리셋 탭 = 후속(탭은 자리만, disabled).

---

### Task 1: 백엔드 — 카탈로그 원자적 commit 엔드포인트

**Files:**
- Create: `backend/src/services/catalogCommit.service.ts`
- Create: `backend/src/controllers/catalog.controller.ts`
- Create: `backend/src/routes/catalog.routes.ts`
- Modify: `backend/src/index.ts` (라우터 등록)
- Test: `backend/tests/catalogCommit.service.test.ts`

**Interfaces:**
- Produces: `commitCatalog(input)` 가 `prisma.$transaction` 으로 assetCategories + assetTypes 의 creates/updates/deletes 를 원자 적용. `POST /api/catalog/commit`(adminOnly).
- Input:
```ts
interface CollDelta<C, U> { creates: C[]; updates: { id: string; patch: U }[]; deletes: { id: string }[] }
interface CatalogCommitInput {
  assetCategories?: CollDelta<{ id: string; name: string; sortOrder?: number }, { name?: string; sortOrder?: number }>;
  assetTypes?: CollDelta<{ id: string; name: string; categoryId: string | null }, { name?: string; categoryId?: string | null }>;
}
```

- [ ] **Step 1: 실패 테스트** — `backend/tests/catalogCommit.service.test.ts` (prisma 모킹 — $transaction 은 콜백에 mock tx 전달):
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { commitCatalog } from '../src/services/catalogCommit.service.js';
import prisma from '../src/config/prisma.js';

const tx = {
  assetCategory: { create: vi.fn(), update: vi.fn(), delete: vi.fn() },
  assetType: { create: vi.fn(), update: vi.fn(), delete: vi.fn(), findUnique: vi.fn(), count: vi.fn() },
};
vi.mock('../src/config/prisma.js', () => ({
  default: { $transaction: vi.fn(async (fn: (t: unknown) => unknown) => fn(tx)) },
}));

beforeEach(() => { vi.clearAllMocks(); });

describe('commitCatalog', () => {
  it('카테고리 생성 후 그 id 로 타입 생성(원자, FK 순서)', async () => {
    await commitCatalog({
      assetCategories: { creates: [{ id: 'cat-1', name: '광전송' }], updates: [], deletes: [] },
      assetTypes: { creates: [{ id: 'type-1', name: '송변전광단말', categoryId: 'cat-1' }], updates: [], deletes: [] },
    });
    expect(tx.assetCategory.create).toHaveBeenCalledWith(expect.objectContaining({ data: expect.objectContaining({ id: 'cat-1', name: '광전송' }) }));
    const typeData = tx.assetType.create.mock.calls[0][0].data;
    expect(typeData).toMatchObject({ id: 'type-1', name: '송변전광단말', categoryId: 'cat-1', role: 'device' });
    expect(typeData.code).toMatch(/^MOD-/);
  });

  it('role 보유 타입 삭제는 차단', async () => {
    tx.assetType.findUnique.mockResolvedValue({ id: 't', role: 'ofd' });
    await expect(commitCatalog({ assetTypes: { creates: [], updates: [], deletes: [{ id: 't' }] } }))
      .rejects.toThrow('삭제할 수 없습니다');
  });

  it('device 타입 사용 중이면 삭제 차단', async () => {
    tx.assetType.findUnique.mockResolvedValue({ id: 't', role: 'device' });
    tx.assetType.count.mockResolvedValue(0); // (사용여부는 asset.count — 아래 구현 참조)
    // asset.count 모킹
    (tx as any).asset = { count: vi.fn().mockResolvedValue(2) };
    await expect(commitCatalog({ assetTypes: { creates: [], updates: [], deletes: [{ id: 't' }] } }))
      .rejects.toThrow('사용 중');
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd /Users/jsk/1210/digital/backend && npx vitest run tests/catalogCommit.service.test.ts` → FAIL.

- [ ] **Step 3: 서비스 구현** — `backend/src/services/catalogCommit.service.ts`:
```ts
import { randomUUID } from 'node:crypto';
import prisma from '../config/prisma.js';
import { ConflictError, ValidationError } from '../utils/errors.js';

interface CollDelta<C, U> { creates: C[]; updates: { id: string; patch: U }[]; deletes: { id: string }[] }
export interface CatalogCommitInput {
  assetCategories?: CollDelta<{ id: string; name: string; sortOrder?: number }, { name?: string; sortOrder?: number }>;
  assetTypes?: CollDelta<{ id: string; name: string; categoryId: string | null }, { name?: string; categoryId?: string | null }>;
}

// prisma 트랜잭션 클라이언트 타입(느슨) — 도메인 메서드만 사용.
type Tx = Parameters<Parameters<typeof prisma.$transaction>[0]>[0];

export async function commitCatalog(input: CatalogCommitInput): Promise<void> {
  await prisma.$transaction(async (tx) => {
    const t = tx as unknown as typeof prisma;
    // 1) 분류: 생성 → 수정 → 삭제(생성 먼저: 타입이 새 분류 참조 가능)
    const cat = input.assetCategories;
    if (cat) {
      for (const c of cat.creates) await t.assetCategory.create({ data: { id: c.id, name: c.name.trim(), sortOrder: c.sortOrder ?? 0 } });
      for (const u of cat.updates) await t.assetCategory.update({ where: { id: u.id }, data: { ...(u.patch.name !== undefined ? { name: u.patch.name.trim() } : {}), ...(u.patch.sortOrder !== undefined ? { sortOrder: u.patch.sortOrder } : {}) } });
    }
    // 2) 타입: 생성(항상 role=device, code 자동) → 수정(role-bearing 분류변경 금지) → 삭제(가드)
    const ty = input.assetTypes;
    if (ty) {
      for (const c of ty.creates) {
        await t.assetType.create({ data: { id: c.id, code: `MOD-${randomUUID().slice(0, 8).toUpperCase()}`, name: c.name.trim(), role: 'device', categoryId: c.categoryId, requiredToCreate: ['name'] } });
      }
      for (const u of ty.updates) {
        const ex = await t.assetType.findUnique({ where: { id: u.id } });
        if (!ex) throw new ValidationError(`존재하지 않는 종류: ${u.id}`);
        if (ex.role !== 'device' && u.patch.categoryId !== undefined) throw new ConflictError('시스템 종류는 분류를 변경할 수 없습니다.');
        await t.assetType.update({ where: { id: u.id }, data: { ...(u.patch.name !== undefined ? { name: u.patch.name.trim() } : {}), ...(u.patch.categoryId !== undefined ? { categoryId: u.patch.categoryId } : {}) } });
      }
      for (const d of ty.deletes) {
        const ex = await t.assetType.findUnique({ where: { id: d.id } });
        if (!ex) continue;
        if (ex.role !== 'device') throw new ConflictError('시스템 종류는 삭제할 수 없습니다.');
        const inUse = await t.asset.count({ where: { assetTypeId: d.id } });
        if (inUse > 0) throw new ConflictError(`사용 중인 자산 ${inUse}개가 있어 삭제할 수 없습니다.`);
        await t.assetType.delete({ where: { id: d.id } });
      }
    }
    // 3) 분류 삭제(타입 삭제 후 — FK)
    if (cat) {
      for (const d of cat.deletes) {
        const inUse = await t.assetType.count({ where: { categoryId: d.id } });
        if (inUse > 0) throw new ConflictError(`사용 중인 종류 ${inUse}개가 있어 분류를 삭제할 수 없습니다.`);
        await t.assetCategory.delete({ where: { id: d.id } });
      }
    }
  });
}
```

- [ ] **Step 4: 컨트롤러 + 라우트** — `catalog.controller.ts`:
```ts
import { Request, Response, NextFunction } from 'express';
import { commitCatalog } from '../services/catalogCommit.service.js';
export const catalogController = {
  async commit(req: Request, res: Response, next: NextFunction): Promise<void> {
    try { await commitCatalog(req.body); res.json({ message: '저장되었습니다.' }); } catch (e) { next(e); }
  },
};
```
`catalog.routes.ts`:
```ts
import { Router } from 'express';
import { catalogController } from '../controllers/catalog.controller.js';
import { authenticate, adminOnly } from '../middleware/auth.js';
const router = Router();
router.post('/commit', authenticate, adminOnly, catalogController.commit);
export { router as catalogRouter };
```
`index.ts`: `import { catalogRouter }` + `app.use('/api/catalog', catalogRouter);`.

- [ ] **Step 5: 통과 + 타입** — `npx vitest run tests/catalogCommit.service.test.ts && npx tsc --noEmit | tail -5` → PASS.

- [ ] **Step 6: 커밋** — `feat(catalog): 원자적 catalog commit 엔드포인트(assetTypes+categories) (P4)`.

---

### Task 2: 프론트 — catalogStore (범용 엔진 재사용) + useAssetCategories

**Files:**
- Create: `frontend/src/features/catalog/catalogStore.ts`
- Create: `frontend/src/features/assets/hooks/useAssetCategories.ts`
- Create: `frontend/src/types/assetCategory.ts`
- Test: `frontend/src/features/catalog/catalogStore.test.ts`

**Interfaces:**
- Produces: `useCatalogStore` — collections `assetTypes`/`assetCategories` overlay(over base), `effectiveTypes()`/`effectiveCategories()`, `stageCreateType/stageUpdateType/stageDeleteType` + category 대응, `dirtyCount()`, `load()`, `commit()`, `discard()`. `newCatalogId()`(클라 uuid). `useAssetCategories()`(GET /api/asset-categories).

- [ ] **Step 1: 타입 + 분류 훅** — `types/assetCategory.ts`:
```ts
export interface AssetCategory { id: string; name: string; sortOrder: number; isActive: boolean }
```
`hooks/useAssetCategories.ts` (useAssetTypes 패턴, GET /api/asset-categories).

- [ ] **Step 2: 실패 테스트** — `catalogStore.test.ts`: stageCreateType → effectiveTypes 에 반영 / stageDeleteType → 제외 / dirtyCount / discard → 0 / commit() 이 buildDelta 로 POST /catalog/commit 호출(api 모킹). 핵심:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
vi.mock('../../utils/api', () => ({ api: { post: vi.fn().mockResolvedValue({ data: {} }), get: vi.fn().mockResolvedValue({ data: { data: [] } }) } }));
import { useCatalogStore, newCatalogId } from './catalogStore';
import { api } from '../../utils/api';
beforeEach(() => { useCatalogStore.getState().discard(); useCatalogStore.setState({ baseTypes: [], baseCategories: [] } as never); vi.clearAllMocks(); });
describe('catalogStore', () => {
  it('stageCreateType → effective 반영, dirty 1', () => {
    const id = newCatalogId();
    useCatalogStore.getState().stageCreateType({ id, name: '새종류', categoryId: null });
    expect(useCatalogStore.getState().effectiveTypes().some((t) => t.id === id)).toBe(true);
    expect(useCatalogStore.getState().dirtyCount()).toBe(1);
  });
  it('commit() 이 delta 로 POST /catalog/commit', async () => {
    const id = newCatalogId();
    useCatalogStore.getState().stageCreateType({ id, name: 'X', categoryId: null });
    await useCatalogStore.getState().commit();
    expect(api.post).toHaveBeenCalledWith('/catalog/commit', expect.objectContaining({ assetTypes: expect.objectContaining({ creates: expect.any(Array) }) }));
  });
  it('discard → dirty 0', () => {
    useCatalogStore.getState().stageCreateType({ id: newCatalogId(), name: 'Y', categoryId: null });
    useCatalogStore.getState().discard();
    expect(useCatalogStore.getState().dirtyCount()).toBe(0);
  });
});
```

- [ ] **Step 3: 실패 확인** — `cd /Users/jsk/1210/digital/frontend && npx vitest run src/features/catalog/catalogStore.test.ts` → FAIL.

- [ ] **Step 4: 스토어 구현** — `catalog/catalogStore.ts` (overlay.ts/effective.ts/delta.ts 재사용):
```ts
import { create } from 'zustand';
import { api } from '../../utils/api';
import type { AssetType } from '../../types/asset';
import type { AssetCategory } from '../../types/assetCategory';
import { emptyOverlay, stageCreate, stageUpdate, stageDelete, overlayDirtyCount, type Overlay } from '../workingCopy/overlay';
import { mergeEffective } from '../workingCopy/effective';
import { buildDelta } from '../workingCopy/delta';

export function newCatalogId(): string {
  // 클라 생성 id(카테고리→타입 FK 즉시 참조). secure-context 무관 uuid v4.
  const b = new Uint8Array(16); crypto.getRandomValues(b);
  b[6] = (b[6] & 0x0f) | 0x40; b[8] = (b[8] & 0x3f) | 0x80;
  const h = [...b].map((x) => x.toString(16).padStart(2, '0')).join('');
  return `${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`;
}

type TypeP = Partial<Pick<AssetType, 'name' | 'categoryId'>>;
type CatP = Partial<Pick<AssetCategory, 'name' | 'sortOrder'>>;

interface CatalogState {
  baseTypes: AssetType[]; baseCategories: AssetCategory[];
  typeOverlay: Overlay<AssetType, TypeP>; catOverlay: Overlay<AssetCategory, CatP>;
  load: () => Promise<void>;
  effectiveTypes: () => AssetType[]; effectiveCategories: () => AssetCategory[];
  stageCreateType: (t: AssetType) => void; stageUpdateType: (id: string, p: TypeP) => void; stageDeleteType: (id: string, isNew: boolean) => void;
  stageCreateCategory: (c: AssetCategory) => void; stageUpdateCategory: (id: string, p: CatP) => void; stageDeleteCategory: (id: string, isNew: boolean) => void;
  dirtyCount: () => number;
  commit: () => Promise<void>;
  discard: () => void;
}

export const useCatalogStore = create<CatalogState>((set, get) => ({
  baseTypes: [], baseCategories: [],
  typeOverlay: emptyOverlay<AssetType, TypeP>(), catOverlay: emptyOverlay<AssetCategory, CatP>(),
  load: async () => {
    const [t, c] = await Promise.all([
      api.get<{ data: AssetType[] }>('/asset-types'),
      api.get<{ data: AssetCategory[] }>('/asset-categories'),
    ]);
    set({ baseTypes: t.data.data, baseCategories: c.data.data, typeOverlay: emptyOverlay(), catOverlay: emptyOverlay() });
  },
  effectiveTypes: () => mergeEffective(get().baseTypes, get().typeOverlay),
  effectiveCategories: () => mergeEffective(get().baseCategories, get().catOverlay),
  stageCreateType: (t) => set((s) => ({ typeOverlay: stageCreate(s.typeOverlay, t.id, t) })),
  stageUpdateType: (id, p) => set((s) => ({ typeOverlay: stageUpdate(s.typeOverlay, id, p) })),
  stageDeleteType: (id, isNew) => set((s) => ({ typeOverlay: stageDelete(s.typeOverlay, id, isNew) })),
  stageCreateCategory: (c) => set((s) => ({ catOverlay: stageCreate(s.catOverlay, c.id, c) })),
  stageUpdateCategory: (id, p) => set((s) => ({ catOverlay: stageUpdate(s.catOverlay, id, p) })),
  stageDeleteCategory: (id, isNew) => set((s) => ({ catOverlay: stageDelete(s.catOverlay, id, isNew) })),
  dirtyCount: () => overlayDirtyCount(get().typeOverlay) + overlayDirtyCount(get().catOverlay),
  commit: async () => {
    const td = buildDelta(get().typeOverlay); const cd = buildDelta(get().catOverlay);
    await api.post('/catalog/commit', {
      assetCategories: { creates: cd.creates.map((c) => ({ id: c.id, name: c.name, sortOrder: c.sortOrder })), updates: cd.updates.map((u) => ({ id: u.id, patch: u.patch })), deletes: cd.deletes.map((d) => ({ id: d.id })) },
      assetTypes: { creates: td.creates.map((t) => ({ id: t.id, name: t.name, categoryId: t.categoryId ?? null })), updates: td.updates.map((u) => ({ id: u.id, patch: u.patch })), deletes: td.deletes.map((d) => ({ id: d.id })) },
    });
    await get().load();
  },
  discard: () => set({ typeOverlay: emptyOverlay(), catOverlay: emptyOverlay() }),
}));
```
> `mergeEffective`/`buildDelta` 의 정확한 시그니처(인자 순서·baseVersion)는 `overlay.ts`/`effective.ts`/`delta.ts` 확인 후 맞춘다. baseVersion(OCC)은 카탈로그(관리자 단일 편집)에서 미사용 — buildDelta 가 요구하면 snapshotBaseVersions 없이 null 허용.

- [ ] **Step 5: 통과 + 타입** — `npx vitest run src/features/catalog/catalogStore.test.ts && npx tsc --noEmit | tail -5` → PASS.

- [ ] **Step 6: 커밋** — `feat(catalog): catalogStore(범용 overlay 엔진 재사용) + useAssetCategories (P4)`.

---

### Task 3: 자산관리 셸 (라우트·헤더 메뉴·페이지·탭·저장바)

**Files:**
- Create: `frontend/src/features/catalog/AssetManagementPage.tsx`
- Create: `frontend/src/features/catalog/CatalogSaveBar.tsx`
- Modify: `frontend/src/App.tsx` (라우트), `frontend/src/components/AppShell.tsx` (헤더 메뉴)

**Interfaces:**
- Produces: `/asset-management` 라우트(관리자). 헤더에 "자산관리" 링크(admin 만). 탭(설비종류 활성 / 케이블종류·프리셋 disabled) + `CatalogSaveBar`(dirty N · 저장/취소).

- [ ] **Step 1: 라우트** — `App.tsx` 의 AppShell 그룹 안에 `<Route path="/asset-management" element={<AssetManagementPage />} />` 추가(import).
- [ ] **Step 2: 헤더 메뉴** — `AppShell.tsx` 헤더(로그아웃 옆 또는 Breadcrumb 우측)에 `useIsAdmin()` 가드 Link:
```tsx
{isAdmin && <Link to="/asset-management" className="text-sm text-content-muted hover:text-content transition-colors">자산관리</Link>}
```
(import useIsAdmin.)
- [ ] **Step 3: 페이지 + 탭 셸** — `AssetManagementPage.tsx`: 마운트 시 `useCatalogStore.getState().load()`(useEffect). 관리자 아니면 `<Navigate to="/" replace />`(useIsAdmin). 탭 헤더(설비종류 활성 / 케이블종류·프리셋 `disabled` 회색). 본문 = `<AssetTypesTab />`(Task 4). 하단 `<CatalogSaveBar />`. CONVENTIONS 색/타이포.
- [ ] **Step 4: CatalogSaveBar** — `dirtyCount()` 가 0 이면 null, 아니면 "변경 N건" + 저장(`commit()`)/취소(`discard()`) 버튼(Button 공용). 저장 중 disabled + 에러 토스트.
- [ ] **Step 5: 타입+빌드** — `npx tsc --noEmit | tail -5 && npm run build 2>&1 | tail -3` → 성공.
- [ ] **Step 6: 커밋** — `feat(catalog): 자산관리 셸(라우트·헤더 메뉴·탭·저장바) (P4)`.

---

### Task 4: 설비종류 탭 (분류 + 종류 CRUD)

**Files:** Create `frontend/src/features/catalog/AssetTypesTab.tsx`, Test `frontend/src/features/catalog/AssetTypesTab.test.tsx`

**Interfaces:** effective 분류/종류를 읽어 렌더. 분류별 그룹 + 그 안 종류. 추가/이름수정/분류이동/삭제 = catalogStore stage. role 보유 종류(role≠device)는 잠금(이름만 수정·삭제버튼 없음, 잠금 Badge).

- [ ] **Step 1: 실패 테스트** — `AssetTypesTab.test.tsx`(catalogStore 실데이터 또는 setState 주입): 분류별 종류 렌더 / "+ 종류" → stageCreateType 호출 / device 종류 삭제 버튼 존재 + role 보유는 없음 / 이름 편집 → stageUpdateType.
```tsx
// 예: store 에 baseTypes/baseCategories 주입 후 렌더, 버튼 상호작용 → store 상태 검증.
```
- [ ] **Step 2: 구현** — `AssetTypesTab.tsx`:
  - `const types = useCatalogStore((s) => s.effectiveTypes());`(셀렉터는 dirty 재계산 위해 overlay 의존) — 실제론 `useCatalogStore()` 구독 + effectiveTypes() 호출.
  - 분류 섹션: effectiveCategories 별로 DetailCard, 헤더에 분류명(EditableField → stageUpdateCategory) + 삭제(빈 분류만). "+ 분류"(stageCreateCategory, newCatalogId).
  - 각 분류 안: 그 categoryId 종류 목록. SectionItem/행: 종류명(EditableField → stageUpdateType), 분류 이동(분류 select → stageUpdateType{categoryId}), 삭제(device 만; role 보유는 잠금 Badge). "+ 종류"(stageCreateType{ id:newCatalogId(), name:'새 종류', categoryId: 그 분류 }).
  - role 보유 종류: 이름만 EditableField, 삭제 버튼 숨김, `<Badge>시스템</Badge>`.
  - 신규(overlay.creates)면 stageDelete(id, true=isNew), 기존이면 (id, false).
- [ ] **Step 3: 통과 + 회귀 + 빌드** — `npx vitest run src/features/catalog && npm run build 2>&1 | tail -3` → PASS+빌드.
- [ ] **Step 4: 커밋** — `feat(catalog): 설비종류 탭(분류+종류 CRUD, role 보유 잠금) (P4)`.

---

### Task 5: 전체 회귀 + 스모크

- [ ] **Step 1: 프론트** — `cd /Users/jsk/1210/digital/frontend && npx vitest run && npm run build 2>&1 | tail -3` → 전체 PASS.
- [ ] **Step 2: 백엔드** — `cd /Users/jsk/1210/digital/backend && npx vitest run && npx tsc --noEmit | tail -3` → PASS.
- [ ] **Step 3: 수동 스모크** — `npm run dev`(관리자 로그인): 헤더 "자산관리" → 분류/종류 추가·이름수정·삭제 → dirty 바 → 저장(원자) → 새로고침 반영 / 취소 → 폐기 / role 보유 종류 잠금 확인 / 비관리자는 메뉴·라우트 차단.

---

## Self-Review

**1. Spec(§8) 커버:** 헤더 전역 메뉴·관리자(T3) / A안=드래프트+저장(카탈로그 워킹카피 T2 + 저장바 T3) / 설비종류 탭(T4) / 케이블종류·프리셋 탭=자리만(C4·후속). **원자적 저장**(T1 $transaction). role 보유 잠금(T4).
**2. Placeholder scan:** 코드/명령 구체. T2 의 mergeEffective/buildDelta 시그니처만 파일 확인 지시(엔진 재사용이라 계약 고정 필요) — 환경 의존.
**3. Type consistency:** `catalogStore`(effectiveTypes/Categories·stage*·dirtyCount·commit·discard)·`CatalogCommitInput`(assetCategories/assetTypes creates/updates/deletes)·`newCatalogId` 가 T1~T4 일관. 신규 id=클라 uuid(remapping 없음).

## 주의 / 후속
- **저장 단위 분리:** 카탈로그 저장 ≠ 변전소 저장(WorkingCopyCommitBar 는 변전소용, CatalogSaveBar 는 카탈로그용). 같은 overlay 엔진, 다른 스코프·다른 commit.
- **C4(케이블종류 탭)**: 같은 catalogStore 에 cableGroups/cableCategories 컬렉션 추가 + catalog commit 에 그 델타 추가 + insert bar 데이터화(C3 이월 Task 5). **프리셋 탭**도 후속.
- **C5**: 구컬럼/enum 드롭(cableType·label·code·displayGroup·placementKind) — P4·C4 후.
- OCC: 카탈로그는 관리자 단일 편집이라 baseVersion(OCC) 미사용(last-write-wins). 필요 시 후속.
