# 연결 대장 그리드 일반화 (선번장·계통 통합) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 선번장·계통 그리드의 공통 셸(컨테이너→connectionKind 자식(섹션)→테이블→행클릭선택)을 단일 제네릭 `ConnectionRegisterGrid` 로 추출하고, 자식별 차이(행 출처·컬럼·편집·far·헤더)는 `RegisterDescriptor`(렌더-함수 컬럼)로 공급한다. 두 뷰를 각 descriptor로 재구성해 **동작 100% 보존**하면서 중복 제거. 향후 슬롯형 대장(접지)은 descriptor 추가로 끝.

**Architecture:** 제네릭 컴포넌트(도메인 로직 0) + `fiberRegisterDescriptor`(OFD→conduit→코어, 편집·trace far) + `powerRegisterDescriptor`(분전반→distributor→CB, 읽기전용). `FiberRegisterView`/`PowerView` 사이드패널 래퍼는 유지, 내부 그리드만 교체. 구 `OfdFiberRegister`/`PanelCircuitView` 본문 삭제. **모델/백엔드 무변경.**

**Tech Stack:** React/TS/Zustand, Vitest.

**참조 spec:** `docs/superpowers/specs/2026-06-15-연결대장-그리드일반화-design.md`. 재사용: `buildSlotCoreRows`/`SlotCoreRow`(slotRegister), `useTraceGraph`/`remoteSlotSubstation`(traceGraph), `useEffectiveAssets`/`useEffectiveCables`, `useSelectionStore`, `usePathHighlightStore`.

---

## 환경 규칙 (공통)
- DB dev(떠있음). 프론트 `cd frontend && npx tsc --noEmit && npm run build && npx vitest run`. 단일 테스트 `npx vitest run <file>`.
- 순수 프론트 리팩토링 — 동작 보존이 핵심. 기존 `FiberRegisterView.test.tsx` 가 새 셸 위에서 통과해야 함(보존 증명).

## 현재 구조 (확인됨)
- **OfdFiberRegister**(`features/fiber/components/OfdFiberRegister.tsx`): `assets.filter(parentAssetId===ofdId && connectionKind==='conduit')` → 슬롯별 섹션, `buildSlotCoreRows(slot, cables, graph)` → 행. 헤더=`로컬sub - 대국sub`. 7컬럼(코어/근접자산/상대국측/용도·수용내역=input/융착·사용=select + `commitMeta`). 행클릭→`setSelectedAssetId(near ?? ofd)`. 행 active=`tracingCableId===row.cableId`.
- **PanelCircuitView**(`features/power/components/PanelCircuitView.tsx`): `assets.filter(DIST)` → 분전반(h2) → `filter(parentAssetId===panel && connectionKind distributor)` 피더별 섹션. cbCables=피더 OUT 케이블 → CbRow(cableId/loadAssetId/loadName/cbNumber/capacity/switchState/spec). 5컬럼(번호/부하/용량/규격/SW). 헤더=피더명+`사용 ON/total`. 행클릭→`setSelectedAssetId(load ?? feeder)`. 읽기전용.
- **FiberRegisterView**: ofds(placementKind OFD)별 `<OfdFiberRegister>` + `StagedAssetDetailPanel`. **PowerView**: `<PanelCircuitView>` + 사이드패널.

## File Structure
- `frontend/src/features/connections/registerGrid/registerTypes.ts` (Create) — descriptor 타입.
- `frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx` (Create) — 제네릭 셸.
- `frontend/src/features/fiber/fiberRegisterDescriptor.tsx` (Create) — 선번장 descriptor.
- `frontend/src/features/power/powerRegisterDescriptor.tsx` (Create) — 계통 descriptor.
- `frontend/src/features/power/powerRegisterDescriptor.test.ts` (Create) — 계통 섹션 단위테스트.
- `frontend/src/features/fiber/components/FiberRegisterView.tsx` (Modify) — 그리드 교체.
- `frontend/src/features/power/components/PowerView.tsx` (Modify) — 그리드 교체.
- `frontend/src/features/fiber/components/OfdFiberRegister.tsx` (Delete), `frontend/src/features/power/components/PanelCircuitView.tsx` (Delete).

---

## Task 1: 제네릭 ConnectionRegisterGrid + descriptor 타입

**Files:** `frontend/src/features/connections/registerGrid/registerTypes.ts`, `frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx`

### Step 1: 타입 — Create `registerTypes.ts`:
```ts
import type { ReactNode } from 'react';
import type { Asset } from '../../../types/asset';
import type { TraceGraph } from '../../trace/traceGraph';

export interface RegisterCtx {
  assets: Asset[];
  cables: unknown[]; // effective Cable rows (descriptor가 좁혀 읽음)
  graph: TraceGraph | null;
}
export interface RegisterColumn<Row> { label: string; width?: string; cell(row: Row): ReactNode }
export interface RegisterSection<Row> { key: string; title: string; usedLabel: string; rows: Row[] }

export interface RegisterDescriptor<Row> {
  emptyMessage: string;
  /** 컨테이너 자산(OFD / 분전반) 선택. */
  selectContainers(assets: Asset[], substationId: string): Asset[];
  /** 자식(섹션) = parentAssetId===container && assetType.connectionKind===childKind. */
  childKind: 'conduit' | 'distributor';
  /** 컨테이너 헤더(분전반명 등). 없으면 컨테이너 헤더 미표시. */
  containerHeader?(container: Asset, ctx: RegisterCtx): string | null;
  buildSection(child: Asset, ctx: RegisterCtx): RegisterSection<Row>;
  columns: RegisterColumn<Row>[];
  /** React key. */
  rowKey(row: Row): string | number;
  /** 행 클릭 시 선택할 assetId(null=무시). */
  onRowClick(row: Row, child: Asset): string | null;
  /** 이 행이 활성 하이라이트면 그 trace 시드 cableId(없으면 미정의 → 비활성). */
  rowTraceCableId?(row: Row): string | null;
}
```

### Step 2: 제네릭 컴포넌트 — Create `ConnectionRegisterGrid.tsx`:
```tsx
import { useMemo } from 'react';
import { useEffectiveAssets, useEffectiveCables } from '../../workingCopy/hooks';
import { useSelectionStore } from '../../workspace/selectionStore';
import { usePathHighlightStore } from '../../pathTrace/stores/pathHighlightStore';
import { useTraceGraph } from '../../trace/traceGraph';
import type { Asset } from '../../../types/asset';
import type { RegisterColumn, RegisterDescriptor, RegisterSection } from './registerTypes';

const TH = 'px-2 py-2 text-[12px] font-medium tracking-wide text-content-muted whitespace-nowrap';
const TD = 'px-2 text-[13px] align-middle whitespace-nowrap';

/** 컨테이너 자산 → connectionKind 자식(섹션) → 테이블 → 행클릭 선택. 도메인 로직은 descriptor. */
export function ConnectionRegisterGrid<Row>({ substationId, descriptor }: {
  substationId: string; descriptor: RegisterDescriptor<Row>;
}) {
  const assets = useEffectiveAssets() as Asset[];
  const cables = useEffectiveCables();
  const { graph } = useTraceGraph();
  const ctx = { assets, cables: cables as unknown[], graph };

  const containers = useMemo(
    () => descriptor.selectContainers(assets, substationId),
    [assets, substationId, descriptor],
  );
  if (!containers.length) return <p className="p-3 text-sm text-content-faint">{descriptor.emptyMessage}</p>;

  return (
    <div className="space-y-6">
      {containers.map((container) => {
        const header = descriptor.containerHeader?.(container, ctx) ?? null;
        const children = assets.filter(
          (a) => a.parentAssetId === container.id && a.assetType?.connectionKind === descriptor.childKind,
        );
        return (
          <div key={container.id} className="space-y-4">
            {header && <h2 className="px-1 text-sm font-bold text-content">{header}</h2>}
            {children.map((child) => (
              <SectionView
                key={child.id}
                section={descriptor.buildSection(child, ctx)}
                columns={descriptor.columns}
                rowKey={descriptor.rowKey}
                rowTraceCableId={descriptor.rowTraceCableId}
                onRowClick={(row) => {
                  const id = descriptor.onRowClick(row, child);
                  if (id) useSelectionStore.getState().setSelectedAssetId(id);
                }}
              />
            ))}
          </div>
        );
      })}
    </div>
  );
}

function SectionView<Row>({ section, columns, rowKey, rowTraceCableId, onRowClick }: {
  section: RegisterSection<Row>; columns: RegisterColumn<Row>[];
  rowKey: (row: Row) => string | number;
  rowTraceCableId?: (row: Row) => string | null;
  onRowClick: (row: Row) => void;
}) {
  return (
    <section>
      <header className="mb-1.5 flex items-baseline gap-2 px-1">
        <h3 className="text-sm font-semibold text-content">{section.title}</h3>
        <span className="ml-auto text-[12px] tabular-nums text-content-faint">{section.usedLabel}</span>
      </header>
      <table className="w-full border-collapse">
        <thead>
          <tr className="text-left bg-surface-2 border-b border-line-strong">
            {columns.map((c, i) => <th key={i} className={`${TH}${c.width ? ` ${c.width}` : ''}`}>{c.label}</th>)}
          </tr>
        </thead>
        <tbody>
          {section.rows.map((row) => (
            <RowView key={rowKey(row)} row={row} columns={columns} traceCableId={rowTraceCableId?.(row) ?? null} onClick={() => onRowClick(row)} />
          ))}
        </tbody>
      </table>
    </section>
  );
}

function RowView<Row>({ row, columns, traceCableId, onClick }: {
  row: Row; columns: RegisterColumn<Row>[]; traceCableId: string | null; onClick: () => void;
}) {
  const tracingCableId = usePathHighlightStore((s) => s.tracingCableId);
  const active = !!traceCableId && tracingCableId === traceCableId;
  return (
    <tr
      onClick={onClick}
      className={`h-9 cursor-pointer border-b border-line transition-colors ${
        active ? 'bg-info-bg shadow-[inset_3px_0_0_var(--primary)]' : 'hover:bg-surface-2 active:bg-surface-3'
      }`}
    >
      {columns.map((c, i) => <td key={i} className={TD}>{c.cell(row)}</td>)}
    </tr>
  );
}
```
> 셀의 클래스(truncate/정렬/색)는 각 컬럼 `cell` 이 자기 내용을 `<span className=…>` 등으로 감싸 표현(td 는 공통 TD 만). 편집 셀은 `cell` 안에서 `onClick={e=>e.stopPropagation()}` 로 행클릭 차단.

### Step 3: tsc + build — `cd frontend && npx tsc --noEmit && npm run build` → PASS(미사용 제네릭 컴포넌트라 아직 소비 없음).

### Step 4: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/connections/registerGrid/registerTypes.ts frontend/src/features/connections/registerGrid/ConnectionRegisterGrid.tsx
git commit -m "feat(그리드일반화): 제네릭 ConnectionRegisterGrid + RegisterDescriptor(렌더-함수 컬럼) 셸"
```

---

## Task 2: fiberRegisterDescriptor + FiberRegisterView 재배선

**Files:** `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`(Create), `frontend/src/features/fiber/components/FiberRegisterView.tsx`(Modify), `frontend/src/features/fiber/components/OfdFiberRegister.tsx`(Delete)

구 `OfdFiberRegister` 로직(슬롯 발견·헤더·7컬럼·commitMeta·행클릭·active)을 descriptor 로 이전. **셀 마크업/동작은 기존 CoreRow 와 동일.**

### Step 1: descriptor — Create `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`:
```tsx
import { useSubstationWorkingCopy } from '../workingCopy/substationStore';
import { remoteSlotSubstation, type TraceGraph } from '../trace/traceGraph';
import { buildSlotCoreRows, type SlotCoreRow } from './slotRegister';
import type { Asset } from '../../types/asset';
import type { RegisterCtx, RegisterDescriptor } from '../connections/registerGrid/registerTypes';

const CELL_INPUT = 'w-full text-[13px] border border-line rounded px-1.5 py-1 bg-surface text-content';

/** 점유 코어 한 필드를 OUT 케이블 specParams 에 머지 스테이징(기존 키 보존). */
function commitMeta(cableId: string, field: string, value: string | null) {
  const wc = useSubstationWorkingCopy.getState();
  const cable = wc.effectiveCables().find((c) => c.id === cableId);
  const prev = ((cable?.specParams as Record<string, unknown>) ?? {});
  wc.patch('cables', cableId, { specParams: { ...prev, [field]: value } });
}

const faint = (v: string | null | undefined) => v ?? '—';

export const fiberRegisterDescriptor: RegisterDescriptor<SlotCoreRow & { __ofdId: string; __nameById?: Map<string, string> }> = {
  emptyMessage: '이 변전소에 OFD(광단국)가 없습니다.',
  childKind: 'conduit',
  selectContainers: (assets, substationId) =>
    assets.filter((a) => a.assetType?.placementKind === 'OFD' && a.substationId === substationId),
  buildSection: (slot, ctx: RegisterCtx) => {
    const graph = ctx.graph as TraceGraph | null;
    const ofdId = slot.parentAssetId ?? slot.id;
    const rows = buildSlotCoreRows(slot as never, ctx.cables as never[], graph)
      .map((r) => ({ ...r, __ofdId: ofdId, __nameById: graph?.nameById }));
    const used = rows.filter((r) => r.usage === '사용').length;
    const localSub = graph?.subNameById.get(ofdId) ?? null;
    const remoteSub = graph ? remoteSlotSubstation(slot.id, graph) : null;
    const title = [localSub, remoteSub].filter(Boolean).join(' - ') || (slot.name ?? '광경로');
    return { key: slot.id, title, usedLabel: `사용 ${used}/${rows.length}`, rows };
  },
  rowKey: (row) => row.coreNumber,
  rowTraceCableId: (row) => row.cableId,
  onRowClick: (row, slot) => row.nearAssetId ?? (slot.parentAssetId ?? slot.id),
  columns: [
    { label: '코어', width: 'w-14', cell: (r) => <span className="tabular-nums text-content-muted">{r.coreNumber}</span> },
    { label: '근접자산', cell: (r) => {
        const name = r.nearAssetId ? (r.__nameById?.get(r.nearAssetId) ?? null) : null;
        return <span className="text-content max-w-[12rem] truncate inline-block align-bottom" title={name ?? undefined}>{name ?? <span className="text-content-faint">—</span>}</span>;
      } },
    { label: '상대국측', cell: (r) => <span className="text-content-muted max-w-[12rem] truncate inline-block align-bottom" title={r.farName ?? undefined}>{r.farName ?? <span className="text-content-faint">—</span>}</span> },
    { label: '용도', cell: (r) => (
        <input aria-label="용도" placeholder="용도" defaultValue={r.purpose ?? ''} disabled={!r.cableId}
          key={`${r.coreNumber}-purpose-${r.purpose ?? ''}`}
          onClick={(e) => e.stopPropagation()}
          onBlur={(e) => { const v = e.target.value || null; if (r.cableId && v !== r.purpose) commitMeta(r.cableId, 'purpose', v); }}
          className={CELL_INPUT} />
      ) },
    { label: '수용내역', cell: (r) => (
        <input aria-label="수용내역" placeholder="수용내역" defaultValue={r.circuitText ?? ''} disabled={!r.cableId}
          key={`${r.coreNumber}-circuit-${r.circuitText ?? ''}`}
          onClick={(e) => e.stopPropagation()}
          onBlur={(e) => { const v = e.target.value || null; if (r.cableId && v !== r.circuitText) commitMeta(r.cableId, 'circuitText', v); }}
          className={CELL_INPUT} />
      ) },
    { label: '융착', width: 'w-24', cell: (r) => (
        <select aria-label="융착" value={r.spliceType ?? ''} disabled={!r.cableId}
          onClick={(e) => e.stopPropagation()}
          onChange={(e) => { const v = e.target.value || null; if (r.cableId && v !== r.spliceType) commitMeta(r.cableId, 'spliceType', v); }}
          className={CELL_INPUT}>
          <option value="">—</option><option value="융착">융착</option><option value="패치">패치</option>
        </select>
      ) },
    { label: '사용', width: 'w-28', cell: (r) => (
        <select aria-label="사용" value={r.usageOverride ?? '자동'} disabled={!r.cableId}
          onClick={(e) => e.stopPropagation()}
          onChange={(e) => { const v = e.target.value === '자동' ? null : e.target.value; if (r.cableId && v !== r.usageOverride) commitMeta(r.cableId, 'usageOverride', v); }}
          className={CELL_INPUT}>
          <option value="자동">자동{r.occupied ? '(사용)' : '(미사용)'}</option><option value="사용">사용</option><option value="미사용">미사용</option>
        </select>
      ) },
  ],
};
void faint;
```
> `void faint;` 미사용이면 제거. SlotCoreRow 에 `__ofdId`/`__nameById` 를 얹는 건 cell 이 ctx 없이 이름 해소를 하기 위함(대안: cell 시그니처에 ctx 추가 — 단순화 위해 row 에 동봉). Row 타입이 지저분하면 cell 들이 closure 로 graph 를 잡도록 descriptor 를 함수로 만들 수도 있으나, **정적 descriptor + row 동봉**이 가장 단순.

### Step 2: FiberRegisterView 재배선 — `FiberRegisterView.tsx` 내부의 `ofds.map(<OfdFiberRegister>)` 를 `<ConnectionRegisterGrid substationId={substationId} descriptor={fiberRegisterDescriptor} />` 로 교체. 사이드패널(`StagedAssetDetailPanel`)·selection 래퍼는 유지. (emptyMessage 가 descriptor 에 있으므로 기존 "OFD 없음" 분기 제거.)

### Step 3: 구 컴포넌트 삭제 — `git rm frontend/src/features/fiber/components/OfdFiberRegister.tsx`. (grep 으로 다른 import 0 확인 — FiberRegisterView 만 썼음.)

### Step 4: 검증 — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS. **기존 `FiberRegisterView.test.tsx` 가 통과해야 함**(동작 보존). 테스트가 `OfdFiberRegister` 를 직접 mock/참조하면 새 구조(ConnectionRegisterGrid + descriptor)에 맞게 최소 수정 — 단, 단언(렌더·행클릭·편집 patch)은 보존.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add -A
git commit -m "feat(그리드일반화): fiberRegisterDescriptor + FiberRegisterView 재배선, 구 OfdFiberRegister 삭제(동작 보존)"
```

---

## Task 3: powerRegisterDescriptor + PowerView 재배선

**Files:** `frontend/src/features/power/powerRegisterDescriptor.tsx`(Create), `frontend/src/features/power/powerRegisterDescriptor.test.ts`(Create), `frontend/src/features/power/components/PowerView.tsx`(Modify), `frontend/src/features/power/components/PanelCircuitView.tsx`(Delete)

### Step 1: 실패 테스트 — Create `frontend/src/features/power/powerRegisterDescriptor.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildPowerRows } from './powerRegisterDescriptor';

const cables = [
  { id: 'cb1', sourceAssetId: 'feeder1', targetAssetId: 'load1', sourceRole: 'OUT', targetRole: null, categoryName: 'CV', specParams: { cbNumber: '1', capacity: '20A', switchState: 'ON' } },
  { id: 'cb2', sourceAssetId: 'load2', targetAssetId: 'feeder1', sourceRole: null, targetRole: 'OUT', categoryName: 'CV', specParams: { cbNumber: '2', capacity: '30A', switchState: 'OFF' } },
  { id: 'x', sourceAssetId: 'a', targetAssetId: 'b', sourceRole: null, targetRole: null, specParams: {} },
];
const nameById = new Map([['load1', '부하A'], ['load2', '부하B']]);

describe('buildPowerRows', () => {
  it('피더 OUT 케이블만 CB행으로(양방향 끝점), 부하=반대편', () => {
    const rows = buildPowerRows('feeder1', cables as never[], nameById);
    expect(rows.map((r) => r.cableId).sort()).toEqual(['cb1', 'cb2']);
    const r1 = rows.find((r) => r.cableId === 'cb1')!;
    expect(r1.loadAssetId).toBe('load1');
    expect(r1.loadName).toBe('부하A');
    expect(r1.cbNumber).toBe('1');
    expect(r1.switchState).toBe('ON');
    const r2 = rows.find((r) => r.cableId === 'cb2')!;
    expect(r2.loadAssetId).toBe('load2');   // target=feeder 면 source 가 부하
  });
});
```
Run `cd frontend && npx vitest run src/features/power/powerRegisterDescriptor.test.ts` → FAIL.

### Step 2: descriptor + 순수 빌더 — Create `frontend/src/features/power/powerRegisterDescriptor.tsx`:
```tsx
import type { Asset } from '../../types/asset';
import type { RegisterCtx, RegisterDescriptor } from '../connections/registerGrid/registerTypes';

export interface CbRow { cableId: string; loadAssetId: string | null; loadName: string | null; cbNumber: string; capacity: string; switchState: string; spec: string }
interface PowerCable { id: string; sourceAssetId?: string | null; targetAssetId?: string | null; sourceRole?: string | null; targetRole?: string | null; categoryName?: string | null; specParams?: Record<string, unknown> | null }

const asStr = (v: unknown): string => (v === null || v === undefined || v === '' ? '' : String(v));

/** 한 피더의 CB행 — 피더 끝점 role OUT 케이블, 부하=반대편. 순수. */
export function buildPowerRows(feederId: string, cables: PowerCable[], nameById: Map<string, string>): CbRow[] {
  const cb = cables.filter(
    (c) => (c.sourceAssetId === feederId && c.sourceRole === 'OUT') || (c.targetAssetId === feederId && c.targetRole === 'OUT'),
  );
  return cb.map((c) => {
    const loadAssetId = c.sourceAssetId === feederId ? c.targetAssetId ?? null : c.sourceAssetId ?? null;
    const sp = (c.specParams ?? {}) as Record<string, unknown>;
    return {
      cableId: c.id, loadAssetId, loadName: (loadAssetId && nameById.get(loadAssetId)) || null,
      cbNumber: asStr(sp.cbNumber), capacity: asStr(sp.capacity), switchState: asStr(sp.switchState), spec: asStr(c.categoryName),
    };
  });
}

export const powerRegisterDescriptor: RegisterDescriptor<CbRow> = {
  emptyMessage: '이 변전소에 분전반이 없습니다.',
  childKind: 'distributor',
  selectContainers: (assets, substationId) =>
    assets.filter((a) => a.substationId === substationId && (a.assetType?.code === 'DIST' || a.assetType?.placementKind === 'DIST')),
  containerHeader: (panel) => panel.name,
  buildSection: (feeder, ctx: RegisterCtx) => {
    const nameById = new Map((ctx.assets as Asset[]).map((a) => [a.id, a.name]));
    const rows = buildPowerRows(feeder.id, ctx.cables as PowerCable[], nameById);
    const used = rows.filter((r) => r.switchState.toUpperCase() === 'ON').length;
    return { key: feeder.id, title: feeder.name, usedLabel: `사용 ${used}/${rows.length}`, rows };
  },
  rowKey: (row) => row.cableId,
  onRowClick: (row, feeder) => row.loadAssetId ?? feeder.id,
  columns: [
    { label: '번호', width: 'w-14', cell: (r) => <span className="tabular-nums text-content-muted">{r.cbNumber || <span className="text-content-faint">—</span>}</span> },
    { label: '부하', cell: (r) => <span className="text-content max-w-[12rem] truncate inline-block align-bottom" title={r.loadName ?? undefined}>{r.loadName ?? <span className="text-content-faint">—</span>}</span> },
    { label: '용량', width: 'w-20', cell: (r) => <span className="text-content-muted">{r.capacity || <span className="text-content-faint">—</span>}</span> },
    { label: '규격', cell: (r) => <span className="text-content-muted max-w-[12rem] truncate inline-block align-bottom" title={r.spec || undefined}>{r.spec || <span className="text-content-faint">—</span>}</span> },
    { label: 'SW', width: 'w-20', cell: (r) => {
        const on = r.switchState.toUpperCase() === 'ON';
        return r.switchState
          ? <span className="inline-flex items-center gap-1.5">{on && <span className="inline-block h-1.5 w-1.5 rounded-full bg-success" />}<span className={on ? 'text-content' : 'text-content-faint'}>{r.switchState}</span></span>
          : <span className="text-content-faint">—</span>;
      } },
  ],
};
```

### Step 3: 통과 — `cd frontend && npx vitest run src/features/power/powerRegisterDescriptor.test.ts` → PASS; `npx tsc --noEmit` → PASS.

### Step 4: PowerView 재배선 — `PowerView.tsx` 내부의 `<PanelCircuitView substationId>` 를 `<ConnectionRegisterGrid substationId={substationId} descriptor={powerRegisterDescriptor} />` 로 교체. 사이드패널 래퍼 유지.

### Step 5: 구 컴포넌트 삭제 — `git rm frontend/src/features/power/components/PanelCircuitView.tsx`(grep import 0 확인 — PowerView 만 썼음).

### Step 6: 검증 — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS.

### Step 7: Commit
```bash
cd /Users/jsk/1210/digital
git add -A
git commit -m "feat(그리드일반화): powerRegisterDescriptor(+행 빌더 테스트) + PowerView 재배선, 구 PanelCircuitView 삭제"
```

---

## Task 4: 검증 + 최종 리뷰

- [ ] **Step 1: 전체** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS(기존 FiberRegisterView.test 보존 통과 + powerRegisterDescriptor 신규).
- [ ] **Step 2: 중복 제거 확인** — `OfdFiberRegister`·`PanelCircuitView` 삭제됨(`git diff main --stat`). `ConnectionRegisterGrid` 가 둘의 공통 셸. descriptor 2개가 도메인 로직.
- [ ] **Step 3: 수동 스모크** — 선번장(편집·행클릭·대국명·빈코어)·계통(분전반→피더→CB·SW점) 둘 다 기존과 시각·동작 동일.
- [ ] **Step 4: 최종 리뷰** — subagent-driven 최종 리뷰(동작 보존: 선번장 7컬럼·편집 patch·active 하이라이트·행클릭 선택 / 계통 5컬럼·점유만·ON수 / 셸이 도메인 로직 0 / 빈코어 disabled / graph null 안전 / 접지 descriptor 추가가 trivial 한지 구조 점검).

---

## Self-Review
- **스펙:** 공통 셸(ConnectionRegisterGrid) + descriptor 2개(fiber/power) = 컨테이너→자식→섹션→테이블→선택 일반화. 동작 보존(기존 테스트 통과). 접지=descriptor 추가. ✅
- **렌더-함수 컬럼(사용자 선택):** 컬럼 cell 이 정적/편집/far 자유 렌더 — type 분류 없음. 편집 셀은 stopPropagation. ✅
- **동작 보존 매핑:** 선번장 7컬럼·commitMeta·active(rowTraceCableId)·헤더(로컬-대국)·빈코어 disabled = 그대로. 계통 5컬럼·점유만·ON수·분전반 헤더·부하=반대편 = 그대로. onRowClick(near/load ?? container). ✅
- **타입 일관:** RegisterDescriptor<Row>, RegisterCtx(assets/cables/graph), SlotCoreRow(+row 동봉 nameById/ofdId), CbRow. buildSlotCoreRows/remoteSlotSubstation 재사용. ✅
- **위험/확인(구현 중):**
  - fiber cell 이 graph.nameById/ctx 없이 이름 해소 → row 에 `__nameById`/`__ofdId` 동봉(또는 cell 시그니처에 ctx 추가 — 단순화로 동봉). 리뷰서 깔끔함 점검.
  - 기존 FiberRegisterView.test 가 OfdFiberRegister 내부를 mock 하면 새 구조로 최소 수정(단언 보존).
  - graph 로딩 중 far/대국명 fallback = 기존 동일.
  - ConnectionRegisterGrid 의 `useTraceGraph` 는 계통도 구독(미사용이나 dedup) — 비용 미미.
- **다음:** 접지계통 벌티컬(백엔드 접지함체 assetType + 단자 connectionKind + 접지 케이블카테고리) → groundRegisterDescriptor 추가. 전체 SLD 자동작도.
