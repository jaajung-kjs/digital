# 연결 대장 그리드 셀 편집 일관화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** `AssetInspector.Field` 의 "호버→연필(✎)→인라인 편집, Enter 커밋" 패턴을 공유 `EditableField` 로 추출하고, 두 그리드 descriptor(선번장·계통)가 이를 써서 케이블 필드를 그리드에서 직접·일관되게 편집한다. 계통도 편집 가능(빠진 진입점), Esc 취소 추가, 행클릭→사이드패널 보존(✎만 편집). 인스펙터 `Field` 도 같은 컴포넌트 채택.

**Architecture:** 신규 `EditableField`(text/select/date, 셀-스코프 호버 ✎, Enter/blur 커밋, Esc 취소, `valueClickEdits` 로 값-클릭=편집[인스펙터] vs 값-클릭=패널[그리드] 분기). 두 descriptor 의 편집셀이 `<EditableField onCommit=commitMeta>`. 인스펙터 `Field`→`EditableField(valueClickEdits)`. **케이블 편집은 그리드 전용(현 구조 유지).**

**Tech Stack:** React/TS/Zustand, Vitest.

**참조 spec:** `docs/superpowers/specs/2026-06-15-그리드셀-편집일관화-design.md`. 기준 컴포넌트 `AssetInspector.Field`(`AssetInspector.tsx:64-95`), 상수 INLINE_INPUT/PENCIL(:44-52), `IconAction`(`detail/SectionShell.tsx`).

---

## 환경 규칙 (공통)
- 프론트 `cd frontend && npx tsc --noEmit && npm run build && npx vitest run`. 단일 `npx vitest run <file>`.
- 동작 보존: 기존 `FiberRegisterView.test`·`AssetInspector.test` 통과(편집 트리거만 ✎ 로 갱신, 단언 보존).

## 현재 구조 (확인됨)
- `AssetInspector.Field`(text/date): ROW(group) + 값(클릭→편집 button) + 호버 PENCIL(IconAction). 편집=input, onBlur 커밋, Enter→blur, date=onChange 즉시. **Esc 없음.**
- `fiberRegisterDescriptor` 편집셀: 항상-노출 `<input>`(용도/수용내역)·`<select>`(융착/사용), onBlur/onChange commitMeta, 빈코어 disabled. (호버/✎/Enter 없음.)
- `powerRegisterDescriptor`: 전부 읽기 전용 span(번호/부하/용량/규격/SW).
- commitMeta(fiber): `patch('cables', cableId, {specParams:{...prev,[field]:value}})`.

## File Structure
- `frontend/src/features/assets/components/EditableField.tsx` (Create) — 공유 인라인 편집.
- `frontend/src/features/assets/components/EditableField.test.tsx` (Create).
- `frontend/src/features/fiber/fiberRegisterDescriptor.tsx` (Modify) — 편집셀→EditableField.
- `frontend/src/features/fiber/components/FiberRegisterView.test.tsx` (Modify) — 편집 트리거 ✎ 로.
- `frontend/src/features/power/powerRegisterDescriptor.tsx` (Modify) — 편집 추가 + commitMeta.
- `frontend/src/features/power/powerRegisterDescriptor.test.ts` (Modify) — 편집 단위테스트 추가(또는 신규 컴포넌트 테스트).
- `frontend/src/features/assets/components/AssetInspector.tsx` (Modify) — Field→EditableField.

---

## Task 1: 공유 EditableField (TDD)

**Files:** `frontend/src/features/assets/components/EditableField.tsx`, `EditableField.test.tsx`

### Step 1: 실패 테스트 — Create `EditableField.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { EditableField } from './EditableField';

describe('EditableField', () => {
  it('읽기모드: 값 표시 + ✎ 버튼, 입력창 없음', () => {
    render(<EditableField value="보호" ariaLabel="용도" onCommit={() => {}} />);
    expect(screen.getByText('보호')).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /수정/ })).toBeInTheDocument();
    expect(screen.queryByRole('textbox')).toBeNull();
  });
  it('✎ 클릭 → 입력, Enter → onCommit(변경값)', () => {
    const onCommit = vi.fn();
    render(<EditableField value="보호" ariaLabel="용도" onCommit={onCommit} />);
    fireEvent.click(screen.getByRole('button', { name: /수정/ }));
    const input = screen.getByRole('textbox');
    fireEvent.change(input, { target: { value: '예비' } });
    fireEvent.keyDown(input, { key: 'Enter' });
    fireEvent.blur(input);
    expect(onCommit).toHaveBeenCalledWith('예비');
  });
  it('Esc → 취소(onCommit 미호출)', () => {
    const onCommit = vi.fn();
    render(<EditableField value="보호" ariaLabel="용도" onCommit={onCommit} />);
    fireEvent.click(screen.getByRole('button', { name: /수정/ }));
    const input = screen.getByRole('textbox');
    fireEvent.change(input, { target: { value: '예비' } });
    fireEvent.keyDown(input, { key: 'Escape' });
    fireEvent.blur(input);
    expect(onCommit).not.toHaveBeenCalled();
  });
  it('select: onChange → onCommit', () => {
    const onCommit = vi.fn();
    render(<EditableField value="" type="select" ariaLabel="융착"
      options={[{ value: '', label: '—' }, { value: '융착', label: '융착' }]} onCommit={onCommit} />);
    fireEvent.click(screen.getByRole('button', { name: /수정/ }));
    fireEvent.change(screen.getByRole('combobox'), { target: { value: '융착' } });
    expect(onCommit).toHaveBeenCalledWith('융착');
  });
  it('disabled: ✎ 없음', () => {
    render(<EditableField value="" ariaLabel="용도" disabled onCommit={() => {}} />);
    expect(screen.queryByRole('button', { name: /수정/ })).toBeNull();
  });
});
```

### Step 2: 실패 확인 — `cd frontend && npx vitest run src/features/assets/components/EditableField.test.tsx` → FAIL.

### Step 3: 구현 — Create `frontend/src/features/assets/components/EditableField.tsx`:
```tsx
import { useRef, useState, type ReactNode } from 'react';
import { Pencil } from 'lucide-react';
import { IconAction } from './detail/SectionShell';

const INLINE_INPUT =
  'min-w-0 w-full px-1.5 py-0.5 rounded text-sm bg-surface border border-line ' +
  'focus:border-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/20 transition-colors';
const PENCIL =
  'shrink-0 opacity-0 group-hover:opacity-60 hover:!opacity-100 group-focus-within:opacity-100 transition-opacity';

export interface EditableFieldOption { value: string; label: string }

/**
 * 인라인 편집 primitive — 읽기=텍스트 + 셀 호버 시 ✎, ✎ 클릭→입력(text/select/date),
 * Enter/blur 커밋, Esc 취소. AssetInspector.Field 패턴 추출(그리드·인스펙터 공유).
 * valueClickEdits: 값 클릭이 편집을 열지(인스펙터=true) 아니면 부모(행클릭=패널)로 버블(그리드=false).
 */
export function EditableField({
  value, type = 'text', options, placeholder, disabled, display, ariaLabel, valueClickEdits = false, onCommit,
}: {
  value: string; type?: 'text' | 'select' | 'date'; options?: EditableFieldOption[];
  placeholder?: string; disabled?: boolean; display?: (v: string) => ReactNode;
  ariaLabel?: string; valueClickEdits?: boolean; onCommit: (v: string) => void;
}) {
  const cancel = useRef(false);
  const [editing, setEditing] = useState(false);
  const start = () => { if (!disabled) setEditing(true); };

  if (editing) {
    if (type === 'select') {
      return (
        <select autoFocus aria-label={ariaLabel} value={value} className={INLINE_INPUT}
          onClick={(e) => e.stopPropagation()}
          onChange={(e) => { onCommit(e.target.value); setEditing(false); }}
          onBlur={() => setEditing(false)}
          onKeyDown={(e) => { if (e.key === 'Escape') setEditing(false); }}>
          {options?.map((o) => <option key={o.value} value={o.value}>{o.label}</option>)}
        </select>
      );
    }
    return (
      <input autoFocus type={type} aria-label={ariaLabel} placeholder={placeholder} defaultValue={value}
        className={INLINE_INPUT}
        onClick={(e) => e.stopPropagation()}
        onBlur={(e) => { if (!cancel.current && e.target.value !== value) onCommit(e.target.value); cancel.current = false; setEditing(false); }}
        onChange={type === 'date' ? (e) => { onCommit(e.target.value); setEditing(false); } : undefined}
        onKeyDown={(e) => {
          if (e.key === 'Enter') (e.target as HTMLInputElement).blur();
          else if (e.key === 'Escape') { cancel.current = true; (e.target as HTMLInputElement).blur(); }
        }} />
    );
  }

  const shown = display ? display(value) : (value || <span className="text-content-faint">—</span>);
  return (
    <span className="group inline-flex items-center gap-1 min-w-0 w-full">
      {valueClickEdits ? (
        <button type="button" onClick={(e) => { e.stopPropagation(); start(); }}
          className="min-w-0 flex-1 truncate text-left text-sm text-content hover:text-primary transition-colors">{shown}</button>
      ) : (
        <span className="min-w-0 flex-1 truncate text-sm text-content">{shown}</span>
      )}
      {!disabled && (
        <span className={PENCIL} onClick={(e) => e.stopPropagation()}>
          <IconAction onClick={() => start()} title={`${ariaLabel ?? ''} 수정`}>
            <Pencil size={13} />
          </IconAction>
        </span>
      )}
    </span>
  );
}
```
> **IconAction 이벤트 주의:** 펜슬 클릭이 행클릭(패널)을 트리거하지 않도록, IconAction 을 감싼 `<span onClick={stopPropagation}>` 으로 버블 차단(IconAction 이 이벤트를 forward 안 해도 안전). 만약 IconAction 의 룩/패딩이 셀에 안 맞으면 플레인 `<button>` + Pencil 로 대체 가능(같은 PENCIL 클래스).
> `aria-label` 이 `용도 수정` 형태면 테스트 `name:/수정/` 매칭. select 의 ✎ 도 동일.

### Step 4: 통과 + tsc — `npx vitest run src/features/assets/components/EditableField.test.tsx` → PASS(5); `npx tsc --noEmit` → PASS.

### Step 5: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/assets/components/EditableField.tsx frontend/src/features/assets/components/EditableField.test.tsx
git commit -m "feat(편집일관화): 공유 EditableField(호버 ✎ 인라인 편집, text/select/date, Enter 커밋·Esc 취소)"
```

---

## Task 2: fiberRegisterDescriptor → EditableField

**Files:** `frontend/src/features/fiber/fiberRegisterDescriptor.tsx`, `frontend/src/features/fiber/components/FiberRegisterView.test.tsx`

### Step 1: 편집셀 교체 — `fiberRegisterDescriptor.tsx` 의 4개 편집 컬럼(용도/수용내역/융착/사용) cell 을 `EditableField` 로:
```tsx
import { EditableField } from '../assets/components/EditableField';
// ...
// 용도
{ label: '용도', cell: (r) => (
    <EditableField value={r.purpose ?? ''} ariaLabel="용도" placeholder="용도" disabled={!r.cableId}
      onCommit={(v) => r.cableId && commitMeta(r.cableId, 'purpose', v || null)} />
  ) },
// 수용내역
{ label: '수용내역', cell: (r) => (
    <EditableField value={r.circuitText ?? ''} ariaLabel="수용내역" placeholder="수용내역" disabled={!r.cableId}
      onCommit={(v) => r.cableId && commitMeta(r.cableId, 'circuitText', v || null)} />
  ) },
// 융착
{ label: '융착', width: 'w-24', cell: (r) => (
    <EditableField value={r.spliceType ?? ''} type="select" ariaLabel="융착" disabled={!r.cableId}
      options={[{ value: '', label: '—' }, { value: '융착', label: '융착' }, { value: '패치', label: '패치' }]}
      onCommit={(v) => r.cableId && commitMeta(r.cableId, 'spliceType', v || null)} />
  ) },
// 사용
{ label: '사용', width: 'w-28', cell: (r) => (
    <EditableField value={r.usageOverride ?? ''} type="select" ariaLabel="사용" disabled={!r.cableId}
      display={(v) => v || `자동${r.occupied ? '(사용)' : '(미사용)'}`}
      options={[{ value: '', label: `자동${r.occupied ? '(사용)' : '(미사용)'}` }, { value: '사용', label: '사용' }, { value: '미사용', label: '미사용' }]}
      onCommit={(v) => r.cableId && commitMeta(r.cableId, 'usageOverride', v || null)} />
  ) },
```
> commitMeta 는 기존 함수 그대로(specParams 병합). `CELL_INPUT` 상수는 더 안 쓰면 제거. 코어/근접자산/상대국측 컬럼은 그대로(읽기). `usageOverride` 빈값=자동 → value `''`, display 가 `자동(사용)` 표기.

### Step 2: FiberRegisterView.test 갱신 — 편집 단언이 "input 에 직접 입력 후 blur" 였다면, 이제 "행에 호버 없이도 ✎ 버튼 클릭→input→blur" 로. 테스트는 jsdom 이라 호버 불필요(✎ 버튼은 DOM 에 존재, opacity 만 0) — `getByRole('button', {name:/용도 수정/})` 클릭 후 input 입력→blur→patch 단언. **patch 호출 단언은 보존.** (구체 수정은 현재 테스트를 읽고 최소 변경.)

### Step 3: 검증 — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS(FiberRegisterView.test 포함). 행클릭→선택 보존 확인(✎/input stopPropagation).

### Step 4: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/fiber/fiberRegisterDescriptor.tsx frontend/src/features/fiber/components/FiberRegisterView.test.tsx
git commit -m "feat(편집일관화): 선번장 편집셀을 EditableField(호버 ✎)로 — 항상-노출 입력 제거"
```

---

## Task 3: powerRegisterDescriptor 편집 추가 (TDD)

**Files:** `frontend/src/features/power/powerRegisterDescriptor.tsx`, `frontend/src/features/power/powerRegisterDescriptor.test.ts`(또는 `.tsx` 컴포넌트 테스트)

계통 CB 의 번호/용량/SW 를 편집 가능하게(선번장과 동일 UX). 부하/규격은 읽기.

### Step 1: commitMeta + 편집셀 — `powerRegisterDescriptor.tsx`:
```tsx
import { useSubstationWorkingCopy } from '../workingCopy/substationStore';
import { EditableField } from '../assets/components/EditableField';

function commitMeta(cableId: string, field: string, value: string | null) {
  const wc = useSubstationWorkingCopy.getState();
  const cable = wc.effectiveCables().find((c) => c.id === cableId);
  const prev = ((cable?.specParams as Record<string, unknown>) ?? {});
  wc.patch('cables', cableId, { specParams: { ...prev, [field]: value } });
}
```
컬럼 교체(번호/용량/SW 편집, 부하/규격 읽기):
```tsx
// 번호
{ label: '번호', width: 'w-14', cell: (r) => (
    <EditableField value={r.cbNumber} ariaLabel="번호" placeholder="번호"
      onCommit={(v) => commitMeta(r.cableId, 'cbNumber', v || null)} />
  ) },
// 부하 (읽기)
{ label: '부하', cell: (r) => <span className="text-content max-w-[12rem] truncate inline-block align-bottom" title={r.loadName ?? undefined}>{r.loadName ?? <span className="text-content-faint">—</span>}</span> },
// 용량
{ label: '용량', width: 'w-20', cell: (r) => (
    <EditableField value={r.capacity} ariaLabel="용량" placeholder="용량"
      onCommit={(v) => commitMeta(r.cableId, 'capacity', v || null)} />
  ) },
// 규격 (읽기)
{ label: '규격', cell: (r) => <span className="text-content-muted max-w-[12rem] truncate inline-block align-bottom" title={r.spec || undefined}>{r.spec || <span className="text-content-faint">—</span>}</span> },
// SW
{ label: 'SW', width: 'w-20', cell: (r) => (
    <EditableField value={r.switchState} type="select" ariaLabel="SW"
      options={[{ value: '', label: '—' }, { value: 'ON', label: 'ON' }, { value: 'OFF', label: 'OFF' }]}
      display={(v) => {
        const on = v.toUpperCase() === 'ON';
        return v ? <span className="inline-flex items-center gap-1.5">{on && <span className="inline-block h-1.5 w-1.5 rounded-full bg-success" />}<span className={on ? 'text-content' : 'text-content-faint'}>{v}</span></span> : <span className="text-content-faint">—</span>;
      }}
      onCommit={(v) => commitMeta(r.cableId, 'switchState', v || null)} />
  ) },
```
> CbRow 에 `cableId` 이미 있음. usedLabel(ON 수)·containerHeader·onRowClick 그대로.

### Step 2: 테스트 — `powerRegisterDescriptor.test.ts` 에 편집 단위(컴포넌트) 테스트 추가(또는 `.test.tsx` 신규): SW select 의 onCommit → `patch('cables', ...)` 호출. (buildPowerRows 기존 테스트 유지.) 컴포넌트 렌더가 store 를 타므로, 가장 단순히 `EditableField` 가 호출하는 commitMeta 의 patch 를 spy 하거나, 컬럼 cell 을 렌더해 ✎→입력→patch 단언. **buildPowerRows 순수 테스트는 그대로.**

### Step 3: 검증 — `cd frontend && npx vitest run src/features/power/powerRegisterDescriptor.test.ts` → PASS; `npx tsc --noEmit && npm run build && npx vitest run` → PASS.

### Step 4: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/power/powerRegisterDescriptor.tsx frontend/src/features/power/powerRegisterDescriptor.test.ts
git commit -m "feat(편집일관화): 계통 CB(번호/용량/SW) EditableField 편집 추가 — 빠졌던 진입점, 선번장과 동일 UX"
```

---

## Task 4: 인스펙터 Field → EditableField

**Files:** `frontend/src/features/assets/components/AssetInspector.tsx`

`Field`(text/date)를 `EditableField(valueClickEdits)` 로 교체 — 단일 컴포넌트·Esc 일관. `DescField`/`StatusField` 유지.

### Step 1: Field 래퍼 교체 — `AssetInspector.tsx` 의 `Field` 함수를 EditableField 사용 래퍼로:
```tsx
import { EditableField } from './EditableField';
// ...
function Field({ label, value, onCommit, type = 'text' }: { label: string; value: string; onCommit: (v: string) => void; type?: 'text' | 'date' }) {
  return (
    <div className="flex items-center gap-2 text-sm py-1">
      <span className={LABEL}>{label}</span>
      <EditableField value={value} type={type} ariaLabel={label} valueClickEdits onCommit={onCommit} />
    </div>
  );
}
```
> 기존 `Field` 호출부(이름/담당자=text, 설치일=date) 시그니처 동일. `openPicker`/`INLINE_INPUT`/`PENCIL`/`ROW` 가 Field 외에서 안 쓰이면 제거(DescField 가 INLINE_INPUT/PENCIL 쓰면 유지). date 는 EditableField 가 onChange 즉시 커밋(인스펙터 기존 동작과 동일).

### Step 2: 검증 — `cd frontend && npx tsc --noEmit && npx vitest run src/features/assets/components/AssetInspector.test.tsx` → PASS(이름/담당자/설치일 편집 단언 보존 — 편집 트리거가 값-클릭 또는 ✎ 둘 다 가능하므로 기존 단언 대개 통과; 안 되면 트리거를 ✎/값클릭으로 최소 수정). `npm run build && npx vitest run` → 전체 PASS.

### Step 3: Commit
```bash
cd /Users/jsk/1210/digital
git add frontend/src/features/assets/components/AssetInspector.tsx
git commit -m "feat(편집일관화): 인스펙터 Field 를 공유 EditableField 로(단일 컴포넌트·Esc 일관)"
```

---

## Task 5: 검증 + 최종 리뷰

- [ ] **Step 1: 전체** — `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` → PASS(EditableField 5 + fiber/power/inspector 보존+추가).
- [ ] **Step 2: 일관성 확인** — 선번장·계통·인스펙터 모두 `EditableField`(호버 ✎·Enter·Esc) 단일 컴포넌트. 항상-노출 input 0(`grep CELL_INPUT` 잔재 없음).
- [ ] **Step 3: 수동 스모크** — 선번장(✎→용도 편집→Enter 커밋→저장바)·계통(✎→SW ON/OFF·번호 편집)·행클릭→사이드패널·빈코어 비편집·Esc 취소·인스펙터(이름/설치일) 동일.
- [ ] **Step 4: 최종 리뷰** — subagent-driven 최종 리뷰(EditableField: Enter/Esc/blur/select/date·stopPropagation 행클릭 차단·disabled / 선번장·계통 편집 patch(specParams 병합 마커 보존) / 인스펙터 보존 / valueClickEdits 분기 / 빈코어 비편집).

---

## Self-Review
- **스펙:** 공유 EditableField(호버 ✎·Enter 커밋·Esc 취소) + 선번장 통일 + 계통 편집 추가 + 인스펙터 채택 = 케이블뷰 편집 일관. 행클릭→패널 보존(✎/입력 stopPropagation). ✅
- **렌더-함수 컬럼 유지:** 컬럼 cell 이 `<EditableField>`(편집) 또는 span(읽기) 반환 — 타입-드리븐 아님(기존 패턴). ✅
- **동작 보존:** commitMeta(specParams 병합, `__migration` 마커 보존) 그대로. 빈코어 disabled→✎ 없음→읽기→행클릭=패널. 인스펙터 text/date 보존. ✅
- **위험/확인(구현 중):**
  - IconAction 이벤트 forward — 펜슬 래퍼 `<span onClick=stopPropagation>` 로 행클릭 차단 보장. 룩 안 맞으면 플레인 버튼.
  - FiberRegisterView.test/AssetInspector.test 편집 트리거가 "값 클릭" 가정이면 ✎ 클릭/valueClickEdits 로 최소 수정(단언 보존).
  - select 편집모드 진입 후 즉시 onChange 커밋 — 편집모드 토글이 한 클릭 더 필요(✎→select 노출→선택). 기존 항상-노출 select 대비 클릭 1회 증가(일관성 trade — 허용).
  - date 는 onChange 즉시 커밋(인스펙터 동일). text 는 Enter/blur.
- **다음:** (선택) 번호 유일성 인라인 검증, 케이블 사이드패널 편집(현 비목표), 접지 descriptor 시 동일 EditableField 재사용.
